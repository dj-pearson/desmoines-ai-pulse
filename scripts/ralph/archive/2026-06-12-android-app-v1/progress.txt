# Ralph Progress Log — Admin Follow-ups
Started: 2026-05-20

## Codebase Patterns

(Carried-over from prior PRDs — add new ones here as discovered.)

### Admin edge-function auth gate
1. `handleCors(req)` → early return on OPTIONS
2. `checkRateLimit(req, { windowMs, max, message })` → 429 + Retry-After
3. Reject non-GET/POST → 405
4. Build service-role client with `Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")`
5. Read `Authorization: Bearer <token>`, call `supabase.auth.getUser(token)`
6. Lookup `profiles.role` and require `admin` or `root_admin` (or use the
   `is_admin()` SQL helper introduced in ADMIN-SECURITY-001 from prior PRD)
7. Run queries, return JSON with `Cache-Control: private, max-age=N`

### Encrypted credential storage
- AES-GCM via Deno Web Crypto, 32-byte base64 key in env var
- Service-role-only RLS on the underlying table
- Safe view that omits encrypted columns + exposes booleans
- All writes go through an edge function that has both key + service role

### Realtime subscription for admin queues
```ts
useEffect(() => {
  const ch = supabase
    .channel("name")
    .on("postgres_changes",
        { event: "*", schema: "public", table: "…" },
        () => load())
    .subscribe();
  return () => { supabase.removeChannel(ch); };
}, [filters]);  // rebuild channel when query changes
```

---

## 2026-05-20 — B2B-KANBAN-001 (drag-to-update kanban)
6/6 — Admin Follow-ups PRD complete.

Files modified:
- `package.json` + `package-lock.json` (added @dnd-kit/core ^6.3,
  @dnd-kit/sortable ^8)
- `src/components/admin/PartnershipsManager.tsx`
  (DndContext + DraggableAppCard + DroppableColumn + DragOverlay)

**Learnings for future iterations:**
- @dnd-kit PointerSensor needs `activationConstraint: { distance: 4 }`
  on a card that also acts as a click target — without it, every
  click is interpreted as a drag start. Distance 4px is the dnd-kit
  default suggestion.
- For accessibility, @dnd-kit's `KeyboardSensor` is plug-and-play
  (Tab to focus, Space to pick up, arrows to move, Space to drop),
  but the wrapping `<div role="button" tabIndex={0}>` should still
  call `onOpen` on Enter so the detail Sheet stays reachable.
- DragOverlay rendering: take the dragged row's data (rows.find),
  render with rotate-1 + shadow-lg + opacity-90 for the floating
  card. Don't reuse the same JSX that renders inside the column —
  the overlay is positioned by dnd-kit, not by column layout.

---

## 2026-05-20 — DEAL-RECURRING-001 (recurring rules on deals)

Files added:
- `supabase/migrations/20260520000015_deals_recurrence.sql`

Files modified:
- `src/components/admin/DealManager.tsx`

**Learnings:**
- Postgres TIME columns serialize as 'HH:MM:SS' strings — strip
  seconds with `.slice(0, 5)` before binding to an `<input type="time">`
  which expects 'HH:MM'.
- "Both times set or neither" is cleanly expressed as a CHECK
  constraint, AND validated in the UI before the network round-trip.
  Doing it twice protects against direct SQL inserts later.

---

## 2026-05-20 — FEEDBACK-REPLY-001 (reply thread + Resend reply)

Files added:
- `supabase/migrations/20260520000014_create_feedback_replies.sql`
- `supabase/functions/send-feedback-reply/index.ts`

Files modified:
- `src/components/admin/FeedbackInbox.tsx`

---

## 2026-05-20 — EMAIL-WEBHOOK-001 (Resend webhook + delivery tracking)

Files added:
- `supabase/migrations/20260520000013_newsletter_delivery_tracking.sql`
- `supabase/functions/resend-webhook/index.ts`

Files modified:
- `supabase/functions/send-newsletter-campaign/index.ts`
- `supabase/functions/dispatch-scheduled-newsletters/index.ts`
- `src/components/admin/NewsletterCampaignsManager.tsx`

**Learnings:**
- Svix-based webhook verification: use `new Webhook(secret).verify(rawBody, headers)`
  with the three `svix-id`, `svix-timestamp`, `svix-signature` headers.
  Reading `req.text()` once and passing the raw string is critical —
  `req.json()` would re-serialize and break the signature.
- Rank-aware status update for engagement events: if a `delivered`
  event arrives AFTER an `opened` event (Resend sometimes orders
  these out of sequence), don't downgrade the recipient's recorded
  status. Map each status to a rank and only update when the new
  event's rank is >= the previous one.
- Unknown event types should return 200 OK with `{ ignored: type }`,
  not 400 — otherwise Resend treats it as a delivery failure and
  retries forever.

---

## 2026-05-20 — EMAIL-SCHED-001 (scheduler cron for newsletter campaigns)

Files added:
- `supabase/functions/dispatch-scheduled-newsletters/index.ts`
- `supabase/migrations/20260520000012_schedule_newsletter_dispatch.sql`

**Learnings:**
- Atomic claim pattern with `FOR UPDATE SKIP LOCKED`:
  ```sql
  WITH due AS (
    SELECT id FROM ... WHERE status='scheduled' AND scheduled_for <= now()
    ORDER BY scheduled_for ASC
    LIMIT p_limit
    FOR UPDATE SKIP LOCKED
  )
  UPDATE ... SET status='sending' FROM due WHERE id=due.id RETURNING ...
  ```
  Lets multiple worker invocations run concurrently without
  double-dispatching, and keeps the lock window tiny.
- pg_cron + net.http_post pattern requires the
  `app.settings.supabase_url` and `app.settings.supabase_service_role_key`
  session settings to be configured. Established in
  20250107000006_schedule_event_scraping.sql.

---

## 2026-05-20 — TRENDING-FN-001 (rewire calculate_trending_scores)

Files added:
- `supabase/migrations/20260520000011_rewire_calculate_trending_scores.sql`

Migration creates `events.trending_score` (NUMERIC, default 0, indexed
DESC NULLS LAST) and replaces `public.calculate_trending_scores()`
with a version that:

1. Reads weights from `trending_config` (singleton row id=1) with
   documented defaults as fallback if the row is missing
2. Counts engagement from user_event_interactions (view/click/share/save)
   + user_event_feedback (thumbs_up/interested as proxy for favorites)
   over the last 7 days
3. Applies the formula: (sum of weighted counts) * exp(-age_hours /
   half_life) + (featured_boost if is_featured)
4. Zeros out rows younger than min_age_hours
5. Updates events.trending_score for every event with date >= today

SECURITY DEFINER + search_path = public. Returns the count of rows
updated (INTEGER).

**Learnings for future iterations:**
- When wiring a new "scheduled scoring" function for a table that
  already has trigger-maintained columns, USE A SEPARATE COLUMN.
  Sharing the column makes the trigger and the scheduled job fight
  every time a user edits the row. Future cost of switching
  consumers to the new column is trivial compared to debugging
  silent score reverts.
- The dashboard-created version of calculate_trending_scores() (which
  the calculate-trending edge function expects) was never in any
  migration. The new version DROP-IF-EXISTS-then-CREATEs to be the
  single source of truth. After db push, the edge function continues
  to work because the new function has the same parameterless
  signature (return type went from VOID to INTEGER, which is a
  superset — callers that ignore the return value see no difference).

---
