# Ralph Progress Log
Started: 2026-03-27T14:05:42.010Z
---

## 2026-03-27 - AND-001 (Android)
- Initialized complete Android project scaffold with Kotlin 2.1.0, AGP 8.7.3, Jetpack Compose BOM 2024.12.01
- Created version catalog (libs.versions.toml) with all dependencies
- Set up Hilt DI (@HiltAndroidApp + @AndroidEntryPoint from the start)
- Created Material 3 theme with light/dark/dynamic color support
- MainActivity displays "Hello Des Moines" placeholder screen
- Added Android gitignore entries
- Files created: android/ directory with 18 files (build configs, manifest, theme, app class, activity)
- **Note**: No Android SDK/Java on this machine — build cannot be verified locally. Structure follows standard Android project conventions and should compile on first try with SDK present.
- **Learnings for future iterations:**
  - Version catalog at gradle/libs.versions.toml is the canonical place for dependency versions
  - KSP (2.1.0-1.0.29) used instead of KAPT for Hilt compiler — faster builds
  - Package: com.desmoines.aipulse matching iOS bundle ID
  - Theme uses dynamic colors on Android 12+ with brand color fallback
---

## 2026-03-27 - AND-002 (Android)
- Created full package structure: data/model, data/remote, data/local, data/repository, di, ui/components, ui/screens, ui/navigation, util
- Created di/AppModule.kt providing singleton Json instance and ApplicationContext
- Created di/RepositoryModule.kt (abstract module, bindings added in future stories)
- Added package-info.kt files in each package for documentation and to ensure directories are tracked by git
- **Note**: No JDK on this machine — build cannot be verified locally. Structure follows standard Android/Hilt conventions.
- **Learnings for future iterations:**
  - AppModule provides Json with ignoreUnknownKeys=true (needed for Supabase responses with extra fields)
  - RepositoryModule is abstract class (uses @Binds), AppModule is object (uses @Provides)
  - Package-info.kt files serve dual purpose: git tracking + IDE documentation
  - Supabase client provision will be added in AND-003 when supabase-kt dependencies are added
---

## 2026-03-27 - AND-003 (Android)
- Added supabase-kt BOM 3.1.1 (postgrest, auth, realtime, storage) and Ktor OkHttp engine 3.0.3
- Added BuildConfig fields for SUPABASE_URL and SUPABASE_ANON_KEY read from local.properties
- Created util/Config.kt mirroring iOS Config.swift (coordinates, page size, cache TTL, auth constants)
- Created data/remote/SupabaseClient.kt (SupabaseClientProvider singleton) mirroring iOS SupabaseService.swift with PKCE auth, custom redirect scheme, X-Client-Info header, and graceful null-client when credentials missing
- Created data/remote/ConnectivityTest.kt for simple events table query verification
- Updated di/AppModule.kt to provide SupabaseClient via Hilt DI
- Added auth callback deep link intent filter to AndroidManifest.xml
- Added Ktor/OkHttp proguard rules
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Supabase credentials flow: local.properties → build.gradle.kts buildConfigField → BuildConfig.SUPABASE_URL/KEY
  - SupabaseClientProvider.client is nullable — all data sources must handle null client gracefully
  - Auth uses PKCE flow with scheme=com.desmoines.aipulse, host=auth-callback (matches iOS redirect pattern)
  - supabase-kt 3.x uses install() blocks for plugins (Auth, Postgrest, Realtime, Storage)
  - Version catalog: supabase-bom manages all supabase-kt module versions
---

## 2026-03-27 - AND-004 (Android)
- Created all data models mirroring iOS Models/ directory:
  - AppEnums.kt: EventCategory (12 types with icon names, Color), PriceRange, LocationArea, AttractionType, ContentType, UserRole, EventSortOption, RestaurantSortOption (6 options), DateFilterPreset (6 presets with dateRange computation using java.time), SubscriptionTier (free=3 maxFavorites, insider/vip=Int.MAX_VALUE)
  - Event.kt: @Serializable data class with all 22 fields, @SerialName for snake_case mapping, computed properties (eventCategory, parsedDate, coordinate, isFree, displayDescription, displayLocation, urgencyLabel, featuredCardAccessibilityLabel), preview companions
  - Restaurant.kt: @Serializable data class with all 22 fields, isOpenNow() method parsing business_hours JSONB (supports flat and nested formats, 12h/24h time), computed properties (coordinate, displayDescription, displayLocation, ratingText, priceLevel, callUri, websiteUri, openStatusText, compactCardAccessibilityLabel)
  - Attraction.kt: @Serializable data class with all 13 fields, computed properties (attractionType, coordinate, websiteUri, ratingText)
  - UserProfile.kt: @Serializable data class with computed displayName/initials/role, plus UserEventInteraction, UserRestaurantInteraction, UserRating models
  - LatLng.kt: Simple latitude/longitude data class (placeholder until Google Maps SDK added in later story)
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Used own LatLng data class to avoid premature Google Maps SDK dependency — will swap when Maps story arrives
  - Restaurant.businessHours uses JsonElement (not a custom Codable struct) because kotlinx.serialization handles the flexible JSONB shape natively via JsonObject inspection
  - DateFilterPreset.dateRange uses java.time (LocalDate/LocalDateTime) not kotlinx-datetime — java.time is standard on Android 26+ (our minSdk)
  - Event.parsedDate returns OffsetDateTime to preserve timezone info from ISO 8601 strings
  - All @SerialName annotations match exact Supabase column names (snake_case)
  - EventCategory.from() does case-insensitive matching with contains() fallback, matching iOS init(from:) logic
---

## 2026-03-27 - AND-005 (Android)
- Enhanced Color.kt with category colors (all 12 EventCategory types), status colors (success/error/warning), semantic colors, and additional surface variant/outline colors for light and dark themes
- Created Dimens.kt with standard spacing scale (2dp-48dp) and component-specific dimensions (card radius, icon sizes, hero height, etc.)
- Updated Theme.kt with surfaceVariant, outline color slots and status bar color management via SideEffect
- Updated AppEnums.kt EventCategory colors to match PRD spec: Family→blue, Sports→red, Charity→rose, Holiday→amber
- Files changed: Color.kt, Theme.kt, Dimens.kt (new), AppEnums.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Category colors are defined in two places: Color.kt (theme constants) and AppEnums.kt (enum properties) — keep them in sync
  - PRD specifies specific category colors that differ from iOS in some cases (iOS Family=cyan, PRD Family=blue; iOS Sports=mint, PRD Sports=red)
  - Status bar color is set via SideEffect in Theme.kt — uses surface color for seamless look
  - Dimens object provides centralized spacing constants to avoid magic numbers in composables
---

## 2026-03-27 - AND-006 (Android)
- Built bottom navigation with 6 tabs matching iOS MainTabView.swift: Home, Dining, Search, Map, Saved, Profile
- Created NavGraph.kt with sealed class Route definitions for all screens (tabs, details, flows) with type-safe arguments
- Created BottomNavTab enum with filled/outlined icon pairs for selected/unselected states
- Created MainNavHost.kt with NavHost containing all route destinations organized into tab, detail, and flow groups
- Created MainScreen.kt with Scaffold + NavigationBar, haptic feedback on tab selection, bottom bar hidden on detail screens
- Created placeholder screens for all 6 tabs with navigation callback signatures ready for future stories
- Updated MainActivity.kt to use MainScreen instead of HelloDesMoinesScreen placeholder
- Created screen directories for all future feature screens (eventdetail, restaurantdetail, attractiondetail, auth, onboarding, subscription)
- Files created: NavGraph.kt, MainNavHost.kt, MainScreen.kt, HomeScreen.kt, RestaurantsScreen.kt, SearchScreen.kt, MapScreen.kt, FavoritesScreen.kt, ProfileScreen.kt
- Files modified: MainActivity.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Navigation uses popUpTo(findStartDestination) + saveState/restoreState for proper tab backstack management
  - Bottom bar visibility is controlled by checking currentDestination.route against tabRoutes set
  - Detail/flow routes use string-based arguments with navArgument — will serve until type-safe nav args are needed
  - Each placeholder screen already has navigation callback params (onNavigateToEventDetail, etc.) so future stories just fill in the composable body
  - BottomNavTab enum centralizes tab config — icons, labels, and routes in one place
  - Haptic feedback uses HapticFeedbackType.LongPress (closest to iOS .light impact in Compose)
  - Restaurant icon uses Icons.Filled.Restaurant / Icons.Outlined.Restaurant (Material Icons Extended)
---

## 2026-03-27 - AND-007 (Android)
- Created EventsRemoteDataSource.kt with all 6 methods matching iOS EventsService.swift:
  - fetchEvents(query) with full-text search (websearch), category/date/featured filters, pagination with count
  - fetchEvent(id) single event by ID
  - fetchFeaturedEvents(limit) for featured carousel
  - fetchRelatedEvents(eventId, category, limit) same category excluding self
  - fuzzySearchEvents(query, limit) via RPC 'fuzzy_search_events'
  - fetchNearbyEvents(lat, lng, radiusMiles, limit) via RPC 'search_events_near_location' with client-side Haversine fallback
- Created EventsRepository interface with Result<T> return types for all methods
- Created EventsRepositoryImpl wrapping remote data source with runCatching error handling
- Updated RepositoryModule.kt with @Binds binding for EventsRepository → EventsRepositoryImpl
- Created EventsRemoteDataSourceTest with unit tests for query defaults, Haversine distance, and miles-to-meters conversion
- Files created: EventsRemoteDataSource.kt, EventsRepository.kt, EventsRepositoryImpl.kt, EventsRemoteDataSourceTest.kt
- Files modified: RepositoryModule.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - supabase-kt 3.x: use `client.from("table").select { filter { ... } }` DSL for queries
  - supabase-kt 3.x: use `client.postgrest.rpc("fn", params)` for RPC calls — params must be @Serializable
  - Count requires `count = Count.EXACT` in select(), result via `result.countOrNull()`
  - For IS NOT NULL filters, use client-side filtering as supabase-kt filter DSL varies across versions
  - Haversine formula is in EventsRemoteDataSource.Companion for reuse by other data sources (restaurants, attractions)
  - Repository pattern: interface returns Result<T>, impl uses runCatching + onFailure for logging
  - EventsQuery is a plain data class (not @Serializable) — it's only used client-side to build Supabase queries
---

## 2026-03-27 - AND-008 (Android)
- Created RestaurantsRemoteDataSource.kt with all 5 methods matching iOS RestaurantsService.swift:
  - fetchRestaurants(query) with full-text search, cuisine/priceRange/location in-filters, minRating gte, featured eq, 6 sort options with exact ordering chains matching iOS
  - fetchRestaurant(id) single restaurant by ID
  - fuzzySearchRestaurants(query, limit) via RPC 'fuzzy_search_restaurants'
  - fetchNearbyRestaurants(lat, lng, radiusMiles, limit) via RPC 'restaurants_within_radius' with client-side Haversine fallback
  - fetchAvailableCuisines() returning distinct sorted cuisine strings
- Created RestaurantsRepository interface with Result<T> return types
- Created RestaurantsRepositoryImpl wrapping remote data source with runCatching error handling
- Updated RepositoryModule.kt with @Binds binding for RestaurantsRepository → RestaurantsRepositoryImpl
- Files created: RestaurantsRemoteDataSource.kt, RestaurantsRepository.kt, RestaurantsRepositoryImpl.kt
- Files modified: RepositoryModule.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - RestaurantsQuery uses List<String> for cuisines/priceRanges/locations (vs single String for EventsQuery.category) — isIn() filter for multi-select
  - Sort options apply multiple order() calls in when-block inside the select DSL (not separate execute calls like iOS switch)
  - Nearby restaurants RPC uses different param names than events: center_lat/center_lng/radius_miles/limit_count (vs user_lat/user_lon/radius_meters/search_limit)
  - fetchAvailableCuisines uses Columns.list("cuisine") for column selection and client-side distinct() + sorted()
  - Reused EventsRemoteDataSource.haversineDistance() companion for table fallback distance filtering
---

## 2026-03-27 - AND-009 (Android)
- Created AttractionsRemoteDataSource.kt with all 3 methods matching iOS AttractionsService.swift:
  - fetchAttractions(query) with multi-field ILIKE search on name/type/location via or{} filter, type eq, minRating gte, isFeatured eq, order by created_at desc, pagination with count
  - fetchAttraction(id) single attraction by ID
  - fetchNearbyAttractions(lat, lng, limit) with client-side Haversine distance filtering (no PostGIS RPC for attractions)
- Created AttractionsRepository interface with Result<T> return types for all methods
- Created AttractionsRepositoryImpl wrapping remote data source with runCatching error handling
- Updated RepositoryModule.kt with @Binds binding for AttractionsRepository → AttractionsRepositoryImpl
- Files created: AttractionsRemoteDataSource.kt, AttractionsRepository.kt, AttractionsRepositoryImpl.kt
- Files modified: RepositoryModule.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Attractions use ILIKE search (not full-text search like events/restaurants) — simpler pattern with or{} block containing 3 ilike() calls
  - No PostGIS RPC for attractions yet — fetchNearbyAttractions uses purely client-side distance filtering with haversineDistance()
  - Reused EventsRemoteDataSource.haversineDistance() companion for consistency across all data sources
  - Default limit for nearby attractions is 50 (matching iOS), vs default page size 30 for paginated list
  - AttractionsQuery is simpler than Events/Restaurants: no date range, no sort options, no multi-select filters
---

## 2026-03-27 - AND-010 (Android)
- Built complete offline cache layer with Room database mirroring iOS QueryCache.swift:
  - Added Room 2.6.1 dependencies (room-runtime, room-ktx, room-compiler via KSP)
  - Created 4 Room entities: CachedEvent, CachedRestaurant, CachedAttraction, CacheMetadata — all with indexed cache_key and cached_at columns
  - Created 4 DAOs: EventDao, RestaurantDao, AttractionDao, CacheMetadataDao with insertAll, getAllByKey, getById, deleteAll, deleteExpired, deleteByKey, count
  - Created AppDatabase with all 4 entities, version 1, fallbackToDestructiveMigration
  - Created CacheManager singleton with: getCached*/cache* methods for each type, TTL checking via CacheMetadata.isExpired, stale cache serving (allowStale param), pruneExpired() removing entries >24h, clearAll(), static cache key builders
  - Created DatabaseModule providing AppDatabase + all DAOs via Hilt
- Updated all 3 repository implementations with cache-first strategy:
  - EventsRepositoryImpl: cache check → remote fetch + cache write → stale fallback on error
  - RestaurantsRepositoryImpl: same pattern, cuisine list not cached (simple/cheap)
  - AttractionsRepositoryImpl: same pattern
  - Cache keys include all query parameters for distinct result sets
- Updated DesMoinesInsiderApp to prune expired cache on launch via coroutine scope
- Removed data/local/package-info.kt placeholder (replaced by real files)
- Files created: AppDatabase.kt, CacheManager.kt, 4 entities, 4 DAOs, DatabaseModule.kt
- Files modified: libs.versions.toml, build.gradle.kts, DesMoinesInsiderApp.kt, EventsRepositoryImpl.kt, RestaurantsRepositoryImpl.kt, AttractionsRepositoryImpl.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Room entities store serialized JSON strings (not individual columns) to avoid complex TypeConverters for JsonElement, List<String>, etc.
  - Cache keys are built with static CacheManager.eventsKey/restaurantsKey/attractionsKey helpers — pass all query params as varargs
  - CacheMetadata.isExpired computed property: (now - cachedAt) > (ttlMinutes * 60_000)
  - Repository pattern: isCacheFresh → return cached, else runCatching remote → recoverCatching with stale cache
  - fetchById always checks cache first (any cache key), remote fallback caches under "single|{id}" key
  - EventsQuery has no sortBy field (unlike RestaurantsQuery) — don't include it in cache keys
  - Response types use Int for totalCount + hasMore: Boolean — cached responses set hasMore=false
  - DatabaseModule is separate @Module from AppModule — keeps DB concerns isolated
  - Room fallbackToDestructiveMigration used since cache is expendable data
---

## 2026-03-27 - AND-011 (Android)
- Created NetworkMonitor.kt using ConnectivityManager.NetworkCallback matching iOS NetworkMonitor.swift:
  - Exposes isConnected as StateFlow<Boolean> (default true)
  - Exposes showReconnected as StateFlow<Boolean> that is true for 5 seconds after regaining connection
  - Tracks wasOffline state to detect offline→online transitions
  - Uses NetworkCapabilities.NET_CAPABILITY_INTERNET + NET_CAPABILITY_VALIDATED for accurate connectivity check
  - Registered as @Singleton via Hilt constructor injection (no separate DI module entry needed)
- Created OfflineBanner.kt composable:
  - Orange banner with CloudOff icon when offline ("No internet connection")
  - Green banner with CloudDone icon for 5 seconds after reconnecting ("Back online")
  - AnimatedVisibility with expandVertically + fadeIn/fadeOut for smooth transitions
  - LiveRegion.Polite semantics for TalkBack accessibility announcements
- Updated MainScreen.kt to accept NetworkMonitor param and display OfflineBanner at top of content (below status bar, above NavHost)
- Updated MainActivity.kt to @Inject NetworkMonitor and pass it to MainScreen
- Files created: NetworkMonitor.kt, OfflineBanner.kt
- Files modified: MainScreen.kt, MainActivity.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - NetworkMonitor uses constructor injection (@Inject constructor) so Hilt auto-provides it as @Singleton — no AppModule @Provides needed
  - ConnectivityManager.NetworkCallback fires onCapabilitiesChanged for quality changes, not just connect/disconnect — check NET_CAPABILITY_VALIDATED to avoid false positives
  - OfflineBanner uses LiveRegion.Polite so TalkBack announces connectivity changes without interrupting current speech
  - MainScreen now uses Column with weight(1f) on MainNavHost to accommodate banner at top
  - NetworkMonitor's scope uses SupervisorJob + Dispatchers.Main to safely handle coroutines without lifecycle owner
---

## 2026-03-27 - AND-012 (Android)
- Created AuthRemoteDataSource.kt with all methods matching iOS AuthService.swift:
  - signIn(email, password) using Supabase Auth Email provider
  - signUp(email, password, firstName, lastName, interests) with profile creation after signup
  - signOut() clearing session
  - resetPassword(email) sending reset email
  - signInWithGoogle(idToken) using Supabase Auth Google provider (replaces iOS Apple Sign-In)
  - fetchProfile(userId) from profiles table with eq filter on user_id
  - createProfile(userId, email, firstName, lastName, interests) inserting into profiles table
  - updateProfile(userId, ...) updating profiles table with eq filter
  - checkAdminRole(userId) checking user_roles table first, falling back to profiles.user_role
  - sessionStatus Flow exposed for auth state listening
- Created AuthRepository interface with Result<T> return types for all methods
- Created AuthRepositoryImpl wrapping data source with runCatching error handling
- Created AuthViewModel.kt with full state management:
  - Auth state: currentProfile, isAuthenticated, isAdmin, isLoading as StateFlow
  - Form state: isSigningIn, isSigningUp, errorMessage, showVerificationAlert
  - Email validation with same regex as iOS
  - Password strength scoring matching iOS exactly (6 criteria, weak/medium/strong thresholds)
  - Rate limiting: 5 failed attempts in 5 min window triggers 60-second lockout countdown
  - signIn/signUp/signOut/resetPassword/signInWithGoogle/updateProfile methods
  - Auth state listener collecting SessionStatus flow (Authenticated/NotAuthenticated/Initializing)
  - availableInterests companion matching iOS: Food, Music, Sports, Arts, Nightlife, Outdoor, Family, Business
- Added Google Credential Manager dependencies (credentials 1.3.0, googleid 1.1.1) to version catalog and build.gradle.kts
- Added GOOGLE_WEB_CLIENT_ID BuildConfig field from local.properties
- Updated Config.kt with GOOGLE_WEB_CLIENT_ID property
- Updated RepositoryModule.kt with @Binds binding for AuthRepository → AuthRepositoryImpl
- Files created: AuthRemoteDataSource.kt, AuthRepository.kt, AuthRepositoryImpl.kt, AuthViewModel.kt
- Files modified: RepositoryModule.kt, Config.kt, build.gradle.kts, libs.versions.toml
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - supabase-kt 3.x auth: signInWith(Email) { email=; password= } and signUpWith(Email) { ... data = buildJsonObject {} }
  - supabase-kt 3.x auth: Google Sign-In uses signInWith(Google) { idToken = token } — differs from iOS Apple Sign-In which uses signInWithIdToken
  - SessionStatus is a sealed class: Authenticated(session), NotAuthenticated, Initializing — collect it from auth.sessionStatus Flow
  - User ID from session: status.session.user?.id (String, not UUID like iOS)
  - AuthException is a sealed class (not enum like iOS AuthError) — data objects for NotConfigured, InvalidToken, NoUser
  - Rate limiting uses System.currentTimeMillis() instead of iOS Date() — same 5 attempts / 5 min window / 60 sec lockout
  - Google Credential Manager (androidx.credentials 1.3.0) replaces deprecated Google Sign-In SDK — configured via GOOGLE_WEB_CLIENT_ID in local.properties
  - AuthViewModel uses @HiltViewModel + viewModelScope.launch for all async operations (maps to iOS @MainActor + Task)
---

## 2026-03-27 - AND-013 (Android)
- Created AuthScreen.kt matching iOS AuthView.swift with full sign in / sign up UI:
  - TabRow with Sign In / Sign Up tabs (mirrors iOS segmented Picker)
  - Sign In form: Email field, Password field with visibility toggle, Forgot Password link, Sign In button with loading spinner
  - Sign Up form: First Name + Last Name row, Email, Password with strength indicator bar, Confirm Password, Interests grid (8 options via FlowRow + FilterChip)
  - Password strength bar (Weak=red, Medium=orange, Strong=green) with animated LinearProgressIndicator
  - Inline validation: email format error, password mismatch error
  - Lockout warning with Lock icon and countdown when rate limited
  - "or" divider between primary action and social sign-in
  - Google Sign-In button using Credential Manager API (replaces iOS Apple Sign-In)
  - Verification email AlertDialog after successful sign up
  - Error messages shown via Snackbar
  - Auto-navigate back on successful authentication via LaunchedEffect
  - All form fields have proper keyboard types (Email, Password) and IME actions (Next, Done)
- Wired AuthScreen into MainNavHost.kt replacing placeholder comment
- Files created: AuthScreen.kt
- Files modified: MainNavHost.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Google Sign-In uses CredentialManager + GetGoogleIdOption (not deprecated GoogleSignInClient)
  - CredentialManager.getCredential() is a suspend function — use rememberCoroutineScope() in Compose, not MainScope()
  - Set<String> is not natively saveable with rememberSaveable — use remember for non-primitive collections in form state
  - OutlinedTextField with supportingText replaces iOS's separate error Text below field — cleaner integration with Material 3
  - FilterChip with selectedContainerColor/selectedLabelColor maps well to iOS's capsule interest buttons
  - TabRow is the Material 3 equivalent of iOS segmented Picker for auth mode switching
  - AlertDialog in Compose is a standalone composable (not a modifier like iOS .alert)
---

## 2026-03-27 - AND-014 (Android)
- Created OnboardingScreen.kt with 3-page HorizontalPager matching iOS OnboardingView.swift:
  - Page 1: LocationCity icon, "Welcome to Des Moines Insider", 3 discovery highlights
  - Page 2: Favorite (heart) icon in red, "Save Your Favorites", 3 favorites highlights
  - Page 3: Map icon in blue, "Explore What's Nearby", 3 map highlights
  - Animated page indicator with wider capsule for active page (spring animation)
  - Back button on pages 2-3, Next/Get Started buttons, Skip button on pages 1-2
  - CheckCircle icons for highlight items colored per page
- Created OnboardingPreferences.kt using Jetpack DataStore (replaces iOS UserDefaults):
  - hasCompletedOnboarding Flow<Boolean> for reactive state
  - setOnboardingCompleted() and resetOnboarding() suspend functions
  - Singleton via Hilt @Inject constructor
- Updated MainActivity.kt with first-launch onboarding logic:
  - Reads DataStore on launch, shows OnboardingScreen if not completed
  - On complete: persists to DataStore and transitions to MainScreen
  - Uses initial=true default to avoid flash of onboarding on subsequent launches
- Wired OnboardingScreen into MainNavHost.kt (replacing placeholder comment)
- Added DataStore Preferences dependency (1.1.1) to version catalog and build.gradle.kts
- Files created: OnboardingScreen.kt, OnboardingPreferences.kt
- Files modified: MainActivity.kt, MainNavHost.kt, build.gradle.kts, libs.versions.toml
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - HorizontalPager (foundation) replaces iOS TabView(.page) for swipeable pages
  - DataStore preferencesDataStore delegate is a Context extension — defined at file level, not inside a class
  - collectAsState(initial = true) prevents flash: defaults to "completed" so onboarding doesn't briefly show on subsequent launches
  - OnboardingPreferences uses @Singleton + @Inject constructor so Hilt auto-provides — no manual @Provides needed
  - animateDpAsState with spring animation replicates iOS capsule page indicator width animation
  - Page indicator uses semantics { contentDescription = "Page X of 3" } for TalkBack accessibility
  - OnboardingScreen is rendered directly in MainActivity (not via NavHost) for first-launch — avoids navigation complexity with conditional start destinations
---

## 2026-03-27 - AND-015 (Android)
- Built complete reusable UI components library (9 new files, 2 already existed from prior stories):
  - LoadingView.kt: LoadingView (full-screen with CircularProgressIndicator + message), InlineLoadingView (compact for list "load more"), DetailHeroSkeleton (shimmer animation with infiniteRepeatable + linearGradient brush)
  - ToastOverlay.kt: ToastMessage data class with SUCCESS/INFO/ERROR styles and companion factory methods, ToastOverlay composable with AnimatedVisibility slide+fade, 2-second LaunchedEffect auto-dismiss
  - CachedAsyncImage.kt: Coil 3 SubcomposeAsyncImage with loading spinner, error fallback (BrokenImage icon), crossfade transition; Coil handles memory+disk cache automatically
  - PremiumGate.kt: Tier access check (FREE always, INSIDER checks insider||vip, VIP checks vip only), locked overlay with Lock icon + tier name + feature description + unlock Button
  - PremiumBadge.kt: Capsule badge with Lock icon + tier name, orange for Insider, purple for VIP
  - SubscriptionBanner.kt: FULL style (free=sparkles card with upgrade button, subscribed=tier card with manage chevron) and COMPACT style (inline upgrade prompt for free users only); FavoritesLimitBanner with progress bar (LinearProgressIndicator), limit/near-limit/at-limit color states
  - AdBannerView.kt: Upgrade prompt card for free users with "Remove Ads" CTA, hidden for subscribers
  - FullScreenImageViewer.kt: Pinch-to-zoom (detectTransformGestures, 0.5x-5x), double-tap toggle (1x↔2.5x via detectTapGestures.onDoubleTap), swipe-down-to-dismiss (detectVerticalDragGestures, 150dp threshold), pan when zoomed, close button overlay
  - WebViewScreen.kt: AndroidView hosting WebView with TopAppBar + back button, LinearProgressIndicator while loading, JavaScript enabled
  - CategoryBadge.kt and EmptyStateView.kt already existed from prior story work
- Wired WebViewScreen into MainNavHost.kt with URL decoding from route arguments
- All components include @Preview composables for design verification
- Files created: LoadingView.kt, ToastOverlay.kt, CachedAsyncImage.kt, PremiumGate.kt, PremiumBadge.kt, SubscriptionBanner.kt, AdBannerView.kt, FullScreenImageViewer.kt, WebViewScreen.kt
- Files modified: MainNavHost.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Coil 3 uses SubcomposeAsyncImage (not AsyncImage) for custom loading/error composables — simpler than iOS's custom two-tier ImageCache
  - Shimmer animation: rememberInfiniteTransition + Brush.linearGradient with animated offset creates shimmer effect without third-party library
  - PremiumGate takes currentTier as parameter (not injected) for testability — caller provides subscription state
  - SubscriptionBanner and AdBannerView both early-return for non-free tiers — consistent pattern for subscription-gated UI
  - FavoritesLimitBanner: maxFavorites is 3 for free tier, so isNearLimit triggers at 2 (maxFavorites - 1)
  - FullScreenImageViewer uses three separate pointerInput modifiers for transform, tap, and vertical drag gestures — Compose doesn't combine these in a single modifier easily
  - WebViewScreen uses AndroidView interop — the WebView is a platform view, not a Compose composable
  - ToastOverlay uses inverseOnSurface/inverseSurface Material 3 colors for contrast (like iOS .ultraThickMaterial)
---

## 2026-03-27 - AND-016 (Android)
- Built complete Home screen matching iOS HomeView.swift with all UI sections:
  - HomeScreen.kt: Main composable with LargeTopAppBar (collapsing), pull-to-refresh (PullToRefreshBox), LazyColumn with all sections, filter button with active count badge
  - HeaderSection: "What's happening in Des Moines" subtitle with total event count
  - DatePresetsRow: Horizontal LazyRow with all 6 DateFilterPreset entries as toggle chips
  - CategoryChipsRow: Horizontal LazyRow with first 8 EventCategory entries, icon + label, colored selection state
  - FeaturedEventsSection: Star icon header + horizontal LazyRow of FeaturedEventCard composables
  - RestaurantsSection: Fork icon header + horizontal LazyRow of CompactRestaurantCard composables (max 10)
  - ActiveFiltersBar: Filter count text + "Clear All" button
  - ErrorBanner: Warning icon + message + Retry button
  - Events list with skeleton loaders (6 skeletons), offline/empty states, load-more at 5 items from end
  - AdBannerView for free users, haptic feedback on interactions
- Created EventCardView.kt matching iOS EventCardView.swift:
  - Card with hero image (180dp), category badge overlay, favorite button, date badge
  - Content area with title, date/time row, location row, price badge (FREE/paid), urgency label, featured badge
  - Full accessibility label combining all visible info
- Created FeaturedEventCard.kt matching iOS FeaturedEventCard:
  - 260dp width, 150dp image with category badge at bottom-left, title, date, FREE badge
- Created CompactRestaurantCard.kt matching iOS CompactRestaurantCard:
  - 180dp width, 110dp image, name, cuisine + price + rating row with star icon
- Created EventCardSkeleton.kt matching iOS EventCardSkeleton:
  - Shimmer animation with infiniteRepeatable linearGradient brush
  - Placeholder layout matching EventCardView structure (image area, category badge, favorite button, date badge, title, time, location, price)
- Created DateBadge composable matching iOS DateBadge:
  - Day of week (short uppercase), day number, month (abbreviated uppercase) in frosted surface background
- Updated MainNavHost.kt to pass onNavigateToSubscription callback to HomeScreen
- HomeScreenState data class holds all UI state — will be replaced by ViewModel StateFlow in AND-017
- Files created: HomeScreen.kt (rewritten), EventCardView.kt, FeaturedEventCard.kt, CompactRestaurantCard.kt, EventCardSkeleton.kt
- Files modified: MainNavHost.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - HomeScreen uses HomeScreenState data class as state holder — ViewModel in AND-017 will expose this as StateFlow
  - PullToRefreshBox (Material 3 experimental) replaces deprecated SwipeRefresh — simpler API
  - LargeTopAppBar with exitUntilCollapsedScrollBehavior + nestedScroll creates the iOS-like large title collapsing effect
  - LazyColumn with snapshotFlow on listState for load-more detection — watches lastVisibleIndex vs totalItemsCount
  - Category chips show first 8 entries (matching iOS .prefix(8)) — EventCategory.entries.take(8)
  - DateBadge is a separate composable in EventCardView.kt (shared between EventCardView and potentially detail screens)
  - EventCardView accepts onFavoriteClick as nullable — null means hide the favorite button entirely
  - All carousel items use .clickable + .semantics { contentDescription = ... } for accessibility (child elements use clearAndSetSemantics)
---

## 2026-03-27 - AND-017 (Android)
- Created EventsViewModel.kt as @HiltViewModel with full state management matching iOS EventsViewModel.swift:
  - State: events, featuredEvents, restaurants, isLoading, isLoadingMore, isRefreshing, hasMore, totalCount, errorMessage as StateFlow
  - Filters as StateFlow: searchText (debounced 300ms), selectedCategory, selectedDatePreset, showFeaturedOnly, showFreeOnly (premium), maxDistance (premium), minRating (premium), currentTier
  - loadInitialData(): parallel fetch of events, featured events, and popular restaurants; guard prevents duplicate initial loads
  - refresh(): parallel re-fetch of all data with isRefreshing state for pull-to-refresh
  - fetchEvents(reset): builds EventsQuery from current filter state, applies premium filters client-side, manages pagination offset
  - loadMoreIfNeeded(currentIndex): triggers at count-5, guarded by isLoadingMore to prevent duplicates
  - applyPremiumFilters(): filters by isFree, filters by maxDistance using Haversine distance (meters→miles conversion)
  - clearFilters(): resets all filter properties and triggers debounced refetch
  - activeFilterCount: computed from all 7 filter dimensions
  - resetAndFetch(): 300ms debounce via coroutine Job cancellation (matches iOS Task.sleep pattern)
  - Combined uiState StateFlow using kotlinx.coroutines.flow.combine (two 9-parameter combines chained) producing HomeScreenState
  - fetchPopularRestaurants(): fetches featured restaurants sorted by popularity for home carousel
- Updated HomeScreen.kt: changed HomeScreenState comment from "will be replaced" to reference EventsViewModel
- Updated MainNavHost.kt: wired EventsViewModel via hiltViewModel(), connected all HomeScreen callbacks (category, date preset, clear filters, refresh, load more), added LaunchedEffect for loadInitialData()
- Files created: EventsViewModel.kt
- Files modified: MainNavHost.kt, HomeScreen.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - kotlinx.coroutines.flow.combine supports up to 5 typed overloads; for 6+ flows use the array transform variant (Array<*>) → cast elements
  - Two chained combine() calls work well for separating "data state" (9 flows) from "filter state" (9 flows) into a single uiState
  - SharingStarted.WhileSubscribed(5_000) keeps the combined flow active 5s after last subscriber — prevents re-computation on config changes
  - EventsRemoteDataSource.haversineDistance returns meters — must divide by 1609.34 for miles comparison
  - RestaurantsQuery.sortBy takes RestaurantSortOption enum, not String
  - hiltViewModel() in NavHost composable scopes ViewModel to the backstack entry — correct for tab-level ViewModels
  - LaunchedEffect(Unit) in composable destination is the right place for initial data loading (fires once per navigation)
  - ViewModel's hasLoadedInitialData flag prevents duplicate loads even if LaunchedEffect re-fires
---

## 2026-03-27 - AND-018 (Android)
- Created FilterSheet.kt as ModalBottomSheet matching iOS FilterSheet.swift:
  - Header with "Filters" title, Clear button, and Close (X) button
  - Category section: 2-column LazyVerticalGrid with all 12 EventCategory entries, each showing icon + displayName + checkmark when selected, color-coded borders and backgrounds
  - Date section: 2-column LazyVerticalGrid with all 6 DateFilterPreset entries, primary color when selected with checkmark
  - Options section: "Featured Events Only" toggle (Switch)
  - Advanced Filters section wrapped in PremiumGate(requiredTier=INSIDER):
    - "Free Events Only" toggle with ticket icon
    - "Max Distance" slider (1-50 miles, 1-mile steps) with clear/set toggle and prominent value display
    - "Min Rating" slider (1.0-5.0, half-star steps=7) with clear/set toggle and prominent value display
  - Done button at bottom
  - All filters have proper accessibility semantics (contentDescription with selected state, slider values)
- Updated MainNavHost.kt to wire FilterSheet into Home tab:
  - Added showFilterSheet local state (remember { mutableStateOf(false) })
  - Collected showFreeOnly, maxDistance, minRating from ViewModel as additional StateFlows
  - Connected all FilterSheet callbacks to ViewModel setter methods
  - Upgrade button dismisses sheet and navigates to Subscription screen
- Files created: FilterSheet.kt
- Files modified: MainNavHost.kt, prd-android.json
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - ModalBottomSheet with skipPartiallyExpanded=true shows the full sheet (matching iOS .presentationDetents(.large))
  - Slider local state (remember) avoids constant ViewModel updates during drag — only commit via onValueChangeFinished
  - LazyVerticalGrid inside ModalBottomSheet needs fixed height and userScrollEnabled=false to avoid nested scroll conflicts
  - Min rating slider uses steps=7 for half-star increments (1.0, 1.5, 2.0, ..., 5.0 = 9 values, 7 intermediate steps)
  - FilterSheet is rendered as a sibling of HomeScreen (not inside it) in the composable destination — ModalBottomSheet manages its own overlay
  - Category/DatePreset grids use Surface(onClick) for the entire row clickable area (cleaner than Row + clickable modifier)
  - WindowInsets.navigationBars on ModalBottomSheet prevents content from being hidden behind gesture bar
---

## 2026-03-27 - AND-019 (Android)
- Created Event Detail screen matching iOS EventDetailView.swift with all 5 sub-sections:
  - EventDetailHeader: 300dp hero image with gradient overlay, category badge, title text, tap-to-view full-screen image
  - EventDetailDateTime: Calendar icon, formatted date/time, urgency label badge (Today/Tomorrow/In X days) with icon+text (not color alone)
  - EventDetailInfo: Location row with venue/address/city + Get Directions button (Google Maps with generic geo fallback), Price row (Free with checkmark / paid / not listed), Distance row, About description section
  - EventDetailActions: Add to Calendar button (Insider+ gated with lock icon + PremiumBadge for free users, opens CalendarContract intent with 2h duration), More Info external link button, Remind Me toggle button (placeholder for notification service)
  - EventDetailInsiderTips: Premium content card (orange, star icon, tip text generated per category/venue/free status matching iOS exactly) for subscribers, locked teaser card with lock icon + upgrade CTA for free users
  - EventDetailRelated: Horizontal LazyRow of RelatedEventCard (180dp width, image, title, date) with navigation to other event details
- Created EventDetailViewModel.kt as @HiltViewModel:
  - loadEvent(id): fetches event + related events from repository
  - toggleFavorite(): placeholder (wired in AND-024)
  - createCalendarIntent(): CalendarContract.Events insert intent with title, location, description, 2h duration
  - createDirectionsIntent(): Google Maps navigation intent with generic geo fallback
  - getInsiderTipText(): contextual tips matching iOS logic (free/downtown/category-based)
  - shareText: formatted share string with title, date, location
- Updated MainNavHost.kt: wired EventDetailScreen with ViewModel, share intent, calendar intent, directions, subscription navigation
- Top app bar with Share and Favorite buttons (heart toggles red), back navigation
- Full-screen image viewer integration via FullScreenImageViewer component
- Files created: EventDetailScreen.kt, EventDetailViewModel.kt
- Files modified: MainNavHost.kt, prd-android.json
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Calendar integration uses Intent(ACTION_INSERT) with CalendarContract — no WRITE_CALENDAR permission needed for insert intent (system calendar app handles it)
  - Directions: try Google Maps package first (com.google.android.apps.maps), fall back to generic geo: URI if not installed
  - EventDetailScreen is a stateless composable — all state comes from ViewModel via MainNavHost, matching the pattern established in HomeScreen
  - generateInsiderTip is a private function (not in ViewModel) since it's pure UI text generation — also duplicated in ViewModel for potential non-UI use
  - RelatedEventCard reuses Event.featuredCardAccessibilityLabel for semantics — same as FeaturedEventCard in HomeScreen
  - FullScreenImageViewer is shown as an overlay (not navigation) matching iOS .fullScreenCover behavior
  - PremiumBadge() used inline in the locked calendar button — same pattern as iOS EventDetailActions
---

## 2026-03-27 - AND-020 (Android)
- Built complete Restaurants list screen (Dining tab) matching iOS RestaurantsView.swift + RestaurantsViewModel.swift:
  - RestaurantsViewModel.kt: @HiltViewModel with full state management:
    - Filter state: selectedCuisines (Set), selectedPriceRanges (Set), sortBy (6 options), showOpenNowOnly, searchText
    - allRestaurants vs restaurants separation for client-side Open Now filtering (same pattern as iOS)
    - loadInitialData(): cache-first with parallel restaurant + cuisine fetch
    - refresh(): bypass cache pull-to-refresh
    - loadMoreIfNeeded(currentIndex): triggers at count-5 with isLoadingMore guard
    - resetAndFetch(): 300ms debounce via Job cancellation
    - clearFilters(): resets all filter state and sort to defaults
    - activeFilterCount: computed from cuisines, priceRanges, searchText, openNowOnly
    - Combined uiState StateFlow using chained combine() producing RestaurantsScreenState
  - RestaurantsScreen.kt with full UI:
    - SortPicker: horizontal LazyRow with Open Now toggle (green FilterChip) + 6 sort option chips
    - ActiveFiltersBar: filter count + Clear All button
    - ErrorBanner: warning icon + message + retry button
    - RestaurantCard: HStack matching iOS RestaurantCardView — 100dp image, name+favorite, cuisine+price, star rating (1-5 with half-star), location+open/closed status dot
    - RestaurantCardSkeleton: shimmer placeholder matching iOS skeleton layout
    - RestaurantFilterSheet: ModalBottomSheet with cuisine horizontal LazyRow multi-select + price range row ($/$$/$$$/$$$$) + Clear/Done buttons
    - Pull-to-refresh, skeleton loaders (4), empty state, load-more, AdBannerView for free users
    - LargeTopAppBar with filter button + badge count
    - Haptic feedback on filter/sort interactions
  - Updated MainNavHost.kt: wired RestaurantsViewModel via hiltViewModel(), connected all callbacks, LaunchedEffect for loadInitialData, filter sheet state management
- Files created: RestaurantsViewModel.kt
- Files modified: RestaurantsScreen.kt (full rewrite), MainNavHost.kt
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - allRestaurants / restaurants split pattern from iOS is essential for client-side Open Now filter — must filter displayed list without losing server data
  - RestaurantFilterSheet uses ModalBottomSheet without skipPartiallyExpanded (iOS uses .medium detent) — shows at medium height by default
  - PriceRange.entries provides all 4 values ($/$$/$$$/$$$) — use range.symbol for filter matching (same strings stored in DB)
  - FilterChip with custom selectedContainerColor=green for Open Now matches iOS green background toggle
  - RestaurantCard uses Row(image | Column(content)) layout, not Card with vertical stacking — matches iOS HStack pattern
  - Star rating uses tint alpha trick: full=yellow, half=yellow@0.5f, empty=gray@0.3f (approximation of iOS half-star icons)
  - activeFilterCount counts each filter category as 1 (not individual items) — matches iOS exactly
---

## 2026-03-27 - AND-021 (Android)
- Built complete Restaurant Detail screen matching iOS RestaurantDetailView.swift with all 4 sub-sections:
  - RestaurantDetailHeader: 300dp hero image with gradient overlay, cuisine capsule badge (ultraThinMaterial style), restaurant name (up to 3 lines), tap-to-view full-screen image, fork.knife icon placeholder when no image
  - RestaurantDetailInfo: Star rating display (1-5 with half-stars using Star/StarHalf/StarOutline icons + numeric), price range in green, Featured badge (star+text in orange), Location with Get Directions icon button, Distance row, Phone row, Website row, Open/Closed status with CheckCircle/Info icons, About description section with dividers between groups
  - RestaurantDetailActions: HStack of up to 3 equal-width buttons — Call (green, ACTION_DIAL), Website (surfaceVariant, ACTION_VIEW), Directions (primary/blue, Google Maps with geo fallback), all with haptic feedback and accessibility labels
  - RestaurantDetailDiningTips: Premium content card (orange, star icon, contextual tip text based on price + cuisine matching iOS exactly) for Insider+ users, locked upgrade prompt card with lock icon + chevron + subscription CTA for free users
  - AdBannerView at bottom for free users
- Created RestaurantDetailViewModel.kt as @HiltViewModel:
  - loadRestaurant(id): fetches restaurant from repository
  - toggleFavorite(): placeholder for AND-024
  - createCallIntent(): ACTION_DIAL with cleaned phone URI
  - createWebsiteIntent(): ACTION_VIEW with website URI
  - createDirectionsIntent(): Google Maps navigation intent with fallback
  - shareText: formatted share string matching iOS (name + cuisine + location + rating)
  - getDiningTipText(): contextual tips matching iOS logic exactly (price-based + cuisine-based)
- Updated MainNavHost.kt: wired RestaurantDetailScreen with ViewModel, share intent, call intent, website intent, directions with Google Maps + geo fallback, subscription navigation
- Top app bar with Share and Favorite buttons (heart toggles red), back navigation
- Full-screen image viewer integration via FullScreenImageViewer component
- Files created: RestaurantDetailScreen.kt, RestaurantDetailViewModel.kt
- Files modified: MainNavHost.kt, prd-android.json
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Restaurant detail follows same pattern as EventDetail: stateless Screen composable + ViewModel + NavHost wiring
  - Phone intent uses ACTION_DIAL (not ACTION_CALL) to avoid CALL_PHONE permission — opens dialer pre-filled
  - Dining tips use the same generateDiningTip() logic in both ViewModel and Screen (private fun) — Screen version used for remember{} caching
  - Cuisine badge uses Surface with RoundedCornerShape(50) + White@0.2f alpha for frosted glass effect (iOS .ultraThinMaterial equivalent)
  - RestaurantDetailActions conditionally shows buttons only when data is available (callUri, websiteUri, coordinate) — matches iOS conditional Links
  - Star rating uses Icons.Filled.StarHalf for proper half-star display (better than alpha trick used in list cards)
---

## 2026-03-27 - AND-022 (Android)
- Built Attraction Detail screen matching iOS AttractionDetailView.swift with all sections:
  - AttractionDetailHeader: 300dp hero image with gradient overlay, type badge (frosted capsule), attraction name (up to 3 lines), teal gradient placeholder with Place icon when no image, tap-to-view full-screen image
  - AttractionDetailInfo: Star rating display (1-5 with half-stars + numeric), Featured badge in orange, Location row with Get Directions icon button, Distance placeholder row, Website "Visit Website" link row with dividers between groups
  - AttractionDetailActions: HStack of up to 2 equal-width buttons — Website (primary), Directions (blue), conditionally shown based on data availability
  - AttractionDescriptionSection: "About" title + description text in secondary color
  - No premium gating, no favorites — attractions fully accessible to all users (matching iOS)
- Created AttractionDetailViewModel.kt as @HiltViewModel:
  - loadAttraction(id): fetches from AttractionsRepository
  - createWebsiteIntent(): ACTION_VIEW with websiteUri
  - createDirectionsIntent(): Google Maps navigation with package targeting
  - createDirectionsFallbackIntent(): generic geo: URI fallback
  - shareText: formatted matching iOS exactly (name + type + location + rating + "Found on Des Moines Insider")
- Updated MainNavHost.kt: wired AttractionDetailScreen with ViewModel, share intent, website intent, directions with Google Maps + geo fallback
- Top app bar with Share button, back navigation
- Full-screen image viewer integration via FullScreenImageViewer component
- Files created: AttractionDetailScreen.kt, AttractionDetailViewModel.kt
- Files modified: MainNavHost.kt, prd-android.json
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - AttractionDetail is simpler than Event/Restaurant detail: no favorites toggle, no premium gating, no dining/insider tips section
  - iOS AttractionDetailView has no separate ViewModel — all logic is inline. Android version creates a simple ViewModel for consistency with other detail screens
  - Distance row uses "Des Moines area" placeholder — real distance from LocationService will be wired in AND-029
  - Teal (0xFF009688) matches iOS .teal color for the image placeholder gradient
  - Attraction type badge uses Place icon as generic fallback — could map AttractionType.iconName to Material Icons in future
---

## 2026-03-27 - AND-023 (Android)
- Built unified Search screen matching iOS SearchView.swift + SearchViewModel.swift:
  - SearchViewModel.kt as @HiltViewModel with full state management:
    - SearchTab enum (EVENTS, RESTAURANTS, ATTRACTIONS) with displayName
    - SearchScreenState data class with eventResults, restaurantResults, attractionResults, isSearching, hasSearched, totalResults, isEmpty, countFor(tab)
    - Debounced search (300ms) via Job cancellation — same pattern as EventsViewModel
    - Parallel search across all 3 content types using coroutine launch + join
    - Fuzzy search fallback for events and restaurants when full-text search returns empty (matching iOS exactly)
    - Attractions use standard query only (no fuzzy RPC — matching iOS)
    - clearSearch() and refresh() matching iOS SearchViewModel
    - Combined uiState StateFlow using combine() with SharingStarted.WhileSubscribed(5_000)
  - SearchScreen.kt with full UI matching iOS SearchView.swift:
    - Search bar with OutlinedTextField, search icon, clear (X) button, rounded shape
    - Tab picker with animated indicator: icon + tab name + count badge (primary color when selected, capsule background)
    - Search suggestions (pre-search state): Popular Categories grid (8 EventCategory entries with icon + name) and Try Searching list (5 quick suggestions with search icon + arrow)
    - Results list with tab switching: EventCardView (reused from HomeScreen), RestaurantSearchCard (new, horizontal layout with image/name/cuisine/rating/location/chevron), AttractionCardView (new, matching iOS AttractionCardView exactly)
    - Loading view with CircularProgressIndicator + "Searching..." text
    - Empty states: offline (WifiOff icon) and no results (SearchOff icon) with action buttons
    - Haptic feedback on tab selection and category/suggestion taps
    - Full accessibility: tab announces "X results", cards have combined semantics
  - AttractionCardView: Horizontal card with 80dp image (teal placeholder when no image), name, type with icon, rating with star, location, chevron — exact iOS match
  - RestaurantSearchCard: Horizontal card with 80dp image, name, cuisine + price, star rating, location, chevron
- Updated MainNavHost.kt: wired SearchViewModel via hiltViewModel(), connected all callbacks (searchTextChanged, tabSelected, clearSearch, navigation)
- Files created: SearchViewModel.kt
- Files modified: SearchScreen.kt (full rewrite), MainNavHost.kt, prd-android.json
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - SearchScreen is stateless composable receiving SearchScreenState — same pattern as HomeScreen/RestaurantsScreen
  - EventCardView has no onClick param — use Modifier.clickable on the caller side (same as HomeScreen)
  - LazyVerticalGrid inside LazyColumn needs fixed height + userScrollEnabled=false to avoid nested scroll conflicts
  - AttractionCardView is defined in SearchScreen (matching iOS where it's defined in SearchView.swift, not a separate file)
  - Parallel search uses launch + join (not async/await) since results are written to individual StateFlows, not returned
  - Fuzzy fallback: events and restaurants have fuzzySearch RPCs, attractions do not (ILIKE search only)
  - Category icon mapping duplicated from CategoryBadge — could be extracted to shared util in future
---

## 2026-03-27 - AND-024 (Android)
- Built complete Favorites/Saved screen matching iOS FavoritesView.swift + FavoritesViewModel.swift + FavoritesService.swift:
  - FavoritesRemoteDataSource.kt: loadEventFavoriteIds, addEventFavorite, removeEventFavorite, fetchFavoriteEvents, loadRestaurantFavoriteIds, addRestaurantFavorite, removeRestaurantFavorite, fetchFavoriteRestaurants — all matching iOS Supabase queries exactly (user_event_interactions + user_restaurant_interactions tables, interaction_type='favorite')
  - FavoritesException sealed class: NotConfigured, NotAuthenticated, LimitReached(max) — matches iOS FavoritesError enum
  - FavoritesRepository interface + FavoritesRepositoryImpl with:
    - In-memory cache of favorite IDs (_favoriteEventIds, _favoriteRestaurantIds)
    - Free tier limit enforcement (3 total favorites max, matching iOS SubscriptionTier.free.maxFavorites)
    - Local SharedPreferences fallback for restaurant favorites when table unavailable (matching iOS UserDefaults fallback)
    - runCatching error handling matching other repositories
  - FavoritesViewModel.kt as @HiltViewModel:
    - loadFavorites() with guard for duplicate loads
    - refresh() for pull-to-refresh
    - Fetches favorite IDs, then full Event/Restaurant objects
    - Splits events into upcoming/past using OffsetDateTime comparison (matching iOS Date() comparison)
    - removeEventFavorite/removeRestaurantFavorite with optimistic UI update
    - Uses AuthRepository.currentUserId for user identification
    - Combined uiState StateFlow using chained combine() producing FavoritesScreenState
  - FavoritesScreen.kt with full UI matching iOS FavoritesView.swift:
    - Unauthenticated state: FavoriteBorder icon + "Sign In to Save Items" + Sign In button
    - Loading state: CircularProgressIndicator + "Loading saved items..."
    - No favorites: EmptyStateView + SubscriptionBanner (compact)
    - Saved content: FavoritesLimitBanner for free users, total count header, 3 collapsible sections (Upcoming Events, Restaurants, Past Events) with section headers (icon + title + count)
    - FavoriteEventRow: 80dp image + title + date/time + location + past event badge (orange) + red heart remove button with haptic feedback
    - FavoriteRestaurantRow: 80dp image + name + cuisine/price + star rating + location + red heart remove button with haptic feedback
    - LargeTopAppBar with "Saved" title, pull-to-refresh
    - All cards use RoundedCornerShape(14dp) with shadow elevation matching iOS
    - Past events dimmed with 0.6 alpha (matching iOS)
    - Accessibility: semantics contentDescription on cards and remove buttons
  - Updated RepositoryModule.kt with @Binds binding for FavoritesRepository → FavoritesRepositoryImpl
  - Updated MainNavHost.kt: wired FavoritesViewModel via hiltViewModel(), connected all callbacks (navigation, remove favorites, refresh), LaunchedEffect for loadFavorites()
- Files created: FavoritesRemoteDataSource.kt, FavoritesRepository.kt, FavoritesRepositoryImpl.kt, FavoritesViewModel.kt
- Files modified: FavoritesScreen.kt (full rewrite), RepositoryModule.kt, MainNavHost.kt, prd-android.json
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - AuthRepository.currentUserId provides synchronous user ID access — no need to access SupabaseClientProvider directly from ViewModels
  - FavoritesRepository acts as both data access layer AND in-memory state store for favorite IDs — matches iOS FavoritesService.shared singleton pattern
  - Free tier limit counts total favorites (events + restaurants combined) — must check both sets before adding
  - Restaurant favorites have two-level fallback: remote Supabase → local SharedPreferences (table may not exist)
  - FavoritesScreenState.totalFavoriteCount uses favoriteEventIds.size + favoriteRestaurantIds.size (matching iOS FavoritesService.favoriteEventIds.count + favoriteRestaurantIds.count)
  - Event upcoming/past split uses OffsetDateTime.now() comparison, with null dates defaulting to "upcoming" for events and "not past" (matching iOS behavior)
  - FavoritesLimitBanner and SubscriptionBanner already exist from AND-015 — just wire them with correct params
---

## 2026-03-27 - AND-025 (Android)
- Built complete Map/Explore screen with Google Maps matching iOS EventMapView.swift + MapViewModel.swift:
  - Added Google Maps Compose 6.2.1, Play Services Location 21.3.0, Play Services Maps 19.0.0 dependencies
  - Added ACCESS_FINE_LOCATION and ACCESS_COARSE_LOCATION permissions to AndroidManifest.xml
  - Added Google Maps API key as manifest placeholder from local.properties + BuildConfig field
  - MapViewModel.kt as @HiltViewModel with full state management:
    - loadNearbyContent(): tries FusedLocationProviderClient for user location, falls back to Des Moines default
    - fetchAllContent(): independent parallel fetch of nearby events, restaurants, attractions (one failure doesn't block others)
    - search(): fuzzy search events + restaurants, ILIKE search attractions, fits map region to results
    - Toggle visibility: showEvents, showRestaurants, showAttractions
    - Selection state: selectedEvent/Restaurant/Attraction with clearSelection()
    - EventAnnotation with urgencyColor (red=today, orange=≤7 days, blue=future) matching iOS exactly
    - regionFitting(): calculates optimal camera position from coordinate bounds with 30% padding
    - Combined uiState StateFlow using chained combine() producing MapScreenState
  - MapScreen.kt with full UI matching iOS EventMapView.swift:
    - GoogleMap with custom MarkerComposable for each content type
    - Event pins: color-coded by category with category-specific Material Icons, spring animation on selection
    - Restaurant pins: orange with fork/knife icon, selection animation
    - Attraction pins: green with place icon, selection animation
    - Search bar overlay with frosted surface, clear button, search-on-submit
    - Toggle controls (top-right): Events/Dining/Places with active color indicators
    - Pin count badge (bottom center) showing total visible pins
    - Error overlay with Map icon, message, Try Again button
    - Loading pill with "Finding nearby places..." text
    - Popup cards at bottom with slide-in animation: image, title, subtitle, action button (navigate/call/website), close
    - EventPopup: category-colored placeholder, date, location, chevron to detail
    - RestaurantPopup: cuisine, rating star, location, call button (if phone) or navigate chevron
    - AttractionPopup: type name, rating, website button (if available) or navigate chevron
    - User location blue dot shown only when location permission granted
    - Handles offline gracefully (shows cached pins via repository layer)
  - Updated MainNavHost.kt: wired MapViewModel via hiltViewModel(), connected all callbacks
- Files created: MapViewModel.kt
- Files modified: MapScreen.kt (full rewrite), MainNavHost.kt, libs.versions.toml, build.gradle.kts, AndroidManifest.xml, prd-android.json
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Google Maps Compose uses MarkerComposable (not Marker) for custom pin composables — keys array triggers recomposition on state changes
  - FusedLocationProviderClient.getCurrentLocation requires suspendCancellableCoroutine bridge — Priority.PRIORITY_BALANCED_POWER_ACCURACY is good enough for city-level maps
  - MapProperties.isMyLocationEnabled requires location permission at runtime — check with ContextCompat.checkSelfPermission before enabling
  - CameraPosition.fromLatLngZoom is the Google Maps equivalent of iOS MapCameraPosition.region — zoom level ~12-13 covers a city area
  - regionFitting uses zoom heuristic from lat/lng span (Google Maps doesn't use MKCoordinateSpan directly)
  - MapUiSettings.mapToolbarEnabled=false removes the "open in Google Maps" toolbar that appears on marker tap — cleaner for custom popups
  - AnimatedVisibility with slideInVertically + fadeIn creates the iOS .transition(.move(edge: .bottom).combined(with: .opacity)) effect
  - Google Maps API key flows: local.properties → manifestPlaceholders in build.gradle.kts → AndroidManifest meta-data
  - LatLng in this story uses com.google.android.gms.maps.model.LatLng (not data/model/LatLng) — they'll coexist until model LatLng is deprecated
---

## 2026-03-27 - AND-026 (Android)
- Built complete Profile screen and Settings screen matching iOS ProfileView.swift + SettingsView.swift + ProfileViewModel.swift:
  - ProfileViewModel.kt as @HiltViewModel:
    - Auth state listener collecting SessionStatus flow (same pattern as AuthViewModel)
    - loadProfile(): fetches profile and populates editable field StateFlows (firstName, lastName, phone, location, selectedInterests)
    - saveProfile(): updates profile via AuthRepository, refreshes on success, shows success dialog
    - deleteAccount(): calls delete-user-account edge function via supabase-kt Functions plugin, then signs out
    - signOut(): clears auth state
    - resetOnboarding(): delegates to OnboardingPreferences (for debug settings)
    - All dialog state managed via StateFlows (showSaveSuccess, showDeleteConfirmation, errorMessage)
  - ProfileScreen.kt with full UI matching iOS ProfileView.swift:
    - Authenticated view: Avatar circle with initials + display name + email, SubscriptionBanner (FULL), Personal Info section (4 OutlinedTextFields), Interests FlowRow with FilterChips (8 options), Save Changes button with loading spinner, Settings/Website/SignOut/Delete Account buttons
    - Guest view: Person icon, welcome message, Sign In CTA button, SubscriptionBanner (COMPACT), Settings button
    - AlertDialogs: save success, delete confirmation (destructive), error
  - SettingsScreen.kt matching iOS SettingsView.swift:
    - Account section (authenticated only): Subscription row with tier name + chevron, Restore Purchases
    - General: Version display from BuildConfig.VERSION_NAME + VERSION_CODE
    - Notifications: opens Android notification settings (ACTION_APP_NOTIFICATION_SETTINGS)
    - About: Website (ACTION_VIEW), Contact Support (mailto:), Privacy Policy (WebView), Terms of Service (WebView), Rate App (Play Store with market:// + https fallback)
    - Data & Privacy (authenticated only): Delete Account with confirmation dialog
    - Debug section (BuildConfig.DEBUG only): Reset Onboarding via OnboardingPreferences
    - TopAppBar with back navigation
  - Added supabase-functions-kt dependency to version catalog and build.gradle.kts
  - Installed Functions plugin in SupabaseClientProvider for edge function calls
  - Updated MainNavHost.kt: wired ProfileScreen with ProfileViewModel (14 StateFlow collectors), wired SettingsScreen with ProfileViewModel for shared auth/delete state, connected all navigation callbacks
- Files created: ProfileViewModel.kt, SettingsScreen.kt
- Files modified: ProfileScreen.kt (full rewrite), MainNavHost.kt, SupabaseClient.kt, libs.versions.toml, build.gradle.kts, prd-android.json
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - supabase-kt Functions plugin: install(Functions) in client config, access via client.functions.invoke("fn-name")
  - ProfileViewModel shares auth state listener pattern with AuthViewModel — could extract to shared base class in future
  - SubscriptionBanner uses BannerStyle enum (not SubscriptionBannerStyle) and onUpgradeClick param (not onUpgrade)
  - Settings notification intent: ACTION_APP_NOTIFICATION_SETTINGS + EXTRA_APP_PACKAGE for Android O+ system notification settings
  - Play Store intent: market://details?id=pkg first, fallback to HTTPS URL if Play Store app not installed
  - ProfileViewModel and SettingsScreen share delete account logic — ViewModel manages the state, both screens can trigger/display it
  - OnboardingPreferences injected into ProfileViewModel via Hilt for debug reset — clean DI, no manual Context access needed
  - BuildConfig.DEBUG used for conditional debug section (matches iOS #if DEBUG)
---

## 2026-03-27 - AND-027 (Android)
- Built complete Subscription screen and Google Play Billing integration matching iOS StoreKitService.swift + SubscriptionView.swift:
  - Added Google Play Billing Library 7.1.1 (billing-ktx) dependency to version catalog and build.gradle.kts
  - Created BillingService.kt as @Singleton with full BillingClient lifecycle management:
    - PurchasesUpdatedListener implementation for purchase callbacks
    - connect() with BillingClientStateListener + ensureConnected() suspend retry
    - loadProducts(): queries SUBS product details, sorts by price, exposes via StateFlow
    - launchPurchaseFlow(activity, productDetails): launches Google Play purchase UI with offer token
    - handlePurchase(): acknowledges purchase, updates purchased products, syncs to backend
    - Handles PURCHASED, PENDING, USER_CANCELED, ITEM_ALREADY_OWNED states
    - updatePurchasedProducts(): queries active subscriptions and resolves tier
    - resolveTier(): VIP first, then INSIDER, with legacy product ID fallback (matching iOS currentTier logic exactly)
    - restorePurchases(): queries all active subs and syncs all to backend
    - syncEntitlementToBackend(): calls validate-android-receipt edge function with purchaseToken, productId, userId, packageName
    - Retry logic: 3 attempts with exponential backoff (1s, 2s, 4s) matching iOS exactly
    - Grace period: validation failures logged but do NOT revoke local entitlements
    - isTransientError(): detects 5xx, timeout, network, connection errors
    - Exposes currentTier, products, isLoading, errorMessage as StateFlow
    - Helper methods: getInsiderProduct(), getVipProduct(), getFormattedPrice()
  - Created SubscriptionViewModel.kt as @HiltViewModel:
    - Combined uiState StateFlow from BillingService flows + local selectedTier
    - selectTier(), purchase(activity), restorePurchases(), clearError()
  - Created SubscriptionScreen.kt matching iOS SubscriptionView.swift:
    - Sparkles (AutoAwesome) icon, "Unlock Premium" title, subtitle
    - CurrentTierBadge with icon (Person/Star/AutoAwesome) and color (gray/orange/purple)
    - Two TierSelectionCards with pricing, selection state, current plan indicator
    - Purchase button with loading spinner, disabled when no selection or same as current tier
    - Restore Purchases button
    - Insider features list (7 items) with orange icons matching iOS exactly
    - VIP features list (6 items) with purple icons matching iOS exactly
    - Terms of Service / Privacy Policy links navigating to WebView
    - Error message display
  - Updated Config.kt with INSIDER_MONTHLY_PRODUCT_ID and VIP_MONTHLY_PRODUCT_ID
  - Updated MainNavHost.kt:
    - Wired SubscriptionScreen with SubscriptionViewModel, purchase flow (Activity cast), terms/privacy navigation
    - Replaced all hardcoded SubscriptionTier.FREE with live BillingService state via SubscriptionViewModel in: ProfileScreen, EventDetailScreen, RestaurantDetailScreen, SettingsScreen
    - Wired onRestorePurchases in SettingsScreen to SubscriptionViewModel.restorePurchases()
- Files created: BillingService.kt, SubscriptionScreen.kt, SubscriptionViewModel.kt
- Files modified: libs.versions.toml, build.gradle.kts, Config.kt, MainNavHost.kt, prd-android.json
- **Note**: No JDK on this machine — build cannot be verified locally.
- **Learnings for future iterations:**
  - Google Play Billing 7.x: PurchasesUpdatedListener is set on BillingClient.Builder, not as a separate registration
  - enablePendingPurchases() is REQUIRED since billing 3.x — builder will throw without it
  - ProductDetails.subscriptionOfferDetails is a list — first entry contains the base offer with offerToken needed for BillingFlowParams
  - formattedPrice comes from subscriptionOfferDetails[0].pricingPhases.pricingPhaseList[0].formattedPrice (deep nesting)
  - acknowledgePurchase is required within 3 days or Google refunds the purchase — handle it immediately in handlePurchase()
  - BillingService is @Singleton so all SubscriptionViewModels (profile, detail screens, settings) share same billing state
  - Activity reference for launchBillingFlow obtained via context cast in composable — safe since Compose is always hosted in an Activity
  - Backend sync uses buildJsonObject{} (kotlinx.serialization) not @Serializable data class — simpler for one-off payloads
  - supabase-kt functions.invoke returns HttpResponse — parse body as String for simple validation response checking
  - Product IDs (insider_monthly, vip_monthly) differ from iOS (prod_U4oa7Cpn0bRnuo, prod_U4oaGFEy12auTx) — configured per platform in respective stores
---
