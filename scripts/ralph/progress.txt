# Ralph Progress Log
Started: 2026-06-12T01:18:32.104Z

## Codebase Patterns (read before starting)
- **PRD lives at repo-root `prd.json` on `main`** (461 stories). NOT scripts/ralph/prd.json. Pick highest-priority story with passes:false. CORRECTED 2026-08-22: `claude/web-platform-autonomy`, which prd.json still named as branchName, is 556 commits behind main and carries a DIFFERENT, older prd.json (46 stories, 0 open). `develop` is 639 behind main with a third prd.json (41 stories, 31 open). The gitflow in CLAUDE.md is not what the repo does - work branches off `main` and PRs back into `main` (see PR #366). Branch off main.
- **New DB tables/views/RPCs are NOT added to generated `src/integrations/supabase/types.ts`.** Access them with `supabase.from("name" as never)` / `supabase.rpc("name" as never, {...} as never)` and cast results `as unknown as T[]` (see JobHealthPanel, MergeReviewPanel). BUT when you ADD A COLUMN to an EXISTING table you DO hand-add it to the Row block in types.ts (kept alphabetical) so `.eq()/.select()` stay typed.
- **Edge function skeleton:** `handleCors`/`getCorsHeaders` from `_shared/cors.ts`, `requireAdminOrApiKey` from `_shared/apiKeyAuth.ts` (accepts X-API-Key, the SERVICE-ROLE bearer that cron sends, OR an admin JWT — so one gate covers cron + admin re-run), `runJob` from `_shared/jobRunner.ts`. Register the fn in `supabase/config.toml` with `verify_jwt = false` so your own auth gate runs. `Deno.serve(async (req)=>{ const c=handleCors(req); if(c) return c; const cors=getCorsHeaders(origin); const fail=await requireAdminOrApiKey(req,cors); if(fail) return fail; ... })`.
- **jobRunner:** `await runJob('name', async (ctx)=>{ ctx.processed(n); ctx.failed(m); ctx.meta({...}); return summary }, {maxAttempts})`. Returns `{ok,status,result,...}`. `ok=true` even when status==='partial' (ctx.failed>0 but fn didn't throw). Never throws. Counts/metadata land in `automation_job_runs` + the Job Health panel. Add re-runnable jobs to `RERUNNABLE` in JobHealthPanel.tsx.
- **pg_cron migration pattern:** wrap in `IF EXISTS (SELECT 1 FROM pg_extension WHERE extname='pg_cron')`, `cron.unschedule(name) WHERE EXISTS(... )` then `cron.schedule(name, 'min hour dom mon dow', $cron$ SELECT net.http_post(url := current_setting('app.settings.supabase_url')||'/functions/v1/<fn>', headers := jsonb_build_object('Authorization','Bearer '||current_setting('app.settings.supabase_service_role_key'),'Content-Type','application/json','x-trigger-source','cron')) $cron$)`.
- **Admin-gated SECURITY DEFINER RPC (callable by BOTH the service-role edge fn and admin users):** pin `SET search_path = public, pg_temp`; gate with `IF auth.uid() IS NOT NULL AND NOT public.is_admin() THEN RAISE EXCEPTION ...` (service role → uid null → allowed; non-admin authenticated → blocked); then `REVOKE ALL ON FUNCTION ... FROM PUBLIC; GRANT EXECUTE ... TO authenticated, service_role`. `public.is_admin()` (no-arg, uses auth.uid()) already exists.
- **Trigram fuzzy infra:** pg_trgm enabled + GIN indexes on `events(title,venue,location)` and `restaurants(name,cuisine,location)` (migration 20250107000007). Use `a.col % b.col` (index-probing operator) + `similarity(a.col,b.col) >= threshold`. Compute geo distance in plain SQL via haversine (acos/sin/cos/radians) from latitude/longitude — avoids a PostGIS dependency.
- **Repointing all child FKs generically** (merge/dedupe/delete-with-children): loop `pg_constraint` where `contype='f' AND confrelid=<table>::regclass AND array_length(conkey,1)=1` and the referenced col is `id`; `EXECUTE format('UPDATE public.%I SET %I=$1 WHERE %I=$2', child_table, child_col, child_col)`. Catch `unique_violation OR check_violation` and fall back to a ctid row-by-row move that DELETEs only the genuine collisions.
- **Admin tabs:** add `{id,label,icon}` to `CONTENT_TABS` (AdminContent.tsx) or `SYSTEM_TABS` (AdminSystem.tsx) + render `{activeTab === "id" && <Panel/>}`. Panels: `useQuery` with `refetchInterval`, admin-gated by RLS `is_admin()` + `useAdminAuth()`.
- **Favorites are NOT a `favorites` table** — event favorites live in `user_event_interactions (event_id, interaction_type='favorite')`.
- **This environment HAS production database access.** `.env` carries `SUPABASE_DB_URL` and `psql` is on PATH. Multiple stories (WEB-QA-003, WEB-QA-020, WEB-CI-026, WEB-SEO-017) assert "this environment has no production DB access" and every one of them is wrong. Read freely; to test a change safely wrap it in `BEGIN; ... ROLLBACK;` and call the function through PostgREST afterwards to confirm the client-visible behaviour. Applying a migration to production is the OWNER'S call - ask first.
- **`RETURNS TABLE(col, ...)` makes every listed column an OUT PARAMETER in scope for the whole PL/pgSQL body.** Any unqualified reference to that name inside the body - especially in a subquery against a table that has a column of the same name - raises 42702 "column reference is ambiguous". Always alias the table and qualify (`FROM ad_impressions ai WHERE ai.campaign_id = ...`). Do NOT reach for `#variable_conflict use_column`: it resolves the ambiguity silently and hides the next occurrence.
- **Fixing one error reveals the next one; do not assume the story's recorded error is still the live error.** get_active_ads went PGRST203 (overload) -> 42702 (ambiguous column) once the overload was dropped. The event crawler went missing-secrets -> `temperature` kwarg -> 0-events-extracted, one per run. Re-measure against production before trusting any note, however recent.
- **Uniform failure durations are a fingerprint.** Eight crawler runs at ~1m45s meant one early step, not flaky scraping. Conversely, do not read a job list's duration column without checking WHICH job failed - the e2e ~30m entries were the quarantined suite being cancelled, while the required Smoke job had already failed at 3m06s.
- **`gh` resolves to the WRONG repo by default.** Two remotes: origin `dj-pearson/desmoines-ai-pulse` (real) and upstream `dj-pearson/blank-canvas-events`. A bare `gh` command answers about UPSTREAM - it reports 1 workflow instead of 22 and no runs since 2025-11-11, which reads exactly like "CI is dead" and is not. Run `gh repo set-default dj-pearson/desmoines-ai-pulse` (git-config-local, does not survive a fresh clone) or always pass `-R`. Distrust any prior story note sourced from a bare gh call.
- **GitHub Actions secrets are split across two scopes.** Repository secrets hold the VITE_*/mobile-signing set; `ANTHROPIC_API_KEY` and `SUPABASE_SERVICE_ROLE_KEY` live ONLY in the **`Scrape` environment**. A job that does not declare `environment: Scrape` gets the empty string for those two, with no warning - the job just fails downstream. Check with `gh api repos/OWNER/REPO/environments/Scrape/secrets`. `EDGE_FUNCTION_API_KEY` and `CLOUDFLARE_DEPLOY_HOOK_URL` exist in NEITHER scope.
- **Lint:** `npm run lint` (`eslint .`) exits 1 due to a PRE-EXISTING parser false-positive at `src/components/media/VideoPlayer.tsx:587` (',' expected) — tsc accepts the file; it predates these stories. Verify YOUR files with targeted `npx eslint <files>` (warnings like no-console are allowed across the repo; only 0 errors matter).
- **Deno parse check (no Supabase in container):** `npx esbuild supabase/functions/<fn>/index.ts --bundle --external:https://* --format=esm --platform=neutral --outfile=/dev/null`. Migrations can't be applied locally — write them additive + idempotent (IF NOT EXISTS / CREATE OR REPLACE / guarded DO blocks).

- **Soft-hide lifecycle pattern (is_hidden + hidden_at):** like is_merged but with a re-hide guard. The hide job only sets is_hidden where `hidden_at IS NULL`; restore (`unhide`) sets `is_hidden=false` but LEAVES `hidden_at` populated — so an admin-restored row is permanently skipped by the nightly hide job. Public sweep is small in practice because most event queries already `.gte('date', today)` (hidden ⇒ >N days past ⇒ already excluded); only queries that fetch PAST events (by-id from trending_scores, featured-without-date, the no-date sitemap lib) genuinely needed the filter. When adding `.neq('is_hidden', true)` to a `.single()` fetch, switch it to `.maybeSingle()` — the filter can now legitimately return zero rows and `.single()` throws.
- **cleanup-old-events was a hole:** it was verify_jwt=true (not in config.toml) with no admin check, so ANY authenticated user could invoke it and trigger deletes. Modernizing a cron/admin edge fn to verify_jwt=false + requireAdminOrApiKey both closes that AND lets the cron service-role bearer + admin-panel JWT through one gate.
- **archived_events (LIKE events INCLUDING ALL) is now STALE** — events has gained geom/is_merged/merged_*/recurrence_*/instance_date/is_hidden that archived_events lacks, so any `INSERT INTO archived_events SELECT *` (legacy cleanup_old_events) now fails on column mismatch. Prefer a NEW explicit-column snapshot table over `SELECT *` into a LIKE-clone.
- **Reviews live in `user_ratings` (rating + review_text), NOT a reviews/event_reviews table.** Write path: `useRatings.submitRating` (upsert onConflict user_id,content_type,content_id). ONLY public reader of user_ratings is `useRatings.fetchRatings` (feeds RatingSystem); the two GooglePlaces*Tools are admin-only. So a review visibility filter only needs to go in fetchRatings. `content_rating_aggregates` (numeric rollup) is maintained by a server trigger and is independent of review_text visibility.
- **Anonymous UGC can't self-invoke an auth-gated edge fn.** Contact submissions are mostly anon (contact_submissions.user_id null), and `supabase.functions.invoke` from an anon browser sends only the anon key → fails an owner/admin in-fn check. For anon write-path side-effects (moderation, notifications), use an `AFTER INSERT` trigger that calls the fn via `pg_net` (`net.http_post`) with the service-role bearer (`current_setting('app.settings.supabase_service_role_key')`) — server-side, works for anon, and no anon caller ever reaches paid AI. Guard the trigger body in `IF EXISTS (pg_extension WHERE extname='pg_net')` + a BEGIN/EXCEPTION WHEN OTHERS THEN NULL so a failed enqueue never blocks the insert. Logged-in UGC (reviews) CAN invoke inline with the user's JWT (owner check passes) when you want a synchronous verdict.
- **Hidden-until-scored vs default-visible:** add the visibility column with `DEFAULT 'approved'` (existing rows + old readers stay visible = backward-safe). For PUBLIC content that must be hidden until scored (reviews), have the CLIENT insert the column = 'pending' EXPLICITLY (the default only protects existing/other-path rows). For ADMIN-ONLY content (contact inbox), keep the default 'approved' and let moderation only DEMOTE — don't hide legitimate intake behind a scoring delay.
- **Job Health re-run sends `{manual:true}`** (JobHealthPanel.rerun), not your custom body. A fn with multiple modes must treat `manual===true` as its default/safe mode (e.g. a sweep), else the re-run button 400s. Other single-purpose jobs just ignore the manual flag.
- **PAUSE semantics for a GATING pipeline:** when the paused job is a visibility GATE (not a generator), pausing must TRUST-THROUGH (force-approve pending items) — otherwise pending content is stranded hidden forever. Contrast generator pipelines (article/digest) where pause just early-exits with no side effect.

---
## 2026-06-12 - WEB-AUTO-006 (Stale-event lifecycle: soft-hide → archive → purge)
Replaced the single-phase hard-delete with a two-phase, observable, recoverable lifecycle.

Files added:
- supabase/migrations/20260612000012_event_lifecycle_columns.sql — additive events.is_hidden (NOT NULL default false) + hidden_at + partial index.
- supabase/migrations/20260612000013_event_lifecycle_functions.sql — event_archive snapshot table (RLS admin-read) + hide_stale_events / archive_and_purge_events / unhide_stale_event RPCs (search_path-pinned, REVOKE FROM PUBLIC, admin/service-role gated).
- supabase/migrations/20260612000014_event_lifecycle_cron.sql — retire legacy 'cleanup-old-events-weekly' SQL cron, schedule 'event-lifecycle-cleanup' nightly 04:15 UTC → cleanup-old-events fn.

Files changed:
- supabase/functions/cleanup-old-events/index.ts — full rewrite: requireAdminOrApiKey gate, phase-1 hide RPC + phase-2 (storage cleanup → archive_and_purge RPC), counts via jobRunner, dryRun + back-compat retentionMonths preserved.
- src/integrations/supabase/types.ts — is_hidden/hidden_at on events Row/Insert/Update.
- Public sweep (.neq('is_hidden', true)): src/hooks/useEvents.ts, src/lib/sitemap.ts, src/lib/sitemapEnhanced.ts, supabase/functions/generate-sitemaps/index.ts, src/hooks/useTrending.ts (+ by-id → maybeSingle), src/hooks/usePersonalizedRecommendations.ts, src/hooks/useSmartRecommendations.ts (+ by-id → maybeSingle), src/hooks/useEnhancedRecommendations.ts, src/hooks/useAdvancedSearch.ts, src/components/SearchSection.tsx, src/components/SearchAutocomplete.tsx, src/pseo/components/sections/PseoLiveListings.tsx.
- src/components/admin/JobHealthPanel.tsx — 'Event Lifecycle' KPI card (hidden/archived/deleted).
- docs/AUTOMATION_JOBS.md — job row + two-phase/recovery section.
- prd.json — WEB-AUTO-006 passes:true.

Defaults: hide 30d past date, archive+delete 180d past date (180 ≈ the old 6-month retention, so deletion timing is backward-compatible — just now hidden first + archived first).

Quality: type-check passes; cleanup-old-events + generate-sitemaps parse via esbuild; 12 edited frontend files lint 0 errors (only pre-existing no-console/no-explicit-any/unused-var warnings). Migrations additive/idempotent, not applied in-container.

**Learnings for future iterations:**
- ⚠️ CONCURRENT EXTERNAL WORKING-TREE CHANGES during this iteration: 11 edge functions were DELETED (backfill-coordinates, backfill-all-coordinates-force, bulk-event-updater, extract-catchdesmoines-urls, populate-playgrounds, generate-sitemap, generate-writeup, restart-web-server, test-ai-model, test-article-webhook, test-google-api) with matching config.toml entry removals, plus 3 pre-existing migration-correctness mods (deals CHECK, contact_submissions is_admin(), rls_audit_fixes to_regclass guard). NONE are mine and several deleted fns were security-gated in WEB-SEC-001, so I did NOT commit them (committing endpoint removals risks backward-compat breaks). My config.toml `[functions.cleanup-old-events] verify_jwt=false` edit is entangled in that same file with the external removals, so I LEFT config.toml UNSTAGED — cleanup-old-events therefore runs under the default verify_jwt=true, which is fine: requireAdminOrApiKey is the authoritative gate (cron service-role bearer + admin JWT both pass; anon/non-admin rejected). Follow-up: register verify_jwt=false once the config.toml churn settles, if X-API-Key-without-JWT callers are needed.
- The branch named in prd.json (claude/web-platform-autonomy) is STALE — actual work HEAD is on claude/ios-layout-monetization-plan-5IQFu, which is AHEAD of it (contains WEB-AUTO-005). Stay on the current branch; checking out the named branch would lose committed work.
- Next story WEB-AUTO-007 (AI article pipeline) is the pattern: scheduled fn → suggest-article-topics → generate-article → score → publish/draft/discard, jobRunner-wrapped, with a pause config flag. articles.review_status already exists.

---
## 2026-06-12 - WEB-AUTO-005 (Duplicate venue/event detection + merge queue)
Implemented automatic duplicate detection/merge for events + restaurants, with an admin review queue for ambiguous pairs.

Files added:
- supabase/migrations/20260612000008_dedupe_columns.sql — additive is_merged/merged_into/merged_at on events + restaurants (+ partial indexes). is_merged default false (old readers unaffected); merged_into = plain uuid (no FK) so the repoint loop ignores it.
- supabase/migrations/20260612000009_dedupe_queue_and_audit.sql — content_merge_candidates (0.7-0.9 review queue, unique pair) + content_merges (immutable audit/reversal log); RLS admin-read only.
- supabase/migrations/20260612000010_dedupe_functions.sql — find_duplicate_events / find_duplicate_restaurants (trigram self-join + haversine + richness), merge_duplicate_content (dynamic FK repoint + keep-richer + mark-loser + audit + queue-resolve), unmerge_content (30-day reversal), dismiss_merge_candidate. All search_path-pinned, REVOKE FROM PUBLIC, admin/service-role gated.
- supabase/migrations/20260612000011_dedupe_cron.sql — weekly cron Mon 03:15 UTC → dedupe-content.
- supabase/functions/dedupe-content/index.ts — orchestrator: calls the finder RPCs, auto-merges high-confidence pairs (events: name>=0.9 + same date + <=150m/no-coords; restaurants: name>=0.9 + <=150m), queues the rest, skips far-apart same-name restaurants (franchise guard), all wrapped in runJob.
- src/components/admin/MergeReviewPanel.tsx — Duplicates review UI (side-by-side Keep/Remove, confidence + reason chips, Merge / Swap-which-to-keep / Not-a-duplicate, Run-scan).

Files changed:
- supabase/config.toml — register dedupe-content (verify_jwt=false).
- src/integrations/supabase/types.ts — add is_merged/merged_at/merged_into to events + restaurants Row.
- src/hooks/useEvents.ts, src/hooks/useRestaurants.ts — list queries add .neq('is_merged', true) so merged losers drop from browse.
- src/pages/AdminContent.tsx — new 'Duplicates' tab → MergeReviewPanel.
- src/components/admin/JobHealthPanel.tsx — dedupe-content added to re-runnable jobs.
- docs/AUTOMATION_JOBS.md — job row + "Reversing a merge" section.
- prd.json — WEB-AUTO-005 passes:true.

Quality: `npm run type-check` passes; dedupe-content parses via esbuild; targeted eslint on all edited frontend files = 0 errors (MergeReviewPanel clean). Only lint error repo-wide is the pre-existing VideoPlayer.tsx:587 parser false-positive (untouched).

**Learnings for future iterations:**
- The dynamic-FK repoint loop (pattern above) is the clean way to "repoint child references" without enumerating ~20 child tables; it auto-covers favorites/reviews/attendance/sponsored and anything added later. Composite FKs (array_length>1) are skipped — fine because content ids are single-col PKs.
- Restaurants need a HARD proximity gate (identical-name franchises must never auto-merge); events use same-calendar-date as the cheap join-bounding predicate. Build dedupe rules per content type, not one-size-fits-all.
- merge_duplicate_content also handles the admin path (richer survivor chosen client-side, decided_by='admin') AND resolves the queue row — so the panel just calls the RPC; dismiss needs its own RPC because RLS only grants SELECT on the queue.
- For WEB-AUTO-006 (stale-event soft-hide): the is_merged/.neq pattern + types.ts column-add is the template for is_hidden; that story owns the FULL public-query sweep (this story only filtered the two primary browse hooks).
---

## 2026-06-12 - WEB-AUTO-007 (AI article pipeline: topic → draft → quality gate → auto-publish)
Built the self-running daily local-SEO article pipeline that glues suggest-article-topics + generate-article + a quality gate together with no human in the loop.

Files added:
- supabase/migrations/20260612000015_ai_article_pipeline.sql — additive articles.is_auto_published(bool default false)/quality_score(int)/pipeline_reasons(text[]) + seeds feature_flags row 'ai_article_pipeline_enabled'=true (the pause toggle).
- supabase/migrations/20260612000016_ai_article_pipeline_cron.sql — schedule 'ai-article-pipeline-daily' 0 11 * * * → ai-article-pipeline fn (pg_cron pattern).
- supabase/functions/ai-article-pipeline/index.ts — orchestrator wrapped in runJob: pause-flag check → daily-cap check → pick top deduped topic → generate-article draft → score (wordcount/relevance/readability/uniqueness/SEO + Claude safety) → publish(>=80)/draft-review(50-79)/discard(<50).

Files changed:
- supabase/functions/generate-article/index.ts — ADDITIVE auth: user-JWT path unchanged (authors as that user); service-role/API-key caller (pipeline) may now generate with author_id=null via requireAdminOrApiKey. No tightening; anon still rejected.
- src/integrations/supabase/types.ts — is_auto_published/quality_score/pipeline_reasons on articles Row/Insert/Update.
- src/hooks/useArticles.ts — same 3 fields on the Article interface.
- src/components/ArticlesManager.tsx — 'AI Auto {score}' badge next to auto-published titles.
- src/components/admin/JobHealthPanel.tsx — ai-article-pipeline added to RERUNNABLE.
- docs/AUTOMATION_JOBS.md — job-table row + "AI article pipeline" section (flow, thresholds, pause).
- prd.json — WEB-AUTO-007 passes:true.

Quality: type-check passes; ai-article-pipeline + generate-article parse via esbuild; edited frontend files lint 0 errors. Migrations additive/idempotent (not applied in-container).

**Learnings for future iterations:**
- generate-article ALWAYS inserts a draft row (it has no dry/return-only mode), and its insert is the cheapest way to reuse the full SEO prompt. So the pipeline generates the draft first, THEN scores from the returned article.content, then updates (publish/draft) or deletes (discard). Discards do a real insert+delete — acceptable, keeps generate-article untouched in shape.
- The "additive auth broadening" trick (try auth.getUser(token); if a user, use it; else fall through to requireAdminOrApiKey) is the clean, backward-compat-safe way to let an internal/service caller invoke a function that was previously user-JWT-only WITHOUT breaking the human path. author_id becomes nullable for the non-user caller. Reuse this for any other "make a manual fn schedulable" story.
- PAUSE FLAG pattern: reuse the existing feature_flags table (flag_key/enabled, public-read + admin-write RLS already there) rather than inventing a config table. Seed the flag enabled=true in the migration; the fn reads it with .maybeSingle() and exits early when enabled===false. This is the template for WEB-AUTO-008/010/012 pause toggles.
- config.toml STILL has the external uncommitted churn (11 deleted edge fns + entry removals + 3 migration mods + supabase/.temp) from before WEB-AUTO-005/006 — NOT ours. Do NOT git add config.toml or those files; stage only your own files explicitly. New verify_jwt=false fns run fine under the DEFAULT verify_jwt=true because the cron service-role bearer and admin JWT both clear the Supabase gateway and requireAdminOrApiKey is the real gate.
- Articles consumers key on status==='published' (Articles.tsx, sitemapGenerator) — setting status='published' + published_at is all that's needed for a new article to enter the public site, sitemap, and newsletter pool. No extra wiring.
- Next story WEB-AUTO-008 (auto weekly digest): send-newsletter-campaign(send_now/schedule) + dispatch-scheduled-newsletters exist; missing piece is automated campaign CREATION (assemble top events/restaurants/newest article → create+schedule a weekly_digest campaign). Use the same jobRunner + feature_flags-pause + pre-send validation-gate pattern.
---

## 2026-06-12 - WEB-AUTO-008 (Auto-assembled weekly digest newsletter)
Built the self-running weekly digest: assemble → validation-gate → schedule, sent through the existing dispatch path.

Files added:
- supabase/migrations/20260612000017_weekly_digest_pipeline.sql — additive newsletter_campaigns.campaign_type (nullable; NULL=manual) + partial index + seeds feature_flags 'weekly_digest_enabled'=true (pause toggle).
- supabase/migrations/20260612000018_weekly_digest_cron.sql — schedule 'assemble-weekly-digest' Tuesday 14:00 UTC (pg_cron pattern, body action:'assemble').
- supabase/functions/assemble-weekly-digest/index.ts — orchestrator (requireAdminOrApiKey + runJob): pause check → 6-day idempotency → gather (top events next 7d not hidden/merged, top-rated restaurants, newest published article) → renderEmail (shared CAN-SPAM layout) → pre-send gates (subscribers>0, subject≤120, ≥1 item, links well-formed https under origin) THROW on failure → insert scheduled newsletter_campaigns row (campaign_type='weekly_digest', scheduled_for=now()). Plus action:'preview' (assemble-only, no write) for the admin UI.
- src/components/admin/WeeklyDigestControls.tsx — admin card: Switch pause toggle (writes feature_flags) + 'Preview next week's digest' (invokes preview, renders sanitized HTML + counts).

Files changed:
- supabase/functions/dispatch-scheduled-newsletters/index.ts — wrapped the dispatch loop in runJob (delivered/failed per campaign → Job Health), but ONLY when a batch is claimed (fires every minute), maxAttempts:1 (sends not idempotent). Empty path + response shape unchanged.
- src/components/admin/JobHealthPanel.tsx — assemble-weekly-digest added to RERUNNABLE.
- src/pages/AdminEmail.tsx — render WeeklyDigestControls above the campaigns table.
- docs/AUTOMATION_JOBS.md — job rows + 'Weekly digest newsletter' section.
- prd.json — WEB-AUTO-008 passes:true.

Quality: type-check passes; both edge fns parse via esbuild; WeeklyDigestControls/JobHealthPanel/AdminEmail lint 0 errors. Staged ONLY my files (external churn left unstaged). Migrations additive/idempotent, not applied in-container.

**Learnings for future iterations:**
- TWO separate "weekly digest" systems exist and must not be confused: (1) send-weekly-digest = PERSONALIZED per-user email (RSVPs/favorites/recs) to registered users via user_email_preferences (Sunday cron). (2) newsletter_campaigns / send-newsletter-campaign / dispatch-scheduled-newsletters = BROADCAST campaigns to newsletter_subscribers. WEB-AUTO-008 is the broadcast one.
- SCHEDULE-DON'T-SEND pattern to reuse a send path without an admin JWT: instead of calling send-newsletter-campaign (which requires an admin user JWT + role check), insert a newsletter_campaigns row directly with status='scheduled' + scheduled_for=now(). The existing dispatch-scheduled-newsletters cron (every minute, claim_scheduled_newsletter_campaigns RPC, service-role) picks it up and sends through the identical Resend path — so unsubscribe/compliance/webhooks are byte-identical to a manual send, and the scheduled function never needs to impersonate an admin. This is the clean template for any "make an admin-only send automatable" story (e.g. WEB-AUTO-012 social).
- VALIDATION-GATE-AS-FAILURE: to satisfy "gate failure aborts + alerts via WEB-AUTO-001", just `throw` inside the runJob fn after ctx.meta({aborted,gateFailures}). runJob records status='failed' and fires sendJobAlert. No campaign row is created before the gate, so a failed gate leaves zero side effects.
- jobRunner on a HIGH-FREQUENCY cron (every minute): do the cheap claim/early-exit OUTSIDE runJob and only enter runJob when there's real work, else you create 1440 automation_job_runs/day and bury the panel. Pattern: claim first → if empty return plain 200 → else runJob the batch.
- dispatch reuse: dispatch-scheduled-newsletters sends ONE body_html to all recipients (no per-recipient token substitution), so campaign unsubscribe links are the token-less /profile?tab=settings fallback (renderEmail with a generic recipient). That's an existing limitation of the broadcast path, not something to "fix" here.
- Event detail URLs in emails use /events/${createSlug(title)} (slug derived from title — events have NO slug column); restaurants use /restaurants/${slug||id}; articles use /articles/${slug} (articles DO have a required slug). Mirror createSlug from send-weekly-digest.
- Next story WEB-AUTO-009 (UGC auto-moderation): no moderation exists on review/contact writes. Build a moderate-content edge fn (Claude toxicity/spam scores) called post-insert (default row hidden until scored), a moderation queue table + admin panel, fail-open-with-flag. Prereq for WEB-FEAT-010 photo reviews — route those through the same seam.
---

## 2026-06-12 - WEB-AUTO-009 (AI auto-moderation for UGC: reviews + contact)
Built the write-time moderation seam: AI scores user content, publishes the clean majority instantly, queues judgment calls, and is the reusable seam WEB-FEAT-010 photo reviews plug into.

Files added:
- supabase/migrations/20260612000019_content_moderation_columns.sql — additive moderation_status (default 'approved') on user_ratings + contact_submissions + partial indexes (WHERE <> 'approved').
- supabase/migrations/20260612000020_content_moderation.sql — content_moderation table (one row/item: scores+verdict+admin override, unique(content_type,content_id), RLS admin-read) + decide_moderation(p_id,p_decision) RPC (SECURITY DEFINER, admin/service gated, search_path-pinned, REVOKE FROM PUBLIC) + seeds feature_flags 'content_moderation_enabled'=true.
- supabase/migrations/20260612000021_content_moderation_cron.sql — enqueue_contact_moderation AFTER INSERT trigger on contact_submissions (pg_net server-side, guarded + exception-swallowing) + 'moderate-content-sweep' cron every 15 min.
- supabase/functions/moderate-content/index.ts — single mode {contentType,contentId} (internal/owner/admin auth, jobRunner-wrapped, returns {decision}) + sweep mode {mode:'sweep'} | {manual:true} (admin/service, re-moderates pending reviews <=25/run). Claude haiku scoring -> toxicity/spam/off_topic -> verdict bands; pause = trust-through.
- src/components/admin/ModerationQueuePanel.tsx — Admin queue: flagged/rejected/pending/error items + scores/reasons/excerpt + Approve&publish / Keep hidden (decide_moderation) + Re-moderate-pending (sweep).

Files changed:
- src/integrations/supabase/types.ts — moderation_status on user_ratings Row/Insert/Update.
- src/hooks/useRatings.ts — submitRating inserts review text as 'pending' (bare rating 'approved'), invokes moderate-content inline + awaits verdict (polite toasts), fetchRatings filters pending/flagged/rejected from the public list.
- src/pages/AdminContent.tsx — new 'Moderation' tab -> ModerationQueuePanel.
- src/components/admin/JobHealthPanel.tsx — 'moderate-content' added to RERUNNABLE.
- docs/AUTOMATION_JOBS.md — sweep job row + 'UGC auto-moderation' section.
- prd.json — WEB-AUTO-009 passes:true.

Quality: type-check passes; moderate-content parses via esbuild; useRatings/ModerationQueuePanel/JobHealthPanel/AdminContent lint 0 errors (only pre-existing warnings). Staged ONLY my files (external config.toml/deleted-fn churn left unstaged). Migrations additive/idempotent (not applied in-container).

**Learnings for future iterations:** (key patterns hoisted to Codebase Patterns above)
- Two-mechanism write-path: logged-in UGC (reviews) invokes the fn inline with the user JWT for a SYNCHRONOUS verdict; anon UGC (contact) uses a pg_net DB trigger with the service-role bearer. Don't try to make anon browsers invoke an auth-gated AI fn.
- content_rating_aggregates still counts a rejected review's numeric rating (only review_text is hidden via fetchRatings). Out of scope to rewire the aggregate trigger; note if a future story needs hidden ratings excluded from the average.
- WEB-FEAT-010 (photo reviews) seam is ready: add 'review_photo' to the content_moderation.content_type CHECK + the decide_moderation branch + a loadItem('review_photo') branch in moderate-content (score the caption / run image safety), and insert photos with their own gate column 'pending'. event_photos already has is_approved — reconcile that with moderation_status or route through this queue.
- Next story WEB-AUTO-010 (campaign creative auto-review): same shape — on campaign_creatives insert run automated checks (image dims/aspect, target URL https+resolves, Claude brand-safety on text, advertiser standing); pass -> is_approved auto-set, fail -> stays pending with machine-readable reasons in AdminCampaigns. BACKWARD COMPAT: get_active_ads RPC behavior unchanged (iOS reads it), campaigns/advertisements additive only. Reuse the content_moderation pattern OR campaign-specific columns; AdminCampaigns (~lines 180-200) has the manual is_approved toggle today.
---

## 2026-08-22 - WEB-SEO-017 + WEB-CI-025 (CI was not broken, it was unplugged)
Both priority-1 CI stories turned out to share one cause nobody had looked for: secrets in an environment scope no workflow declared.

Files changed:
- .github/workflows/event-crawler.yml - `environment: Scrape` on crawl-events and on update-ai-config, with the why inline.
- prd.json - branchName retargeted to claude/prd-p1-loop (claude/web-platform-autonomy is 556 behind main and holds a different PRD); WEB-SEO-017 and WEB-CI-025 notes rewritten with the root cause and the remaining blockers.
- scripts/ralph/progress.txt - two new Codebase Patterns (gh default repo, split secret scopes) + corrected the PRD-location pattern.

Repo settings changed (not files):
- `gh workflow enable pr-checks.yml` and `gh workflow enable event-crawler.yml` - both were state=disabled_manually, both now active.
- `gh repo set-default dj-pearson/desmoines-ai-pulse`.

Evidence:
- WEB-CI-025 AC5: npm run validate exit 0 (0 errors, 5111 warnings); npm run build exit 0, vite 23.4s, prerender 35/35 hub routes, 0 failed. Main is green.
- WEB-SEO-017 AC4: eight consecutive scheduled crawler failures 2026-02-05..2026-02-12, each ~1m45s, then disabled on 2026-02-12. Uniform duration = same early step every time.
- Root cause: crawlers/catchdesmoines_crawler.py:90 raises "SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set". Those secrets are environment-scoped to Scrape; crawl-events declared no environment.

**Learnings for future iterations:**
- A story that says a workflow is "switched OFF" is describing the symptom. Ask what it was doing before it was switched off - `gh run list --workflow=X` survives the disable and the uniform ~1m45s duration across eight runs is what pointed at a single early step rather than flaky scraping.
- Expired logs are not a dead end. `gh run view --log-failed` returns HTTP 410 after ~90 days, but `gh run view --json jobs` still returns job names and conclusions forever. "Lint & Typecheck failed, Build skipped" was enough to date the pr-checks failure to a workflow structure that no longer exists.
- Secret existence is not secret reachability. `gh secret list` showed SUPABASE_URL present and looked fine; the two keys that mattered were invisible to that command because they are environment secrets. Always check `gh api repos/.../environments/<name>/secrets` too.
- Before adding `environment:` to a job, check `protection_rules` and `deployment_branch_policy` on that environment. Scrape has neither, so it adds no approval gate - an environment with required reviewers would silently park every scheduled run in a pending approval instead.
- Next: WEB-SEO-017 AC2 cannot fully pass until CLOUDFLARE_DEPLOY_HOOK_URL exists (publish-freshness warns and exits 0 without it, so a successful crawl still reaches no crawler). WEB-CI-025 AC4 needs a throwaway PR to confirm the check reports. Both need a human.
---

## 2026-08-22 (2) - WEB-CI-026 + corrections to WEB-QA-003 / WEB-QA-020
The required smoke gate was red for one reason, and it was not the one the story guessed.

Files changed:
- supabase/migrations/20260822000001_fix_get_active_ads_ambiguous_campaign_id.sql - NEW. Aliases ad_impressions as ai in both frequency-cap subqueries and qualifies every column.
- prd.json - WEB-CI-026 root cause + fix; WEB-QA-003 and WEB-QA-020 premises corrected; WEB-SEO-017 verification.
- scripts/ralph/progress.txt - four new Codebase Patterns.

Applied to production (owner-approved): 20260822000001, recorded in supabase_migrations.schema_migrations.

Evidence:
- Smoke job run 32190783843: 79 passed, 1 failed, 3m06s. Failing assertion tests/route-smoke.spec.ts:72. Console error: get_active_ads 42702 ambiguous campaign_id.
- Cause: RETURNS TABLE(campaign_id uuid, ...) puts campaign_id in scope as an OUT parameter; both subqueries referenced it unqualified against ad_impressions.
- Verified in a rolled-back transaction with all three argument shapes, then applied and re-verified through PostgREST as anon: all four placements return [] instead of 42702.
- Migration state measured directly: 323 applied, 326 on disk, 2 genuinely pending (not the 8 WEB-QA-020 claims). Nothing applied-but-missing-from-disk.

**Learnings for future iterations:** (patterns hoisted to Codebase Patterns above)
- The canonical get_active_ads (enum signature, three params) is NOT defined in any migration in this repo - only the superseded TEXT overload is, in 20260227000000. It existed in the live database with no checked-in source. 20260822000001 now carries the full body, so the file and the database finally agree. Watch for other RPCs in the same state: `SELECT proname FROM pg_proc ...` against the live DB will find them, grep of supabase/migrations/ will not.
- 20260817000002_close_unused_public_bucket.sql is pending and it TIGHTENS access (storage bucket user-uploads -> private). Its filename asserts the bucket is unused and that assertion is unverified. Audit readers before applying it; it is the one pending migration that can actually break something.
- The event crawler's remaining problem is extraction quality, not plumbing: it now calls the API successfully and Claude returns an empty array from ~429k characters of the events page. Worth its own story - likely JS-rendered markup or a prompt that no longer matches the page structure.
---

## 2026-08-27 - WEB-SEO-017 (partial: the ingest path)
- Fixed the crawler's duplicate detection. Two stacked defects, and the first is the one that
  produced the 2026-08-23 "Chef George's Steak Bar" triple with "Duplicates skipped: 0":
  _check_duplicate read event["venue"] raw while _insert_event wrote venue-or-location, so every
  venueless catchdesmoines row compared against "" and could never match a stored row. Both paths
  now share _record_title/_record_venue. Second: the check only ever asked the database and
  returned False under --dry-run, so nothing tracked what this run had already inserted; added
  _seen_keys, populated on both the real and the dry-run insert.
- Key moved from title+venue to title+calendar-date+venue, matching check-duplicate-entities.ts.
  This dedupes LESS on purpose - title+venue collapses a weekly trivia night into one row.
- Files: crawlers/catchdesmoines_crawler.py, crawlers/test_dedupe.py (new, 15 cases),
  .github/workflows/event-crawler.yml, prd.json (notes only).
- Story stays passes:false. AC2 needs a run whose sitemap/prerendered output demonstrably changes,
  and AC4 (why it was switched off) is not recoverable - GitHub purged the February logs.
- **Learnings for future iterations:**
  - A duplicate check must derive its key from the same code the INSERT uses. Any read of the raw
    extraction dict where the write applies a fallback is a permanent dedupe hole, not a race.
  - Assert the harness works before asserting the behaviour. Three cases in test_dedupe.py passed
    green while every _parse_event_datetime call was failing on an unbound CENTRAL_TZ - the
    function swallows its exceptions and returns None, and None == None. The first check in that
    section now proves a date parses at all.
  - crawlers/test_*.py exec a source region of catchdesmoines_crawler.py with the heavy imports
    stubbed, so they run on a bare python BEFORE the ~5min crawl4ai install in CI. test_clean_html
    starts the region at EVENT_LINK_RE; anything needing CENTRAL_TZ must start at
    CATCHDESMOINES_BASE_URL instead.
  - npm run lint cannot run in a fresh remote container - @eslint/js is declared in package.json
    but node_modules/@eslint is absent. npm run type-check works.
---

## 2026-08-27 - WEB-LEGAL-012 (partial: the web read/write path)
- Corrected the story's premise. It records that the web UI "cannot write" event_alerts_enabled
  because useEmailPreferences' interface omits it. It can: useSavedSearchAlerts upserts the column
  and SavedSearchesTab renders it as the dashboard master switch. Different hook owns it.
- The real defect was the READ next to it. alertsPrefQuery discarded its error, so any failure gave
  `undefined !== false` -> true -> the switch renders ON for a user who had turned alerts OFF. Now
  throws; the hook exposes alertsPrefUnavailable/alertsPrefLoading and the switch is DISABLED with
  explanatory copy rather than defaulting to a value it does not have.
- setAlertsPref had onSuccess and no onError, so a failed write was silent - the controlled switch
  just snapped back. Added the error toast.
- useEmailPreferences' existence check used .single() with the error discarded. .single() raises
  PGRST116 for "no row", so it could not tell "no preferences yet" from a network/RLS failure; both
  took the INSERT branch and a real existing row then failed the unique constraint. Now .maybeSingle()
  with the error checked. Its console.error also moved to @/lib/logger.
- Files: src/hooks/useSavedSearchAlerts.ts, src/hooks/useEmailPreferences.ts,
  src/components/dashboard/SavedSearchesTab.tsx, prd.json (notes only).
- Story stays passes:false. AC3 (iOS writes only UserDefaults), AC4 (no Android surface), AC6
  (per-agent tests) and AC2's convergence decision are all untouched.
- **Learnings for future iterations:**
  - RUN `npm ci` FIRST in a fresh remote container. node_modules did not exist, so `npm run
    type-check` and `npm run lint` ran off npx-fetched binaries and reported nothing about the
    source: tsc resolved to 6.0.2 against the `^5.5.3` pin and died on `ignoreDeprecations: "5.0"`
    before reading a file, and eslint died on a missing @eslint/js. Both look like repo breakage and
    are not. After npm ci both are clean.
  - `undefined !== false` is this repo's signature consent bug and it appears on BOTH sides. A
    discarded read error plus a `!== false` default means the UI asserts consent it never read. When
    fixing one, check whether the same expression guards the sender too.
  - Before "the web has no surface for X", grep the column name across src/ rather than reading the
    one hook whose name matches. event_alerts_enabled is written by useSavedSearchAlerts, not by
    useEmailPreferences.
---

## 2026-08-27 - WEB-PERF-020 (AC1 met; AC5 open)
- Critical-path JS 206.4 -> 196.7 KB gz, inside the 200 KB CLAUDE.md budget for the first time.
  Entry chunk 76.5 -> 66.8 KB gz. Baseline locked at 196.7.
- Cause: SessionManager is mounted at the app root and statically imported SessionTimeoutWarning,
  so the whole Radix AlertDialog stack (dialog, alert-dialog, focus-scope, dismissable-layer,
  presence, react-remove-scroll, react-remove-scroll-bar, aria-hidden) rode into the
  render-blocking entry for a dialog that shows after 25 minutes of inactivity. Now lazy, and
  rendered only while isWarning rather than mounted with open={false}.
- No behaviour change: the dialog's own activity listener is redundant with useSessionTimeout's,
  which listens on window for the whole session and dismisses the warning itself.
- Fixed check-bundle-budget.mjs printing "still -3.3 KB over" now that the goal is met.
- Files: src/components/auth/SessionManager.tsx, scripts/check-bundle-budget.mjs,
  bundle-budget-baseline.json, prd.json (notes only).
- Story stays passes:false on AC5 alone (re-measure LCP), which needs a deployment with real data.
- **Learnings for future iterations:**
  - `npm ci` first, then build with placeholder env vars - vite.config's
    assert-html-env-placeholders plugin hard-fails without VITE_SUPABASE_URL, and the values do not
    affect bundle shape:
      VITE_SUPABASE_URL=https://placeholder.supabase.co VITE_SUPABASE_ANON_KEY=x \
      VITE_SITE_URL=https://desmoinesinsider.com npx vite build
    Use `npx vite build` alone, not `npm run build`, which also runs generate-sitemaps and prerender
    (both need a database).
  - To find WHAT is in the entry chunk, attribute its sourcemap rather than guessing: decode
    .mappings, sum generated-column spans per source index, aggregate by module. It named
    tailwind-merge, react-helmet-async and the tooltip/floating-ui graph in one pass and is how the
    AlertDialog stack was spotted.
  - The root-mounted headless component is this repo's recurring bundle defect - sonner via
    GuestFavoriteMigrator, AccessibilityWidget's panel, now SessionManager's dialog. Check
    SessionManager, GuestFavoriteMigrator, GlobalUpgradeModal, OfflineBanner and CookieConsentBanner
    first whenever the entry chunk grows.
  - Lazy-loading a dialog opens a gap before its own event listeners attach. Check whether the
    parent hook already covers those events before assuming the split is behaviour-neutral.
---

## 2026-08-27 - WEB-BE-032 (8 sites, both payment webhooks)
- 239 -> 231 discarded-error sites, 176 -> 170 in edge functions. All eight are AC2 control
  decisions; two of them were the previous sessions' fixes sitting unbaselined.
- stripe-webhook: the existingSubscription lookup decides UPDATE vs INSERT (a dropped error sent a
  paying customer down INSERT - duplicate row or constraint failure after the charge); the invoice
  lookup used .single() so PGRST116 and a real failure were the same non-result; the
  .update({status:'active'}) beside it is the entitlement write and was silent. All three throw now.
  The campaigns lookup does NOT throw but is no longer silent - campaign?.user_id gates the
  advertiser's payment-confirmation notification.
- play-rtdn-webhook: the voided-purchase lookup dropped its error, so a failure left a refunded user
  entitled AND wrote play_rtdn_log status 'no_match' - an affirmative false claim in the audit trail.
  Throws before the log is written. Same for the revocation update and the main subscription lookup.
  The subscription_plans tier lookup stays non-fatal but now logs.
- Files: supabase/functions/stripe-webhook/index.ts,
  supabase/functions/play-rtdn-webhook/index.ts, supabase-error-baseline.json, prd.json.
- **Learnings for future iterations:**
  - THERE IS NO TYPE CHECK ON EDGE FUNCTIONS. subscription-sync-tests.yml runs `deno test` on ten
    files in _tests/ and no `deno check` on any function; tsconfig excludes supabase/. Deno cannot be
    installed in this container - deno.land is 403 through the agent proxy. Parse-only verification
    via the TypeScript compiler API's parseDiagnostics is the most that is available here, and it
    catches syntax, not types. Do not wire a `deno check` step you cannot run.
  - Throwing is right in the two payment webhooks and wrong in most of the remaining 231. Both are
    at-least-once webhooks with a working idempotency guard whose id is recorded only after success,
    so a 500 costs one redelivery. The agent files (agent-subscription-nurture has 9) would abort a
    batch instead.
  - Re-baseline in the same commit as the fix. Two sites sat "fixed but unbaselined" for two
    sessions, and whoever next runs --write locks in whatever the tree happens to hold.
  - Check ratchet exit codes WITHOUT a pipe (`cmd > file 2>&1; echo $?`), and negative-control by
    adding a violating file, not by trusting the green.
---

## 2026-08-27 - WEB-QA-005 (the spend, not the funnel decision)
- generate-itinerary now probes trip_plans before the Claude call and refuses on 42P01/PGRST205 only.
  Stops the recorded sequence where a paying Insider passes the paywall, is billed for a full
  itinerary, and is then shown "Failed to save trip plan" because the table does not exist.
- Fail-open on every OTHER error is preserved deliberately - it is the right rule for a transient
  failure. The only case closed is the one where continuing is guaranteed to cost money and fail.
- No contract change (AC4): same 500 and same { success, error } envelope the catch block already
  returned for this failure. `code: 'trip_storage_unavailable'` is additive.
- The web client needed no change - useTripPlanner already reads the structured body off
  FunctionsHttpError's context, which is how 403 upgrade_required and 429 quota_exceeded reach the UI.
- Files: supabase/functions/generate-itinerary/index.ts, prd.json (notes only).
- Story stays passes:false: AC1's funnel decision and AC2's homepage CTA copy are untouched.
- **Learnings for future iterations:**
  - "Fail open, never block a paying user on our bug" is right for transient errors and wrong for
    42P01. Separate the two by error CODE rather than by whether the read succeeded - the same
    distinction .single() vs .maybeSingle() forces elsewhere in this repo.
  - Put the cheap existence probe BEFORE the expensive call, not in the error handler after it.
    The quota count here was already reading trip_plans and already had the error in hand; it just
    failed open on it, three hundred lines before the insert that would fail.
  - supabase.functions.invoke sets `error` and null `data` on any non-2xx, so a structured body only
    reaches the UI if the caller reads error.context.json(). useTripPlanner.ts:170-185 is the
    reference implementation in this repo.
---

## 2026-08-27 - correction to the WEB-BE-032 entry above
- eslint DOES cover supabase/functions. eslint.config.js matches **/*.{ts,tsx}, ignores only dist and
  tools, `npm run lint` is `eslint .`, and pr-checks.yml runs it. A syntax error in an edge function
  fails a PR today. The parse check I ran was redundant; `npx eslint --quiet` on the three edited
  functions is zero errors.
- The real gap is narrower and still real: no TYPE check. tsconfig excludes supabase/, no workflow runs
  `deno check`, and eslint runs without type-aware rules - so a wrong property on a Supabase response
  passes everything. The ask is `deno check` on changed functions, not lint coverage.
---

## 2026-08-27 - WEB-BE-032 (21 sites; the duplicated-agent finding)
- 231 -> 210 sites, 170 -> 149 in edge functions.
- STRUCTURAL: 40 agent-* edge functions, 36 _shared/agents modules, 12 names in BOTH, and both are
  live - pg_cron posts to the standalone, agent-runner dispatches the shared one via registry.ts.
  Each standalone carries its own full inline implementation, not a wrapper. Earlier WEB-BE-032 work
  fixed the SHARED copies of subscription-nurture, churn-winback, billing-selfservice and
  lead-sourcing and left the standalones. All 12 pairs now agree; re-measured from source, 0 diverge.
- Two sites reached a user, both in subscription-nurture: cappedRecently returned false on error
  ("not capped", so mail sends) and the entitlement check returned null ("not subscribed", so a
  paying customer got an upgrade prompt under a comment calling it entitlement-safe). Both fail
  closed now.
- Files: the four agent-*/index.ts, supabase-error-baseline.json, prd.json.
- **Learnings for future iterations:**
  - BEFORE fixing an agent, check for a twin: `ls supabase/functions/_shared/agents/` against
    `ls -d supabase/functions/agent-*`. Twelve names exist in both and both run. Fixing one is half
    a fix, and the ratchet cannot see it because it counts per file.
  - The treatment convention, taken from the shared copies: work-list read THROWS (runAgent records
    status failure in automation_job_runs); per-item read LOGS and continues; cap and entitlement
    reads FAIL CLOSED, because both read as permission to send when they fail; a multi-source sweep
    collects into unreadableSources and names the count in ctx.summary.
  - runAgent turns a throw into a recorded failure rather than an aborted batch with no trace, which
    is why "never throw in an agent" was too blunt a rule - it is right per-item and wrong per-batch.
---

## 2026-08-27 - WEB-LEGAL-006 second code path + correction to yesterday's entry
- CORRECTION to the entry above: pg_cron does NOT post to the standalone agent-* functions. Migration
  20260710000000_repoint_agent_crons_to_runner.sql repointed all 34 agent crons to
  /functions/v1/agent-runner/<key>, and it is not in the drift baseline, so it applied. The scheduler
  runs the _shared copy. The standalones are admin/API-key reachable only, and have been drifting
  unscheduled since 2026-07-10.
- WEB-LEGAL-006 is marked done and its fix was on the shared copy only. The standalone still gated the
  trial notice on consented() (AC1) and scoreOutput() (AC2) and sent "Keep your Insider perks" with no
  amount, date or cancel path (AC3). Ported the shared implementation; pair overlap 70% -> 88%.
- _tests/trial-notice.test.ts read one of the two files, which is why nothing caught it. It now loops
  over both. Negative-controlled against git HEAD's standalone: all five assertions fail on it.
- Files: supabase/functions/agent-subscription-nurture/index.ts,
  supabase/functions/_tests/trial-notice.test.ts, prd.json.
- **Learnings for future iterations:**
  - difflib.SequenceMatcher defaults to autojunk=True and is USELESS on file-sized strings - it reported
    two 90%-identical files as 20% similar. Compare normalized-line overlap instead.
  - A source-contract test that names one file is a guarantee about that file, not about the behaviour.
    When an agent or module exists twice, the test must loop over both paths.
  - Before claiming which code path production runs, find the migration that wired it and check it
    against .github/migration-drift-baseline.json. In this repo "the migration exists" and "the
    migration ran" are different facts.
---

## 2026-08-27 - WEB-LEGAL-005 second code path, compliance-monitor, ratchet blind spot
- CORRECTION: there are 34 duplicated agent pairs, not 12. I matched by name equality; 22 pairs have
  different names on each side (agent-backup-verify <-> backup-verifier.ts). The authoritative
  mapping is the VALUES table in 20260710000000_repoint_agent_crons_to_runner.sql. Overlap runs
  78-98%.
- WEB-LEGAL-005 (crisis response, marked done): agent-support-responder had NO crisis handling at
  all. Ported detectCrisisIntent + crisisMessageText before retrieveKb. crisis-support.test.ts drives
  off a SURFACES table that listed the shared file only; added the standalone as a fifth surface,
  negative-controlled against HEAD.
- agent-compliance-monitor's count() did `const { count } = ...; return count ?? 0` with the error
  dropped, so an unreadable table counted as zero and every compliance gate read as "no violations".
  Ported the shared null-returning count() and its exceeds() helper.
- Same shape in agent-backup-verify (failed safe but reported "a core table returned 0 rows" when the
  count could not run). Ported, and closed a gap present in BOTH copies: unreadableTables set
  ok=false but was missing from the incident condition, so it reported failure to nobody.
- check-discarded-supabase-errors.mjs skipped destructures without `data`. It now matches data OR
  count; 7 new sites surfaced, 2 fixed, 5 baselined.
- Files: agent-support-responder, agent-compliance-monitor, agent-backup-verify,
  _shared/agents/backup-verifier, _tests/crisis-support.test.ts,
  scripts/check-discarded-supabase-errors.mjs, supabase-error-baseline.json, prd.json.
- **Learnings for future iterations:**
  - Pair agent files by the migration's mapping table, NOT by name. Name equality finds 12 of 34.
  - A story marked done means done on the path someone checked. In both WEB-LEGAL-005 and -006 the
    AC, the fix and the test all named the same single file, so the second copy was invisible from
    every angle at once. When a story names a file, grep the repo for a second implementation.
  - `count ?? 0` with the error dropped is the same defect as `data ?? []` and reads worse: an empty
    result at least renders as an empty state, a zero count renders as a passing check.
  - The ratchet's own exclusions are assumptions. "`{ count }` alone is usually deliberate" was
    written into the code and was wrong in the single worst place it could have been.
---

## 2026-08-27 - WEB-BE-032 AC4: a check for duplicated-agent drift
- scripts/check-agent-pair-drift.mjs. Pairs come from the VALUES table in migration 20260710000000,
  not from name matching. Compares two exact sets per pair - imported modules, and top-level
  function/SCREAMING_CASE names - rather than a similarity percentage.
- Rejected the percentage-overlap ratchet: the pairs sit at 78-98% for ordinary reasons, so any
  tolerance wide enough not to fire on an unrelated edit is wider than a three-line safety guard.
- Negative-controlled in git worktrees at fe1ae39 and b6e458d: flags all three real divergences, each
  named by the artefact that carried the fix (trialNotice/sendNotice, crisisSupport, exceeds).
- First run found fetchWithTimeout in two standalones and not their shared copies. NOT a defect - the
  shared calls already passed AbortSignal.timeout, which binds before the wrapper's outer bound.
  Converged the shared copies rather than adding an exemption.
- Wired into pr-checks.yml (node builtins, no npm ci) and `npm run check-agent-drift`.
- Files: scripts/check-agent-pair-drift.mjs, .github/workflows/pr-checks.yml, package.json,
  _shared/agents/support-responder.ts, _shared/agents/ticket-classifier.ts, prd.json.
- **Learnings for future iterations:**
  - To prove a check would have caught a past defect, run it in a git worktree at the commit before
    the fix: `git worktree add -q --detach <dir> <sha>` then copy the script in. Asserting it "would
    have" without that is the same as not testing it.
  - When a new check flags something real but harmless, converge the code rather than exempting it.
    Every exemption is a place the check has been told not to look, and this repo already has one
    that hid the worst instance of the defect its own script was written for.
  - The check cannot see a guard that adds no import and no function - an inlined `if (x) continue`
    or a flipped comparison. cappedRecently's fail-closed fix would have slipped through it.
---

## 2026-08-27 - WEB-SEC-025 AC7: the guard now covers all nine tally tables
- check-ballot-reads.sh watched `votes` alone. It now watches votes plus the eight AC7 found with the
  same shape (a count rendered, a user identity exposed). Authored-content tables from the same sweep
  are deliberately excluded.
- WINDOW BUG fixed first: the guard read 8 lines after each .from() looking for an own-row filter,
  which is enough for a chained .eq() and not for a multi-line .select(`...`) template.
  ProfilePage.tsx:42 and :148 put .eq('user_id', ...) eleven lines down, so widening the list without
  this would have reported two correct reads as leaks. Now reads to the statement's semicolon,
  capped at 40 lines.
- 18 references classified across web, iOS and Android; exactly one is a real tally read
  (useCommunityFeatures.ts:217, event_attendance). Recorded in ALLOWED_TALLY_READS keyed on FILE and
  TABLE, not on a line number.
- Negative-controlled both ways: a new unfiltered read on photo_likes exits 1; emptying the allowlist
  exits 1 naming :217.
- Files: scripts/check-ballot-reads.sh, prd.json.
- **Learnings for future iterations:**
  - Before widening a guard's INPUT, check its DETECTION on the new inputs. The 8-line window had been
    correct for every votes query ever written and was wrong for the first event_attendance one.
  - `git checkout -- <file>` to undo a one-line test edit throws away every other edit in that file.
    I lost the whole patch that way and had to reapply it. Undo the line, not the file.
  - Reachability from this container, measured: the agent proxy allows package registries, GitHub and
    api.anthropic.com. desmoinesinsider.com and deno.land both answer 403 to CONNECT. So no live
    verification of SEO claims, no Supabase, no Deno - `curl -sS "$HTTPS_PROXY/__agentproxy/status"`
    lists the policy and the recent denials.
---

## 2026-08-27 - WEB-BE-032: the security and fraud detectors report their own blindness
- 231 -> 206 sites. 10 fixed across four files (two agents, both copies each).
- Prioritised by what actually runs: 46 of the remaining sites were in _shared/agents modules, the
  path agent-runner dispatches and pg_cron reaches. Sort the baseline by that prefix.
- security-anomaly-detector: all three reads are detector work lists. A dropped error yields an empty
  array, no incident, and an "all clear" run. Its own comment records that the login_attempts query
  used to 42703, so the credential-stuffing detector has never seen an attempt.
- fraud-abuse-detector: both tables (payments, usage_events) are 42P01 today, so both detectors have
  always reported nothing.
- Neither throws. Independent detectors run in one pass and one unreadable table must not stop the
  others; the failure is raised as a highest-tier finding through the same channel as a real one,
  deduped on `detector_source_unreadable:<table>`, and named in ctx.summary.
- Files: _shared/agents/{security-anomaly-detector,fraud-abuse-detector}.ts,
  agent-{security-monitor,fraud-monitor}/index.ts, supabase-error-baseline.json, prd.json.
- **Learnings for future iterations:**
  - The treatment convention has FOUR cases, not three: work-list THROW, per-item LOG,
    cap/entitlement FAIL CLOSED, detector-source RAISE AS A FINDING. A detector that throws stops
    its siblings; a detector that logs is invisible in the surface people actually read.
  - Prioritise WEB-BE-032 sites by `supabase/functions/_shared/` prefix. Those are the ones cron
    reaches; the agent-* standalones are admin-reachable only.
  - check-agent-pair-drift.mjs now binds real work: both fixes added a top-level function, so
    forgetting the twin would have failed CI rather than becoming next week's finding.
---

## 2026-08-27 - WEB-BE-032: the three messaging agents (18 sites)
- 206 -> 188 overall; scheduled path 46 -> 32. dormant-reengagement, onboarding-drip and
  weekly-digest-personal, each on both copies.
- Two work lists now throw: onboarding-drip's new-profile read and weekly-digest's recipient read.
  Both sent NO mail on a failed read and still reported success, so either campaign could stop
  entirely without anyone seeing it stop.
- The digest's tier read is DELIBERATELY not fail-closed. A failed read drops `tier` to "free", so a
  paying subscriber gets the free digest; failing closed would send them nothing, which is the worse
  harm for a digest they asked for. Logged instead.
- Files: _shared/agents/{dormant-reengagement,onboarding-drip,weekly-digest-personal}.ts and
  agent-{reengagement,onboarding-drip,weekly-digest}/index.ts, supabase-error-baseline.json, prd.json.
- **Learnings for future iterations:**
  - "Fail closed" is NOT a rule about entitlement reads. It is a rule about which direction reaches a
    user with something they did not ask for. subscription-nurture's upgrade prompt fails closed
    because not knowing whether someone pays is a reason to say nothing; the weekly digest's tier read
    must NOT, because failing closed there withholds a send the user asked for. Ask what the failure
    SENDS, not what it reads.
  - An activation stamp guarded by `activated_at IS NULL` is always safe to log-and-continue: the row
    stays eligible and the next run retries it.
  - After each burn-down, re-sort the baseline by the `supabase/functions/_shared/` prefix to see what
    is left on the path cron actually reaches. 32 sites in 20 files now, none above 4 - the rest is
    long rather than deep.
---

## 2026-08-27 - WEB-BE-032: lead-enrichment + outreach-sequencer (12 sites)
- 188 -> 176 overall; scheduled path 32 -> 26.
- outreach-sequencer's template guardrail already failed SAFE and did so silently: a dropped error
  left step1 undefined, so the sequence stopped and queued an approval - indistinguishable from the
  template genuinely not being approved. It still stops; it now says which happened.
- lead-enrichment's per-item lookups return `empty` on error, whose nulls are written as the
  enrichment RESULT and scored. "We looked and there is nothing" and "we could not look" are
  different claims about a lead.
- Two more work lists throw (new crm_leads, qualified leads).
- **Learnings for future iterations:**
  - A guard that fails safe is not the same as a guard that is working, and the ratchet cannot tell
    them apart - both are a site with no `error` binding. Read the failure path before assuming a
    fail-safe site is low value; the silent version costs someone a debugging session.
  - Where a helper returns a neutral-looking default on error (`empty`, `{}`, `0`), check what
    CONSUMES it. If the default gets persisted or scored, it is an assertion, not an absence.
---

## 2026-08-27 - the edge functions are type-checked now (partly), without Deno
- `npm run type-check:edge` -> tsconfig.edge.json, wired into pr-checks.yml. Two shims in
  supabase/functions/_typecheck/: remote.d.ts declares the eleven names imported from https
  specifiers as `any` (mapped via paths: {"https://*": [...]}), deno.d.ts declares the Deno globals
  actually used. No safety at the boundary; full checking inside our own modules.
- SCOPED TO _shared/ deliberately. Measured: supabase/functions/** is 132 errors loose / 181 with
  strictNullChecks, overwhelmingly shim imprecision; _shared/** is ZERO. A wide gate would be red on
  day one - the WEB-CI-020/021 trap.
- Negative-controlled with a wrong property on a locally-typed object in _shared/crisisSupport.ts:
  exit 2 naming file, line and property, in 2.1s.
- **Learnings for future iterations:**
  - A type check configured with strictNullChecks:false does not just find fewer bugs, it INVENTS a
    class of them. The first wide run flagged 67 "Property 'reason' does not exist on ClaudeTextResult"
    across nine functions; the code is correct, and narrowing a `ok: true | false` discriminated union
    needs strictNullChecks. I had the finding half written before checking the definition. Check a
    flagged union against the compiler options before believing it.
  - tsc CAN check Deno code: map the https specifiers with `paths` to a loose .d.ts and declare the
    Deno globals. `moduleResolution: Bundler` + `allowImportingTsExtensions` handles the .ts imports.
  - Measure the wide scope before choosing it. _shared/ was clean at zero and the full tree was not
    close - that measurement, not a preference, is what set the scope.
---

## 2026-08-27 - the new edge type check, run wide, found a dead deployed function
- assign-role/index.ts imported `corsHeaders` from _shared/cors.ts, which exports getCorsHeaders and
  handleCors and never exported that name. Deno throws "does not provide an export named" at MODULE
  LOAD, so this DEPLOYED function could not start at all and every role assignment through it failed.
  Fixed to handleCors + getCorsHeaders with the per-request headers threaded through json().
- WHY NOTHING CAUGHT IT: `npm run check-imports` is WEB-QA-001's own answer to this exact defect and
  its ROOTS are src/ only; tsconfig excludes supabase/; eslint runs no type-aware rules there.
- scrape-events had a second return block, unreachable behind the return above it, referencing five
  out-of-scope identifiers. Never ran, and read as if the function checks its insert error and
  reports duplicates_skipped. Deleted.
- NOT fixed, checked first: six of eight extractors omit source_url from a return type that requires
  it, but the insert compensates with `eventData.source_url || job.config.url`. Type inaccuracy, no
  runtime consequence; churning eight signatures for no behaviour change is worse than a note.
- **Learnings for future iterations:**
  - Run a new check WIDE as a measurement even when you gate it narrow. The gate covers _shared/
    only; assign-role is in a function directory and would never have been found by the wired check.
  - `Cannot find name 'x'` in a long function usually means unreachable code after a return, not a
    typo. Read the surrounding control flow before "fixing" the identifier.
  - Before writing up a type error as a defect, find what CONSUMES the value. source_url looked like
    six broken extractors and is one deliberate fallback at the insert.
---

## 2026-08-27 - the wide edge type check is a gate now (as a ratchet)
- scripts/check-edge-types.mjs, npm run check-edge-types, wired into pr-checks.yml. Per-FILE counts,
  fails only when a file gains an error. 166 errors across 32 files baselined; runs in 4s.
- Needed separately from type-check:edge: assign-role was found by running that gate wide as a one-off,
  not by the gate. Negative-controlled with the same shape (a named import of a symbol cors.ts does
  not export) - exits 1 naming file, line, TS code and message.
- strictNullChecks ON here, OFF in tsconfig.edge.json. With it off, 57 correct uses of
  ClaudeTextResult report as errors because a `ok: true | false` union cannot narrow. A baseline over
  a third fabricated is one people learn to ignore. Price: 18 real "possibly null" findings baselined.
- ~34 of the 166 are shim artifacts (TS2503/TS2688/TS2614/TS2347) and the script header says so.
- **Learnings for future iterations:**
  - A relocated tsconfig relocates EVERY path inside it. I made `include` absolute and left `paths`
    relative, so the https://* mapping stopped resolving and the count came back 423 across 195 files
    instead of 166 across 32. That reads as a worse codebase, not a broken harness, and I nearly
    baselined it. Check a new measurement against the number you already had.
  - When a ratchet's baseline contains known non-defects, name the TS codes in the header. Otherwise
    the next reader treats the whole baseline as a bug list and stops trusting it.
---

## 2026-08-27 - the edge type ratchet's first burn-down: two more broken deployed functions
- 166 -> 163 errors.
- publish-article-webhook read `ssrfResult.safe` / `.reason`; validateURLForSSRF returns
  { valid, error, url }. `.safe` was always undefined, so this DEPLOYED function rejected every
  webhook URL with "Invalid webhook URL: undefined". Fail-closed, so NOT an SSRF hole - article
  publishing through it has simply never worked. Seven other callers read `.valid`. Also switched to
  https-only + blockPrivateIPs to match test-article-webhook.
- ai-crawler called .toISOString() on `parseEventDateTime(x) || new Date(x)`. That function returns an
  OBJECT, not a Date, so the SUCCESS path threw and only an unparseable date reached the fallback.
  Fixed to `?.event_start_utc ?? new Date(x)`, which is what the two sibling call sites already do.
- **Learnings for future iterations:**
  - "Property 'x' does not exist on type 'never'" is `const a = []` inferred as never[], not a defect.
    Triage the baseline by message, not by count: the two real finds were a wrong property on a
    LOCALLY-DEFINED interface, which is the shape worth chasing.
  - When one call site disagrees with seven others, the seven are the spec. Both fixes here were
    confirmed against sibling call sites rather than reasoned out.
  - Three deployed functions in two days were broken by a name that was never right. Each needed only
    a type checker, and none of them had one.
---

## 2026-08-27 - WEB-OPS-022 recorded, and WEB-BE-032's four monitors (14 sites)
- Added WEB-OPS-022 for the venue-default-image work (commit ea1b737) so the pivot is tracked in
  prd.json rather than living as loose commits. AC1-AC4 done; AC5-AC8 are the owner's.
- 176 -> 162 discarded-error sites; scheduled path 26 -> 19.
- edge-metrics-monitor and ci-health-watcher: work lists, now THROW. edge-metrics compared an EMPTY
  current window against an empty baseline and reported healthy, so an error spike and an unreadable
  metrics table were the same output.
- link-content-monitor: both work lists throw. The first is the expired-event archive sweep, so a
  dropped error archived nothing and reported success - the exact outcome WEB-BE-034 AC4 asks someone
  to verify, unverifiable while the read was silent.
- uptime-monitor: LOG, not throw - both reads are per-target. An empty result makes `sustained` false
  so a down target reports as up, which is why unreadable targets are now named in ctx.summary as
  "UNCHECKED: ...". "0 failing" and "could not check" are different answers.
- **Learnings for future iterations:**
  - When inserting after a multi-line Supabase call, find the statement's terminating token by INDEX
    from the anchor and derive the indent from the anchor's line. Guessing the indent literal failed
    on the first try; a helper that computes it did not.
  - `^import [^\n]*\n` matches the OPENING line of a multi-line import. That broke two files this
    session. Anchor on the last line ending in `";` instead.
---

## 2026-08-27 - WEB-BE-032 CLOSED: the scheduled path is at zero
- 30 more sites across two batches. 162 -> 127 overall. Every _shared/agents module agent-runner
  dispatches, and every _shared middleware they run through, now surfaces its read failures.
- Remaining: 64 in admin/API-key-invoked edge functions (a caller sees the response) and 63 in src/
  (a browser console surfaces them). AC5 says this is a cleanup PATTERN not a bug list, and the
  ratchet gates new ones, so those shrink as people touch the files.
- The last batch's real find: imageStorage.ts discarded the error on the media_assets INSERT that runs
  AFTER the upload. A failure left the file in the bucket with nothing pointing at it - invisible to
  consumers AND to reclaim-venue-images.ts, which finds files through media_assets. A storage leak
  with no record, in the same week as a story about reclaiming storage.
- imageFallbacks: a failed local venue lookup looked like "no match" and fell through to a PAID Google
  Places call.
- check-edge-types caught `ctx` out of scope in agent-test-gen - a real error I introduced by porting a
  shared-module fix into a standalone whose wrapper differs. The ratchet paid for itself on my own diff.
- **Learnings for future iterations:**
  - Porting a fix between an agent's two copies is not mechanical: the shared module has `ctx` from the
    AgentRun signature, the standalone builds it inside runAgent. Check scope, do not assume symmetry.
  - A discarded error on a write that follows an upload is a RESOURCE LEAK, not a lost message. Look
    for what was already spent before the failing call.
---

## 2026-08-27 - WEB-OPS-023: serve-time egress, the other half of the image work
- cdnUrlFor wrote a supabase.co storage URL into every image_url and NOTHING cached it. public/_headers'
  immutable rules apply to files Cloudflare Pages serves out of dist/, not to another origin. So every
  card image was billed as Supabase egress once per VIEWER - ingest downloads an image once ever,
  serving downloads it once per visitor.
- Built and inert: functions/media/[[path]].ts (Cloudflare edge cache in front of storage), an
  env-gated switch in cdnUrlFor (MEDIA_CDN_BASE), and scripts/repoint-media-urls.ts for existing rows.
  Nothing changes until the variable is set.
- 27 offline checks, weighted toward the refusals: the path guard is an ALLOWLIST because the route
  forwards a caller-supplied path to a storage origin, and the rewrite must leave external URLs alone.
- **Learnings for future iterations:**
  - When someone asks about egress from scraping, check the SERVE path too. Ingest is O(images),
    serving is O(images x viewers), and the second one had no cache at all.
  - `public/_headers` only governs what Cloudflare Pages serves. A Cache-Control rule there for
    /*.jpg looks like it covers your images and does not if they live on another origin.
---

## 2026-08-27 - WEB-QA-022 CLOSED: the dead event-photo path is deleted
- AC1 asked to decide "BEFORE applying 20260718000003". That migration is applied AND effective, so
  AC1's outcome is a fact, not a preference: Option A shipped. AC4 is not itself a decision, it is an
  instruction conditional on that outcome - "DELETE the losing path rather than leaving it dormant".
- Re-verified before deleting: zero consumers for uploadEventPhoto/getEventPhotos across src/, and
  BOTH paths shared the `event-photos` storage bucket, so nothing was orphaned. That last check
  mattered - deleting an upload path that owned its own bucket would have stranded bytes.
- 108 lines gone. A comment at the same spot records which implementation won and that adopting
  event_photos later needs a moderation column first (`is_approved` has never existed).
- No bundle change: 196.7 KB before and after. Dead code with no importer was already tree-shaken.
- **Learnings for future iterations:**
  - "Decide X before Y" expires when Y happens. Re-read a reserved decision against what has since
    shipped; the reservation may be protecting a choice that no longer exists.
  - Before deleting an upload path, check whether it shares its storage bucket with the surviving one.
    A bucket only the dead path wrote to leaves objects nothing can reach.
---

## 2026-08-27 - AOS-MANAGE-005: the missing write, not the missing dashboard
- The dashboard already exists. ProviderCostTile is mounted in AgentControlPlane:400 and reads
  provider_spend_mtd, which sums a ledger half (automation_job_runs, written by agentRun.ts) and a
  reported half (provider_usage). The reported half had NO WRITER since the table was created in
  20260709000033 - not one insert anywhere in supabase/ or src/. Every non-agent Claude call landed
  in that column, so it landed nowhere.
- New _shared/providerUsage.ts writes it, and is now called from the four functions that spend
  outside the agent runtime: generate-itinerary, discover-chat, ai-crawler, test-ai-model.
- Two placement rules are load-bearing and both are commented in the code:
  - NOT inside runToolLoop. runAgent wraps it and already books the same tokens into the LEDGER half.
    Recording there would put every agent run in both halves of a view that ADDS them, and the
    watchdog would pause agents at half the real spend.
  - In generate-itinerary, BEFORE extractClaudeText and before the trip_plans insert. Everything
    after that point can throw, and all of it throws after Anthropic has already billed. The runs
    that cost money and delivered nothing are precisely the ones worth seeing.
- Pricing moved out of agentRuntime.ts into the shared table. What it replaced had opus at $15/$75
  and haiku at $0.80/$4 - both stale since the 4.6 generation - and folded cache reads into
  input_tokens at the full input rate. discover-chat caches its system prompt and tool schemas and
  re-reads them on all six steps, so that overstated its cost by ~10x on the cached portion.
- Unknown model ids price at the most expensive member of their family, deliberately. A watchdog
  throttling early is recoverable; a watchdog that never fires is what this story exists because of.
- 37 offline checks in scripts/__tests__/provider-usage.test.mjs, including that a failed insert
  returns false rather than throwing - the money is spent by then, and failing the user's request
  over a bookkeeping error trades something they paid for against nothing.
- Also wired the four offline suites into pr-checks.yml. test:venue-images, test:media-route and
  test:media-urls had been written across earlier iterations and never run by CI.
- **Learnings for future iterations:**
  - "Build the dashboard" can mean "the dashboard is built and reads a column nothing writes". Check
    for the writer before adding a view; an all-zeroes panel looks like good news.
  - When a view SUMS two sources, where you record decides whether you double-count. Find the other
    writer before adding one.
  - Record spend at the point the provider bills, not at the point the request succeeds. Those are
    different lines, and the gap between them is where the unexplained invoice lives.
---

## 2026-08-27 - WEB-SEC-025: the last blocking tally read on event_attendance is cleared
- The guard's ALLOWED_TALLY_READS held exactly one entry and it is now EMPTY. getEventCheckIns
  selected `status` for every attendee of an event and counted them in the browser, which meant the
  anon key could enumerate who is going to which event - while the UI it feeds (EventCheckIn.tsx)
  renders four numbers and never a name.
- New migration 20260827000002: event_attendance_tallies(uuid), SECURITY DEFINER, per-status counts,
  no user_id and no row ids. Same shape as voting_category_tallies(), which cleared votes.
- Grouped by the raw `status` text rather than a fixed four-column return, because the column is
  plain text with no CHECK. An unrecognised status still comes back and still counts toward the
  total instead of being dropped by the aggregate.
- THE POLICY TIGHTENING IS NOT IN THE MIGRATION, and the reason is the ordering: a migration applies
  when it is pushed, Cloudflare Pages deploys separately, so tightening in the same file opens a
  window where the live bundle still reads the raw table and every event shows one attendee. The
  exact follow-up SQL is in the migration header. Unlike the votes half of this story it needs no
  mobile release - neither shipped binary references event_attendance - it needs one web deploy.
- Every reader was enumerated before deciding that: ProfilePage.tsx:42 and :148 are own-row,
  getUserEventCheckIn is own-row, updateEventCheckIn is a write, userDataTables.ts is service role,
  ios/ and android/ have zero references.
- check-schema-usage.mjs gained an RPC case for PENDING_MIGRATIONS. It already had the concept for
  tables and columns; without it a function defined by an unapplied migration had to go into
  schema-baseline.json, a file whose own header says it must only ever shrink.
- Negative-controlled three ways: removing the pending entry re-reports the RPC, a bogus .rpc() name
  is still caught, and a re-added raw .from('event_attendance').select('status') fails the ballot
  guard. 7 new vitest cases, one of which fails if the bigint-as-string coercion is removed - "4"
  plus "5" is "45", and the tile would show 45 attendees.
- **Learnings for future iterations:**
  - An allowlist that reaches empty is worth saying out loud in the file. An empty string looks like
    a dead variable to the next reader, and deleting it removes the thing that makes a new entry
    visible.
  - PostgREST returns bigint as a JSON string. Any count() aggregate read straight into arithmetic
    concatenates instead of adding, and the result looks like a plausible number.
  - When a checker already models "pending migration" for one kind of object, extend it rather than
    baselining the new kind. The baseline is the wrong home for something that resolves next push.
---

## 2026-08-27 - WEB-LEGAL-012 AC6: the opt-out is now tested by running the senders
- marketing-consent-contract.test.ts compares the key the clients WRITE against the key the
  classifier READS. That catches a rename and cannot catch a sender that reads the right key and
  mails anyway, or one added later that never checks. The new suite RUNS the agents.
- One per family, as AC6 asks: onboarding-drip (nurture), dormant-reengagement (re-engagement),
  churn-winback (churn), milestone-recognition (milestone), outreach-sequencer (outreach).
- outreach-sequencer does NOT check messagingAllowed and should not. It mails business contacts from
  crm_leads, not users, so its opt-out is the outreach_suppression list. A naive reading of AC6 would
  have added the wrong control to it.
- EVERY TEST IS PAIRED. A mock that returns nothing everywhere makes "no email was sent" true for
  reasons unrelated to consent, so each family also has an opted-IN run asserting it reaches the
  quality gate. Without that half the negative tests prove nothing.
- ALL FIVE NEGATIVE CONTROLS RUN AND ONE FAILED THE FIRST TIME. Forcing consent true in each agent
  made four tests fail; milestone-recognition's still PASSED. `emailed === 0` was true for the wrong
  reason - the quality gate also holds at zero. Fixed by asserting gated === 0, because an opted-out
  user must never reach the gate at all. The other four discriminate on their own.
- The suite DELETES RESEND_API_KEY and the Anthropic keys before importing anything. A CI runner that
  holds them would otherwise have turned this into real outbound mail to example.com addresses.
- check-edge-types caught two shim gaps in my own diff: Deno.env.delete and node:assert were both
  absent from _typecheck/deno.d.ts. Fixed there rather than in the test.
- **Learnings for future iterations:**
  - Deno is reachable here via `npx deno@2` even though deno.land and esm.sh both 403 through the
    proxy. Run the agent suites with --no-check and an --import-map stubbing
    https://esm.sh/@supabase/supabase-js@2, which is only ever imported for createClient and never
    called under test. CI has the network and needs neither flag.
  - Run the negative control for EVERY case, not one representative. Four of five discriminated and
    the fifth did not, and nothing in the passing run said which was which.
  - A test that deletes an API key before importing the module under test is not tidiness. These
    modules POST on the happy path, and the test fixtures carry email addresses.
---

## 2026-08-27 - WEB-SEO-014 AC6: Search Console performance in the SEO admin
- The keywords tab was a placeholder promising GSC integration "coming soon". The tables have held
  1,630 keyword rows and 2,060 page rows since March. The integration was finished; nothing showed it.
- THE PAGE THE AC NAMES HAD NO ROUTE. src/pages/SEODashboard.tsx had zero importers anywhere in src/,
  so building into it would have satisfied the AC and delivered nothing. Routed at /admin/seo with
  AdminNav and listed under Insights. /admin/oauth/callback -> AdminGscCallback had been routed since
  March, so the connect flow was reachable and the page showing its output was not.
- The panel leads with freshness, not with metrics: data window, age of the newest row, last sync,
  next scheduled sync, and a countdown on the OAuth refresh token. Stale past 7 days raises a
  destructive alert; inside 45 days of Google's six-month idle limit raises the token warning.
- AC5 Q1 and Q2 are rendered rather than only documented: queries at position 11-30, and top-ten
  pages with zero clicks. The second excludes pages that never rank - position 42 with no clicks is a
  ranking problem and rewriting its title changes nothing.
- 9 vitest cases against a pure aggregateGscPerformance(property, rows, now). Both load-bearing ones
  negative-controlled by breaking the code; each broke exactly one test.
- One test failed first for MY error, not the code's: I asserted a weighted position of 10.99 landed
  in the page-2 bucket. It does not - the floor is 11. Retuned the fixture to 900 impressions at 12
  against 1 at 3, where the weighted answer (11.99) and the plain mean (7.5) fall on opposite sides
  of the filter. A fixture that does not discriminate proves nothing even when it passes.
- critical path 196.7 -> 197.2 KB gz. Left the baseline at 196.7 rather than ratcheting for 0.5 KB.
- **Learnings for future iterations:**
  - CHECK THE PAGE IS ROUTED BEFORE BUILDING INTO IT. grep for the component name across src/; an
    admin page with zero importers is not a surface. This one had been unrouted long enough that an
    acceptance criterion was written telling someone to use it.
  - PostgREST returns BIGINT as a JSON string, again. This is the second entry in this file about it.
    Any sum over impressions/clicks concatenates and the result still looks like a plausible number.
  - Supabase caps a response at 1000 rows and reports no error. Both GSC perf tables are larger, so a
    plain .select() truncates the aggregate silently. Page with .range().
  - An average position must be weighted by impressions. A row mean lets a single impression at a
    good position rewrite a query's standing.
  - Make the aggregation a pure function taking a now argument. It removes the need to mock the
    client entirely, and freshness assertions are untestable without a pinned clock.
---

## 2026-08-27 - WEB-SEO-013: sitemap-pseo.xml submits the shippable set, not every published row
- 244 URLs -> 18. The sitemap selected is_published + quality_score >= 0.6, capped at 300. AC1/AC2
  were built; the SELECTION was not, and that is the half AC5 exists for.
- quality_score scores the generated PROSE. The floor counts the ENTITIES the page lists. A page can
  be well written about nothing, which is this story's whole failure mode, so it is out of the
  selection rather than kept beside the new filters.
- THE FINDING: a claimed slug does not render the pSEO page AT ALL. React Router ranks a static
  segment above a dynamic one, so /restaurants/asian resolves to /restaurants/:slug
  (RestaurantDetails, looking for a restaurant called "asian"), not to the pSEO catch-all. My first
  version selected 22 URLs and 13 were claimed - submitted soft-404s. AC7 had already counted all 75;
  nothing had connected that list to what the sitemap submits.
- Extracted scripts/lib/pseoShippable.ts and scripts/lib/pseoRouteClaims.mjs so the audit and the
  generator cannot disagree. Verified the refactor by running the old and new check side by side:
  identical 123 pass / 43 thin / 78 empty, and the collision check byte-identical.
- The old comment said the gate could not live in the sitemap step "without N queries". The N queries
  cost 19 seconds for 244 pages. The gate it deferred to never arrived.
- 17 offline checks, 3 negative controls. One control did NOT fire at first and the reason is the
  lesson: my fixture declared the literal route before the parameterised one, so literal-first and
  first-match-wins agreed and the test passed under both. A fixture that cannot distinguish the two
  behaviours proves nothing even when green.
- **Learnings for future iterations:**
  - A SITEMAP IS A STRONGER SIGNAL THAN REACHABILITY. Leaving a thin page reachable is not the same
    as telling Google to crawl it. Any "which pages are good" rule must be shared with whatever
    submits them, or the audit reports a clean surface while the generator publishes the opposite.
  - Before trusting a per-page measurement, check that the URL actually routes to the page you
    measured. Replaying a component's query says nothing about a URL that component never renders.
  - A failed measurement that reads as zero is indistinguishable from a real zero. Where a shrink is
    the ACTION (fewer sitemap URLs), count the errors separately and refuse to act on a partial run -
    otherwise a flaky request publishes a smaller file and looks like a clean one.
  - Filter before electing a group representative, not after. Post-filtering elects the excluded
    member and then drops the whole group.
  - vitest here only includes src/**; scripts/__tests__ use a hand-rolled check() harness run with
    tsx and a `test:*` npm script. Follow that rather than fighting the include.
---

## 2026-08-27 - WEB-QA-018 AC1: which drifted migrations have code waiting on them
- 31 migrations are ledgered as applied and produced nothing. AC1 asks for a per-module decision and
  nobody had the numbers, because the two checks that hold them were never joined:
  check-migration-drift (migration -> missing object) and check-schema-usage (code -> missing name).
  scripts/report-drift-decisions.mjs joins them. `npm run report-drift-decisions`.
- 17 have readers, 14 have none a static scan can see; 174 proven call sites. The big ones are
  crm_infrastructure (78), add_payments_and_invoices (24) and add_ai_trip_planner_nlp_search (19).
- MY FIRST RUN WAS WRONG AND THAT IS WHERE THE THIRD SIGNAL CAME FROM. It said the seven GSC helper
  functions (WEB-SEO-014 AC3 is blocked on them) and create_update_coordinates_function had no
  readers. True of TypeScript, false of the database: update_coordinates is called by a trigger in
  20250731000002_create_location_triggers.sql, which CLAUDE.md documents. Added a count of references
  from OTHER migrations; it moved 5 migrations out of the delete pile. Shipping after one run would
  have recommended deleting the geocoding trigger's function.
- Renamed the verdict from RETIRE to NO-READERS for the same reason. A static scan cannot see dynamic
  SQL or pg_cron job bodies, so it names a place to look, not a conclusion.
- 11 offline checks, 2 negative controls. Dropping the SQL signal fails 1; substring instead of
  word-boundary matching fails 2. The first attempt at the second control produced a syntax error
  rather than a working substring matcher, so it reported nothing and looked like a passing control -
  a "control" that cannot run is worse than no control, because it reads as evidence.
- **Learnings for future iterations:**
  - A REFERENCE COUNT OVER src/ IS NOT A REFERENCE COUNT. Postgres objects are called from triggers,
    other functions and cron job bodies. Any "is this used" question about a database object has to
    scan supabase/migrations/ too, or it will recommend deleting live plumbing.
  - Name a verdict for what the evidence supports. "NO READERS" and "RETIRE" describe the same rows
    and only one of them is defensible from a static scan.
  - When two error directions cost different amounts, say so in the code and make the rule
    asymmetric. Here any single signal means REAPPLY, because unused objects are cheap and a deleted
    feature is not.
  - Verify a negative control actually RAN. Grep for the FAIL line, not just for the absence of
    success - a control that crashes prints nothing and passes review.
  - A script that both exports helpers and runs at top level will execute on import. Guard with
    `process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href` - argv[1] is
    undefined under `node -e`.
---

## 2026-08-27 - WEB-SEO-011: the ingest-to-discoverable gap is now measured nightly
- scripts/check-sitemap-freshness.ts compares the live sitemap against the database: 413 events in the
  7-day window, 407 <loc> live, 11 ingested and not yet advertised, 6 advertised past the window.
- AC7 wants median days from ingest to INDEXED, which needs the expiring GSC grant. Median days from
  ingest to ADVERTISED needs nothing, is computable today, and is the half we control.
- THE DESIGN IN THE STORY NOTES WAS WRONG AND I MEASURED BEFORE BUILDING IT. The note proposed
  comparing newest lastmod against the last deploy. sitemap-pseo carries 2026-03-18 and
  sitemap-articles 2025-10-14 because their content genuinely has not changed - so the rule would fire
  on the two files that are behaving. lastmod is now printed without a verdict.
- Report, never gate, and in the NIGHTLY not pr-checks. The prior note refused to build this at all
  because a CI gate on it goes red during a quiet week. The fix was the venue, not the metric.
- FOUND ON THE FIRST RUN: 407 <loc> entries, 392 distinct. 15 URLs are submitted twice, including the
  chef-george-s-steak-bar triple WEB-SEO-017 recorded. Duplicate rows were known; that they reach
  Google as repeated sitemap entries was not. Added to WEB-SEO-017's notes too.
- Extracted scripts/lib/sitemapSlugs.ts so the check cannot build a URL the generator would not write.
  Proved it behaviour-preserving by running the COMMITTED generator (git show HEAD:...) and diffing -
  byte-identical. My first attempt at that proof used `git stash push` on a file plus an untracked
  one, the stash silently did not apply, and I compared the new code against itself.
- LATENT DEFECT FOUND BY THE TEST: createEventSlug's catch never fired for a bad date, because
  parseISO returns an Invalid Date instead of throwing. The slug came out `title-NaN-NaN-NaN`. Not
  reachable today; guarded anyway.
- **Learnings for future iterations:**
  - A TIMEZONE TEST PASSES FOR FREE ON A MACHINE IN THAT TIMEZONE. This host is America/Chicago, so
    deleting the toZonedTime call left every assertion green locally while CI (UTC) would have caught
    it. The test now re-execs itself under TZ=UTC, passing process.execArgv so the child keeps the tsx
    loader. Any test asserting on local date fields must pin TZ.
  - `git stash push <tracked> <untracked>` can no-op silently. Verify a before/after comparison
    actually ran against the OLD code - `git show HEAD:path > tmp` and run that.
  - parseISO/new Date do not throw on garbage, they return Invalid Date. A try/catch around date
    parsing is not a guard; `Number.isNaN(d.getTime())` is.
  - When a story's notes propose a design, measure the premise before building it. Two of the four
    sitemaps legitimately carry months-old lastmod, which the proposed rule would have flagged.
  - Freshness of production DATA belongs in the nightly report; freshness of CODE belongs in CI. The
    permanently-red trap comes from putting the first in the second.
---

## 2026-08-27 - WEB-SEO-017: duplicate rows no longer reach Google as duplicate sitemap URLs
- Yesterday's freshness check reported 407 <loc> for 392 distinct URLs. Regenerating locally found the
  same shape in a file nobody had looked at: playgrounds, 69 for 67.
- Deduped in the single function every generator funnels through, so a sitemap added later cannot
  reintroduce it. events 413 -> 397, playgrounds 69 -> 67. Verified across all nine files: zero dupes.
- THE FIX IS THE SYMPTOM, NOT THE CAUSE, and the code says so at the dedupe. The 10 event groups still
  need their per-group merge. Every collapse is logged on every build so the cause stays visible.
- Extended check-duplicate-entities to playgrounds. It keys on the URL SLUG, not name+address like the
  restaurant check, because a playground URL is built from the name alone - keying on what the URL is
  built from is what makes the check agree with the symptom that found it.
- The generators were logging urls.length, the count BEFORE dedupe, so the build printed 413 while
  writing 397. writeSitemap now returns what it actually wrote.
- **Learnings for future iterations:**
  - A COUNT THAT DESCRIBES THE INPUT IS NOT A COUNT. "Generated 413 URLs" was printed on every build
    for months while 397 were written. If a function filters, the caller must report the post-filter
    number or the log actively conceals the filtering.
  - When a detector is written for one table, ask which tables it does NOT cover before trusting a
    clean run. Restaurants -> events -> playgrounds, each extension found real rows.
  - Key a uniqueness check on whatever the USER-VISIBLE identifier is built from. name+address would
    have separated the two Union Parks; the URL does not, and the URL is what collides.
  - Fixing a symptom is fine when the code names the cause and keeps counting it. Silently collapsing
    duplicates would have removed the only evidence anyone had.
---

## 2026-08-27 - WEB-CI-027: scripts/ is now type-checked, which it never was
- WEB-CI-027's notes recorded "tsconfig does not include scripts/, so the file is invisible to the
  type-checker entirely" as the explanation for a stray brace reaching CI, and nothing closed it.
  Re-verified: appended `const x: number = "nope"` to a scripts file, `npm run type-check` exit 0.
- 19 errors in the whole directory: 10 Puppeteer page.evaluate callbacks, 6 from two Deno files
  misfiled under scripts/, 3 genuine. Gates at zero rather than baselining.
- ONE OF THE GENUINE THREE WAS MINE from iteration 2: lib/pseoShippable.ts called body.map() on a
  fetch response with no Array.isArray guard, where its sibling fetchAll has one. Caught on the
  lane's first run, which is the argument for the lane.
- DOM in `lib` makes Response.json() `any` instead of `unknown`, retiring that exact finding. Tried a
  per-file `/// <reference lib="dom" />` to scope it - `lib` is program-wide, so it behaves
  identically. Kept DOM, stated the cost in the config.
- **Learnings for future iterations:**
  - `npm run type-check` DOES NOT COVER scripts/ OR supabase/functions/. It reads tsconfig.json,
    which references tsconfig.app.json ("src") and tsconfig.node.json ("vite.config.ts") only. Any
    claim of "tsc clean" about work in those directories was true and meaningless until now. Run
    `npm run type-check:scripts` and `npm run type-check:edge` for those.
  - What actually covered scripts/ before was `eslint .` (which does read them) and running them.
    That catches syntax errors and unused vars, not type errors.
  - A tsconfig `lib` entry is program-wide and a per-file `/// <reference lib="..." />` does not
    scope it. Verified by trying it: adding the directive to four files masked a finding in a fifth.
  - When adding DOM to a Node project, know that you are trading away `unknown` on Response.json().
  - When a story's notes diagnose a defect in passing while explaining something else, the defect
    usually stays. That note was three days old and the hole was still open.
---

## 2026-08-27 - functions/ is now type-checked (WEB-SEO-006 / WEB-CI-027 follow-on)
- Same gap as scripts/, one directory over, and not build tooling: _middleware.ts runs on every
  production request. Its only import - EventContext from @cloudflare/workers-types - resolves to
  nothing, because the package is not installed and nothing ever said so.
- 3 errors before, 0 after. Gates rather than ratchets.
- TRIED THE DEPENDENCY FIRST AND REVERTED IT. `npm install --save-dev @cloudflare/workers-types` on
  Windows pruned 31 @esbuild/<platform> optional binaries from package-lock.json, including
  @esbuild/linux-x64 that CI needs. Net -550 lockfile lines for a types-only package.
- Shim instead: functions/_typecheck/cloudflare.d.ts, following the deno.d.ts precedent. Left the
  source import alone so the file stays correct if the real package is installed later.
- Negative-controlled four ways: context.reqeust, .transfrom, a stray brace, and a type error in the
  media route to prove that file is in the glob too.
- **Learnings for future iterations:**
  - `npm install` ON WINDOWS PRUNES OTHER PLATFORMS' OPTIONAL BINARIES FROM THE LOCKFILE. Adding one
    types-only devDependency removed 31 @esbuild entries. ALWAYS `git diff --stat package-lock.json`
    after an install here; a net deletion means the tree was reconciled, not just extended.
  - Three directories had zero type coverage and two are now closed: scripts/ and functions/. The
    third, supabase/functions/, is partly covered - type-check:edge is scoped to _shared/ only, with
    a per-file ratchet for the ~120 others.
  - A shim must be negative-controlled or it is decoration. Typos on the shimmed surface must fail;
    if `context.anything` type-checks, the shim proves nothing.
  - When a story says a behaviour "cannot be executed in CI", that is where static checking is worth
    the most, and it is exactly where nobody had set it up.
---

## 2026-08-27 - WEB-SEO-013 AC7: the forward-looking collision check was blind and unwired
- Audited all 34 scripts/check-* for whether anything runs them. Three were run by nothing.
- Two of the three were NOT checks - check-catchdesmoines-urls.ts and check-harbour-update.ts print
  rows and exit 0. Named check-* in a directory of 32 real ratchets, so the audit had to open each
  one. Renamed inspect-*, header added, one docs line updated. Nothing else referenced them.
- The third was real: check-pseo-collisions.ts, AC7's before-a-generation-run half. Its route set
  used `.filter((p) => !p.includes(':'))`, dropping every parameterised route, so it found 10
  collisions and ZERO shadowings. After pointing it at the shared pseoRouteClaims module: 10 literal,
  257 shadowed across 5 entity-detail routes - including /outdoors/:slug and /sports/:slug, which the
  published-side check cannot see because no published slug reaches them yet.
- **Learnings for future iterations:**
  - A CHECK NOTHING RUNS IS NOT A CHECK. Auditing `scripts/check-*` against workflows and
    package.json took one command and found three. Worth repeating whenever a ratchet is added.
  - Naming matters for auditability. Two ad-hoc diagnostics named check-* meant the audit could not
    be done from the file list alone.
  - The same defect can hide in the second implementation of a rule. The published-side collision
    check handled parameterised routes; the taxonomy-side one, written separately, did not. That is
    the third time this session that one rule in two places disagreed - shippable set, slug
    construction, and now route claims. Shared module every time.
  - A check written before the thing it guards exists tends to be under-tested. This one had never
    run in CI, so its blind spot survived from the day it was written.
---

## 2026-08-28 - WEB-PERF-023 AC1: /map was 57% Leaflet runtime images
- Measured composition of the live prerendered routes rather than counting elements again. /map's
  1,124 <img> are 1,121 Leaflet nodes: 517 marker icons, 517 marker shadows, 87 tiles, against 550
  words of text. All rebuilt by react-leaflet on hydration, so all discarded.
- stripLeafletRuntime added to the existing transform chain in lazy-preload-patterns.mjs, next to the
  modulepreload and JSON-LD strippers. #root 1,958 -> 837, words 550 -> 550. Verified against the
  HTML production serves today, not a fixture.
- THE SPRITE LEVER IS SMALLER THAN IT LOOKS: inline lucide is 2.4-2.8 nodes, SpriteIcon is 2.0. So
  converting /playgrounds entirely saves ~283 of 2,389 (12%), not the ~800 that "icons are 34% of the
  DOM" suggests. Recorded so nobody re-derives it.
- AC1 for the LIST routes is in real tension with WEB-SEO-006, which spent this month increasing
  those counts on purpose. /map was not that question - the removed nodes were not content.
- **Learnings for future iterations:**
  - COUNT WHAT THE ELEMENTS ARE, NOT JUST HOW MANY. This story had a per-route element count for
    months and nine routes over the flag. One composition pass showed the worst route was not a list
    problem at all, and its fix removed no content.
  - Measure against the live prerendered HTML. It needs no build, it is what users and crawlers
    actually get, and here it was better evidence than a local dist/.
  - A transform that DELETES nodes from shipped HTML needs its tests weighted toward what it must
    keep. Matching every <img> passed four of my twelve checks and failed the four that mattered.
  - Verify a negative control APPLIED. Two of mine failed to patch the file at all and printed
    nothing; the run looked identical to a control that fired and found nothing.
  - `<img>` in prerendered output is worth auditing generally: 517 of these carried role="button"
    tabindex="0", so the static page had 517 fake buttons in the tab order before hydration.
---

## 2026-08-28 - WEB-PERF-023: verified the Leaflet strip in a real build, then made AC1 decidable
- Ran a full `npm run build`. The prerender log reports "1121 Leaflet runtime image(s)" - the exact
  figure measured against the live page, so the transform behaves the same on captured output.
  dist/map/index.html: 0 leaflet <img>, 7 pane divs and .leaflet-container kept, 61 h3, 563 words.
- /map 1957 -> 836 and off the over-1500 list. That is a >50% drop, which the DOM budget check
  correctly reported as a COLLAPSE - a false alarm I had introduced by not re-baselining. Fixed
  through the check's own --update path.
- --update rewrites all 35 routes, so it also absorbed three data-driven moves that are NOT mine
  (/events/this-weekend 1609->2390, /events/today 450->1504, / 1944->1207). CHECKED BEFORE BAKING
  THEM IN, because a thin capture looks identical to a smaller page: / has 13 h2 / 20 h3 / 1355 words
  / 6 JSON-LD, and /events/today now has 28 h3 and an ItemList where WEB-SEO-006 recorded 5 h3 and
  none. Both full captures; the second is the settle fix working.
- Ran every dist-dependent check with the fresh build: bundle-budget, csp-hashes,
  spa-fallback-canonical, dead-lib-modules, dom-budget. All exit 0. Critical path still 197.2 KB.
- AC1 arithmetic, which the story never had: cards are 48 elements not the guessed 60, so 21 cards
  fit under 1500 on three routes, not "about a dozen". /playgrounds is a different shape - its cards
  are ~20 elements each and its FILTER PANEL is ~795 (99 chips + 75 badges, each an inline
  lucide-check).
- **Learnings for future iterations:**
  - A CHANGE THAT MOVES A MEASURED NUMBER INVALIDATES ITS BASELINE. I shipped the Leaflet strip
    without re-baselining and left a collapse report behind. Check which baselines name the thing you
    just changed before committing.
  - Before absorbing someone else's movement into a baseline, verify it is real. A thin prerender
    capture and a genuinely smaller page produce the same element count; h2/h3/word/JSON-LD counts
    tell them apart.
  - Estimating per-item cost from a page total is unreliable. "Icons are 34% of the DOM" suggested a
    big sprite win; the per-icon delta made it 12%. "About a dozen cards" was 21.
  - Two routes with the same element count can need opposite fixes. /events/* is cards;
    /playgrounds is a filter panel, and capping its card list would aim at the wrong half.
---

## 2026-08-28 - WEB-PERF-023: the icon lever, measured properly this time
- Converted the two per-card icons that were already in the sprite but rendered inline.
  /playgrounds 2388 -> 2238, /restaurants/dietary 2163 -> 2127. RestaurantCard is on nine pages.
- I HAD THE SPRITE NUMBER WRONG two days ago (~283 on /playgrounds). generate-icon-sprite.mjs already
  documented the rule: a sprite instance is ALWAYS 2 nodes, so a single-path icon saves nothing. The
  two highest-count icons on these routes - check x174 and star x197 - are both single-path and save
  zero. Real availability was 181 and 98, and 186 of that is now taken.
- The icon lever is finished. What remains needs new symbols for 31 and 46 elements.
- THE HOMEPAGE CAPTURE IS UNSTABLE: 1944, then 1207, then 2022 across three builds. Yesterday I
  validated 1207 against h2/h3/word counts and called it full. This build shows 37 h3 / 1840 words
  against that capture's 20 / 1355 - it was partial. Baselined at the high figure so the collapse
  check can still catch a thin capture.
- **Learnings for future iterations:**
  - READ THE GENERATOR BEFORE ESTIMATING ITS PAYOFF. The rule that killed my 283 estimate was in the
    header of the file I was proposing to extend.
  - Per-item cost has to be measured per ITEM TYPE. "Icons are 34% of the DOM" is true and useless;
    the two icons making up most of that count convert at zero saving.
  - A SINGLE BUILD CANNOT VALIDATE A PRERENDER BASELINE. h2/h3/word counts look healthy on a partial
    capture. Compare two builds, and when a route is known to vary, baseline the HIGH figure so the
    collapse direction stays detectable.
  - When correcting your own recorded number, correct it where it was recorded. The wrong figure was
    in prd.json where the next person would read it first.
---

## 2026-08-28 - the offline suites are discovered now, and two had never run
- Audited what runs. 10 *.test.mjs suites exist; test:offline listed 4. Two were in NOTHING:
  functions/__tests__/middleware-canonical.test.mjs and scripts/__tests__/list-edge-deploys.test.mjs.
- The first is the ONLY guard on WEB-SEO-006's canonical rewrite, which that story says cannot be
  verified any other way because HTMLRewriter does not exist outside the Workers runtime.
- FOUR OF THE SIX GAPS WERE MINE. This session I added four suites and wired each as its own
  pr-checks step, never having noticed the aggregator. Same mistake, same file, four times.
- scripts/run-offline-suites.mjs globs both __tests__ dirs. Adding a suite makes it run. It exits 1
  when the glob matches nothing, because a discovery runner that finds zero must not report success.
- pr-checks 26 steps -> 22. Negative-controlled: breaking one suite makes the runner exit 1, print
  that suite's output and name it.
- **Learnings for future iterations:**
  - CHECK FOR AN AGGREGATOR BEFORE ADDING A CI STEP. `npm run test:offline` already existed and I
    walked past it four times, each time making the list less true.
  - Audit test wiring the same way as check wiring. Iteration 8 found three checks nothing ran; the
    identical audit on tests found two suites nothing ran, one of them load-bearing.
  - Prefer discovery to a list for anything that grows. A hand-maintained list of suites is a
    baseline keyed on someone remembering.
  - A discovery runner must fail on an empty match. Otherwise a moved directory turns the whole
    safety net into a green tick.
---

## 2026-08-28 - WEB-SEO-008: eight landing pages intermittently shipped with no FAQPage
- The recorded residual named two URLs. Auditing all 35 prerendered routes showed the missing set
  CHANGES EVERY BUILD: altoona+johnston, then free+kids+this-weekend+today, then none.
- Cause is the original fix seen from the other side. `faqData={isLoading ? undefined : faqData}` on 8
  pages avoided react-helmet-async appending two different FAQPage blocks - and shipped ZERO whenever
  the prerenderer captured before the query resolved.
- Proof it was a race: /events/altoona and /events/clive have identical h3 and ItemList counts, and
  only one of them had a FAQPage on that build.
- Took the live counts out of every FAQ answer, so both renders emit identical JSON and it is always
  passed. FAQPage 20 -> 24 of 35 routes; all 13 /events/* now have one; still zero duplicates.
  Verified across two consecutive builds because the defect WAS the variance.
- **Learnings for future iterations:**
  - WHEN A NOTE NAMES SPECIFIC URLS AS A RESIDUAL, CHECK WHETHER THE SET IS STABLE. These were a
    sample of a race, and fixing "those two URLs" would have fixed nothing.
  - A fix for a duplicate can create a zero. Withholding data to stop two copies being emitted means
    emitting none whenever the withholding condition is still true at capture time.
  - Structured data must not interpolate a live count. It differs between renders, it goes stale
    against the page, and it is the thing that forced the withhold in the first place.
  - `--update` on a whole-file baseline silently reverses deliberate per-entry decisions. It moved /
    off the high-water mark I set two commits earlier; edited back by hand and recorded.
  - The prerender content race is still live: /events/this-weekend captured 422 elements with zero
    cards on one build and 2,390 with 40 on the next. Never baseline a build without checking.
---

## 2026-08-28 - WEB-SEO-006: the prerender settle waits for queries now, and captures are deterministic
- The two existing waits answered the wrong question. Helmet's data-rh proves the page MOUNTED; a stable
  #root count proves it STOPPED CHANGING - and a loading skeleton is static, so both fire mid-query.
- Evidence before: /events/this-weekend captured 422 el / 0 cards then 2,390 / 40 on identical code;
  / measured 1944, 1207, 2022, 1210 across four builds.
- src/components/PrerenderSignal.tsx publishes useIsFetching as <html data-queries-settled>.
  prerender.mjs waits on it, fails open to the old settle, and reports routes captured without it.
- After: three consecutive builds byte-identical on every route, home included.
- **Learnings for future iterations:**
  - WHEN A NOTE SAYS "no generic signal was available", CHECK AGAIN AGAINST THE STACK. useIsFetching is
    exactly that signal and had been in the app the whole time; the two proxies in use were both
    measuring side effects of loading rather than loading.
  - A stability signal must handle "zero because nothing started yet". Writing settled=true on the
    first zero would have shipped a change that measured nothing and looked like a fix.
  - Fail open, and COUNT the fail-opens. A settle that silently falls back is indistinguishable from
    one that works; the run now prints how many routes had no signal.
  - Deterministic output makes every downstream baseline honest. The high-water hack I needed for /
    two commits earlier is now dead code, and the DOM collapse detector stops being ambiguous.
  - This also explains WEB-SEO-008's wandering FAQPage: same race, different symptom. One root cause
    was producing two separately-filed defects.
---

## 2026-08-28 - WEB-SEO-013 AC5: the hand-built hubs had no inventory gate
- check-pseo-inventory covers the 244 generated pages. The routes in sitemap-static.xml are
  hand-written, prerendered and submitted the same way, and nothing counted what they list.
  7 of 8 are below AC5's floor of 8; /events/johnston, /events/clive and /events/free list nothing.
- /events/free CANNOT list anything: of 332 upcoming events, 330 have price "See website" and 2 have
  "$12-$35". Nothing is free, 0 or null. Its FAQ claims otherwise. That is ingestion, and the owner's.
- I WROTE THE CHECK THE WRONG WAY FIRST: counting ItemList out of dist/. A hub that renders nothing
  emits no ItemList, so the three emptiest routes were skipped as "no list here"; and /restaurants
  emits an empty ItemList while listing 15 restaurants, so it reported empty when it is not.
  Rewrote it to ask the database.
- Found in passing: /restaurants ships an ItemList with zero items while listing 15 restaurants.
- **Learnings for future iterations:**
  - A CHECK THAT MEASURES ABSENCE MUST NOT INFER IT FROM MARKUP THAT IS ALSO ABSENT. "No ItemList"
    meant both "not a list page" and "a list page with nothing in it", and the second is the finding.
  - Run a new check and READ every row before trusting it. /restaurants at 1,455 elements reported as
    EMPTY, which is what exposed the flaw - a summary count alone would have shipped it.
  - Inventory can only be judged from prerendered output once capture is deterministic. This check
    would have been unbuildable a day ago and would have reported races as content problems.
  - When a filter returns zero, check the DATA SHAPE before the filter. The free-events filter is
    correct; the column it reads has no free-ness in it at all.
---

## 2026-08-28 - WEB-SEO-006: /restaurants was shipping an empty list to crawlers
- Yesterday I said captures were deterministic. True only for TanStack-backed routes. useRestaurants
  is a manual useState/useEffect fetch, so useIsFetching never saw it.
- /restaurants shipped "Loading restaurants...", an ItemList of numberOfItems 0, and a description
  promising "200+ local restaurants" - on EVERY build, not intermittently.
- The discriminator this story searched for and did not find is aria-busy. SkeletonGroup renders
  role="status" aria-busy="true"; nothing else in the app sets it. One of 35 routes carried it at
  capture, which is what makes it trustworthy rather than a proxy.
- /restaurants 1455 -> 2818 elements, 12 -> 45 restaurant links, ItemList 0 -> 478/20. Stable across
  two builds.
- **Learnings for future iterations:**
  - A "the data has loaded" signal is only as broad as the data layer it watches. This codebase has
    two - TanStack Query and hand-rolled useState/useEffect - and the fix I shipped yesterday covered
    one. Check for the second implementation before claiming a class of bug is closed.
  - AN INTERMITTENT BUG AND A PERMANENT ONE LOOK THE SAME IN ONE BUILD. The queries fix made
    /events/* stable and left /restaurants broken every time; only auditing all 35 routes for a
    loading marker separated them.
  - ARIA attributes are underused as build-time signals. aria-busy is a semantic, app-authored,
    already-present statement that a region is loading - strictly better than sniffing CSS classes.
  - When a note says a signal was searched for and not found, search again with the accessibility
    layer in mind.
---

## 2026-08-28 - WEB-SEO-006: an absolute assertion, because every other guard measured a difference
- Audited all six entity hubs, row count against prerendered output. Every one renders its data now;
  no second /restaurants.
- The real lesson from /restaurants is WHY nothing caught it. check-dom-budget, the collapse detector
  and the prerenderer's quality gate all compare against a baseline or the shell. A route that has
  ALWAYS been wrong matches its baseline and passes everything.
- scripts/check-prerender-content.mjs asserts an absolute property instead: no aria-busy="true" in any
  shipped page. It gates, unlike check-dom-budget, because a skeleton is never correct on any data
  whereas element counts move with a quiet week.
- Checked and cleared two non-defects: /stay's thin schema (it has canonical/description/og:title,
  just fewer JSON-LD types) and the three hubs with no ItemList (absent, not empty - a missed
  opportunity, not the invalid-list bug).
- **Learnings for future iterations:**
  - A BASELINE CANNOT CATCH A BUG THAT PREDATES IT. Every ratchet here measures change; something that
    shipped broken from day one is perfectly stable. Pair ratchets with at least one absolute
    assertion about the artifact.
  - Gate on properties that no legitimate data can violate; report on properties that move with data.
    That is the whole difference between this check and check-dom-budget, and both are right.
  - Verify a new check would have caught the bug that motivated it. I had the pre-fix measurement
    (aria-busy x1) so this was checkable rather than assumed.
  - Measure before filing a suspicion. /stay looked like it was missing its SEO component and was not;
    my own wrong link prefix produced the "0 hotel links" that started it.
---

## 2026-08-28 - WEB-SEO-006: why the existing loading guard missed /restaurants
- prerender.mjs already refused to publish a capture containing "Loading X..." - but only when
  #main-content has under 2,000 chars, deliberately, because rejecting on any spinner "threw away the
  homepage over a single spinner".
- That rule is exactly what let /restaurants through: its main list was a skeleton while the rest of
  the page carried well over 2,000 characters. And no SIZE threshold would have caught it either -
  the skeleton was a small fraction of a large page and still the entire point of the route.
- So the gate I added yesterday has to be absolute, and the cost that comment protects against is
  real. Stated it in the file and gave it ALLOWED_SKELETON_ROUTES - per route, with a reason, empty
  today. Both paths controlled: unlisted skeleton exits 1 and names the route; allowlisted exits 0,
  prints the reason, and reports how many were allowed rather than claiming a clean run.
- **Learnings for future iterations:**
  - WHEN YOU ADD A GATE, GO FIND THE ARGUMENT AGAINST IT - it is usually already written in the file
    you are duplicating. This one was a comment explaining why an earlier, stricter version was
    softened, and it applied directly to what I had just shipped.
  - Proportionality guards cannot see importance. "Small part of a big page" and "the only reason the
    page exists" are the same measurement.
  - Every gate needs a per-case escape hatch with a stated reason, or the first false positive
    deletes the whole check. A baseline that can only shrink, or an allowlist that must stay empty.
  - Control BOTH paths of an allowlist. An escape hatch that silently swallows everything is worse
    than no gate; the pass path must say what it let through and why.
---

## 2026-08-28 - WEB-SEO-002's twelve headless routes are zero, and now guarded
- prerender.mjs said "Twelve hub routes render no SEOHead today". Measured: zero. All 35 carry a
  canonical matching their route, a unique title and a description. Comment corrected with the date.
- Nothing guarded it, so the property could drift either way unseen. check-prerender-head.mjs gates
  on canonical presence, canonical-matches-route, title uniqueness and description presence.
- MY FIRST VERSION FAILED THE HOMEPAGE. Comparing canonical to route by string suffix breaks on "/":
  stripping the trailing slash from "https://host/" leaves no path to match. Compares pathnames now.
- Swept all 35 prerendered pages for undefined / NaN / [object Object] / bare null / Lorem ipsum /
  placeholder tokens / error strings in rendered text. Clean.
- Chased /events/free's root cause and stopped short honestly: the catchdesmoines LISTING carries no
  price data (one "Free Event" string and Handlebars placeholders), so the crawler defaulting to "See
  website" is correct. Whether detail pages carry price needs crawl4ai rendering I cannot cheaply do
  here, so I did not guess.
- **Learnings for future iterations:**
  - A COMMENT ASSERTING A DEFECT COUNT IS A MEASUREMENT WITH NO EXPIRY DATE. This one drove a timeout
    choice and had been wrong for some time. When you read one, check it before reasoning from it.
  - A property that is currently perfect and unguarded is the easiest thing to lose. Gate it while it
    is clean - that is the cheapest moment, and there is no baseline to negotiate.
  - Read the check's OUTPUT, not its exit code. Mine failed for a real-looking reason on the one route
    whose shape I had not thought about, and the row said so plainly.
  - Stop when the measurement needs a tool you do not have. The price question needed a rendered
    crawl; guessing at it would have put a wrong cause in the notes.
---

## 2026-08-28 - a11y sweep: one real defect, two false positives, one impossible check
- /restaurants shipped 3 empty <a class="block" href="..."></a>. Cause: RestaurantOpenings nested a
  "Read More" <a> inside a card-wide <Link>. Chromium splits nested anchors; the empty outer one is
  what ships. Fixed with the stretched-link pattern. Only component in src/ with the nesting.
- FALSE POSITIVES I nearly filed: 29 "unnamed buttons" on /stay are Radix checkboxes and <button> is a
  labelable element, so their <label for> names them. The "unlabelled inputs" were Radix's hidden
  native <select aria-hidden="true">.
- I ADDED A NESTED-ANCHOR CHECK AND ITS CONTROL DID NOT FIRE. Parsers split nested anchors, so
  querySelectorAll('a a') matches nothing in dist/ even on a broken page. Deleted it and wrote down
  why; the empty anchor it leaves behind is the detectable trace, so the unnamed-link rule IS the
  nested-anchor check.
- **Learnings for future iterations:**
  - CONTROL EVERY ASSERTION, INCLUDING THE ONES THAT LOOK OBVIOUS. This one read as solid protection
    and could never trigger. A check nobody controls is a comment.
  - Some defects are only visible through their trace. The parser destroys the evidence of nested
    anchors and leaves an empty one; assert on the trace, not the cause.
  - Know the HTML spec before calling something an a11y bug. <button> is labelable, so <label for>
    gives it a name - 29 of my 32 findings evaporated on that one fact.
  - aria-hidden subtrees must be excluded from every a11y sweep. Radix ships hidden native controls
    for form compatibility and they are correct.
---

## 2026-08-28 - the edge type ratchet was checking nothing on Windows
- It reported "0 errors across 0 files" and "158 fixed. Re-baseline to lock it in." Taking that advice
  would have committed a zeroed baseline and blinded CI too.
- TWO Windows-only faults, each sufficient alone: path.join produced backslashed tsconfig globs (which
  accept only '/'), so tsc matched zero inputs; and after fixing that, the error parser still counted
  zero because tsc writes CRLF and  is a LINE TERMINATOR in a JS regex, so `(.*)$` never matches.
- CI was fine throughout - ubuntu gives '/' and LF - so the committed baseline is accurate. The damage
  was local, on the only thing that type-checks supabase/functions.
- The fix immediately found og-image at 1 -> 3, from main's JPEG commit. Both type-level; baselined at
  160 with the reasons recorded rather than refactoring a deployed function I cannot run.
- **Learnings for future iterations:**
  - A CHECKER THAT SEES NOTHING MUST NEVER REPORT SUCCESS. Third time this session: the offline suite
    runner, the prerender content check, and now this. Every scanner needs a "found zero inputs" guard.
  - TSCONFIG GLOBS ARE '/'-ONLY. path.join is wrong for `include` and `paths` on Windows, always.
  -  IS A LINE TERMINATOR IN JAVASCRIPT REGEX, so `.` will not match it and `$` sits after it. Split
    tool output on /?
/, never '
'.
  - Cross-platform bugs in a checker are worse than in product code: the product fails loudly on the
    broken platform, the checker succeeds quietly on it.
  - "Everything is fixed, re-baseline to lock it in" deserves suspicion proportional to how good the
    news is. 158 errors do not vanish between two commits.
---

## 2026-08-28 - audited all 34 check scripts for the empty-scan failure
- Three had already failed this way (offline runner, prerender content, edge types). Three is a
  pattern, so I audited the rest instead of waiting to trip over the fourth.
- Six had no guard; four can plausibly see empty input and all four printed a clean result:
  check-migration-drift, check-pseo-route-collisions, check-pseo-collisions, check-error-handling.
  Each now exits 2. Each controlled by emptying its own input - file list, slug file, taxonomy,
  ESLint results.
- check-migration-drift is the one that mattered most: it guards the 31 drifted migrations and would
  have said "No ledger drift" over an empty directory.
- Left check-pseo-inventory and check-anon-exposure alone: both read production and exit 0 by design.
- **Learnings for future iterations:**
  - WHEN THE SAME BUG APPEARS THREE TIMES, STOP FIXING INSTANCES AND SWEEP THE CLASS. The audit took
    one heuristic grep plus four readings and found four more.
  - The guard belongs where the INPUT is acquired, not where the result is printed. Every one of these
    was a collection built early and consumed far away.
  - A ratchet's empty-scan failure is worse than a gate's: it does not just pass, it reports
    IMPROVEMENT and invites you to re-baseline the coverage away.
  - Grep for the guard, not for the bug. "refusing to pass" as a phrase turned out to be a reliable
    marker of which scripts had already thought about it.
---

## 2026-08-28 - two clean sweeps, and a ratchet 120 sites behind reality
- Swept for the cross-platform parsing class: only check-edge-types had it. strict-ratchet splits on
  '
' too but its regex has no `$` anchor, so  cannot break it - verified by running it (24/17
  against baseline 24, genuinely parsing).
- Swept all 19 baseline files for position-keyed entries: none. duplicate-events-baseline looks
  line-keyed to a naive regex but its keys are title|date|venue and the match is a timestamp.
- REAL FIND: check-error-handling was at 161 sites / 104 files against a baseline of 281 / 160. 120
  fixed sites were unbanked and therefore unprotected. Re-baselined at 161/104.
- MY FIRST CONTROL FOR THAT WAS INVALID. I replaced `if (error) throw error` with a swallowed branch
  and the ratchet stayed green - because the selector fires on a destructure that OMITS error, not on
  a checked-then-ignored one. Injecting `const { data } = await ...` fails it and names the file.
- **Learnings for future iterations:**
  - A NEGATIVE RESULT IS WORTH RECORDING. Two sweeps found nothing; writing that down stops the next
    person re-running them, and "verified clean" is a different claim from "never looked".
  - Read the rule before writing the control. I wrote a control against my mental model of what
    "discarded error" means rather than against the selector, and a green run nearly read as proof.
  - An unbanked improvement is unprotected. A ratchet only defends the number in the file, so a fix
    nobody baselines can silently regress - check for the gap, not just for regressions.
  - "Every entry reads N -> 0" is the signature of a scan that saw nothing. Confirm the total is
    non-zero before believing a mass improvement.
---

## 2026-08-28 - job-health-watchdog returned { alerts: [] } when its own reads failed
- Prioritised the 161 discarded-error sites by DEPLOYMENT: 51 across 34 deployed functions, 24 in
  undeployed ones. A swallowed error in a function nobody can call is not yet costing anything.
- The watchdog discarded both automation_job_runs errors. A failed read left lastRun null, and the
  alert is gated on `if (lastRun)` - a gate that exists for a good reason. So the failure produced no
  false alarm and NO ALARM: a 200 and an empty alerts array, from a monitor that had just gone blind.
- Now: read failures are collected as UNREADABLE and skipped rather than judged healthy, reported once
  rather than per job, and the response distinguishes `unreadable` from `alerts`.
- 161 -> 159 sites; the file 2 -> 0; baseline re-banked at 159/103.
- **Learnings for future iterations:**
  - A MONITOR MUST BE ABLE TO REPORT THAT IT COULD NOT LOOK. "No alerts" and "I could not check" were
    the same value here, and the second is an incident.
  - A guard added for a good reason can hide a different failure. `if (lastRun)` correctly suppresses
    noise for unwired jobs and incorrectly suppresses it for unreadable ones; the fix is to separate
    the two causes, not to remove the guard.
  - Prioritise swallowed errors by whether the code is DEPLOYED. It split 75 edge sites into 51 that
    can bite today and 24 that cannot, using a baseline the repo already maintains.
  - When alerting on a per-item loop, aggregate the shared-cause failure. One outage should not page
    once per item.
---

## 2026-08-28 - moderate-content: an unreadable queue looked like an empty one
- Second pick from the deployed-function list, by consequence. The scheduled review sweep discarded its
  pending-queue error, so a failed read returned { scanned: 0, ...all zeros } with success: true and a
  200 - identical to "nothing pending". Moderation stops, every signal says healthy, reviews sit
  unmoderated. Now throws so runJob records it.
- loadItem's two sites were NOT the same defect and are handled differently: its null already maps to
  an 'error' verdict that the sweep counts, so an unreadable item is never treated as clean. Only the
  reason was missing, so both log. Tightening those into throws would turn one bad row into a failed
  sweep.
- 159 -> 156 sites; file 3 -> 0; baseline re-banked at 156/102.
- SKIPPED generate-invoice-pdf despite being next by count: its invoices/payments tables come from a
  drifted migration and do not exist, so it cannot run. Recorded a real risk there for whoever
  re-applies it - the existingInvoice lookup uses .single(), so a read failure is indistinguishable
  from "no invoice yet" and would create a second invoice for one payment.
- **Learnings for future iterations:**
  - NOT EVERY DISCARDED ERROR IS THE SAME DEFECT. Three sites in one file: one was a silent failure
    worth throwing on, two were already surfaced by the caller and only needed a log. A uniform fix
    would have made the sweep fail on a single bad row.
  - Check whether the code can even run before hardening it. Two of the highest-count files target
    tables from drifted migrations; the fix belongs with the re-apply, not before it.
  - `.single()` erroring on no-row is a legitimate idiom that quietly merges "absent" with "unreadable".
    Where the consequence is a duplicate write, put the constraint in the schema instead.
---

## 2026-08-28 - triaged the discarded-error backlog against the drift decision
- Joined error-handling-baseline against the objects the 31 drifted migrations define and production
  lacks: 44 of 156 sites are in code touching something missing (BLOCKED on the re-apply), 112 are
  actionable now. The 44 is an upper bound - some drifted names are ordinary columns like reviewed_at.
- Fixed pseo-batch-worker: a failed queue count printed remaining=0, identical to a drained queue.
  Reporting-only, but a number that lies about a backlog is how a stuck queue goes unnoticed.
  156 -> 154; file 2 -> 0.
- CHECKED AND LEFT ALONE: triage-event-submission's two sites are FAIL-CLOSED auth checks (failed
  getUser -> 401, failed role read -> 403); discarding the error denies access, which is the right
  direction. run-scheduled-audit reads three WEB-BE-031 tables that do not exist.
- I BURNED THREE ATTEMPTS ON THE JOIN and the cause is worth recording: my heredoc collapsed `\b`
  to ``, so `new RegExp` got a literal BACKSPACE character instead of a word boundary and matched
  nothing. JSON.stringify of the pattern showed `"seo_settings"` where a real backslash would
  have rendered doubled. The first join reported 16 blocked, the second 0, the third 44.
- **Learnings for future iterations:**
  - NOT EVERY DISCARDED ERROR IS A DEFECT. Fail-closed auth checks are correct as written; so are
    reads whose null the caller already maps to an error. Read the consequence before "fixing" a count.
  - Triage a backlog against what blocks it. 28% of this one waits on a decision nobody has taken, and
    hardening it first would be work thrown away or, worse, work that looks done.
  - When a match returns zero on input you can see with your own eyes, print the pattern. A regex
    built through shell heredocs is a string with its own escaping history.
  - Two failed joins agreed with each other (16, then 0) and both were wrong. Agreement between two
    runs of the same broken method is not corroboration.
---

## 2026-08-28 - the GSC admin dashboard confirmed a disconnect that had not happened
- Largest file in the discarded-error backlog (6 sites). handleDisconnectAll discarded the error on the
  credential read AND both writes, so a failure skipped everything and still rendered "Disconnected"
  with a success toast - while the OAuth credential stayed active and the properties remained.
- Two more showed the wrong STATE: a failed credential read made every token render as not-expired, and
  a failed active-credential read looked like "no credentials" and advised reconnecting.
- A fourth reported "Already showing only your site's properties" with a success toast when the read
  behind the delete had failed. Safe direction, false confirmation.
- 154 -> 150; file 6 -> 2. The two left are auth reads that already deny access on failure.
- **Learnings for future iterations:**
  - A WRITE FLOW MUST CHECK EVERY STEP, not just the last one. Three sequential operations, one error
    check between them, and the success toast fired regardless of which one failed.
  - Name the partial state. "Properties removed but the credential is still active" is a different
    problem from either success or failure, and it is the state a user most needs told.
  - Read failures in admin UI produce CONFIDENT WRONG STATE, not blank panels: a token shown as valid,
    an account shown as disconnected. That is worse than an error, because nobody investigates it.
  - The same triage keeps holding: fail-closed auth reads are fine, reads that gate a write are not.
---

## 2026-08-28 - the false zero, three times in three commits
- AIEnhancementManager counted total and enhanced events, discarded both errors, and computed
  `totalEvents || 0`. A failed read rendered "0 total, 0 enhanced, 0 pending, 0%" - read by an admin
  as "nothing needs enhancing". Its catch could never fire: a Supabase count error returns
  { count: null, error } and does not throw. Same shape one function down in the history panel.
- That is the third instance in three commits: moderate-content's empty queue, pseo-batch-worker's
  remaining=0, and this. 150 -> 147; file 4 -> 1.
- CHECKED AND LEFT ALONE: send-campaign-notification (one fail-closed 403, one that leaves
  recipient_email null in the stored row - visible in the data); MergeReviewPanel (RowCard already
  takes missingId and renders the raw id, so a failed read degrades visibly before a destructive
  merge); triage-event-submission (both fail closed).
- **Learnings for future iterations:**
  - `?? 0` OR `|| 0` ON A VALUE THAT IS NULL BECAUSE THE READ FAILED turns a diagnostic into a
    reassurance. Every instance of the false zero this week has been that one line.
  - A try/catch around Supabase calls does NOT cover query errors. They are returned, not thrown, so a
    component can have visible, well-written error handling that is unreachable.
  - The triage is now stable enough to state: fail-closed auth reads are fine; reads whose null the
    caller already surfaces are fine; reads that gate a write, or that become a displayed zero, are
    not.
  - Checking four files and changing one is a good ratio, not a wasted iteration. The three left alone
    would each have been churn.
---

## 2026-08-28 - turned the false-zero pattern into a search
- Having found it three times by reading files one at a time, I wrote a detector for the SHAPE: a
  destructure taking data/count WITHOUT error, whose binding is defaulted with || 0, ?? 0 or ?? []
  within a dozen lines. 15 candidates across src/ and supabase/functions.
- data-quality-heal was the sharpest: its KPI reported "0 rows missing coordinates/SEO/images" whether
  the data was clean or the count errored - the reassuring number was exactly what a failed read
  produced. Its geocode stage walked `rows ?? []` and reported geocoded: 0 with the job successful.
- ImageBackfill had the AIEnhancementManager shape: needsBackfill = 0 - 0 = 0, telling an admin the
  category was complete.
- 147 -> 144 sites. Detector re-run: 15 -> 12.
- LEFT THE OTHER 12: MergeReviewPanel already renders a missing-row id; agent-support-responder,
  subscription-lifecycle and generate-proposal are undeployed.
- **Learnings for future iterations:**
  - WHEN YOU RECOGNISE A SHAPE THREE TIMES, WRITE THE SEARCH. Reading 101 files one at a time would
    have taken days and missed the ones with only a single site.
  - A pattern search is for TRIAGE, not for a codemod. Twelve of fifteen hits were fine; a blanket fix
    would have added churn and, on the fail-closed ones, made things worse.
  - Deployment status is the cheapest filter for "does this cost anything today". Three of the
    remaining twelve are in functions nobody can call.
  - The most dangerous false zero is on a metric whose whole job is to say something is wrong. A
    data-quality reporter that reports perfect quality when it cannot read is worse than one that
    crashes.
---

2026-08-28 - WEB-BE-032: errors that were captured and then ignored
  The ratchet only catches destructures that OMIT error. Binding error and never reading it passes it, and is
  the same defect. Three real ones, one per severity tier:
  - firecrawl-scraper: the duplicate check gates the insert, and `existingItems = data || []` makes a failed
    lookup look like "no duplicate exists". Worse than a false zero - it manufactures rows rather than losing
    them, on a scheduled re-run. Skips the item now.
  - manage-subscription: a failed read answered "No active subscription found" to a paying customer. Separates
    PGRST116 from a genuine failure; 503 on the latter.
  - social-media-manager: count: "exact" requested, data.length measured, PostgREST caps at 1000.
  Ratchet re-baselined, three commits stale: 127 -> 112 sites.

  The lesson is about the scan, not the fixes. It bounded scope by character distance and was wrong in both
  directions: two confident false positives in useTrending (errors aggregated 118 lines below the window) and
  two missed firecrawl branches (identifier `error` reused inside the window). First reported count was 28, of
  which 22 were noise. A proximity heuristic cannot answer a scope question. The accurate version filters tsc
  TS6133 for error-named bindings; do that before trusting a number from this shape again.

  Caught one of my own: the useTrending patch called `log.error` where the module binds `logger`, which would
  have been a ReferenceError on the fallback path - the exact path that only runs when things are already
  failing. It only surfaced because I went to check the call shape against a neighbouring line. Reverted with
  the rest of that false positive.
---

2026-08-28 - WEB-PERF-020: the tooltip block, and a sitemap that rewrote itself every build
  The story left one item unattempted with an open question - does Radix still need a Provider ancestor?
  It does (no default context, Root throws), but the app-root provider was redundant regardless: all five
  real consumers already self-provide. Only ui/help-tooltip.tsx relied on the global one, and it has zero
  importers, so it would have thrown for its first importer, not for anyone today.
    critical path 197.3 -> 183.4 KB gz; margin under budget 2.7 -> 16.6 KB.

  TWO MEASUREMENT LESSONS, both mine.
  1. My first probe of the built entry grepped for "floating-ui" and found 0, which read as "the block is
     already gone". The package name does not survive minification; its CSS custom properties do
     (radix-popper-*). The note I was checking was right and my probe was wrong.
  2. I ran the offline suites with `node` and got 3/10, then reported nothing until I had read the
     runner's header, which says to run it with tsx because it forwards process.execArgv. 10/10. A
     failing suite is worth one look at how you invoked it before it is worth a diagnosis.

  AND ONE CONTROL THAT FAILED HONESTLY. Every sitemap query ordered by a single column with no
  tiebreaker, and the build rewrote all six with an identical URL set in a different sequence. Fixed with
  .order('id'). But the control - run the OLD code twice, expect different output - came back
  byte-identical, so the fix is not demonstrable by re-running. Unspecified tie order is nondeterministic
  by contract and one instance was observed between the committed files and a fresh run; that is the
  case, and it is weaker than a control. Said so in the commit rather than letting "two runs now match"
  stand in for proof it never could be.
---

2026-08-28 - WEB-SEO-008: an unattributed count, followed to a real defect
  The prerender summary said "dropped 1 duplicate JSON-LD block(s)" and could not say where. Made
  dedupeJsonLd return the dropped @types and prerender name the route: /restaurants (ItemList).

  The duplicate itself was benign - Helmet mid-update, last-wins kept the settled 20-item block. The
  defect was next to it: numberOfItems came from totalCount (478) while itemListElement was sliced to 20.
  The only one of ten ItemList routes where the numbers disagreed. Structured data that contradicts
  itself is a claim a crawler can check and find false.

  THE GAP WAS IN THE GUARD'S OWN HEADER. check-prerender-content.mjs opens with "/restaurants shipped ...
  an ItemList with numberOfItems 0" and asserted the skeleton and the empty anchors but never the list.
  A file can state the defect it exists for and still not cover half of it. Worth re-reading other
  guards' headers against what they actually assert.

  WHAT I DID NOT ASSERT, and the restraint is the point: an EMPTY ItemList. A hub with nothing to show
  emits none, and the DOM cannot tell "no rows" from "rows never arrived". That is check-hub-inventory's
  question because it asks the database - the exact mistake its own header records making in v1.

  dedupeJsonLd had zero tests while deleting structured data for a living. Nine now, and the load-bearing
  ones are the no-ops (never touch a block written straight into index.html). Controlled by removing the
  data-rh guard: fails exactly those two.

  PROCESS NOTE, third time this session: a python heredoc mangled backslash escapes again, twice, and
  produced a JS parse error the second time. Line-based splicing with no escapes worked both times. Stop
  reaching for string anchors that contain \n when editing through a heredoc.
---

2026-08-28 - WEB-PERF-023: /playgrounds 2238 -> 1890, by deleting icons rather than converting them
  The story had correctly concluded the sprite lever was finished on that route: a single-path icon is
  2 nodes inline and 2 as a sprite instance. The question nobody had asked was whether the icons should
  exist. lucide-check x174 = 348 nodes, 15.5% of the page, and neither instance carried information.

  ONE OF THE TWO WAS A CORRECTNESS BUG, not a perf one. The 99 amenity filter chips have no selected
  state at all - clicking one sets the search query - so a check mark, which is the universal "this is
  on" affordance, told every visitor that all 99 filters were already applied. The DOM saving was the
  second-best reason to remove it.

  WHERE I STOPPED, and why it is a boundary rather than laziness. /restaurants is now the worst route
  (2812, 46% of it svg) and the two big blocks are 185 stars and 104 chef-hats. Both are removable, both
  save real nodes, and neither has an argument behind it beyond my taste - the chip icon is identical on
  every chip so it differentiates nothing, but "identical decoration" is a design call. Recorded the
  numbers in the story and left the decision. A DOM budget is not a mandate to strip a UI.

  Also: the story's headline numbers (/map 6564, /events/date-night 6052, /restaurants/dietary 4981) are
  all stale - /map is 836 since the Leaflet strip. Refreshed the whole table in the notes. Numbers in an
  AC decay silently, and this is the third story this week whose stated figures were wrong by enough to
  change a decision.
---

2026-08-28 - WEB-QA-018/019/017: a stale environment caveat, and one fix that was deferred for the wrong reason
  Most of the remaining open stories funnel into WEB-QA-018's re-apply decision, which is genuinely the
  owner's. What is NOT the owner's is making the migrations correct before they run.

  invoices.payment_id is now UNIQUE. WEB-QA-018's own notes reached that recommendation and then declined
  it as "polishing dead code". That reasoning holds for the edge function and not for the migration: the
  constraint is part of the shape the table gets when someone applies it. Two things already assumed at
  most one invoice per payment - billing_history LEFT JOINs on it, and generate-invoice-pdf's
  .single()-with-discarded-error branch - and neither enforced it.

  THE "NO CREDENTIALS IN THIS CONTAINER" CAVEAT IS STALE and it appears in several stories' notes. .env
  has the anon key, the REST path is reachable, and npm run build pulls 478 restaurants through it on
  every run. Read-only verification against production is available here. Anything in these notes that
  says a claim could not be checked from this environment should be re-read before it is believed.
    Used it: 11 of 11 sampled WEB-QA-018 tables absent, 5 of 5 sampled RPCs PGRST202, known_venues.image_url
    42703. Every claim I probed held. The value was confirming the evidence had not decayed, since the
    reports were written days ago and a decision is about to rest on them.
    Still unavailable: anything needing the service-role key, which I did not go looking for.

  WHAT I DID NOT DO: take the decision. WEB-QA-017 AC1, WEB-QA-019's 17 RPCs and WEB-OPS-022's AC5-AC8 all
  wait on it, which is a lot of pressure to decide it in passing. It is a product call about whether entire
  modules ship.
---

2026-08-28 - WEB-QA-005 AC2, and a note on what the backlog now looks like
  The homepage's gold CTA promised "Let AI build your itinerary" and its handler navigates straight to
  /trip-planner, where the planner sits behind <PremiumGate requiredTier="insider">. A logged-out visitor
  got the promise and then the paywall with nothing in between. Both variants now state the requirement.

  THE USEFUL PART IS THAT AC2 DID NOT NEED AC1. The story's notes had listed the copy as open alongside
  the funnel decision, which read as a dependency. It is not one: the copy was describing something that
  is not true under EITHER branch, because neither is live. An AC that follows from a decision is still
  worth re-reading for the half that is just wrong today.

  ON THE SHAPE OF WHAT IS LEFT, because it changes what this loop is worth. I surveyed eight open stories
  this iteration before finding one with unblocked work: XPLAT-008 AC2, WEB-LEGAL-011 AC8 (needs a browser
  session as an approving admin), WEB-OPS-022 AC5-AC8, WEB-QA-017 AC1, WEB-QA-019's 17 RPCs, WEB-BE-031,
  WEB-LEGAL-008 (explicitly reserved for review) and XPLAT-006 are all waiting on an owner decision or a
  credential this environment does not have. XPLAT-006 additionally carries its own advice not to do the
  web half first, which I followed rather than overrode.
  The remaining open backlog is mostly decisions, not work. Worth saying plainly rather than continuing to
  find smaller and smaller adjacent tasks.
---

2026-08-28 - WEB-SEO-017 AC2: the merge plan, and the sitemap filter that had to come first
  AC2 has been "the 10 groups still need their per-group merge" for weeks. The blocker was never the
  decision - it was that nobody had looked at the rows.

  THE FIX WOULD HAVE CAUSED A NEW DEFECT. generate-dynamic-sitemaps filtered neither is_merged nor
  is_hidden while useEventBySlug filters both, so merging would have left eleven rows in
  sitemap-events.xml with detail pages resolving to nothing. Eleven soft-404s, manufactured by the repair.
  Both filters added, and verified inert today: 413 in the window before and after, no NULL rows in
  either column, regenerated sitemaps byte-identical. Check the downstream of a data fix before making it.

  Plan: 1,193 events, 10 groups, 11 rows to merge, 0 ambiguous. --apply needs a service-role key this
  environment does not have, so the story stays open on one command.

  THE CLEAN RESULT IS THE REASON FOR THE TESTS, not a substitute for them. "0 need a human" means the
  ambiguous branch never fired, which is the exact shape of check-edge-types announcing 158 fixed errors
  while parsing nothing. 24 offline cases; the ambiguous branch is the first assertion.
    And they caught me. I asserted the field union carries an image from a loser in a two-row group. It
    cannot: with exactly one image row that row BECOMES the keeper, so a loser can only hold an image in a
    group of three or more. I had written the rule and still got its behaviour wrong from memory.

  ANOTHER SILENT ZERO, mine, caught by the same habit. My first pass reported "0 duplicate groups" because
  I selected events.description - which does not exist - and treated the 42703 error body as an empty page.
  The count check is the only reason I looked. Non-array responses now throw.
---

2026-08-28 - XPLAT-013: the mobile guard checked tables and stopped there
  check-mobile-schema-usage exists because Swift and Kotlin have no generated types. That argument does not
  stop at table names, and the guard did. A missing table kills a feature; one wrong column 42703s, both
  SDKs throw, the caller catches, and it reads as an empty list - the same silence one level down.
  22 explicit column lists now checked against 269 tables. 0 findings, and controlled: injecting a bad
  column fails with file, line, table and column; reverting passes.

  THE PARSER WAS WRONG TWICE ON REAL FILES, which is the reusable part.
    plan:subscription_plans(name)   is an embed, not a column. Split on ':' and take the right side and you
                                    report a missing column that was never a column.
    profiles:user_id (first_name)   is the same syntax meaning something else - the head is a foreign-key
                                    COLUMN, not a table. Two valid spellings, indistinguishable without the
                                    constraint graph. Accepted rather than guessed: under-checking beats a
                                    false failure in a gate that blocks merges.
  Both were found by running against the tree, not by thinking about the grammar. Neither would have
  occurred to me from the docs.

  events HAS NO `description` COLUMN, found while reading production for the merge work and confirmed here.
  iOS declares `var description: String?` - optional, so it decodes to nil. A non-optional would have
  emptied every event list in the app with nothing anywhere reporting a schema error.

  PROCESS, and I am now certain of it: python heredocs in this environment collapse backslash escapes.
  Four separate breakages this session, including one where a str.replace anchor containing an escape
  silently did NOT match, so the edit no-opped and the script kept its old output. Assert on EVERY
  replacement, or edit by line index with no escapes at all. The silent no-op is the dangerous one.
---

2026-08-28 - XPLAT-007: the export said "complete except for legal retention". It was 8 of 80.
  Measured rather than characterised: 62 PURGE_TABLES (rows we delete on request), 8 shown, 54 deleted but
  never shown, plus 18 RETAINED_TABLES with 0 shown. The 18 are the sharper half - data we decline to
  delete is data the user is MORE entitled to see.

  The notice claimed the only omissions were legally-retained records. That was the fixable defect and it
  needed no deploy: the export now names its own scope in tables_included and says it is not complete. An
  export that lists nothing implies it read everything.

  A CONCLUSION THAT REVERSED WHILE MEASURING. My instinct was to port the 62-table list into the client and
  delete the hand-maintained 8. That would be WORSE. RLS decides what a browser can read, and a table the
  user cannot select from returns [] rather than an error - so the export would assert "you have no data
  here" for every denied table. A false zero in a legal-rights document. The server-side function uses the
  service role precisely to avoid that, which is why the fix is a deploy and not a refactor.

  AND THE DEPLOY IS ONE FUNCTION, NOT THE REVIEW THE WORKFLOW WARNS ABOUT. export-user-data has no
  config.toml entry, so verify_jwt defaults to true and it is not among the 71 verify_jwt=false functions
  deploy-edge-functions.yml deliberately withholds. Its erasure counterpart delete-user-account is already
  live. "Blocked on a deploy" was true; "blocked on the 51-endpoint review" was not, and nobody had
  separated the two.

  THREE SILENT ZEROS IN MY OWN TOOLING TODAY, all caught by the same reflex of asserting the input is
  non-empty. The last one was not shell escaping: indexOf('[') after "PURGE_TABLES" finds the bracket in
  `readonly string[]`, balances immediately, and yields an empty list - so the gap reported as 0 tables.
  Anchor on the assignment, not the name.
---

2026-08-28 - WEB-QA-017: the schema check trusted a reference nobody had checked
  check-schema-usage compares code to types.ts. So types.ts is the reference, and nothing verified it.
  CLAUDE.md warns about the direction that is merely annoying (types stale, absence is not proof); the
  dangerous direction is unwritten - a column DECLARED in types.ts but absent in production type-checks,
  PASSES check-schema-usage, and 42703s at runtime. This story's own failure mode, delivered by the tool
  meant to stop it.

  Answer: 270 tables vs a 269-table snapshot, 0 ghost columns, 0 ghost tables. In sync. That is a good
  result and it needed to be a measured one - several stories lean on that reference.

  scripts/check-types-drift.mjs, offline, in pr-checks.yml. Controlled both ways (fake column, fake table),
  types.ts restored and re-verified clean after each.

  THE FALSE POSITIVE IS THE PART WORTH REMEMBERING. First run reported event_promotion_analytics_summary as
  a ghost table. It is not - an anon GET returns 200. It is a VIEW that the snapshot lists under `relations`
  and describes with no `columns`. Had I shipped on the first green-ish run, the check's one and only
  finding would have been wrong, which is how a new guard teaches people to ignore it. Column-less
  relations are now skipped AND counted in the output, so the skip cannot go unnoticed.

  Origin: I found that events has no `description` column while doing the merge work. The useful question
  was not "fix that one" but "does the reference know?" - and that is a question no story had asked.
---

2026-08-28 - WEB-QA-005 AC2 / XPLAT-009 AC4: the same false claim in three places, two of them fixed
  Two iterations ago I fixed the "AI Plan My Night" CTA and treated AC2 as done. It names TWO things, and
  the second - the "24/7 AI Assistant" stat - was the worse one: it sits among three live counts, so it
  reads as a measured figure, and it links to /trip-planner, a different and Insider-gated feature. Web has
  no AI assistant at all; Ask Pulse ships only on iOS and Android.

  THE STRUCTURED DATA MATTERED MORE than the tile, and I only found it because I grepped the BUILT page
  instead of stopping at the component. After fixing the tile, "AI Assistant" still appeared once in
  dist/index.html - from Index.tsx's applicationCategory. That property was wrong twice over: it belongs to
  SoftwareApplication, not WebSite, so it was invalid on that node and contributed nothing even before it
  was false. Checking the artifact rather than the diff is what caught it.

  WHERE I STOPPED, and this one is a real line rather than a shrug. index.html's shell still claims "First
  truly conversational AI city guide ... via web, SMS, voice assistants, and ChatGPT". Same defect, larger,
  machine-read. But the evidence is MIXED: public/.well-known/ai-plugin.json exists and answers 200 in
  production, while twilio has 0 hits anywhere. Some of those channels are real. Rewriting a company's
  positioning statement off a partial grep would risk deleting true claims to fix false ones, and that is
  the owner's call. Flagged with the evidence and its limits instead.
---

2026-08-28 - WEB-SEO-011 / WEB-SEO-017: CI answered two ACs that had been argued about for weeks
  I had gh available the whole time and had not used it. Two corrections fell straight out:

  WEB-SEO-017's TITLE IS WRONG. It says the Daily Event Crawler "is switched OFF". It is ACTIVE and has run
  on schedule every day since 2026-08-23, all green, writing rows - 12 events on the 27th. The scheduled run
  before that was 2026-02-12 and failed, so the six-month disabled stretch was real and is over. Somebody
  re-enabled it and no note records that, which also makes AC4 ("record WHY it was switched off") harder.

  WEB-SEO-011 AC2 asks whether a run completes and whether anything changes as a result. Run 33089115082:
  crawl succeeded in 6m43s, Publish Freshness ran for FOUR SECONDS and both steps skipped. A run completes
  daily and nothing reaches a crawler. That is the thesis, measured instead of argued.

  AND A BLOCKER NOBODY HAD RECORDED: regenerate-sitemaps and google-indexing-api are both 404 - never
  deployed. So setting EDGE_FUNCTION_API_KEY moves that step from "skipped" to "404", not to working.
  "Blocked on two secrets" was incomplete.

  A BUG I WROTE AND CAUGHT BEFORE COMMITTING. My first version of the fixed warning used
  `[ -z "$X" ] && missing=...`. Actions runs run: blocks under bash -e, where a test evaluating FALSE
  returns 1 and aborts the step - so it would have broken precisely when the secret IS set, which is the
  case nobody would have tested. Rewrote with plain if and ran both branches under bash -e first.

  THE HABIT WORTH KEEPING: when a story argues about whether something runs, ask the thing that runs it.
  Three ACs across two priority-1 stories were resolvable with two gh commands.
---

2026-08-28 - WEB-OPS-007: CI had the answer, and one missing token explains three stories
  Second iteration in a row where gh answered what the notes had been arguing. Two findings, both bigger
  than the code change I made.

  WEB-OPS-007's TITLE IS STALE. "57 of 61 have never run successfully" - the audit's own step reports
  16 healthy and 405 successful runs in 24h, up from 5 healthy / 266 the night before. 11 jobs started
  working, caused by b0acfaaf "fix(cron): make scheduled jobs actually run" on main. The largest movement
  this story has ever had and nothing recorded it.

  THE NIGHTLY AUDIT HAS BEEN RED TWO NIGHTS, which by this repo's own rule is how a gate gets ignored. Red
  for two reasons and only one is a problem: the baseline is 11 jobs stale, and outreach-sequencer
  (0 15 * * 3) is a WEEKLY job that is structurally absent from a 24h baseline six days in seven, then gets
  reported as "newly failing" the moment it runs. Fixed the message to distinguish a REGRESSION (was
  healthy at baseline) from a first observation. Re-baselining needs the service key; AC7 says do it
  deliberately, so I left it.

  ONE TOKEN GATES THREE STORIES. Deploy Edge Functions has run once, failed in 13s:
  "SUPABASE_ACCESS_TOKEN is not set on the 'Scrape' environment". So NO edge function has ever deployed
  through it - which is why export-user-data (XPLAT-007), regenerate-sitemaps and google-indexing-api
  (WEB-SEO-011) are all 404. Three stories each record "not deployed" as their own blocker. It is one
  secret, and the workflow already prints the exact fix.

  THE LESSON IS THE SAME AS YESTERDAY'S AND I SHOULD HAVE LEARNED IT SOONER: these notes accumulate
  reasoning about production while CI holds measurements of it. I spent several iterations reading notes
  before running two gh commands.
---

2026-08-28 - A green workflow that had never done anything, 21 times
  Swept every workflow on main rather than picking a story. Two failures I already knew about, and one
  SUCCESS that was worse than either: CI Health Report, 21 consecutive green runs, none of which sent
  anything. EDGE_FUNCTION_API_KEY is empty, so it echoed "secrets not set - skipping" and exited 0.

  THE SECOND DEFECT WAS THE DANGEROUS ONE and only showed up on reading the code after the logs. The post
  was `curl -sS ... && echo "reported ${WF_NAME}=..."`. curl exits 0 on an HTTP error, so it prints
  "reported" whatever comes back - and agent-ci-health is 404. Setting the missing secret would have
  converted 21 silent no-ops into 21 runs actively claiming to have recorded data. A skip that says nothing
  is bad; a success message that is false is worse.

  WEB-CI-004 IS MARKED PASSING AND ITS AC2 HAS NEVER BEEN TRUE. "An E2E run appears in the ingested health
  report after the fix." Its AC1 genuinely works - the trigger fires with the corrected workflow name. It
  was closed on the code change without checking the outcome, which is the thing this repo says over and
  over: verify by outcome, not by absence of error. Left passes=true and filed it under WEB-OPS-007 AC5,
  which is already the owner-facing decision about stories delivered by mechanisms that never ran.

  FOUR STORIES, ONE TOKEN. agent-ci-health joins export-user-data, regenerate-sitemaps and
  google-indexing-api as 404 for the same reason: Deploy Edge Functions has run once and failed in 13
  seconds on a missing SUPABASE_ACCESS_TOKEN.
---

2026-08-28 - WEB-SEO-006: a 200 that serves the wrong content type
  Chased the red PR Checks rather than picking a story. It fails at "Edge functions gained no type errors"
  on og-image/index.ts going 1 -> 3. Those three turned out to be SHIM ARTIFACTS - TS2614 on a remote
  import is listed as exactly that in check-edge-types' own header - so the CI failure was not the defect.
  Probing og-image anyway is what found the real one.

  /og-image.png IS NOT IN public/ AND NEVER HAS BEEN. Cloudflare answers it with the SPA shell: 200
  text/html where an image belongs. No 404, no error, no signal - the link preview just has no picture.
  BRAND.ogImage pointed at it, so six prerendered pages inherited it while 37 passed DMI-Logo.png
  explicitly. The majority looking right is why it survived. All 43 now emit the file that exists.

  TWO THINGS I WANT TO REMEMBER.
  1. A 200 with the wrong content-type is harder to find than a 404, and this project's SPA fallback
     manufactures them for every missing static file. Any check that asks "does this URL answer?" will pass
     on all of them. check-prerender-head verifies og:image is PRESENT, not that it resolves to an image.
  2. My own re-baseline banked a regression. Commit 2d3acd29 fixed the Windows glob that had the edge-type
     ratchet measuring nothing, and writing a correct baseline necessarily absorbed og-image's 1 -> 3 that
     CI had flagged the day before. The re-baseline was right and the absorption was invisible. When a
     ratchet has been blind, re-baselining it is not a neutral act - diff what you are banking.
---

2026-08-28 - WEB-SEO-006: guarding the class, and a control that failed twice
  Followed yesterday's finding rather than a story: the SPA fallback manufactures a 200 for every missing
  static file, so any check asking "does this URL answer?" passes on all of them. Swept all 35 prerendered
  pages for advertised assets and verified each against production - clean, but only because I had just
  fixed the one that was broken.

  So I built the guard. It went wrong twice and the control caught both.
    1. Stripping the host from any absolute URL and fetching the path against our origin turned every
       Supabase storage image into a false positive. Seven of them, confidently reported.
    2. Worse: the offline check looked only at root-relative paths, and its control PASSED. SEOHead builds
       og:image as `${baseUrl}${BRAND.ogImage}` - the reference that actually broke is ABSOLUTE. The guard
       examined 11 assets out of 70 and none was the one it existed for.

  THAT SECOND ONE IS THE LESSON. A guard written FROM a specific bug can still fail to cover that bug, and
  it will look healthy while doing so - 11 assets checked, green tick, nothing to see. The only thing that
  said otherwise was re-injecting the original defect and watching the check pass. Write the control from
  the ORIGINAL bug, not from the abstraction you built afterwards.

  Now: offline mode reports "MISSING not in dist/", --live reports "200 text/html", reverting passes.
  Wired into pr-checks after the build.
---

2026-08-28 - A verification iteration: three claims checked, two corrected, one guard widened
  Probed the sitemap/robots surface end to end instead of picking a story. Most of what I found was already
  known and deliberately handled, which is worth saying plainly rather than dressing up as discovery.

  WHAT WAS NEW:
  - robots.txt declares NINE sitemaps and no page links to any of them, so the asset check built last
    iteration could not see them. A Sitemap: line for a missing file fails exactly like the og:image did.
    Now covered, controlled, 21 assets checked vs 12.
  - WEB-SEO-013's title ("absent from every sitemap") is stale: production advertises 244 pSEO URLs. This
    branch takes that to 18 via the FLOOR shippable set - intentional, and worth recording before someone
    sees 226 URLs vanish from Search Console after the deploy and calls it a regression.

  TWO READINGS I GOT WRONG AND CAUGHT:
  1. classifySlugs reported "18 unrouted" and I nearly filed it as 18 sitemapped soft-404s. It deliberately
     EXCLUDES routes that serve pSEO pages and path="*" - so that output means "no OTHER route steals
     them", the healthy answer. App.tsx:619-629 routes all six pSEO patterns including /:seg1/:seg2.
  2. All 244 live pSEO URLs share the homepage's title, which looked like a duplicate-title defect at
     scale. It is the documented SPA-fallback behaviour: _middleware.ts self-canonicalises and strips the
     homepage JSON-LD, and the title is deliberately left alone because there is no per-entity data at that
     layer. Already reasoned about, already handled, pending PRERENDER_ENTITIES.

  AND ONE CLEANUP I TALKED MYSELF OUT OF: public/sitemap-index.xml duplicates sitemap.xml and is referenced
  nowhere. Deleting an orphan looks free. It is not - on this site a deleted static file serves the SPA
  shell as 200 text/html, so removing it would manufacture a fresh copy of the defect I fixed yesterday.
  Left it, and wrote down why so the next person does not re-derive it.
---

2026-08-28 - I corrected my own headline from two iterations ago
  I reported "11 pg_cron jobs now succeed" as WEB-OPS-007's largest ever movement. Five of those eleven
  call edge functions that return 404. A pg_cron job here posts via net.http_post and SUCCEEDS when the
  POST is ENQUEUED, not when the call lands - so a job firing into nothing reports success forever.

  I took "succeeded" from a health RPC at face value because it was a number from production rather than a
  note, and I had spent two iterations learning to trust production over notes. The lesson does not
  generalise that far: a measurement is only as good as what it measures. cron_health measures the TRIGGER.
  Checking the eleven targets took one loop and should have been part of the original claim.

  IT IS ALSO WEB-OPS-007 AC6's OWN GAP, INVERTED. AC6 says the watchdog observes automation_job_runs, so a
  job that never fires is invisible. cron_health observes the trigger, so a job whose target is missing
  looks healthy. Two monitors, mirror-image blind spots, and nothing between them verifies the work
  happened. send-event-reminders is deployed and answering 500, which neither monitor would surface either.

  THE NUMBER WORTH KEEPING: 65 undeployed edge functions reads as one large security review. It is not.
  52 are verify_jwt=false; THIRTEEN are JWT-gated and can ship individually without adding a single open
  endpoint. That batch alone unblocks WEB-SEO-014's whole OAuth chain (gsc-oauth, gsc-fetch-properties,
  gsc-sync-data are ALL 404, so "complete the OAuth connection" is currently impossible, against a refresh
  token that lapses ~2026-09-30), XPLAT-007's export, XPLAT-008's two Test buttons, and two hollow cron
  jobs. One secret: SUPABASE_ACCESS_TOKEN on the Scrape environment.
---

2026-08-28 - WEB-OPS-023: the switch was set, just not where the code reads it
  The story says the whole chain is "off by default" and unverified. Production disagrees: the /media route
  serves 200 image/jpeg with max-age=31536000 and cf-cache-status HIT, and 277 of 284 events, 326 of 326
  project-hosted restaurants, 16 of 16 attractions and 3 of 3 articles are already on it. AC2 works and
  most of AC5 has happened.

  THE SEVEN THAT ARE NOT ARE THE WHOLE STORY. All seven were created in ONE crawl - 2026-08-27 15:46 - and
  every repointed row predates it. So the backfill ran and the write path kept producing more.

  AC3 POINTS AT THE WRONG ENVIRONMENT. cdnUrlFor switches on Deno.env.get("MEDIA_CDN_BASE") - a Supabase
  edge function variable - and AC3 says to set it "in the Cloudflare Pages environment", which is where the
  Pages route reads its own separate copy. The runbook has it right; the AC does not. Following the AC
  alone would change nothing and give no sign of it, which is exactly what happened.

  ANOTHER SILENT ZERO, MINE. My first ad-hoc count reported "articles: 0 images". articles has no
  image_url column - it is featured_image_url - so a 42703 was read as an empty count. There are 3. The
  committed check errors on any non-2xx, which is the only reason I found out. That is the fourth silent
  zero I have produced this session; the pattern is always the same shape - an error body parsed as data.

  TWO ENV VARS WITH ONE NAME AND TWO HOMES is a trap worth remembering: functions/media reads MEDIA_CDN_BASE
  from Pages, imageStorage reads it from Supabase secrets. Same name, different runtimes, and setting one
  looks like setting both.
---

2026-08-28 - PR Checks had TWO failing jobs and I only chased one
  Last time I followed the edge-types failure and stopped. The same run had a second red job. It has been
  red on every pull request since PR #373 merged: the app type ratchet, 375 errors against a 374 baseline,
  one new error in useCommunityFeatures.ts.

  MIGRATION 20260827000002 WAS MERGED AND NEVER PUSHED. event_attendance_tallies answers PGRST202 in
  production while voting_category_tallies (an earlier migration) answers 200, and it is not in the drift
  baseline - so this is not drift, it is a file nobody ran. getEventCheckIns therefore returns zeros for
  every event, invisible only because event_attendance holds no rows. The comment directly above that call
  exists to prevent a false zero; the unapplied migration reintroduced one three lines below it.

  I ALMOST RE-BASELINED, which the failing output itself suggests. strict-ratchet.mjs's header stopped me:
  re-baseline on LINUX ONLY, because a Windows baseline turned the check permanently red for everyone on
  2026-08-23 and was reverted. Used the repo's existing `as never` idiom instead, which is what the two
  other pending-RPC call sites do. 374/70, baseline held, 7 vitest cases still green.
    Worth recording against that caveat: my local count matched CI exactly (375 errors, 71 files, both). The
    platform gap it warns about may have closed. One matching run is not enough to overturn a warning that
    already cost a revert, so I left the caveat alone and said why.

  THE HABIT TO FIX: when a CI run has multiple failing jobs, read them all before picking one. I lost a
  day's worth of red PRs to reading the first failure and treating it as the failure.
---

2026-08-28 - The third failing check, found only by asking a different question
  Last iteration's lesson was "read all failing jobs, not the first". I did that - `gh run view --json jobs`
  on the PR Checks run, two failures, both handled. There was a THIRD, in a different workflow, and it only
  appears under `gh pr checks`. Reading the RUN and reading the PR are not the same question.

  "Edge function & migration invariants" has been red since 2026-08-27, green on every run before it:
      error: Could not find "@types/node" in a node_modules folder.
  marketing-optout-agents.test.ts is the only one of eleven suites importing an agent that imports
  supabase-js. Deno type-checks the downloaded index.d.mts, which references @types/node, and that job is
  checkout + setup-deno with no npm ci. The run dies before one test executes and takes the suites after it
  down too.

  IT PASSES LOCALLY. I ran the bare command and got 10 passed, because this tree has node_modules/@types/node.
  Reproducing needs a machine without one - every runner and no developer. That is the whole reason a test
  added to prove an opt-out works has been silently voiding its own job for two days.
  Fixed with --no-check=remote: the test's source is still checked, only downloaded .d.ts is skipped.

  AND I NEARLY SHIPPED A LOCKFILE I COULD NOT VERIFY. The verification run added esm.sh integrity hashes to
  deno.lock and they rode along in the commit. esm.sh rebuilds ?target=denonext output independently of the
  package version, so hashes resolved on my machine could differ on a runner - trading an @types/node
  failure for an integrity failure. Reverted in a follow-up. Check what a verification run wrote before
  committing what it wrote.
---

2026-08-28 - Verifying the fix rather than the symptom
  The @types/node failure killed the deno job AT marketing-optout-agents, so the nine suites ordered after
  it had not run in CI since 2026-08-27. Fixing the one suite would have made the job get further, not go
  green - those nine were unknowns. Ran all of them: 17 suites, 131 tests, 0 failures. Also checked both
  directions between disk and workflows: no orphan suite, no missing file.

  ENVIRONMENT CORRECTION, and this is the second one this session. Notes here and in WEB-SEC-025 say deno
  suites need `npx deno@2` with an import map because esm.sh 403s through the agent proxy. deno is
  installed and esm.sh resolves; `deno test` works directly. Combined with the earlier credentials
  correction, the pattern is clear: environment caveats in these notes are the fastest-decaying claims in
  the file and should be retried, not believed.

  WHERE THINGS STAND, since it is worth stating once plainly rather than at the end of every iteration:
  all three PR-blocking CI failures on main are fixed on this branch, npm run validate is clean, eight
  post-build gates and 12/12 offline suites pass, and 17/17 deno suites pass. The remaining open stories
  are overwhelmingly waiting on an owner decision or a credential - most of them on the single
  SUPABASE_ACCESS_TOKEN. The branch has 122 commits and has never been pushed.
---

2026-08-28 - The loose thread from two days ago was a real defect
  When I probed the eleven "now succeeding" cron jobs I noted send-event-reminders returning 500 and wrote
  "worth its own look". Looked:
      42804  Returned type character varying(255) does not match expected type text in column 3.
  get_pending_reminders declares user_email TEXT and selects auth.users.email, which Supabase defines as
  varchar(255). PostgreSQL does not coerce a RETURNS TABLE column, so the function raises before returning
  anything. NO EVENT REMINDER HAS EVER BEEN SENT, on an hourly schedule.

  Fixed with one cast in migration 20260828000001. Deliberately not a blanket cast - the other four text
  columns are genuinely TEXT in their own migrations; only the auth.users column, which this project does
  not own, is varchar. One line of body diff.

  IT IS THE CONCRETE VERSION OF WEB-OPS-007 AC6, with both blind spots firing at once. The cron trigger
  succeeds because net.http_post was ENQUEUED. No automation_job_runs row is written because the function
  raises before reaching that code. So the trigger monitor says healthy and the work monitor says nothing,
  and an hourly job that has never delivered looks exactly like one with nothing to do.

  THE HABIT THAT FOUND IT: I wrote "worth its own look" in a note rather than chasing it, and then actually
  went back. Most of what I have flagged this week for later has been owner-blocked; this one was three
  commands away. Re-read your own parked items before hunting for new ones.
---

2026-08-28 - Re-reading parked items found a real auth hole
  Last iteration's lesson was to re-read my own parked notes. One said dispatch-scheduled-newsletters
  "returned 200" to an anon POST and I had assumed that was fine. It was not.

  Its docstring claims verify_jwt=false plus an admin-bearer check. There is no config.toml entry at all,
  so verify_jwt defaults to TRUE - and verify_jwt only means "a valid Supabase JWT". The publishable ANON
  KEY is one, and ships in every client bundle. No code in the function looked at the role. Verified both
  ways against production: no header -> 401, anon bearer -> 200 and the job ran. It then does service-role
  writes and dispatches mail through Resend.
  Fixed with requireAdminOrApiKey - the cron sends a service-role bearer, which that helper accepts, so the
  scheduled path is unchanged.

  I INVOKED IT BEFORE READING IT, which was the wrong order for an endpoint that sends email. It is a no-op
  with nothing scheduled and nothing was, but I got that right by luck rather than by care. Read the source
  of anything with side effects before poking it.

  THE SWEEP IS TRIAGE AND I ALMOST REPORTED IT AS A FINDING. First pass: "52 deployed functions use a
  service-role client with no guard". That included assign-role, which verifies the caller is admin
  server-side via admin.auth.getUser(bearer) - my regex just did not know that idiom. Tightening the
  definition of "guard" to include auth.getUser, API keys and webhook signatures took it to 23, and several
  of those are public by design. A big scary number from a crude regex is not a finding; the one I
  confirmed and the one I spot-checked are.
  Confirmed second instance: backfill-all-coordinates, zero auth references, service-role client, geocodes
  four tables at cost per call.
---

2026-08-28 - Fixed the second confirmed auth hole, then guarded the class instead of the instances
  backfill-all-coordinates: zero auth references, service-role client, geocodes four tables at a cost per
  address. Guarded with requireAdminOrApiKey after checking BOTH callers survive - pg_cron sends a
  service-role bearer and raises rather than posting unauthenticated (20260823000008:68-74), and the only
  UI caller is CoordinateManager inside AdminTools, behind <ProtectedRoute requireAdmin>. So the route
  already assumed admin and the server simply was not enforcing it. Same shape as WEB-SEC-025's
  event_attendees: a control enforced client-side is not a control.

  I STOPPED FIXING AT TWO, on purpose. The remaining candidates have real but modest side effects and
  unverified caller lists, and mass-guarding them would risk breaking working paths - the exact failure I
  have avoided all session. Built a ratchet instead: 148 service-role functions, 25 verify no caller,
  baselined so the list can only shrink.

  THE CONTROL FAILED THE FIRST TIME AND THAT IS THE INTERESTING PART. I removed the guard CALL from
  backfill-all-coordinates, left the comment explaining it, and the check PASSED - the regex matched
  "requireAdminOrApiKey" in prose. A function whose docstring promises a check it does not perform is
  EXACTLY this defect; dispatch-scheduled-newsletters documented an admin-bearer check and had none. So the
  first version of the guard would have been fooled by the very thing it was written to catch. Comments are
  stripped before matching now, and the control fires.
  That is the third checker I have written this session whose control caught it missing its own bug. The
  pattern is consistent enough to state as a rule: build the control out of the ORIGINAL defect, and run it
  before believing a green result.

  ALSO WORTH KEEPING: my first sweep said 52 unguarded and was wrong, because it did not recognise
  admin.auth.getUser(bearer) as a check - assign-role does exactly that. A scary number from a crude regex
  is not a finding. The two I confirmed by reading and probing are.
---

2026-08-28 - Two more guarded, chosen because their callers were PROVABLE
  Migration 20260823000008 enumerates every pg_cron job against the function it posts to. That turns "who
  calls this" from a grep into a fact for the functions in that table, which is the difference between
  guarding safely and breaking a working path. auto-enrich-restaurants: cron is the only caller anywhere.
  generate-weekend-guide: cron plus WeekendGuideManager, mounted only in AdminTools behind requireAdmin.
  25 -> 23 baselined.

  generate-weekend-guide SPENDS MODEL BUDGET and my own triage said it did not. I had grepped each function
  for anthropic/openai/CLAUDE_API; it calls Claude through _shared/aiConfig. A per-file regex does not see
  through an import, and I used one to decide which functions were worth caring about. The two I ranked as
  "modest side effects" may deserve another look on the same grounds.

  WHAT I AM DELIBERATELY NOT DOING: guarding the other 23 in a batch. Each needs its callers enumerated the
  way these four did, several are public by design (og-image, log-error), and a wrong guard breaks a
  working path silently. The ratchet holds the line; the triage is a human's to work through.
---

2026-08-28 - Re-triaged the 23 by following imports, and got it wrong again in a new way
  Last commit's lesson was that a per-file regex cannot see through an import - it had told me
  generate-weekend-guide did not spend money when it calls Claude via _shared/aiConfig. So I traced one
  level into _shared. The ranking changed materially: 13 of 23 reach a paid API, not the handful the flat
  grep found. Mail senders, model spend, Google APIs.

  THE NEW PASS THEN PRODUCED ITS OWN FALSE POSITIVE, and it is a good one. It flagged log-content-metrics
  for matching "geocode" - inside a COMMENT explaining that the function is public ON PURPOSE (anonymous
  visitors report Core Web Vitals; a guard would delete the feature) and is bounded by a RATE LIMIT
  instead. So the second regex was fooled by prose exactly as the ratchet was two commits ago. Same
  mistake, different tool, one day apart.

  WHAT I DID ABOUT IT rather than just noting it: the baseline now carries a `notes` map - what each entry
  does, whether it spends money, and whether guarding is even correct. log-content-metrics is marked "do
  not guard". A bare list of 23 names invites the batch-guarding I keep refusing; a list with reasons is
  something a human can actually work through. --write regenerates the names and PRESERVES the notes, so
  re-baselining cannot silently throw the triage away.

  THE PATTERN, stated once because it has now happened three times in two days: every regex I write over
  this codebase eventually matches a comment, and comments in this repo are unusually long and quote code.
  Strip comments before matching, or expect prose to answer for code.
---

2026-08-28 - Built the check I had said needed a service-role key, and it corrected me
  Two days ago I wrote that cross-referencing cron targets against deployed functions was "for whoever has
  the service-role key". It is not: the /functions/v1/<name> URLs are written into the migrations, and the
  deploy baseline is committed. Entirely offline.

    57 functions are posted to from migrations; 35 have never been deployed.
  29 are the agent-* fleet - one undeployed subsystem, and the output groups them so it does not read as 29
  separate problems.

  IT ALSO CAUGHT AN ERROR IN MY OWN HEADLINE. I reported FIVE of eleven "now succeeding" jobs firing into
  404s. It is four. enhance-events-morning targets batch-enhance-events (20250107000006:46), which is
  deployed - I had inferred the function name from the job name. I even wrote at the time that the
  hand-mapping was "good enough to report and not good enough to gate". It was not good enough to report.
  Ten of the eleven guesses were right, which is exactly why the wrong one survived.

  THE RULE I KEEP RELEARNING, in a third form this week: do not infer an identifier from a name that
  resembles it. The migration states the mapping; read the mapping. Same family as inferring a column from
  a model field and a table from a story's prose.
---

2026-08-28 - AC5 asked how many closed stories never actually shipped. It is 48.
  WEB-OPS-007 AC5 says "the 17 stories already marked done whose only delivery mechanism is one of these
  jobs". Cross-referenced every closed story against the deploy baseline:
      58  AOS-* stories marked passes = true
       3  every function they name is deployed
      48  name at least one function that has NEVER been deployed
       7  name no edge function at all
  The Autonomy OS is recorded in this backlog as substantially complete and almost none of it runs. Every
  family is affected - CORE, CS, DEV, all seven MAINT, all seven NURTURE, PROSPECT, SEC.

  I RESTRICTED IT TO AOS-* ON PURPOSE. The proxy is "the story names the function", standing in for "the
  story is delivered by the function". That holds for AOS stories, where the agent IS the deliverable
  (AOS-MAINT-003 is "Backup verification agent"; agent-backup-verify is what it shipped). It does not hold
  for a story that mentions a function in passing - WEB-BE-032 names a dozen while burning down discarded
  errors. A naive scan said 40 stories across all prefixes; that number would have been wrong in the way
  that gets repeated.

  ONE CAUSE, NOT 48. Deploy Edge Functions has run once, failed in 13s on a missing SUPABASE_ACCESS_TOKEN.
  So AC5 is not really "what about 17 stories that quietly did not work" - it is "the agent fleet has never
  run at all; deploy it or descope it". That is a different and much larger question, and it is the
  owner's.
---

2026-08-28 - Costing the deploy-or-descope decision instead of restating it
  Yesterday's finding was that 48 closed AOS stories name a function that has never been deployed. The
  useful follow-up is not "that is bad" but "what would it cost to fix, and what would it risk".

    49  undeployed functions behind those stories
    49  are WRITTEN - 8,250 lines of index.ts, zero missing directories
  So deploy = run a deploy. Descope = delete code that was already paid for.

  AND THE SECURITY REVIEW THE WORKFLOW ASKS FOR IS ANSWERABLE. deploy-edge-functions.yml withholds them
  because "51 new public endpoints is a review, not a side effect of merging" and notes they "have
  in-function guards". Checked rather than trusted:
      49 of 49 verify_jwt = false   -> yes, gateway-open
      49 of 49 have an in-function caller check
       0 of 49 have none
  Every one imports requireAdminOrApiKey and calls it as the handler's first statement. Spot-checked three
  in source with comments stripped, because a 49/49 result from a regex is exactly the shape that has
  fooled me three times this week.

  SO THE REVIEW IS DONE AND THE ANSWER IS FAVOURABLE: deploying the fleet adds no unauthenticated endpoint.
  What remains is a scope question - is the Autonomy OS something to run at all - and that is the owner's.
  Worth separating explicitly, because "51 new public endpoints" reads like a safety blocker and is not
  one.
---

2026-08-28 - Audited the CI steps I added but have never watched run
  Seven new steps across five workflows this session, none of which has executed in CI because the branch
  is unpushed. Writing a gate you have never seen run is how the edge-type ratchet came to measure nothing
  on Windows, so I checked them rather than assuming.

    all five workflows parse, with the triggers they had before
    check-asset-content-types reads dist/ and sits at pr-checks:258, after the build at :178
    check-edge-auth, check-types-drift and check-mobile-schema-usage run in migration-safety, which has a
      checkout and NO npm install - and all three import only node:fs/path/url, so that is fine
    check-media-url-drift carries the VITE_SUPABASE_* env block it needs; check-cron-targets is offline

  ONE THING A REVIEWER WILL NOTICE AND SHOULD NOT BE PUZZLED BY: rls-config-audit.yml shows 371 changed
  lines for 55 lines of real content. The file on main has MIXED CRLF/CR/LF terminators and my edits
  rewrote it as LF throughout. .gitattributes declares `* text=auto`, so LF in the repo is the stated
  policy and many workflow blobs simply predate it - normalising follows the policy rather than fighting
  it. I did not renormalise the rest of the repo, which would be a large unrelated change. No other file
  on the branch has EOL churn above 40 lines; I checked all of them.
---

2026-08-28 - Verified the social-card path end to end, and found it uncached
  Chased a loose end: og-image is deployed, but my earlier probe got the FALLBACK. That was my error - it
  takes a PATH (/og-image/<type>/<id>), not query params, so ?title=test correctly fell through. Same
  mistake as inferring a cron target from a job name, twice in three days.

  With the real interface it works, and so does the middleware. Using a REAL sitemapped URL rather than a
  slug I built myself:
      facebookexternalhit -> og:image = .../og-image/event/<real id>, og:title = the real event title
      plain browser       -> the SPA shell, which is correct (Helmet fixes it after hydration)
      that og:image URL   -> 200 image/jpeg, ~30 KB, a genuinely rendered card
  My first attempt used a slug I constructed and got the shell for both, which I nearly wrote up as "the
  middleware does not fire". It fires; my URL did not exist, and the middleware fails open by design.

  THE FINDING IS THAT NOTHING CACHES IT. Two consecutive fetches both return cf-cache-status: DYNAMIC. The
  og:image points at the supabase.co origin, so it never reaches the Cloudflare cache that /media gets -
  /media returns HIT. Every crawler fetch re-invokes the function, re-inits resvg WASM, re-renders and
  re-encodes. Recorded under WEB-OPS-023 as an adjacent term, explicitly smaller than its main one: cards
  are fetched by unfurlers, not by every visitor.
  Not built. Proxying og-image through the site domain would work, but it changes a URL social platforms
  have cached, and that is a design decision.
---

2026-08-28 - WEB-SEO-006: the fix is already built and pointed at the wrong crawlers
  Asked what a search crawler actually receives on an entity page, having verified the social path works.

    facebookexternalhit  <title> = the real event title, og:title, real description
    Googlebot / GPTBot   <title> = "Things to Do in Des Moines This Weekend", homepage description

_middleware.ts:268 rewrites the real <title>, not just og/twitter tags. So the machinery that would give
  Google and every AI crawler a correct title and description across all 884 entity URLs exists, is proven
  in production, and is gated at :237 on a CRAWLER_UA list that includes Slackbot and excludes Googlebot,
  GPTBot, ClaudeBot and PerplexityBot.
  And those crawlers are allowed in - robots.txt has 28 groups; GPTBot, ClaudeBot, PerplexityBot and
  Google-Extended all get Allow: / while anthropic-ai, CCBot, Bytespider and Scrapy get Disallow: /.

  I DID NOT ADD THEM TO THE LIST, and the reason is a line the middleware's own header draws: it removes
  what is "actively harmful" pending PRERENDER_ENTITIES. Widening the UA list is not that - it is adopting
  DYNAMIC RENDERING for search, which Google tolerates when content is equivalent and recommends against
  in favour of the SSR that PRERENDER_ENTITIES provides. One line, real upside, a strategy choice rather
  than a defect. Recorded as a costed option.

  THE HABIT WORTH NAMING: three times this week the most valuable thing was asking what a DIFFERENT client
  sees. Googlebot vs a browser found this. facebookexternalhit vs Googlebot found that <title> is already
  rewritten. An anon bearer vs no bearer found the newsletter dispatcher. The site behaves differently per
  caller in more places than the notes record.
---
