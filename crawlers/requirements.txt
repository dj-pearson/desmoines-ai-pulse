# CatchDesMoines Event Crawler Dependencies
# Requires Python 3.10 or 3.11 (NOT 3.13 - compatibility issues)

# Crawl4AI - LLM-friendly web crawler
crawl4ai>=0.7.7

# HTTP client for Supabase
httpx>=0.25.0

# Supabase Python client
supabase>=2.0.0

# Date parsing
python-dateutil>=2.8.2

# Environment variables
python-dotenv>=1.0.0

# Anthropic Claude API
# Pinned to the 1.x API. anthropic 1.0.0 removed temperature from
# Messages.create(); an unpinned >=0.39 quietly upgraded CI into it.
anthropic>=1.0.0,<2.0.0

# NumPy (pinned for stability)
numpy>=1.26.0,<2.0
