import AdminPlaceholder from "@/components/admin/AdminPlaceholder";

export default function AdminSocial() {
  return (
    <AdminPlaceholder
      title="Social media"
      storyId="ADMIN-SOCIAL-001 / ADMIN-SOCIAL-002"
      description="Two-tab page: Accounts (X/Twitter, Facebook, Instagram, LinkedIn credentials manager) and Queue (scheduled/sent/failed post dashboard with retry and realtime updates)."
    />
  );
}
