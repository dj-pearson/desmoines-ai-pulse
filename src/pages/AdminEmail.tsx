import AdminPlaceholder from "@/components/admin/AdminPlaceholder";

export default function AdminEmail() {
  return (
    <AdminPlaceholder
      title="Email and newsletter"
      storyId="ADMIN-EMAIL-001 / ADMIN-EMAIL-002"
      description="Subscribers tab (list, search, export, segment, unsubscribe) and Campaigns tab (composer, preview, segment selector, scheduled send via Resend)."
    />
  );
}
