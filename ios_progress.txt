# iOS Native App — Progress Log

## Codebase Patterns (iOS-Specific)

### Platform Detection
- Use `isCapacitor()` from `@/lib/capacitorUtils` for runtime detection
- Use `isMobileApp()` for build-time detection (__MOBILE_APP__ flag)
- Use `getPlatform()` to distinguish 'ios' vs 'android' vs 'web'
- CSS: scope iOS-only styles under `.capacitor-app` class (added to html+body in main.tsx)

### Capacitor Plugin Access
- Access plugins via `window.Capacitor?.Plugins?.PluginName`
- Always guard with `isCapacitor()` check first
- Available: Browser, SplashScreen, StatusBar, Haptics, Share, App, PushNotifications, LocalNotifications, Camera, Geolocation, Network, Preferences, Keyboard

### Key Utilities
- `hapticTap()` — light haptic feedback (Capacitor Haptics plugin)
- `openExternalUrl(url)` — opens in system browser via Browser plugin
- `nativeShare(opts)` — native share sheet via Share plugin
- All in `src/lib/capacitorUtils.ts`

### CSS Architecture
- Global Capacitor overrides in `src/index.css` under `/* CAPACITOR / MOBILE APP OVERRIDES */`
- Forces light theme, iOS system font, compact headers, native-feel cards
- Safe area support via `env(safe-area-inset-*)` CSS functions
- Existing utility classes: safe-area-top, safe-area-bottom, safe-area-inset, touch-feedback, swipeable, img-mobile, btn-mobile, card-mobile, sheet-mobile, skeleton-mobile, loading-shimmer

### Mobile Build
- Config: `mobile-app/capacitor.config.ts`
- Vite config: `mobile-app/vite.config.mobile.ts`
- iOS project: `mobile-app/ios/App/`
- Build: uses `__MOBILE_APP__` define for tree-shaking heavy web-only code (Three.js)

### Quality Checks
```
npm run type-check && npm run lint   # Must pass before commit
npm run build                        # Production build must succeed
```

---

## Progress Log

## 2026-03-03 — IOS-001
- **Implemented**: Fix image overflow breaking page layout on iOS
- **Changes**:
  - `src/index.css`: Added global `img, video { max-width: 100%; height: auto }` CSS reset in @layer base
  - `src/index.css`: Added `html.capacitor-app` to overflow-x: hidden rule (was only body/#root)
  - `src/index.css`: Added `.capacitor-app img { max-width: 100% !important }` safety net
  - `src/components/EventCard.tsx`: Added `overflow-hidden` to image container div
  - `src/pages/EventsNearMe.tsx`: Wrapped bare `<img>` in overflow-hidden container
  - `src/pages/Index.tsx`: Wrapped modal event image in overflow-hidden container
- **Root Cause**: Images with intrinsic widths larger than viewport pushed the entire page wider on iOS WKWebView. The `w-full` Tailwind class sets `width: 100%` but without `max-width: 100%`, the image's natural size can expand the parent container.
- **Learnings for future iterations:**
  - Always pair `w-full` with a container that has `overflow-hidden` on images
  - iOS WKWebView needs overflow-x: hidden on the `<html>` element, not just body
  - The Capacitor CSS section only had body/#root — html element was missing
  - Global `img { max-width: 100% }` is a modern CSS reset best practice
---

## 2026-03-03 — IOS-002
- **Implemented**: Improve BottomNav haptic feedback with Capacitor Haptics
- **Changes**:
  - `src/components/BottomNav.tsx`: Replaced `navigator.vibrate(10)` with `hapticTap()` from capacitorUtils
- **Why**: `navigator.vibrate()` is a web API with poor iOS support. The Capacitor Haptics plugin provides proper UIKit haptic feedback (UIImpactFeedbackGenerator) that feels native.
- **Learnings for future iterations:**
  - Always use `hapticTap()` instead of `navigator.vibrate()` for iOS haptics
  - `hapticTap()` is a no-op on web, so safe to call unconditionally
---

## 2026-03-03 — IOS-003
- **Implemented**: Add haptic feedback to favorite, share, and rating actions
- **Changes**:
  - `src/components/FavoriteButton.tsx`: Added `hapticTap()` on favorite toggle click
  - `src/components/ShareDialog.tsx`: Added `hapticTap()` before native share
  - `src/components/RatingSystem.tsx`: Added `hapticTap()` on star rating click
- **Why**: Tactile feedback on key user interactions (favorite, share, rate) makes the app feel significantly more native and responsive.
- **Learnings for future iterations:**
  - Keep haptic calls lightweight — only on user-initiated actions, never on data loads
  - `hapticTap()` is async but fire-and-forget (no need to await)
---

## 2026-03-03 — IOS-019
- **Implemented**: Fix double-tap zoom on iOS interactive elements
- **Changes**:
  - `src/index.css`: Added `touch-action: manipulation` to all interactive elements (button, a, [role="button"], input, select, textarea, label) scoped to `.capacitor-app`
- **Why**: iOS double-tap-to-zoom on buttons/links causes a 300ms delay and unexpected zooming. `touch-action: manipulation` prevents this while preserving pinch-to-zoom on images and maps.
- **Learnings for future iterations:**
  - `touch-action: manipulation` is the modern replacement for the deprecated `<meta name="viewport" ... user-scalable=no>`
  - Scope to `.capacitor-app` so web users keep full zoom capability
  - Applies to all form elements too (prevents zoom on input focus for 16px+ font sizes)
---

## 2026-03-02 — IOS-017
- **Implemented**: Cache images locally for faster repeat loads
- **Changes**:
  - `public/sw.js`: Updated image cache limit from 50 to 100. Added LRU eviction — when cache exceeds limit, oldest entries (first in `cache.keys()`) are deleted. Re-puts cached responses on hit to move to front.
- **Learnings**: `cache.keys()` returns entries in insertion order — re-putting an entry moves it to the end (newest). Deleting from the start of the keys list implements simple LRU.
---

## 2026-03-02 — IOS-016
- **Implemented**: Optimize list rendering with virtualization for long lists
- **Changes**:
  - `src/hooks/useVirtualList.ts`: New hook — lightweight windowed rendering using scroll position + requestAnimationFrame. Only virtualizes when item count exceeds threshold (50). Returns startIndex/endIndex for slicing visible items, totalHeight/offsetTop for spacer positioning.
- **Learnings**: Use `requestAnimationFrame` for scroll handlers to avoid jank. 2-screen overscan buffer prevents visible gaps during fast scrolling.
---

## 2026-03-02 — IOS-015
- **Implemented**: Add iOS-native image viewer with pinch-to-zoom and swipe-to-dismiss
- **Changes**:
  - `src/components/ImageViewer.tsx`: New component — wraps children as a tap target, opens full-screen overlay with pinch-to-zoom (Math.hypot for 2-finger distance) and swipe-down-to-dismiss (opacity fades, 100px threshold). Only in Capacitor.
- **Learnings**: Use `touch-action: none` on the viewer element to prevent browser handling pinch/swipe. Track `isDismissing` ref to distinguish pinch from dismiss gestures.
---

## 2026-03-02 — IOS-014
- **Implemented**: Fix status bar text color on dark hero images
- **Changes**:
  - `src/hooks/useStatusBarStyle.ts`: New hook — switches StatusBar style between LIGHT/DARK based on current route. Detail pages and home use LIGHT (white text), list pages use DARK.
  - `src/App.tsx`: Wired `useStatusBarStyle()` into KeyboardShortcutsProvider
- **Learnings**: StatusBar.setStyle accepts `{ style: 'LIGHT' | 'DARK' }` — LIGHT = white text, DARK = black text (confusingly named).
---

## 2026-03-02 — IOS-013
- **Implemented**: Add swipe-back gesture support for navigation
- **Changes**:
  - `src/hooks/useSwipeBack.ts`: New hook — detects left-edge swipe gestures (30px zone) with threshold (80px) and velocity (0.3px/ms) checks, triggers `navigate(-1)`. Only iOS Capacitor, disabled on home page.
  - `src/App.tsx`: Wired `useSwipeBack()` into KeyboardShortcutsProvider
- **Why**: iOS users expect swipe-from-left-edge to go back. This JS implementation works in WKWebView alongside the native gesture support.
- **Learnings for future iterations:**
  - Edge zone (30px) prevents false positives from normal horizontal scrolling
  - Check `deltaY > deltaX` to distinguish vertical scrolls from horizontal swipes
  - Disable on home page (`/`) since there's nothing to go back to
  - `{ passive: true }` on touch listeners is important for scroll performance
---

## 2026-03-02 — IOS-012
- **Implemented**: Implement deep linking for event/restaurant URLs
- **Changes**:
  - `src/hooks/useDeepLinks.ts`: New hook — listens for `appUrlOpen` event from Capacitor App plugin, parses incoming URL, extracts path, and navigates via React Router. Handles both full URLs and path-only strings.
  - `src/App.tsx`: Wired `useDeepLinks()` into KeyboardShortcutsProvider
  - `public/.well-known/apple-app-site-association`: Already exists with proper paths (/events/*, /restaurants/*, /attractions/*, etc.)
- **Why**: Deep links allow shared URLs to open directly in the app instead of the browser, providing a seamless user experience.
- **Learnings for future iterations:**
  - The Capacitor App plugin fires `appUrlOpen` for both Universal Links and custom URL schemes
  - Always parse the URL to extract just the path — the full URL includes the domain
  - The AASA file must be served from `/.well-known/` with `Content-Type: application/json` (Cloudflare Pages handles this)
  - The listener returns a `{ remove }` object from `addListener`, not a Promise-based cleanup
---

## 2026-03-02 — IOS-011
- **Implemented**: Wire up push notification registration and event reminders
- **Changes**:
  - `src/hooks/usePushNotifications.ts`: New hook — registers with APNs/FCM via Capacitor PushNotifications plugin, stores device token in Supabase `device_tokens` table, handles notification tap navigation to event/restaurant detail pages. Also exports `scheduleEventReminder()` for local notifications.
  - `src/lib/capacitorUtils.ts`: Added PushNotifications and LocalNotifications plugin types
  - `src/components/EventReminderSettings.tsx`: Added local notification scheduling when toggling reminders on Capacitor. Added `eventTitle` and `eventDate` optional props.
  - `src/App.tsx`: Wired `usePushNotifications()` into KeyboardShortcutsProvider for auto-registration on launch
- **Why**: Push notifications are essential for event reminders on mobile. The mobile-app/ directory had a separate implementation, but the main web app (which gets bundled into Capacitor) needed its own version using window.Capacitor.Plugins.
- **Learnings for future iterations:**
  - Capacitor PushNotifications plugin requires `checkPermissions` → `requestPermissions` → `register` flow
  - `device_tokens` table upsert uses `onConflict: 'token'` to prevent duplicates
  - Local notifications need a stable numeric ID — use a hash function on `eventId + reminderType`
  - The `registration` event fires after `register()` with the device token
  - `pushNotificationActionPerformed` fires when user taps a notification — use for deep linking
---

## 2026-03-02 — IOS-010
- **Verified**: Optimize hero section for Capacitor (skip Three.js) — already fully implemented
- **No changes needed**:
  - `src/components/EnhancedHero.tsx`: Already sets `HeroCityLite = null` via `__MOBILE_APP__` build-time constant
  - `src/index.css`: `.capacitor-app section[class*="min-h-"]` already forces `min-height: auto !important`
  - `src/components/QuickActions.tsx`: `QuickActionsMobile` variant already provides large touch targets (h-20) with prominent gold gradient AI action
- **Why no code changes**: All acceptance criteria were already satisfied by previous implementation. Three.js is completely tree-shaken out of mobile builds, hero is compact, quick actions are prominent.
- **Learnings for future iterations:**
  - Build-time constants (`__MOBILE_APP__`) are the most reliable way to exclude heavy dependencies — bundler eliminates entire code paths
  - `min-h-auto !important` in Capacitor CSS is more flexible than a fixed mobile height — lets content flow naturally
---

## 2026-03-02 — IOS-009
- **Implemented**: Add network status indicator for offline/slow connections
- **Changes**:
  - `src/hooks/useNetworkStatus.ts`: Added Capacitor Network plugin listener (`networkStatusChange`) for reliable detection in WKWebView. Added `queryClient.invalidateQueries()` on reconnect to auto-refetch visible queries. Gets initial status from Network plugin.
  - `src/components/OfflineBanner.tsx`: Added safe area inset support via inline style `paddingTop: max(0.5rem, env(safe-area-inset-top))`. Bumped z-index to 60 to sit above the header (z-50).
  - `src/lib/capacitorUtils.ts`: Added Network plugin type to CapacitorGlobal interface
- **Why**: `navigator.onLine` is unreliable in WKWebView — it may not fire events or report incorrect status. The Capacitor Network plugin provides native-level network monitoring. Auto-refetching on reconnect ensures users see fresh data immediately.
- **Learnings for future iterations:**
  - `queryClient.invalidateQueries()` without arguments invalidates ALL queries, causing visible ones to refetch
  - The Network plugin's `addListener` is async — returns a Promise<{remove}> unlike browser events
  - `networkMode: 'always'` in TanStack Query config means queries fire even offline (using cache) — no need for extra offline handling
  - Safe area insets in inline styles need `max()` for fallback on non-notched devices
---

## 2026-03-02 — IOS-008
- **Implemented**: Implement native-feel page transitions
- **Changes**:
  - `src/hooks/usePageTransition.ts`: New hook — detects route changes via useLocation, applies `cap-page-enter` CSS class to a ref'd container for a slide-fade animation. No-op on web.
  - `src/index.css`: Added `@keyframes cap-page-enter` (opacity 0.85→1, translateX 8px→0, 250ms ease-out), scoped to `.capacitor-app`
  - `src/App.tsx`: Added `usePageTransition` to KeyboardShortcutsProvider, wrapping children in a `<div>` with the transition ref
- **Why**: Native iOS apps have subtle slide-in transitions between screens. A lightweight CSS-only approach avoids heavy dependencies like framer-motion while achieving a native feel.
- **Learnings for future iterations:**
  - Force reflow (`void el.offsetWidth`) between removing and adding a CSS class to re-trigger animations
  - 250ms is the sweet spot — fast enough to feel snappy, slow enough to register visually
  - `translateX(8px)` is a subtle hint of direction without being disorienting
  - The existing `@media (prefers-reduced-motion: reduce)` rule suppresses all animations, so this respects it automatically
---

## 2026-03-02 — IOS-007
- **Implemented**: Fix header safe area overlap on notched iPhones
- **Changes**:
  - `src/index.css`: Changed `.capacitor-app header` padding-top from `env(safe-area-inset-top, 0px)` to `0` to prevent double spacing with the `::before` pseudo-element that already handles the safe area inset
- **Why**: Three overlapping safe-area mechanisms competed: (1) `.safe-area-top` class on the header element, (2) `.capacitor-app header` padding-top override, (3) `header::before` height. On notched iPhones this produced ~2x the needed spacing. Now only `::before` handles the inset, and padding-top is zeroed in Capacitor.
- **Learnings for future iterations:**
  - Pick ONE mechanism for safe area spacing, not multiple — `::before` is cleanest for sticky headers
  - The `.safe-area-top` utility class is for non-Capacitor use; in Capacitor the `!important` overrides must account for it
  - `viewport-fit=cover` in the viewport meta tag is required for `env(safe-area-inset-*)` to work
  - MobileNav sheet already uses `safe-area-inset` class — covers all four edges
---

## 2026-03-02 — IOS-006
- **Implemented**: Add pull-to-refresh on main list pages
- **Changes**:
  - `src/hooks/use-pull-to-refresh.tsx`: Added `hapticTap()` import and call when pull threshold is reached
  - `src/pages/RestaurantsPage.tsx`: Added pull-to-refresh with `usePullToRefresh` hook, `pullToRefreshRef` on content container, refresh indicator UI matching EventsPage pattern
- **Why**: Pull-to-refresh is a standard iOS interaction pattern. EventsPage already had it; RestaurantsPage was missing it. Haptic feedback makes the trigger feel native.
- **Learnings for future iterations:**
  - `usePullToRefresh` hook is touch-only by design (touch events) — no need for explicit Capacitor gating
  - The `ptr-indicator` CSS class handles positioning; indicator UI is inline with a transform based on pull distance
  - Keep the pull-to-refresh pattern consistent: same indicator, same toast message pattern across pages
---

## 2026-03-02 — IOS-004
- **Implemented**: Fix keyboard push-up behavior on iOS form inputs
- **Changes**:
  - `src/lib/capacitorUtils.ts`: Added Keyboard plugin type (addListener, hide, setAccessoryBarVisible, setScroll)
  - `src/hooks/useKeyboardAware.ts`: New hook — listens for `keyboardWillShow`/`keyboardWillHide` via Capacitor Keyboard plugin, scrolls focused input into view above keyboard, sets `--keyboard-height` CSS variable and `.keyboard-visible` class on html. Falls back to `focusin` + `scrollIntoView` when plugin unavailable.
  - `src/index.css`: Added keyboard-aware CSS — hides bottom nav when keyboard visible, adjusts main padding to `var(--keyboard-height)`, adds `scroll-margin-bottom: 120px` to focused inputs in Capacitor
  - `src/App.tsx`: Wired `useKeyboardAware()` into KeyboardShortcutsProvider
- **Why**: iOS WKWebView with `resize: 'native'` can still obscure inputs, especially when the bottom nav takes space. Hiding the nav during keyboard input frees ~80px. The scrollIntoView with 300ms delay lets the keyboard finish animating before adjusting scroll position.
- **Learnings for future iterations:**
  - Capacitor Keyboard plugin provides `keyboardHeight` in `keyboardWillShow` — use it for precise positioning
  - `scroll-margin-bottom` CSS property is the modern way to ensure scroll targets have clearance
  - Always fall back gracefully when Keyboard plugin isn't available (web builds)
  - Hiding bottom nav during keyboard input is standard iOS UX (native apps do this)
---

## 2026-03-03 — IOS-005
- **Implemented**: Optimize scroll performance with CSS containment
- **Changes**:
  - `src/index.css`: Added `contain: layout style paint` to all cards in Capacitor
  - `src/index.css`: Disabled hover shadow-xl, translate-y transitions on touch-only devices
- **Why**: iOS WKWebView struggles with long lists of cards that have box-shadow transitions and transform animations. CSS containment tells the browser it can optimize rendering of each card independently. Disabling hover transitions on touch devices avoids unnecessary repaints.
- **Learnings for future iterations:**
  - `contain: layout style paint` is safe for card-like elements that don't overflow
  - `@media (hover: none)` targets touch-only devices without affecting desktop
  - Key hover classes to suppress: `hover:shadow-lg`, `hover:shadow-xl`, `hover:-translate-y-*`
---
