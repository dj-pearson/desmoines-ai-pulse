# Codebase Patterns

## Tech Stack
- React 18.3.1 + TypeScript 5.5.3 + Vite 5.4.1
- Supabase (PostgreSQL, Auth, Edge Functions, Storage, Realtime)
- Tailwind CSS + shadcn/ui (Radix primitives) + Lucide icons
- TanStack Query v5 for server state
- React Router DOM v6 for routing
- Stripe for payments (subscriptions + campaigns)
- Playwright for E2E testing (7 suites)
- Cloudflare Pages for hosting

## Project Structure
- src/components/ui/ — shadcn/ui base components (do NOT modify these directly)
- src/components/[feature]/ — feature-specific components
- src/hooks/ — 103 custom hooks (use-*.ts naming)
- src/lib/ — utility libraries (errorHandler, safeStorage, utils)
- src/contexts/ — React contexts (AuthContext is the primary one)
- src/pages/ — 62 route page components
- src/integrations/supabase/ — Supabase client + generated types
- supabase/functions/ — 76 edge functions with _shared/ middleware
- supabase/migrations/ — 151 SQL migrations

## Key Conventions
- Environment variables: use import.meta.env (NOT process.env) — Vite project
- localStorage: use safeStorage wrapper from @/lib/safeStorage (NOT direct access)
- Error handling: use handleError/withErrorHandling from @/lib/errorHandler
- Auth: use useAuth() from @/contexts/AuthContext
- Subscriptions: use useSubscription() hook + PremiumGate component
- Named exports preferred over default exports (except pages)
- 2-space indentation, single quotes, semicolons

## Build & Quality Commands
- npm run dev — dev server on localhost:8080
- npm run build — production build (strips console.* via esbuild)
- npm run lint — ESLint
- npm run type-check — TypeScript (relaxed mode)
- npm run type-check:strict — TypeScript (strict mode, partial files)
- npm run validate — lint + typecheck combined
- npm test — Playwright E2E tests

## Edge Function Patterns
- CORS middleware from _shared/cors.ts
- Rate limiting from _shared/rateLimit.ts (100 req/15min default)
- Input validation from _shared/validation.ts
- 18 functions have verify_jwt=false (scrapers, crawlers, webhooks)

## Database Patterns
- RLS enabled on all tables
- Public read for content tables (events, restaurants, attractions)
- Authenticated write with role checks
- Admin-only for sensitive operations
- Auto-update timestamps via triggers

## Known Technical Debt (as of PRD creation)
- 682 instances of 'any' type (TypeScript relaxed mode)
- ~~188 files with raw console.log/error/warn~~ (RESOLVED: US-006 through US-009)
- ~~14 files use localStorage directly (should use safeStorage)~~ (RESOLVED: US-015)
- ~~18 unauthenticated edge functions (need audit)~~ (RESOLVED: US-012, see docs/SECURITY_AUDIT.md)
- ~~No unit tests (only Playwright E2E)~~ (RESOLVED: US-002/US-003, vitest with 50 tests)
- ~~No CI/CD PR validation (only event-crawler workflow)~~ (RESOLVED: US-001/US-004)
- ~~Error tracking placeholder (Sentry not configured)~~ (RESOLVED: US-010)
- ~~PearsonASUS file naming artifacts from merge~~ (RESOLVED: deleted in US-018)
- ~~SearchTrafficDashboard commented out (missing migrations)~~ (RESOLVED: US-021)
- ~~TODOs: distance calculation~~ (RESOLVED: US-022, see src/lib/geo.ts)
- ~~No content interaction tracking~~ (RESOLVED: US-032, see src/hooks/useContentTracking.ts)
- ~~Hardcoded metrics in AdminAnalyticsDashboard~~ (RESOLVED: US-033, computed from real data)
- ~~No email capture for free tool users~~ (RESOLVED: US-034, see src/components/EmailCaptureModal.tsx)
- ~~AI/LLM edge functions unprotected from abuse~~ (RESOLVED: US-061, rate limiting at 10 req/15min)
- ~~backfill-images edge function has no auth~~ (RESOLVED: US-062, API key middleware added)
- ~~og:image missing on HotelDetails and conditional on Enhanced SEO components~~ (RESOLVED: US-063)
- ~~MediaLibrary component not wired into admin panel~~ (RESOLVED: US-064, /admin/media route)
- ~~Scraping jobs lost on page refresh~~ (RESOLVED: US-020, database persistence + AbortController stop)
- ~~No composite DB indexes for filter queries~~ (RESOLVED: US-025, 16 indexes added)
- ~~No A/B testing infrastructure~~ (RESOLVED: US-040, useExperiment hook + Experiment component)
- ~~No admin UI for image backfill~~ (RESOLVED: US-065, ImageBackfill component in AdminMedia)
- ~~Bundle bloat from unused deps~~ (RESOLVED: US-024, removed 6 unused deps, puppeteer→devDeps)
- ~~No production deployment runbook~~ (RESOLVED: US-030, docs/PRODUCTION_RUNBOOK.md already complete)
- ~~No community voting system~~ (RESOLVED: US-041, Des Best voting at /best-of)
- ~~No scene updates feed~~ (RESOLVED: US-058, What's New at /whats-new with auto-trigger)
- ~~No API documentation~~ (RESOLVED: US-029, docs/API_REFERENCE.md with all 85 edge functions)
- ~~No curated itineraries~~ (RESOLVED: US-042, /itineraries with 6 seeded itineraries + print + ICS export)
- ~~No deals/coupons section~~ (RESOLVED: US-043, /deals with category filters + claim flow)
- ~~No interactive discovery map~~ (RESOLVED: US-044, /map with toggleable event/restaurant/attraction layers)
- ~~No dedicated music/concert hub~~ (RESOLVED: US-045, /music with venue guide + tonight's shows)
- ~~No sports hub~~ (RESOLVED: US-046, /sports with 7 teams + game schedules)
- ~~No trails/outdoor hub~~ (RESOLVED: US-047, /outdoors with 8 trails + difficulty filters)
- ~~No brewery trail~~ (RESOLVED: US-048, /breweries with digital passport + check-in gamification)
- ~~No transportation/getting around guide~~ (RESOLVED: US-049, /getting-around with parking, skywalk, transit, airport, FAQ)
- ~~No downloadable visitor guide~~ (RESOLVED: US-050, /visitors-guide with digital download + physical mail request)
- ~~No meeting planner portal~~ (RESOLVED: US-051, /group-travel with venue search, RFP submission, 8 seeded venues)
- ~~No seasonal/holiday guides~~ (RESOLVED: US-052, /guides/:slug with 4 seeded seasonal guides)
- TODOs: remaining content hubs (US-053–US-060), mobile push (US-068–069), image pipeline (US-066–067, US-071)

---

## Progress Log

## 2026-05-17 — Ralph Loop 20: Deep-dive security + code-quality (6 done, 13 backlog added)

Prior PRD was 70/70 complete (Loop 19). With no pending stories, ran a
three-track deep dive (security, code quality, UX cohesion) via parallel
audit agents and turned the findings into 19 new PRD stories
(NEW-SEC-001..007, NEW-DX-001..006, NEW-UX-001..006). Implemented the 6
highest-value, low-risk, verifiable ones this loop. Build verified:
`tsc --noEmit` exit 0, `vite build` exit 0, vitest 8 files / 86 tests
(20 new) all green.

- **NEW-SEC-001**: PostgREST filter injection in `nlp-search`
  - Added `sanitizePostgrestPattern()` to
    `supabase/functions/_shared/validation.ts` — builds on
    `sanitizeLikeInput`, additionally replaces `, ( ) * \`` with a space
    and collapses whitespace.
  - Applied to `searchKeyword`, `locationFilter`, `parsedIntent.category`
    and `parsedIntent.cuisine` across all three search blocks
    (events/restaurants/attractions). Function runs with the
    service-role key so this closed an unauth RLS-bypassing
    column-exfiltration vector.

- **NEW-SEC-002**: PostgREST filter injection in `discover-chat`
  - Imported the same helper; replaced the insufficient
    `.replace(/[%_]/g,'')` term sanitization (it left `,` and `)` intact)
    and the category/cuisine/type ilike interpolations across
    `search_events`/`search_restaurants`/`search_attractions`.

- **NEW-SEC-003**: `parse-menu-upload` had no auth despite an
  "admin-only" header comment
  - Added `Authorization` → `supabase.auth.getUser` →
    `profiles.role === 'admin'` gate (mirroring `process-stripe-refund`)
    before any body parse / Anthropic call / DB write. Updated the
    SECURITY header comment. Request/response unchanged for admins.

- **NEW-SEC-004**: `refresh_oauth_token` RPC leaked cross-user tokens +
  app `client_secret`
  - New migration `20260517000000_harden_refresh_oauth_token.sql`.
    `CREATE OR REPLACE` keeps the `(UUID, TEXT)` signature (verified zero
    callers in web/edge — only the original migration + generated types),
    derives the user from `auth.uid()` and ignores the caller-supplied
    `p_user_id`, drops `client_secret` from the return, pins
    `search_path=''` with schema-qualified refs, `REVOKE` from
    PUBLIC/anon + `GRANT` to authenticated/service_role, and narrows the
    over-broad `GRANT ALL ON user_oauth_tokens`.

- **NEW-DX-001**: 6 hooks still used `export default`
  - Removed the redundant trailing `export default useX;` from
    `useMediaLibrary`, `useNLPSearch`, `useWebVitals`,
    `useFocusOnRouteChange`, `useMediaUpload`, `useTripPlanner`. All
    already had named exports and all consumers already used named
    imports, so zero consumer changes; verified no default-import sites
    remain.

- **NEW-DX-002**: zero unit coverage on content auto-approval gates
  - Added `src/lib/__tests__/contentValidation.test.ts` (20 tests):
    URL/phone/email/date/image validators edge cases + `validateEvent`
    happy path / missing-required / score-clamp / generic-title warning
    + `validateRestaurant` shape. (ownership.ts deferred to NEW-DX-006 —
    needs Supabase client mocking.)

### Backlog added (not yet implemented)
- NEW-SEC-005 (email-deliver account-deletion token), NEW-SEC-006
  (encrypt oauth tokens at rest), NEW-SEC-007 (stable Stripe idempotency
  key), NEW-DX-003 (TanStack Query migration of core data hooks),
  NEW-DX-004 (decompose ContentTable.tsx), NEW-DX-005 (remove `any` from
  security/validation libs), NEW-DX-006 (ownership.ts tests), NEW-UX-001
  (generalize favorites), NEW-UX-002 (web push/email alerts), NEW-UX-003
  (Events list/map toggle), NEW-UX-004 (unify filter/sort UX), NEW-UX-005
  (unified Saved hub), NEW-UX-006 (guard/surface orphaned routes).

### Files changed
- supabase/functions/_shared/validation.ts (new sanitizePostgrestPattern)
- supabase/functions/nlp-search/index.ts
- supabase/functions/discover-chat/index.ts
- supabase/functions/parse-menu-upload/index.ts
- supabase/migrations/20260517000000_harden_refresh_oauth_token.sql (new)
- src/hooks/{useMediaLibrary,useNLPSearch,useWebVitals,useFocusOnRouteChange,useMediaUpload,useTripPlanner}.ts
- src/lib/__tests__/contentValidation.test.ts (new)
- prd.json (19 stories appended; 6 marked passes:true; branchName updated)
- progress.txt

### Build Verification
- `npx tsc --noEmit` — ✅ exit 0
- `npx vite build` — ✅ exit 0
- `npx vitest run` — ✅ 8 files, 86 tests (20 new), 0 fail
- **Progress**: 76/89 stories passing; 6 completed this loop
---

## 2026-05-07 — Ralph Loop 19: Final 8 stories — PRD complete (70/70 ✅)

This session closed out every remaining pending story in the active PRD.
Final tally: **70 / 70 stories passing (100%)**. Build verified via
`npm run build` (exit 0). Vitest suite green: 7 files, 66 tests, 0 fail.

- **SUB-SYNC-008**: Refresh backendTier on iOS+Android auth state change
  - iOS AuthService.startAuthListener now calls
    StoreKitService.refreshBackendTier() on initialSession/signedIn/
    tokenRefreshed and StoreKitService.clearBackendTier() on signedOut.
  - Android AuthViewModel mirrors via BillingService injected through
    Hilt. Test wiring updated for the new dependency.

- **SUB-SYNC-012**: Subscription UI shows platform-of-origin
  - useSubscription gains a `subscriptions` array (all active rows) +
    `platform` field on UserSubscription, while keeping the
    single-row `subscription` accessor for back-compat.
  - SubscriptionPlatformBreakdown.tsx renders a per-platform card on
    /subscription Overview when 2+ rows exist, routing the manage
    button to Stripe customer portal (web), App Store deep link (iOS),
    Play Store URL (android).
  - iOS StoreKitService surfaces `crossPlatformSubscriptions` and
    SubscriptionView shows an informational banner ("You also have an
    active VIP subscription via the website…"). Android BillingService
    + SubscriptionScreen mirror the same.

- **SUB-SYNC-013**: Cross-platform sync test coverage
  - Extracts `resolveHighestSubscription()` as a pure helper for
    direct vitest exercising. 8 cases: empty, single-row, web+ios
    cross-tier resolution, android+web tie-breaking, free fallback,
    missing-plan handling, 3-platform top-tier resolution.
  - Deno-side `supabase/functions/_tests/cross-platform-sync.test.ts`
    pins boundary invariants: validate-* functions scope queries to
    their platform, both webhooks dedupe by Apple notification_uuid /
    Pub/Sub message_id, UNIQUE(user_id, platform) constraint exists,
    legacy split migration is idempotent, both webhooks 200-ack
    duplicates.
  - New `.github/workflows/subscription-sync-tests.yml` runs vitest
    + Deno tests on PRs that touch any subscription-related path.

- **IOS-DISCOVER-2026-001**: Ask Pulse conversational AI surface
  - New `discover-chat` edge function: Claude with prompt caching
    (anthropic-beta: prompt-caching-2024-07-31) on the system prompt
    + 4 tool definitions. Tool calls: search_events,
    search_restaurants, search_attractions, return_picks. Up to 6
    tool turns then must call return_picks with structured JSON
    `{ picks, followUpSuggestions }`.
  - Tier-gated daily quotas (free 5, insider 50, vip unlimited) via
    new `discover_chat_usage` table.
  - iOS AskPulseService + AskPulseView (chat-style sheet, welcome
    suggestions, conversation bubbles, AskPulsePickCard, follow-up
    chips, custom FlowLayout, full VoiceOver labels).
  - HomeView gets a search-bar-style askPulseEntryBar above the swipe
    hero.

- **IOS-DISCOVER-2026-003**: Events sort menu (Soonest / Featured / Popularity)
  - Migration: events.popularity_score + view_count,
    calculate_event_popularity_score() helper, recompute trigger,
    fetch_events_by_popularity_rotated() RPC.
  - iOS EventsService.EventsQuery gains sortBy: EventSortOption.
  - EventsViewModel exposes sortBy + cache-key contribution, HomeView
    toolbar mirrors RestaurantsView.sortMenu style.
  - Web parity: useEvents hook accepts sortBy, threads through query
    + useEffect deps.

- **IOS-DISCOVER-2026-005**: Right Now contextual ribbon on Home
  - WeatherService.swift: keyless Open-Meteo client, WMO weather-code
    bucketing, 30-minute UserDefaults snapshot cache, ~5km dedupe.
  - RightNowRibbon.swift: 6 templates (patios, cozy, comfort food,
    live music, lunch, open-now fallback) selected from temperature
    + condition + hour + weekday. Hidden silently when location is
    denied or weather fails — no broken state.
  - HomeView mounts the ribbon above askPulseEntryBar; tapping
    routes through ribbonDiscoverContext to the existing
    fullScreenCover with the matching DiscoverFilterContext + mode.

- **IOS-DISCOVER-2026-008**: Surprise Me single-tap recommendation
  - Migration: get_surprise_pick(lat, lon) RPC that 50/50s between
    event vs restaurant, prefers items matching the user's most-
    favored swipe category/cuisine, falls back to featured +
    popularity_score >= 50. Returns a templated reason string. New
    surprise_pick_outcomes table tracks shown/saved/tried_another for
    acceptance-rate analysis.
  - iOS SurpriseMeService + SurpriseMeView: full-screen reveal with
    hero image, title, reason, prominent Save CTA, smaller Try Another
    button, scale+opacity reveal animation, success haptic.
  - HomeView gets a full-width purple gradient die-icon button.

- **IOS-DISCOVER-2026-002**: For You rail seeded by swipe history
  - Migration: replaces get_personalized_recommendations() with a
    swipe-aware version: boost +50, like +30, detail +10, skip -40.
    Per-item AND per-category aggregates with linear 90-day decay.
    Drops items the user explicitly skipped recently. Random jitter
    so refresh re-rolls.
  - New get_trending_events() RPC for cold-start fallback (<5 swipes).
  - iOS ForYouService probes recent swipe count, calls For You or
    Trending. ForYouRail.swift renders horizontal scroll with the
    recommendation_reason caption and refresh button.
  - Web parity: useForYouRail hook + ForYouRail.tsx, mounted on
    Index.tsx between SearchSection and the top banner ad.

- **IOS-DISCOVER-2026-004**: Time-slider on Map view
  - MapViewModel.mapTime: Date? property. nil = live "Now" view.
  - Event annotations filter to events within ±2h of slider time.
    Restaurants filter via Restaurant.isOpenNow(at:), defaulting to
    "show" when businessHours data is missing.
  - MapTimeSlider.swift: pill-style snap stops (Now, 6pm, 8pm, 10pm,
    this Sat, next Sat), top count pill summary, mounted in
    safeAreaInset(.bottom) and hidden when a selection popup is open.

- **IOS-DISCOVER-2026-007**: Voice search + Siri shortcut
  - PulseAppIntents.swift: FindRestaurantsIntent, FindEventsIntent,
    AskPulseIntent (all openAppWhenRun = true). Payloads posted to a
    PulseIntentDispatcher singleton the SwiftUI views observe.
  - PulseShortcuts (AppShortcutsProvider) donates en-US phrases for
    Spotlight + lock-screen Suggested Shortcuts.
  - SpeechDictationService.swift wraps SFSpeechRecognizer +
    AVAudioEngine with permission gating + .denied → Settings deep
    link.
  - SearchView toolbar mic button toggles dictation, live-updates the
    search field via .onChange(of: dictation.transcript), and pulses
    while listening. SearchView .onChange(of: dispatcher.pending)
    translates structured intent payloads into search strings.

- **IOS-DISCOVER-2026-006**: Group decision shared swipe sessions
  - Migration: swipe_sessions (id, code, host_user_id, mode,
    filter_context JSONB, expires_at) + swipe_session_participants
    (session_id, user_id OR anon_id, display_name). Adds session_id
    + anon_id to swipe_interactions; relaxes user_id NOT NULL with a
    CHECK that one identity field is set so anon participants can
    swipe.
  - generate_swipe_session_code() RPC produces a 6-char DSM- code
    with collision retry. get_swipe_session_matches(p_session_id)
    returns items right-swiped by ≥2 distinct participants.
  - Privacy: only `like` and `boost` swipes are session-scoped;
    `skip` swipes are session-NULL.
  - iOS SwipeSessionService manages host/join/end + matches polling.
    Anon participants get a stable per-device UUID stored in
    UserDefaults (does not survive reinstall, by design).
  - GroupSessionView.swift: host CTA, lobby with monospace code +
    ShareLink for sending the code, end-session button, and a join-
    by-code field. Reachable from a new toolbar icon on DiscoverView.

**Files added (14)**:
- src/components/SubscriptionPlatformBreakdown.tsx
- src/components/ForYouRail.tsx
- src/hooks/useForYouRail.ts
- src/hooks/__tests__/useSubscription.test.ts
- supabase/functions/discover-chat/index.ts
- supabase/functions/_tests/cross-platform-sync.test.ts
- supabase/migrations/20260506000007_discover_chat_usage.sql
- supabase/migrations/20260506000008_events_popularity_score.sql
- supabase/migrations/20260506000009_surprise_me.sql
- supabase/migrations/20260506000010_for_you_swipe_personalization.sql
- supabase/migrations/20260506000011_swipe_sessions.sql
- ios/DesMoinesInsider/Services/{AskPulse,SurpriseMe,ForYou,Weather,SpeechDictation,SwipeSession}Service.swift (6)
- ios/DesMoinesInsider/Views/{AskPulse/AskPulseView,SurpriseMe/SurpriseMeView,GroupSession/GroupSessionView,Map/MapTimeSlider,Components/RightNowRibbon,Components/ForYouRail}.swift (6)
- ios/DesMoinesInsider/Intents/PulseAppIntents.swift
- .github/workflows/subscription-sync-tests.yml

**Files modified (12)**:
- src/hooks/useSubscription.ts (resolveHighestSubscription helper, subscriptions[] array, platform field)
- src/hooks/useEvents.ts (sortBy parameter)
- src/pages/SubscriptionPortal.tsx (mounts SubscriptionPlatformBreakdown)
- src/pages/Index.tsx (mounts ForYouRail)
- ios/DesMoinesInsider/Services/{AuthService,StoreKitService,EventsService}.swift
- ios/DesMoinesInsider/ViewModels/{EventsViewModel,MapViewModel}.swift
- ios/DesMoinesInsider/Views/Home/HomeView.swift (Ask Pulse + Right Now + Surprise Me + For You + sort menu)
- ios/DesMoinesInsider/Views/Subscription/SubscriptionView.swift (cross-platform banner)
- ios/DesMoinesInsider/Views/Map/EventMapView.swift (time slider)
- ios/DesMoinesInsider/Views/Discover/DiscoverView.swift (group session toolbar)
- ios/DesMoinesInsider/Views/Search/SearchView.swift (mic button + intent dispatch)
- ios/DesMoinesInsider/Models/AppEnums.swift (EventSortOption)
- android/app/src/main/java/com/desmoines/aipulse/data/remote/BillingService.kt
- android/app/src/main/java/com/desmoines/aipulse/ui/screens/auth/AuthViewModel.kt
- android/app/src/main/java/com/desmoines/aipulse/ui/screens/subscription/SubscriptionScreen.kt
- android/app/src/test/java/com/desmoines/aipulse/ui/screens/auth/AuthViewModelTest.kt
- prd.json (10 stories flipped to passes:true across this and previous loop)

- **Learnings for future iterations:**
  - Apple App Store deep link `https://apps.apple.com/account/subscriptions` works on iOS Safari + macOS; falls back to the marketing page on other browsers — accept that and don't bother with itms-apps:// (which doesn't work in many browser contexts).
  - For App Intents that only need to land the user inside the app with
    captured parameters, the cleanest pattern is `openAppWhenRun = true`
    + post payload to an @Observable dispatcher the SwiftUI views
    observe. Lets the navigation logic stay in the view layer.
  - SFSpeechRecognizer permission and AVAudioApplication.requestRecordPermission are TWO separate gates — checking only one leaves you with a non-functional mic in production.
  - Open-Meteo's WMO weather codes are dense; bucketing them up front into 6 ribbon-friendly conditions (clear/cloudy/rain/snow/fog/thunder) is far easier than carrying 100 templates.
  - For Postgres functions that return the same column shape across multiple "modes" (For You vs Trending), keeping the RETURNS TABLE definition identical lets the iOS decoder swap RPCs without code changes.
  - On the swipe-personalization RPC, the random jitter (`+ random() * 5`) is what makes "refresh" feel responsive — without it the UI shows the same top-N every time even though the underlying score is unchanged.
  - When supporting anon participants in a multiplayer feature, make user_id nullable + add a CHECK that at least one identity field is set. Don't try to create a fake auth user — Supabase RLS gets confused by it later.
---

## 2026-05-06 — Ralph Loop 17: Cross-Platform Subscription Sync (SUB-SYNC-009, SUB-SYNC-010, SUB-SYNC-011)

Three iterations on the cross-platform subscription sync stack so iOS,
Android, and web (Stripe) subscription state stays consistent across all
three storefronts.

- **SUB-SYNC-010**: Apple App Store Server Notifications v2 webhook
  - New edge function `supabase/functions/appstore-server-notifications-v2/index.ts`
  - Verifies the signedPayload JWS using Apple's PKI: validates the x5c root CA fingerprint against Apple Root CA G3, and verifies the JWS signature with the leaf certificate's ES256 public key (custom ASN.1 SPKI extraction since some Deno targets don't accept full DER cert as `spki`)
  - Decodes notificationType, subtype, notificationUUID, and the embedded signedTransactionInfo + signedRenewalInfo JWSes
  - Handles SUBSCRIBED, DID_RENEW, DID_CHANGE_RENEWAL_STATUS (auto-renew toggle via subtype), DID_FAIL_TO_RENEW, EXPIRED, REVOKE, REFUND, GRACE_PERIOD_EXPIRED
  - Looks up `user_subscriptions` by `apple_original_transaction_id` scoped to `platform=ios` and updates status / current_period_end / cancel_at_period_end / apple_transaction_id / plan_id
  - New table `apple_notification_log` for at-least-once idempotency (Apple retries 5×); keyed by Apple's `notificationUUID`
  - Migration: `supabase/migrations/20260506000004_apple_notification_log.sql`

- **SUB-SYNC-011**: Google Play Real-Time Developer Notifications webhook
  - New edge function `supabase/functions/play-rtdn-webhook/index.ts`
  - Verifies the OIDC bearer token Google attaches to push messages: alg=RS256, fetches Google's JWKS (cached 1 hour), verifies signature with the matching kid's public key, validates iss (accounts.google.com), aud (PLAY_RTDN_AUDIENCE env), and exp
  - Decodes the base64 Pub/Sub message into a DeveloperNotification — handles both `subscriptionNotification` and `voidedPurchaseNotification` paths
  - Re-uses GOOGLE_PLAY_SERVICE_ACCOUNT_KEY to call `purchases.subscriptions.get` for fresh state (expiry, autoRenewing, orderId)
  - Handles SUBSCRIPTION_PURCHASED / RENEWED / RECOVERED / RESTARTED / CANCELED / ON_HOLD / IN_GRACE_PERIOD / PAUSED / REVOKED / EXPIRED
  - New table `play_rtdn_log` for at-least-once idempotency keyed by Pub/Sub `messageId`
  - Migration: `supabase/migrations/20260506000005_play_rtdn_log.sql`

- **SUB-SYNC-009**: One-shot data fix — split legacy mixed-platform subscription rows
  - Migration `supabase/migrations/20260506000006_split_legacy_mixed_platform_subs.sql`
  - For each `user_subscriptions` row with `platform IN ('ios', 'android')` AND `stripe_subscription_id IS NOT NULL`:
    - Inserts a fresh `platform='web'` row carrying the Stripe identifiers + canonical period/status/plan_id
    - Clears `stripe_subscription_id` and `stripe_customer_id` from the original mobile row
  - Idempotency via `WHERE NOT EXISTS` against the per-user `platform='web'` row, plus `ON CONFLICT DO NOTHING` on the audit table
  - New `subscription_split_audit` table maps legacy → web rows for traceability and rollback
  - Deployment checklist, manual reconciliation queries, and rollback procedure documented in `DEPLOYMENT.md` (Subscription Sync — Cross-Platform Cleanup section)

**Build verified**: `npm run build` passes (45s, exit 0, with stub env vars for the sitemap generator)
**Type-check / lint**: edge functions live in `supabase/functions/` (Deno) and are outside `tsconfig.app.json`'s `include: ["src"]`, so they do not touch the Vite TypeScript scope. The pre-existing baseUrl deprecation in `tsconfig.json` and the `@eslint/js` resolution issue are unrelated.

**Progress**: 59/70 stories complete (84%), 11 remaining

**Files changed**:
- `supabase/functions/appstore-server-notifications-v2/index.ts` (NEW)
- `supabase/functions/play-rtdn-webhook/index.ts` (NEW)
- `supabase/migrations/20260506000004_apple_notification_log.sql` (NEW)
- `supabase/migrations/20260506000005_play_rtdn_log.sql` (NEW)
- `supabase/migrations/20260506000006_split_legacy_mixed_platform_subs.sql` (NEW)
- `DEPLOYMENT.md` (cross-platform cleanup section)
- `prd.json` (3 stories marked `passes: true`)

- **Learnings for future iterations:**
  - Deno's WebCrypto subtle.verify accepts JWS-format raw r||s ECDSA signatures directly (no DER conversion needed for verification). Only signing returns DER on some runtimes.
  - Apple's x5c chain verification requires extracting the SPKI from the leaf cert via ASN.1 walking — `crypto.subtle.importKey('spki', certBytes, ...)` on the full cert fails on Deno because the cert is wrapped in a SEQUENCE and importKey expects a bare SubjectPublicKeyInfo.
  - For Pub/Sub OIDC verification, Google's JWKS at `https://www.googleapis.com/oauth2/v3/certs` rotates ~daily — cache for ≤1 hour and refetch on `kid` miss.
  - Apple notificationUUID + Pub/Sub messageId are both safe idempotency keys; both providers redeliver with the same id on retry.
  - When splitting subscription rows, query the post-state via `WHERE NOT EXISTS` rather than a temp table — keeps the migration single-statement-friendly and idempotent against partial reruns.
  - The legacy mixed-row issue was a write scope bug in the validate-receipt functions; the fix in 20260506000006 is one-shot. Re-introducing platform-unscoped UPDATEs anywhere in those edge functions will recreate the data corruption.
---

## 2026-03-31 — iOS Production Readiness: Security Hardening (IOS-001 through IOS-004)

- **IOS-001**: Implemented certificate pinning for Supabase API calls:
  - Created `ios/DesMoinesInsider/Services/CertificatePinningService.swift` with URLSessionDelegate
  - SPKI SHA-256 hash pinning for Let's Encrypt R3, ISRG Root X1, DigiCert Global Root G2
  - Debug mode: report-only (logs mismatch but allows connection for development)
  - Production mode: blocks connections on pinning failure with user-friendly error
  - Validates entire certificate chain, not just leaf certificate
- **IOS-002**: Added encrypted file protection to QueryCache disk storage:
  - Set `NSFileProtectionComplete` attribute on cache directory creation
  - Cache writes use `.completeFileProtection` option — files inaccessible when device locked
  - Added cache version marker (v2) with automatic purge of old unencrypted caches on upgrade
  - Version file at `.cache-version` in cache directory
- **IOS-003**: Added jailbreak detection with soft user warning:
  - Created `ios/DesMoinesInsider/Services/JailbreakDetector.swift` with 4 detection methods
  - Checks: suspicious paths (Cydia/Sileo/bash/sshd), URL schemes, writable system paths, injected dylibs
  - Non-blocking alert on launch ("Security Warning" — does NOT block app usage per App Store guidelines)
  - All checks skipped on `#if targetEnvironment(simulator)` to avoid false positives
  - Integrated into `DesMoinesInsiderApp.swift` task block
- **IOS-004**: Added biometric authentication (Face ID / Touch ID):
  - Created `ios/DesMoinesInsider/Services/BiometricAuthService.swift` with LocalAuthentication framework
  - Supports Face ID, Touch ID, and Optic ID with device detection
  - Enable/disable toggle in SettingsView Security section (only shown when hardware available)
  - Biometric preference stored securely in Keychain (not UserDefaults)
  - BiometricLockView shown on app launch when enabled + session exists, with "Try Again" and "Skip" buttons
  - Comprehensive LAError handling (userCancel, biometryNotAvailable, biometryNotEnrolled, biometryLockout, etc.)
  - Added `NSFaceIDUsageDescription` to Info.plist
  - Biometric preference reset automatically on sign out via AuthService.signOut()
- Files created: CertificatePinningService.swift, JailbreakDetector.swift, BiometricAuthService.swift
- Files modified: QueryCache.swift, DesMoinesInsiderApp.swift, AuthService.swift, SettingsView.swift, Info.plist, prd.json
- **Learnings for future iterations:**
  - SPKI pinning (public key hash) survives certificate renewals — better than pinning the leaf certificate directly
  - iOS `.completeFileProtection` on Data.write() is simpler than setting file attributes after the fact
  - Cache versioning via a `.cache-version` file is a clean upgrade-and-purge pattern
  - JailbreakDetector must use `#if targetEnvironment(simulator)` not `#if DEBUG` — DEBUG is true on device debug builds too
  - BiometricAuthService stores preference in Keychain, not UserDefaults, because UserDefaults is not encrypted
  - LAContext.evaluatePolicy is async in iOS 17+ — no completion handler needed
  - The "Skip" button on BiometricLockView is important: users may change biometric settings while app is backgrounded
---

## 2026-03-15 — Ralph Loop 16: Critical Platform Improvements (SSRF Security, UX Discovery, Attraction Details)

- **US-135**: Added SSRF validation to check-broken-links edge function:
  - Imported `validateURLForSSRF` from `_shared/validation.ts`
  - Validates initial page URL before fetching (returns 400 if blocked)
  - Validates each extracted link before HEAD request (skips private/internal URLs)
  - Blocks private IPs, localhost, embedded credentials, non-HTTP protocols
  - crawl-site already had SSRF validation (verified)
- **US-136**: Added SSRF validation to analyze-competitor and firecrawl-scraper edge functions:
  - analyze-competitor: SSRF check before fetching competitor.website_url in scrapeCompetitorContent; logs blocked attempts
  - firecrawl-scraper: SSRF check on user-provided URL before generating paginated scrape URLs; returns 400 if rejected
- **US-126**: Added "Surprise Me" random discovery button to all 3 listing pages:
  - EventsPage: Shuffle icon button in action row (between date presets and Near Me); navigates to random event from current filtered results
  - Restaurants: Shuffle button in quick action pills (alongside Open Now, Featured); navigates to random restaurant
  - Attractions: Shuffle button next to filter controls; navigates to random attraction from filtered results
  - All buttons disabled when no results available
- **US-115**: Added estimated visit duration to attraction detail pages:
  - Created type-based duration mapping (13 attraction types: Museum 1-3h, Park 30m-2h, Zoo 2-4h, etc.)
  - Replaced "Editor's Pick" stat card (showed "--" for non-featured) with "Est. Visit Time" using Clock icon
  - Added estimated duration to "Things To Know" section
  - Added FAQ question "How long should I spend at [attraction]?" with contextual answer
  - Cleaned up unused Compass and Check icon imports

**Build verified**: `npx vite build` passes (36s, no errors)

**Progress: 120/147 stories complete (82%), 27 remaining**

**Files changed**: supabase/functions/check-broken-links/index.ts, supabase/functions/analyze-competitor/index.ts, supabase/functions/firecrawl-scraper/index.ts, src/pages/EventsPage.tsx, src/pages/Restaurants.tsx, src/pages/Attractions.tsx, src/pages/AttractionDetails.tsx, prd.json, progress.txt
- **Learnings for future iterations:**
  - crawl-site already had SSRF validation via `validateURLForSSRF` with `ALLOWED_CRAWL_DOMAINS` — no changes needed
  - Shuffle icon from lucide-react is perfect for "Surprise Me" / random discovery UX
  - Type-based visit duration estimates are more maintainable than per-record DB columns when data doesn't exist yet
  - filteredAttractions must be defined before handleSurpriseMe callback (ordering matters for non-hook functions referencing useMemo results)
---

## 2026-03-11 — Ralph Loop 15: Critical Platform Improvements (Dark Mode, Security, Restaurant UX, Prefetching)

- **US-086**: Fixed incomplete dark mode styling across platform:
  - RestaurantCard: added dark: variants for star rating empty states, rating text, popularity badge, dietary tag backgrounds, location border
  - ShareDialog: added `dark:brightness-90` to social media buttons for reduced eye strain
  - BottomNav: added `dark:hover:bg-muted/30` for proper dark mode hover contrast
  - Restaurants page: dark mode fixes for cuisine quick-filter buttons, sort dropdown, filter panel, filter chips, headings, FAQ section, SEO content gradients
  - index.css: added dark mode placeholder color override (`hsl(225 15% 55%)`) for WCAG 4.5:1 contrast compliance
- **US-075**: Added authentication and rate limiting to image-proxy edge function:
  - JWT authentication required for non-internal URLs (verifies via Supabase auth)
  - Unauthenticated access allowed only for internal Supabase Storage URLs (pattern match)
  - Rate limiting: 50 requests per 15 minutes per client (bandwidth-intensive endpoint)
  - Response size limit: 10MB max (checked via Content-Length header and actual buffer size)
  - Logs all authenticated proxy requests with user ID for abuse monitoring
  - Returns 413 for oversized images, 401 for invalid/missing auth
- **US-123**: Added "Browse by Cuisine" section to Restaurants listing page:
  - Created `useCuisineCounts()` hook in useRestaurants.ts fetching cuisine names with restaurant counts
  - Section displays all cuisines as clickable pills with count badges (e.g., "Italian (23)")
  - Clicking a cuisine applies filter and smooth-scrolls to results grid
  - Dark mode compatible styling with hover effects
  - Positioned between below-fold ad and SEO content section
- **US-089**: Expanded route prefetching and added hover/touch prefetch:
  - Extended routeModuleMap in prefetch.ts: added /events/today, /events/free, /trip-planner, /best-of, /whats-new (total 9 routes)
  - Fixed incorrect RestaurantsPage import path to match actual Restaurants page module
  - Added `onTouchStart` prefetch trigger to BottomNav links (mobile touch pre-loading)
  - Added DNS prefetch hints to index.html for js.stripe.com and tile.openstreetmap.org
  - DesktopNav already had onMouseEnter prefetch (verified)

**Build verified**: `npx vite build` passes (40s, no errors)

**Progress: 116/147 stories complete (78%), 31 remaining**

## 2026-03-10 — Ralph Loop 14: Critical Platform Improvements (Search UX, Performance, Conversion, Loading States)

- **US-085**: Added search autocomplete with recent searches and suggestions:
  - Created `src/components/SearchAutocomplete.tsx` with dropdown showing Recent Searches and Suggested Results
  - Recent searches stored in safeStorage (last 10, scoped per content type)
  - Database title matches via debounced ilike query (3+ chars, top 5 suggestions)
  - Keyboard accessible: arrow keys navigate, Enter selects, Escape closes
  - Clear recent searches button in dropdown
  - Integrated into EventsPage, Restaurants, and Attractions search inputs
  - Exported `addRecentSearch()` for tracking searches on submit
- **US-087**: Added context-aware loading states and error recovery:
  - EventsPage: loading label shows "Searching for [query]..." or "Loading [category] events..."
  - Restaurants: loading label shows search context or cuisine filter context
  - Attractions: loading label shows search or type filter context
  - RestaurantDetails: labeled skeleton sections (Restaurant Info, Hours & Location, Reviews & Rating) with sr-only status
  - FavoriteButton: spinner replaces heart icon during mutation, aria-label updates to "Updating favorite..."
- **US-104**: Added detail page data prefetching on card hover:
  - Created `src/hooks/usePrefetchDetail.ts` with `usePrefetchRestaurant()` and `usePrefetchAttraction()` hooks
  - Uses `requestIdleCallback` to avoid blocking main thread
  - Guards against repeated prefetch via `useRef<Set>` (fires once per item)
  - RestaurantCard: prefetches restaurant data on mouse enter
  - Attractions page: prefetches attraction data on card hover
  - 2-minute stale time for prefetched data
- **US-122**: Improved footer newsletter conversion:
  - Added benefit line above form: "Weekly digest of trending events + AI-powered recommendations" with sparkle icon
  - Updated placeholder from "Your email" to "Get your weekly Des Moines plan"
  - Changed button text from icon-only to "Subscribe Free"
  - Added subscriber social proof: "Join 15,000+ Des Moines locals. Free forever, unsubscribe anytime."
- **US-106**: Verified already complete - NearbyContent component on all 3 detail pages (EventDetails, RestaurantDetails, AttractionDetails)
- **US-130**: Verified already complete - Breadcrumbs with BreadcrumbListSchema on all detail pages

**Build verified**: `npx vite build` passes (50s, no errors)

**Progress: 112/147 stories complete (76%), 35 remaining**

## 2026-03-08 — Ralph Loop 13: Critical Platform Improvements (Security, SEO, Conversion, UX)

- **US-134**: Added SSRF validation to validate-source-urls and fix-broken-event-urls edge functions:
  - Imported `validateURLForSSRF` from `_shared/validation.ts` (existing but unused)
  - validate-source-urls: SSRF check before fetching source_url, before storing extracted URLs from HTML, before AI fallback fetch, and on AI-returned URLs
  - fix-broken-event-urls: SSRF check on Google search result URLs before storing in database
  - Blocks private IPs (10.x, 172.16.x, 192.168.x), localhost, embedded credentials, non-HTTP protocols
- **US-121**: Added SEOHead component to AttractionDetails page:
  - Added SEOHead with location schema (Place structured data with geo coordinates), breadcrumb schema, keywords, og:image with fallback
  - Replaced separate BreadcrumbListSchema component (now handled by SEOHead)
  - EnhancedAttractionSEO retained for TouristAttraction, FAQPage, Speakable, and LocalBusiness schemas
  - Matches RestaurantDetails SEO pattern
- **US-110**: Added social proof and value proposition to homepage hero:
  - Added trust metrics strip between subheading and stats tiles in EnhancedHero
  - Displays: "15,000+ locals trust us", "4.8/5 user rating", "500+ events weekly"
  - Uses Users, Star (filled), TrendingUp icons from Lucide in gold accent color
  - Responsive flex-wrap layout for mobile
- **US-113**: Added dietary restriction badges to RestaurantCard:
  - Created `inferDietaryTags()` utility that matches restaurant description/cuisine text against dietary keywords
  - Supports Vegan (green), Vegetarian (green), Gluten-Free (amber) badge types
  - Badges rendered with Leaf/Wheat icons in compact pill format between description and location
  - Memoized with useMemo for performance (RestaurantCard is already React.memo wrapped)

**Build verified**: `npm run build` passes (34s, no errors)

**Progress: 106/147 stories complete (72%), 41 remaining**

## 2026-03-07 — Ralph Loop 12: Critical Platform Improvements (Performance, UX, Accessibility)

- **US-144**: Reduced hover animation durations across all card components:
  - RestaurantCard: duration-300 → duration-200 on article, image scale, and CTA strip transitions
  - HotelCard: duration-300 → duration-200 on image scale, overlay, and CTA transitions
  - Attractions page: duration-300 → duration-200 on card hover and image scale
  - EventCard already used duration-200 (verified)
- **US-093**: Replaced raw `<img>` tags with OptimizedImage component:
  - EventCard: switched to OptimizedImage with 640x192 dimensions, aspect ratio, responsive sizes
  - RestaurantCard: switched to OptimizedImage with 640x192 dimensions, responsive sizes
  - Attractions page: switched inline card images to OptimizedImage with aspect-video wrapper
  - All images now get Intersection Observer lazy loading, blur placeholders, and WebP/AVIF via picture element
- **US-088**: Added pagination controls to Restaurants and Attractions pages:
  - Both pages: 30 items per page, page state resets on filter changes
  - Desktop: full pagination controls using shadcn pagination component (Previous, page numbers, Ellipsis, Next)
  - Mobile: "Load More" button pattern for touch-friendly infinite scroll
  - Results count display: "Showing X-Y of Z" on desktop, "Showing X of Z" on mobile
  - Scroll-to-top on page change (desktop)
- **US-094**: Added interactive map accessibility:
  - Added `role="application"` and descriptive `aria-label` to InteractiveMap container
  - Added "Skip map, view as list" link (sr-only, visible on focus) before map
  - Added "Show list view" toggle button for all users
  - Built keyboard-accessible list alternative showing all map locations with name, category, rating, distance
  - List view uses semantic `<ul>` with `<Link>` items for full keyboard navigation
- **US-141/US-142/US-143/US-146/US-120**: Verified already complete in codebase, marked as passed:
  - Card images already have width/height attributes (US-141)
  - All cards have decoding="async" + loading="lazy" (US-142)
  - Image fallback placeholders have role="img" + aria-label (US-143)
  - Three.js skipped on mobile via useMediaQuery (US-146)
  - FAQ sections with schema on Events and Attractions pages (US-120)

**Build verified**: `npm run build` passes (45s, no errors)

**Progress: 102/147 stories complete (69%), 45 remaining**

## 2026-03-05 — Ralph Loop 10: Form A11y, AuthContext Split, Performance, SEO, UX

- **US-079**: Improved form accessibility in EventSubmissionForm:
  - Added FormErrorSummary with clickable field links at form top on validation failure
  - Wrapped all sections in `<fieldset>` + `<legend>` (Basic Info, Location, Additional Details, Tags)
  - Added `aria-invalid` and `aria-describedby` to all error-producing fields
  - Added `autoComplete` attributes: email, tel, street-address, url
- **US-080**: Split AuthContext into state and actions contexts:
  - Created `AuthStateContext` (read-only state) and `AuthActionsContext` (actions only)
  - Added `useAuthState()` — components reading only state won't re-render when action refs change
  - Added `useAuthActions()` — action-only components won't re-render on session changes
  - `useAuth()` remains backward-compatible, combines both contexts
  - Actions memoized via `useMemo` for stable references
- **US-081**: Created centralized TanStack Query configuration:
  - `src/lib/queryKeys.ts` with query key factory for events, restaurants, attractions, favorites, ratings, search, admin, user
  - `CACHE_TIERS` with 4 tiers: static (10m), standard (2m), frequent (30s), realtime (15s)
- **US-082**: Verified Three.js already deferred, added React.memo to HeroCityLite
- **US-083**: Added React.memo to RestaurantCard and SocialEventCard; added useCallback for event detail navigation handler in EventsPage
- **US-090**: Added focus-visible ring styles to SocialEventCard and EventCard for keyboard navigation
- **US-092**: Added FAQ section with Schema.org FAQPage structured data to TripPlanner (5 questions targeting long-tail search)
- **US-095**: Added toast feedback to FavoriteButton; moved Sonner to top-center to avoid BottomNav overlap

**Build verified**: `npm run build` passes (56s, no errors)

**Progress: 84/140 stories complete (60%), 56 remaining**

## 2026-03-04 — Ralph Loop 9: Accessibility, SEO, UX Polish

- **US-077**: Fixed heading hierarchy violations across pages:
  - Demoted duplicate h1 to h2 in Gamification.tsx (authenticated view) and EventPromotionPlanner.tsx (results view)
  - Strengthened accessibility test in tests/accessibility.spec.ts to enforce single h1 and no heading-level skips
- **US-078**: Added aria-live regions for screen reader announcements:
  - Created `src/hooks/use-announce.ts` — useAnnounce hook with configurable politeness, sr-only region, debounced re-announcement
  - Wired to EventsPage, Restaurants, and Attractions listing pages — announces result count on filter/search changes
- **US-084**: Verified empty states with contextual actions on all listing pages:
  - Upgraded Attractions page from plain text empty state to proper EmptyState component with clear-filters and browse-all actions
  - Events and Restaurants already had full EmptyState implementations
- **US-091**: Added BreadcrumbListSchema structured data to all detail pages:
  - RestaurantDetails: breadcrumb chain Home → Restaurants → Cuisine → Restaurant Name
  - HotelDetails: breadcrumb chain Home → Hotels → Hotel Name
  - EventDetails, AttractionDetails, PlaygroundDetails already had BreadcrumbListSchema
- **US-098**: Verified prefers-reduced-motion already fully implemented in index.css (two `@media (prefers-reduced-motion: reduce)` rules disabling all animations/transitions globally)
- **US-099**: Created shared category color map:
  - `src/lib/categoryColors.ts` with CATEGORY_COLORS record (15 categories), getCategoryColor(), getCategoryHex()
  - Wired into InteractiveMap component replacing inline color map
- **US-107**: Verified sort dropdowns on all three listing pages (Events, Restaurants, Attractions) — all functional
- **US-109**: Added active filter badge display to Attractions page:
  - Badges for search query, type, rating, featured filters with individual dismiss (X) buttons
  - Clear All button when any filter is active
  - Accessible aria-labels on all dismiss buttons

**Build verified**: `npm run build` passes (55s, no errors)

**Progress: 76/140 stories complete (54%), 64 remaining**

## 2026-02-28 — Ralph Loop 8: Security Hardening, Code Cleanup, UX Improvements

- **US-072**: Fixed critical Stripe webhook signature verification bypass:
  - Made `STRIPE_WEBHOOK_SECRET` **required** — rejects with 500 if not configured instead of silently skipping verification
  - Removed development bypass that allowed unsigned webhook events to be processed
  - Always verifies signature via `stripe.webhooks.constructEvent()` — no fallback to raw `JSON.parse`
- **US-100**: Removed CORS headers from Stripe webhook endpoint:
  - Replaced `Access-Control-Allow-Origin: *` with no CORS headers (webhooks are server-to-server, not browser-initiated)
  - Removed OPTIONS preflight handler — Stripe never sends CORS preflight
  - Simplified response headers to `Content-Type: application/json` only
- **US-074**: Sanitized ilike query parameters in api-events edge function:
  - Imported `sanitizeString` from `_shared/validation.ts` (was defined but unused)
  - Applied sanitization to `location` and `search` parameters before interpolation into `.or()` ilike filters
  - Prevents SQL injection via special characters in query strings
- **US-076**: Fixed sitemap domain mismatch:
  - Edge function (`generate-sitemap`): reads `VITE_SITE_URL` env var with `desmoinesinsider.com` fallback, replacing hardcoded domain
  - Script (`scripts/generate-sitemap.js`): reads `VITE_SITE_URL` from `process.env`, replacing hardcoded `desmoinespulse.com`
  - Both now use consistent canonical domain
- **US-102**: Removed 13 dead/unused hooks from `src/hooks/`:
  - Deleted: `useCompetitorAnalysis`, `useLocationTracking`, `useEventClone`, `useFormAccessibility`, `useAccountLockout`, `useAdminSecurity`, `useAdvancedAnalytics`, `useCampaignRenewal`, `useDebouncedValue`, `useEnhancedSearch`, `useGeofencing`, `useMagicLink`, `useSecureProfile`
  - Each verified with 0 imports across entire `src/` directory
  - Hook count: 116 → 103
- **US-128**: Improved event time display on EventCard:
  - Updated `formatEventDateShort` to include day-of-week abbreviation: `EEE, MMM d '@' h:mm a` → e.g. "Sat, Mar 15 @ 7:00 PM"
  - Date-only events show `EEE, MMM d` → e.g. "Sat, Mar 15"
  - More compact format aids quick scanning in card grids
- **US-112**: Added Sign Up CTA button in header for non-authenticated users:
  - Desktop-only `Sign Up Free` button (hidden on mobile via `hidden lg:block`)
  - Positioned between navigation and user menu
  - Links to `/auth` with UTM tracking parameters (`utm_source=header&utm_medium=cta`)
  - Disappears when user is authenticated
- Build verified: `npm run build` passes (58.3s, no errors)
- **Progress: 65/130 stories complete (50%)**

## 2026-02-28 — Ralph Loop 7: Visitor & Planner Pages (Transportation, Guide, Meetings, Seasons)

- **US-049**: Built comprehensive getting-around guide:
  - Created `/getting-around` page with 7 sections: Parking & ParkDSM (5 garages with rates/hours), Skywalk System guide, BCycle Bike Share (pricing + stations), Rideshare tips (Uber/Lyft fare estimates), DSM Airport (6 airlines + 5 ground transport options), DART Public Transit (D-Line free downtown circulator), Distance From Downtown table (7 destinations with drive times + transit)
  - Schema.org FAQPage structured data with 5 common transportation questions
- **US-050**: Created downloadable/requestable visitor guide:
  - Created `guide_requests` migration with RLS (public insert, admin read/update)
  - Built `/visitors-guide` page with 2026 Edition hero, What's Inside feature cards
  - Tabbed interface: Digital Download (email capture for lead gen) and Request Free Copy (full mailing address form)
  - Schema.org TouristInformationCenter structured data
- **US-051**: Built meeting planner and group travel portal:
  - Created `meeting_venues` migration with RLS and 8 seeded venues (Iowa Events Center 15K cap, CCCUCC 5K, Marriott 1.2K, Hilton 800, Brenton Plaza, Temple, Jasper Winery, Art Center)
  - Created `rfp_submissions` migration with RLS (public insert, admin read/update)
  - Built `/group-travel` page with Why Des Moines stats, venue search (filter by type/capacity), Group Itineraries link, RFP submission form
  - Created `useMeetingVenues` hook with venue filters and `useSubmitRfp` mutation
- **US-052**: Added seasonal and holiday event guides:
  - Created `seasonal_guides` migration with RLS and 4 seeded guides: Summer 2026, Fall Festivals & Foliage, Holiday Lights & Markets, Iowa State Fair Survival Guide
  - Built `/guides/:slug` seasonal guide detail page with sectioned content, checklist items, Schema.org Article structured data
  - Updated existing GuidesPage to dynamically pull from seasonal_guides table and display seasonal guides grid above static content
  - Created `useSeasonalGuides` hook with season labels/colors
- All pages added to navigation (new "Plan" nav group: Getting Around, Visitor Guide, Group & Meetings)
- Build verified: `npm run build` passes (59s, no errors)
- **Progress: 58/104 stories complete (56%)**

## 2026-02-28 — Ralph Loop 6: Content Hubs (Music, Sports, Outdoors, Breweries)

- **US-045**: Built live music and concert hub:
  - Created `venues` migration with RLS, seeded 9 Des Moines venues (Wells Fargo Arena, Hoyt Sherman Place, Woolys, xBk, Leftys, Val Air Ballroom, Civic Center, Simon Estes Amphitheater, Vibrant Music Hall)
  - Built `/music` hub page with Tonight's Shows, This Weekend, Upcoming Concerts, and Venue Guide sections
  - Built `/music/venues/:slug` venue detail page with upcoming events, capacity, location, directions
  - Created `useVenues` hook (useVenues, useVenue, useVenueEvents)
  - Music events filtered by category=Music/Concert/Live Music
- **US-046**: Built sports hub with team schedules:
  - Created `teams` migration with RLS, seeded 7 teams (Iowa Cubs, Iowa Wild, Iowa Wolves, Iowa Barnstormers, Des Moines Menace, Drake Bulldogs, Grand View Vikings)
  - Built `/sports` hub with Today's Games, This Week's Schedule, Teams & Venues sections with sport emoji icons
  - Built `/sports/:slug` team detail page with Schema.org SportsTeam structured data, upcoming games
  - Created `useTeams` hook (useTeams, useTeam, useTeamGames)
  - Sports events filtered by category=Sport/Game/Baseball/Hockey/Basketball/Football/Soccer
- **US-047**: Created trails and outdoor recreation hub:
  - Created `trails` migration with RLS, seeded 8 trails (High Trestle, Raccoon River Valley, Gray's Lake, Clive Greenbelt, Neal Smith, Great Western, Sycamore, Bill Riley)
  - Built `/outdoors` hub with Featured Trails spotlight, difficulty/activity filters, and All Trails grid
  - Built `/outdoors/:slug` trail detail page with highlights, activities, trailhead directions, distance info
  - Created `useTrails` hook (useTrails, useTrail, useFeaturedTrails) with helper functions
  - Difficulty color badges (green/yellow/red), surface type labels, activity icons
- **US-048**: Built brewery and craft beer trail:
  - Created `brewery_trail_checkins` migration with RLS and UNIQUE(user_id, restaurant_id)
  - Built `/breweries` page with Brewery Passport progress tracker (progress bar, X/Y count, completion message)
  - Brewery grid with check-in status overlay (visited badge + filled circle vs empty)
  - Check-in dialog with optional beer name and 5-star rating input
  - Created `useBreweryTrail` hook (useBreweries, useBreweryCheckins, useCheckinMutation)
  - Gamification: +40 XP per check-in, trail completion badge
- All 4 content hubs added to main navigation (Explore group + flat nav list)
- Build verified: `npm run build` passes (59s, no errors)
- **Progress: 54/104 stories complete (52%)**

## 2026-02-28 — Ralph Loop 5: API Docs, Itineraries, Deals, Discovery Map

- **US-029**: Created comprehensive API documentation:
  - `docs/API_REFERENCE.md` documenting all 85 edge functions
  - Grouped by domain: Content (8), Payment (8), Data Processing (21), AI/ML (9), Analytics (5), SEO (11), Notification (5), Admin (3), Utility (11), Integration (4)
  - Each entry: HTTP methods, auth type, rate limit tier, request/response schemas
  - Authentication methods table (JWT, API Key, None)
  - Rate limit tiers table (Default 100/15min, Write 30/15min, AI/Strict 10/15min)
  - Environment variables reference for all secrets
- **US-042**: Built curated editorial itineraries system:
  - Created `curated_itineraries` migration with RLS, theme/duration indexes
  - Seeded 6 itineraries: Weekend Getaway, Date Day, Family Fun, Craft Beer Trail, Art Walk, Foodie Tour
  - Built `/itineraries` listing page with itineraries grouped by theme (General, Romance, Family, Food & Drink, Arts & Culture)
  - Built `/itineraries/:slug` detail page with step-by-step stop cards, insider tips, time suggestions
  - Print-friendly CSS: `@media print` styles render checkbox checklist, hide header/footer
  - ICS calendar export: "Add All to Calendar" generates .ics file with all stops as events
  - Schema.org TouristTrip structured data on detail pages
  - Created `useItineraries` + `useItinerary` hooks with TanStack Query
- **US-043**: Built deals, coupons, and special offers section:
  - Created `deals` migration with RLS policies (public read for active, auth insert, admin manage)
  - Seeded 5 sample deals: 20% Off Brunch, BOGO Beer, $5 Off Admission, Free Dessert, Hotel Package
  - Built `/deals` page with category filter buttons (All/Dining/Attractions/Hotels/Events/Activities)
  - Created `DealCard` component with deal type badge, expiry countdown, claim/reveal flow, promo code copy
  - Created `useDeals`, `useFeaturedDeals`, `useClaimDeal` hooks
  - Expiry badges: "Last day!", "Expires in 3 days", "New this week"
- **US-044**: Built interactive city discovery map:
  - Created `/map` route with full-screen Leaflet map centered on Des Moines (41.5868, -93.625)
  - Toggleable layers: Events, Restaurants, Attractions with entity counts
  - Marker popups: entity name, category, description, rating, "View Details" link, "Get Directions" (Google Maps)
  - "Near Me" geolocation button
  - Lazy-loaded map component for performance (React.lazy + Suspense)
  - Fetches up to 700 geocoded entities (200 events, 300 restaurants, 200 attractions)

**Build verified**: `npm run build` passes (52.20s)

**PRD Status**: 50/104 stories complete (48%)

## 2026-02-26 — Ralph Loop 4: Bundle Cleanup, Deployment Docs, Des Best Voting, What's New Feed

- **US-024**: Optimized bundle size — removed 6 unused dependencies:
  - Removed from dependencies: cssnano, jsonwebtoken, lightningcss, node-fetch, stripe, terser
  - Moved puppeteer to devDependencies (150+ MB savings from production bundle)
  - Verified Three.js/Leaflet/Tiptap/FullCalendar all properly lazy-loaded
  - Main index chunk: 429 KB → 423 KB; build time: 63s → 50s
- **US-030**: Production deployment runbook already complete:
  - `docs/PRODUCTION_RUNBOOK.md` (932 lines) covers all acceptance criteria
  - Pre-deployment checklist, deployment steps, post-deployment verification
  - Rollback procedures, P1-P4 incident response, troubleshooting guides
- **US-041**: Built Des Best crowdsourced voting system:
  - Created `voting_categories` + `votes` tables with RLS + UNIQUE(category_id, user_id)
  - Seeded 10 categories: Best Pizza, Coffee, Happy Hour, Brunch, Date Night, Hidden Gem, Food Truck, Brewery, Ice Cream, Live Music
  - Built `/best-of` route with category grid + vote counts
  - Built `/best-of/:category` with VotingBooth (search-to-vote + write-in), ranked results with vote percentage bars
  - Created `src/hooks/useVoting.ts` with TanStack Query hooks for categories, results, user vote, cast vote
  - Added to main navigation under Resources
- **US-058**: Built What's New in DSM feed:
  - Created `scene_updates` table with type check constraint, RLS, indexes on type/date/neighborhood
  - Auto-generates "New Opening" entries via DB trigger on restaurant INSERT
  - Built `/whats-new` route with type filter buttons and chronological SceneUpdateCard feed
  - SceneUpdateCard shows type badges (New!/Closing/Renovation/etc.), timeAgo, entity links
  - Created `src/hooks/useSceneUpdates.ts` with TanStack Query hooks
  - Added to main navigation under Resources

**Build verified**: `npm run build` passes (50.31s)

**PRD Status**: 46/71 stories complete (65%)

## 2026-02-26 — Ralph Loop 3: Persistence, Indexes, A/B Testing, Image Backfill UI

- **US-020**: Implemented scraping jobs database persistence:
  - Created `scraping_jobs` migration with RLS (admin CRUD), indexes on status/job_type/last_run
  - Seeded 8 Des Moines event source jobs (Catch DSM, Iowa Cubs/Wolves/Wild/Barnstormers, Events Center, Vibrant Music Hall, Google Events)
  - Rewrote `useScraping.ts`: all operations (add, run, stop, config update) now persist to database
  - Resolved TODO at line 397: `stopAllJobs()` uses `AbortController` to signal loop abort + persists `stopped` status to DB
  - Resolved TODO at line 483: `addJob()` inserts to database via Supabase, generates UUID server-side
  - Replaced all `console.*` calls with structured logger
- **US-025**: Optimized database queries with 16 new indexes:
  - Composite: events(date,category), events(date,city), restaurants(cuisine,city), restaurants(rating DESC), attractions(category,city)
  - Foreign key: favorites(user_id), favorites(event_id), ratings(user_id), reviews(entity_id), reviews(user_id)
  - Partial: events upcoming (WHERE date >= now())
  - Trigram: events.title, restaurants.name, attractions.name (gin_trgm_ops)
  - Analytics: content_metrics(content_id,metric_type), content_metrics(created_at), user_analytics(session_id), user_analytics(event_type)
- **US-040**: Implemented A/B testing infrastructure:
  - Extended `feature_flags` table with `variants` JSONB and `traffic_split` numeric columns
  - Created `experiment_assignments` table with unique constraints on (flag_key, user_id) and (flag_key, session_id)
  - Built `src/hooks/useExperiment.ts` with deterministic hash-based variant assignment (user_id for authenticated, session_id for anonymous)
  - Fire-and-forget tracking of `experiment_assigned` events to `user_analytics`
  - Created `src/components/Experiment.tsx` declarative wrapper: `<Experiment flag="test" variants={{ A: <V1/>, B: <V2/> }} />`
- **US-065**: Built admin UI for backfill-images:
  - Created `src/components/admin/ImageBackfill.tsx` with full backfill control panel
  - Per-category stats cards showing total/with images/needs backfill counts
  - Run Batch (single), Run All (auto-advance), Pause, Reset controls
  - Progress bar with updated/skipped/failed counters
  - Scrollable results log with status badges per record
  - Added as "Backfill Images" tab in AdminMedia.tsx alongside MediaLibrary

**Build verified**: `npm run build` passes with no errors (Vite 5.4.21, 1m 3s)

**PRD Status**: 42/71 stories complete (59%)

## 2026-02-25 — Ralph Loop 2: Preferences, Feature Flags, Audit, Funnel

- **US-019**: Implemented server-side user preferences in `use-user-preferences.ts`:
  - On login: loads preferences from `profiles.communication_preferences.ui_preferences` (JSONB)
  - Server wins on conflict when merging with localStorage defaults
  - Debounced sync to server on preference changes (1s delay)
  - Unauthenticated users continue using localStorage only (graceful degradation)
  - Resolved both TODO comments in the hook
- **US-028**: Enhanced `useFeatureFlag` hook with graceful degradation:
  - Added `DEFAULT_FLAGS` fallback when feature_flags table query fails
  - Migration already existed (`20260221000000_create_feature_flags.sql`)
  - `FeatureFlag` wrapper component already existed
  - Hook now returns default values instead of throwing on database errors
- **US-037**: Created admin audit trail system:
  - Created `src/hooks/useAuditLog.ts` with `logAdminAction()` and `useAuditLogEntries()` hooks
  - Uses existing `security_audit_logs` table with `event_type='admin_action'`
  - Created `src/components/admin/AuditLogPanel.tsx` with scrollable log viewer
  - Added "Audit Log" tab to AdminAnalyticsPage
- **US-039**: Implemented conversion funnel tracking:
  - Created `src/hooks/useConversionFunnel.ts` with `trackFunnelEvent()` and `useConversionFunnelData()` hooks
  - Tracks: pricing_page_viewed, plan_selected, checkout_started, checkout_completed, checkout_abandoned
  - Integrated into Pricing page (page view, plan selection, checkout start, checkout cancel)
  - Created `src/components/admin/ConversionFunnelChart.tsx` with bar visualization and drop-off percentages
  - Added "Conversion Funnel" tab to AdminAnalyticsPage

**Build verified**: `npm run build` passes with no errors (Vite 5.4.21, 6378 modules, 56s)

**PRD Status**: 38/71 stories complete (54%)

## 2026-02-25 — Ralph Loop: Security, SEO, Admin, Code Quality

- **US-001 through US-010**: Verified all 10 stories as complete in codebase; marked `passes: true` in prd.json (CI/CD, Vitest, unit tests, structured logger, console cleanup, Sentry)
- **US-017**: Fixed `metadata?: any` → `Record<string, unknown>` in AuthContext.tsx for strict mode compliance
- **US-031**: Replaced `window.location.href` with no-ops in `defaultShortcuts` (runtime already uses `useNavigate()` in the hook handler); replaced `window.location.href = 'mailto:...'` with `<a href="mailto:...">` via `asChild` in AdvertiseSuccess.tsx and AdvertiseCancel.tsx
- **US-061**: Added rate limiting (10 req/15min) to 5 AI/LLM edge functions:
  - `generate-itinerary` (Claude trip planner)
  - `generate-seo-content` (Claude SEO batch)
  - `personalized-recommendations` (OpenAI recommendations)
  - `generate-writeup` (Claude writeups)
  - `analyze-competitor` (Claude competitor analysis)
  - Documented rate limit tiers in `_shared/rateLimit.ts` header comment
- **US-062**: Added `requireApiKey()` middleware to `backfill-images` edge function; added `x-api-key` to CORS allowed headers
- **US-063**: Guaranteed dynamic `og:image` with fallback on all content detail pages:
  - HotelDetails: Added full og:image, og:image:width/height/alt, and twitter:image tags (was completely missing)
  - EnhancedEventSEO: Changed conditional `{event.image_url && ...}` to always-present with brand fallback, added width/height
  - EnhancedAttractionSEO: Same pattern — unconditional og:image with fallback, added width/height
  - EnhancedPlaygroundSEO: Same pattern — unconditional og:image with fallback, added width/height
- **US-064**: Wired MediaLibrary admin component into the Admin panel:
  - Created `src/pages/AdminMedia.tsx` page wrapper
  - Added lazy import and `/admin/media` route in App.tsx with ProtectedRoute
  - Added "Media" nav item with Image icon to AdminNav.tsx

**Build verified**: `npm run build` passes with no errors (Vite 5.4.21, 6374 modules, 46s)

**PRD Status**: 34/71 stories complete (48%)

## 2026-02-23 — PRD Stories: Content Tracking, Email Capture, Analytics

- **US-032**: Created `src/hooks/useContentTracking.ts` — fire-and-forget hook that tracks views, shares, favorites, and clicks to the `content_metrics` table. Integrated into EventDetails, RestaurantDetails, and AttractionDetails pages with automatic view tracking on mount and share tracking via ShareDialog `onShare` callback.
- **US-034**: Created `src/components/EmailCaptureModal.tsx` — dismissible email capture modal with consent checkbox, name field, and safeStorage "don't show again" support. Integrated into TripPlanner (shown after itinerary generation). EventPromotionPlanner already had its own email capture.
- **US-033**: Replaced all hardcoded metrics in AdminAnalyticsDashboard with real computed data:
  - Bounce rate: computed from sessions with single page view / total sessions
  - Avg session duration: computed from first-to-last event timestamps per session_id
  - Device breakdown: computed from `device_type` field in `user_analytics`
  - Top pages avgTime: estimated from inter-event gaps per page path
  - Removed fake "+12%", "-3%", "+15%" comparison labels with accurate descriptions
- **US-026**: Verified complete — skip navigation link in Header.tsx, `main#main-content` target in App.tsx, focus indicators in index.css, sr-only utility, AccessibleSkipLink component, and aria-labels on key interactive elements
- **US-027**: Verified complete — `useReducedMotion` hook exists, `@media (prefers-reduced-motion: reduce)` CSS rules in index.css (global animation disable), `.a11y-reduced-motion` class for programmatic toggle
- **US-011**: Verified complete — `public/_headers` with CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, and comprehensive cache headers for Cloudflare Pages
- **US-023**: Verified complete — `src/lib/env.ts` with environment variable validation
- **US-038**: Verified complete — `src/lib/fetchWithRetry.ts` with exponential backoff
- Added `onShare` callback prop to ShareDialog component for tracking integration

**Build verified**: `npm run build` passes with no errors (Vite 5.4.21, 6369 modules, 55s)

## 2026-02-21 — PRD Stories Batch Implementation
- **US-012**: Security audit of unauthenticated edge functions; created docs/SECURITY_AUDIT.md with risk classification for all 18 verify_jwt=false functions; added security documentation comments to each function
- **US-013**: Created API key auth middleware (supabase/functions/_shared/apiKeyAuth.ts) with timing-safe comparison and X-API-Key header support
- **US-014**: Verified rate limiting infrastructure already standardized in _shared/rateLimit.ts (100 req/15min default)
- **US-015**: Migrated all direct localStorage usage to safeStorage wrapper across 11 files (hooks, components, lib); only legitimate exceptions remain (AuthContext cleanup, Supabase SDK adapter, safeStorage internal)
- **US-016**: Enabled TypeScript strict mode for src/lib/ directory; fixed ~80 strict mode errors (null checks, unused imports, type assertions, index signature access); tsconfig.strict.json now includes src/lib/**/*.ts with zero errors
- **US-021**: Enabled SearchTrafficDashboard in AdminAnalyticsPage with new "Search Traffic" tab
- **US-022**: Created src/lib/geo.ts with Haversine distance calculation, sortByDistance, distanceScore, isWithinRadius utilities; 12 unit tests all passing
- **US-035**: Created useNetworkStatus hook and OfflineBanner component; integrated into App.tsx
- **US-036**: Created useDebouncedValue hook with configurable delay

## 2026-02-06 — PRD Created
- Created prd.json with 30 user stories for enterprise readiness
- Created progress.txt with codebase patterns
- **Learnings for future iterations:**
  - The codebase has strong foundations but critical operational gaps
  - CI/CD and observability are the highest-priority blockers
  - TypeScript strict mode migration should start from src/lib/ (imported everywhere)
  - Console cleanup needs a structured logger utility first (US-005) before bulk replacement
  - Edge function security is a mix: some intentionally unauthenticated (webhooks), some should be locked down

## 2026-03-05 — Ralph Loop 11 (9 stories: US-096, US-101, US-103, US-105, US-108, US-114, US-117, US-118, US-119)
- US-096: Login attempt throttling in AuthContext (in-memory, max 5 per 15min)
- US-101: Optimistic updates for useFavorites with cache snapshot rollback
- US-103: Replaced 9 'any' types across useAnalytics, useAIConfiguration, useAdminCampaigns
- US-105: Verified already complete (StickyMobileCTA on all detail pages)
- US-108: Featured events section on EventsPage with Star icon, conditional on no active filters
- US-114: LastUpdatedBadge component on all detail pages with relative time + verification checkmark
- US-117: Verified already complete (EmptyState on Attractions page)
- US-118: Keyboard-accessible RestaurantCard CTA strip (group-focus-within:translate-y-0)
- US-119: New Restaurant badge via SocialProofBadge for restaurants < 14 days old
- **Build**: passes (59s)
- **Progress**: 93/140 stories complete (66%), 47 remaining

## 2026-03-28 — Security Audit & Hardening (SEC-001 through SEC-007)

### Comprehensive Security Audit
- Audited entire codebase for XSS, injection, input validation, CORS, auth, rate limiting issues
- Created new prd.json with 30 security-focused Ralph stories (SEC-001 through SEC-030)
- Identified CRITICAL issues in 4 email-generating edge functions, CORS wildcard on 7+ endpoints, missing auth on bulk-update-restaurants, and SQL LIKE wildcard injection

### Fixes Implemented

- **SEC-001**: Created `supabase/functions/_shared/escapeHtml.ts` — shared HTML escaping utility with `escapeHtml()` and `escapeHtmlAttr()` functions. Handles null/undefined, escapes &, <, >, ", ' characters.

- **SEC-002**: Fixed XSS in `send-event-reminders/index.ts` — escaped event_title, event_venue, event_location in HTML template and subject line. Both HTML and plain text email variants secured.

- **SEC-003**: Fixed XSS in `send-weekly-digest/index.ts` — escaped all user data (title, location, category, recommendation_reason) in all 4 email section builders: buildUpcomingRSVPsSection, buildSavedEventsSection, buildRecommendationsSection, buildTrendingSection.

- **SEC-004**: Fixed XSS in `notify-event-submission/index.ts` — escaped eventTitle, eventDate, eventVenue, eventCategory, submitterName, submitterEmail in admin notification, and eventTitle, message, adminNotes in submitter notification. Subject lines also escaped.

- **SEC-005**: Fixed XSS in `send-campaign-notification/index.ts` — escaped title, message, ctaLabel, ctaUrl, campaignName in campaign email template.

- **SEC-006**: Added input validation and bounds checking to `EventSubmissionForm.tsx` — maxLength on title(200), description(5000), venue(200), location(200), address(500), price(100), website_url(2000), contact_email(254), contact_phone(20). Added URL/email/phone format patterns. Tags limited to 50 chars each, 10 max.

- **SEC-007**: Hardened `_shared/validation.ts` — added `sanitizeLikeInput()` function that escapes SQL LIKE wildcards (%, _, \). Added maxLength bounds (500 chars default). Updated `api-events/index.ts` and `api-restaurants/index.ts` to use sanitizeLikeInput for all ilike queries.

### Files Changed
- `supabase/functions/_shared/escapeHtml.ts` (NEW)
- `supabase/functions/_shared/validation.ts` (MODIFIED — added sanitizeLikeInput)
- `supabase/functions/send-event-reminders/index.ts` (MODIFIED — XSS fix)
- `supabase/functions/send-weekly-digest/index.ts` (MODIFIED — XSS fix)
- `supabase/functions/notify-event-submission/index.ts` (MODIFIED — XSS fix)
- `supabase/functions/send-campaign-notification/index.ts` (MODIFIED — XSS fix)
- `supabase/functions/api-events/index.ts` (MODIFIED — LIKE injection fix)
- `supabase/functions/api-restaurants/index.ts` (MODIFIED — LIKE injection fix)
- `src/components/EventSubmissionForm.tsx` (MODIFIED — input validation)
- `prd.json` (NEW — 30 security stories)

### Learnings for future iterations
- Edge functions that generate HTML emails are the primary XSS attack surface — React auto-escapes JSX but template literals in Deno do not
- Supabase client ilike() still interpolates values into the filter string — always use sanitizeLikeInput for user-provided search terms
- CORS wildcard (*) is used in at least 7 edge functions — each needs migration to getCorsHeaders/handleCors from _shared/cors.ts
- In-memory rate limiting in serverless resets on cold starts — document this and plan for Redis/DB-backed alternative
- apiKeyAuth.ts fails open when key is not configured — needs environment-aware fail-closed behavior

### Remaining Work
- ~~23 security stories (SEC-008 through SEC-030) remain with `passes: false`~~ (RESOLVED: See 2026-03-28 entry)
- ~~Highest remaining priorities: CORS wildcard fixes (SEC-008 through SEC-012), auth gaps (SEC-013, SEC-014), rate limiting (SEC-015)~~ (RESOLVED)
- **Progress**: ~~7/30~~ 30/30 security stories complete (100%)
---

## 2026-03-28 — Security Hardening: SEC-008 through SEC-030 (23 stories)

### CORS Wildcard Fixes (SEC-008 through SEC-012)
- **SEC-008**: gsc-oauth — replaced wildcard CORS with handleCors/getCorsHeaders/isOriginAllowed from _shared/cors.ts
- **SEC-009**: manage-subscription — replaced wildcard CORS, added returnUrl domain validation (SEC-028)
- **SEC-010**: oauth-callback — replaced wildcard CORS, added state timestamp validation (10 min expiry), sanitized error messages
- **SEC-011**: delete-user-account — replaced wildcard CORS, sanitized error responses
- **SEC-012**: export-analytics-data — replaced wildcard CORS, added date range validation, added CSV injection protection (SEC-023)

### Authentication & Authorization Gaps (SEC-013, SEC-014, SEC-018)
- **SEC-013**: bulk-update-restaurants — added requireApiKey auth, rate limiting (5 req/15 min), removed Google API key from image URLs
- **SEC-014**: send-campaign-notification — added JWT auth requiring admin role or campaign ownership, rate limiting (10 req/15 min)
- **SEC-018**: update-event-datetime — added requireApiKey auth, UUID validation for eventId, ISO datetime validation, IANA timezone validation, replaced wildcard CORS

### Rate Limiting & Auth Fixes (SEC-015, SEC-016)
- **SEC-015**: gsc-oauth — added checkRateLimit (10 req/15 min), returns 429 with Retry-After
- **SEC-016**: apiKeyAuth.ts — now fails closed in production when EDGE_FUNCTION_API_KEY is not set, allows in dev with console.warn

### Input Validation & Bounds Checking (SEC-017, SEC-026, SEC-029)
- **SEC-017**: api-events + api-restaurants — added max: 10000 to offset validation schemas
- **SEC-026**: register-device-token — added deviceToken validation (non-empty, max 500 chars), platform enum (ios/android/web), replaced wildcard CORS
- **SEC-029**: generate-seo-content — added batchSize validation (1-20 range), requireApiKey auth, replaced wildcard CORS

### Error Response Sanitization (SEC-019)
- Sanitized error responses in 18+ edge functions to return generic messages instead of error.message/error.stack
- Functions fixed: gsc-oauth, manage-subscription, delete-user-account, export-analytics-data, bulk-update-restaurants, publish-article-webhook, process-stripe-refund, generate-seo-content, update-event-datetime, register-device-token, check-core-web-vitals, gsc-fetch-properties, check-security-headers, send-event-reminders, send-weekly-digest, stripe-webhook, create-subscription-checkout, create-campaign-checkout

### SSRF & Webhook Security (SEC-020)
- **SEC-020**: publish-article-webhook — added validateURLForSSRF to block private IPs/localhost/internal domains, added requireApiKey auth, rate limiting (10 req/15 min)

### ReDoS & Injection Protection (SEC-022, SEC-023)
- **SEC-022**: firecrawl-scraper — added HTML size limit (500KB) for regex matching, added rate limiting (10 req/15 min), title escaping was already in place
- **SEC-023**: export-analytics-data — created csvEscapeValue function that prefixes formula chars (=,+,-,@,|) with single quote, properly quotes special characters

### OAuth & CSRF Hardening (SEC-024, SEC-028)
- **SEC-024**: OAuthProviderSetup.tsx — added 16-byte cryptographic nonce via crypto.getRandomValues() to state parameter, server validates timestamp expiry
- **SEC-028**: manage-subscription — added validateReturnUrl that checks against site domain, blocks javascript:/data: protocols, allows relative paths

### Payment & Stripe Security (SEC-027)
- **SEC-027**: process-stripe-refund — added idempotency key generation per refund (refund_{payment_intent_id}_{timestamp}), passed via Stripe API options, logged for audit trail

### Documentation & Infrastructure (SEC-030)
- **SEC-030**: _shared/rateLimit.ts — added comprehensive docs on per-instance limitation, cold start resets, and 3 upgrade paths (Upstash Redis, DB-backed, Cloudflare Rate Limiting)

### ~~Deferred Items~~ (Now Resolved — see 2026-03-28 follow-up below)

### Build Verification
- `npx vite build` — ✅ passes
- `npx tsc --noEmit` — ✅ passes (zero errors)
- **Progress**: 30/30 security stories complete (100%)
---

## 2026-03-28 (follow-up) — Resolve Deferred Security Items (SEC-021, SEC-025)

### CSP Hash-Based Inline Script Protection (SEC-021)
- Computed SHA-256 hashes for all 3 inline `<script>` blocks in index.html:
  - Early error handler (Capacitor/extension recovery): `sha256-Kw9HGjn5It5RZAG5kLMXWmViQDVGjcc+6tbKzXHsM+w=`
  - Service worker cache clearer: `sha256-zt7H85Ukw5RMZzzaysfcKBKobrZ38x1FVnUhNcoAE7g=`
  - Google Analytics deferred loader: `sha256-u7sOXfuX6ewWS5mc0yr7d1B3D09rhsrBm9EkgJKhvqY=`
- Replaced `'unsafe-inline'` in script-src CSP directive with the 3 SHA-256 hashes in `public/_headers`
- `style-src` retains `'unsafe-inline'` (required by Tailwind CSS)
- Added documentation comments in `_headers` with hash regeneration instructions

### Account Deletion Confirmation Tokens (SEC-025)
- Implemented two-step deletion flow in `delete-user-account` edge function:
  - Step 1: `POST { action: "request" }` — generates 32-byte cryptographic token via `crypto.getRandomValues()`, stores in `account_deletion_tokens` table, returns token with 15-minute expiry
  - Step 2: `POST { action: "confirm", confirmation_token: "..." }` — validates token against stored value, checks expiry, cleans up token, then proceeds with full account deletion
- Created migration `20260328000001_create_account_deletion_tokens.sql`:
  - `account_deletion_tokens` table with `user_id`, `token`, `expires_at`, `created_at`
  - RLS enabled with no public policies (service-role only access)
  - Indexes on `(user_id, token)` for fast lookups and `expires_at` for periodic cleanup

### Build Verification
- `npx vite build` — ✅ passes
- All 30/30 security stories now fully implemented (0 deferred)
---


## 2026-05-31 — PRD ROTATED to iOS Layout & Monetization initiative (branch claude/ios-layout-monetization-plan-5IQFu)

Previous prd.json (feat/admin-followups, 6/6 complete) is preserved in git history.
prd.json now holds the iOS Layout & Monetization PRD: 41 stories — 10 baseline
capabilities marked passes:true (already shipped in app v1.1.30 / build 57) and 31
new stories marked passes:false for Ralph to work in array order.

User direction captured in the planning session:
- Build BOTH revenue engines in parallel (ads/featured + subscriptions).
- Full web feature parity (Trip Planner, Weekend, Best-Of, Articles, Stay, Music/Sports/
  Outdoors/Neighborhood hubs, Dashboard, Deals).
- "Best layout possible" for all the information the desktop app carries.

Already shipped (recorded passes:true so the queue reflects reality):
- IOS-BASE-NAV-001        6-tab native shell (MainTabView, iPad split)
- IOS-BASE-SUB-001/002/003 StoreKit 2 + cross-platform entitlement sync + receipt/ASSN-v2 backend
- IOS-BASE-ADS-001        Affiliate hotel banner (free-tier, FTC-labeled)
- IOS-BASE-GATE-001       PremiumGate + insider/dining tips gating
- IOS-BASE-DISCOVERY-001  Ask Pulse AI chat + Swipe Discover + Surprise Me
- IOS-BASE-COMPLY-001/002 Account deletion + privacy manifest (ATT-correct)
- IOS-BASE-PLATFORM-001   Biometric/session/cert-pinning/offline/Spotlight/App Intents/App Clip

New work, grouped (passes:false, execution order = array order):
- EPIC A Layout/IA:  IOS-IA-001 Home content-first redesign; IOS-IA-002 Discover-hub IA for parity;
  IOS-IA-003 unified card+skeleton system; IOS-IA-004 sticky search + persistent filter chips;
  IOS-IA-005 iPad adaptivity; IOS-IA-006 personalized section ordering.
- EPIC B Subscriptions: IOS-SUB-010 contextual paywall; 011 placement/enforcement strategy;
  012 annual SKUs + intro trial; 013 onboarding trial + soft paywall; 014 win-back/offer codes.
- EPIC C Ads/Sponsored (mostly outside Apple's 30%): IOS-ADS-010 first-party campaign serving;
  011 sponsored/featured listings; 012 native in-feed + capped interstitial; 013 house-ad fill;
  014 ad analytics parity; 015 sponsored AI-flow results; 016 advertiser self-serve linkout.
- EPIC D Parity screens: IOS-PARITY-001 Trip Planner (also a premium hook); 002 Articles/Guides;
  003 Stay/hotels (affiliate $); 004 Weekend; 005 Best-Of voting; 006 Music/Sports/Outdoors/
  Neighborhood hubs; 007 Dashboard; 008 saved searches+alerts (premium+push); 009 reviews;
  010 Deals (recurring).
- EPIC E Compliance/polish: IOS-COMPLY-003 accessibility audit; 004 offline/error states;
  005 App Store metadata/screenshots/review notes.

Key constraints captured for implementers:
- Keep ad serving FIRST-PARTY (no IDFA / no 3rd-party ad SDK) so ATT stays unnecessary
  (IOS-BASE-COMPLY-002). A 3rd-party ad SDK would make ATT + privacy-manifest updates a hard
  prerequisite.
- Consumer subscriptions MUST use StoreKit IAP. The advertiser "Promote this listing" flow
  (IOS-ADS-016) links out to the web Stripe campaign checkout because it is advertising for a
  real-world business, not an in-app digital good for the purchasing user — document this for
  App Review.
- Trip Planner is advertised in subscription copy today but has ZERO native implementation —
  IOS-PARITY-001 closes both a parity gap and a broken promise; gate it via IOS-SUB-011 quota.
- New screens must reuse OfflineBanner/NetworkMonitor/QueryCache and the unified card system.

iOS build/verify convention for every story: cd ios && xcodegen generate && xcodebuild
-scheme DesMoinesInsider build, plus keep DesMoinesInsiderTests / UITests green.
---

## 2026-05-31 — IOS-IA-001 (content-first Home + data-driven rails) [awaiting CI build]

Redesigned the Home tab so the first screenful is content, not a stack of
promotional CTA cards.

Files added:
- ios/DesMoinesInsider/Views/Home/HomeSections.swift (631 lines) — all Home
  feed building blocks: WaysToExploreRow, the data-driven HomeRail enum +
  HomeRailsView, HomeRailHeader, HomeEventRail / HomeRestaurantRail /
  HomeAttractionRail, HomeRailSkeleton + HomeRailEmpty (non-collapsing
  loading/empty states), and the card structs moved out of HomeView
  (CompactRestaurantCard, CompactAttractionCard [new], FeaturedEventCard,
  EventCardSkeleton).

Files modified:
- ios/DesMoinesInsider/Views/Home/HomeView.swift (820 -> 398 lines) — removed
  the four full-width CTA cards (askPulseEntryBar, surpriseMeButton,
  discoverHeroCard, exploreAttractionsCard) and the inline featured/restaurant
  sections + duplicated card structs. New order: header -> RightNowRibbon ->
  WaysToExploreRow (one compact row) -> smart presets -> inline filters ->
  HomeRailsView (data-driven) -> events list. Added attractionsVM + a
  dedicated weekendVM (date-scoped to .thisWeekend). Rails are hidden while
  the user is actively filtering so the feed collapses to filtered results.
  "See all" routes via handleSeeAll().
- ios/DesMoinesInsider/Models/Attraction.swift — added
  compactCardAccessibilityLabel mirroring Restaurant's, used by the new
  CompactAttractionCard.

Design decisions:
- Rail order is data-driven ([HomeRail] passed to HomeRailsView, default
  .allCases) precisely so IOS-IA-006 can personalize the sequence without
  another layout refactor. fromBlog is intentionally not in allCases until
  the Articles hub (IOS-PARITY-002) exists.
- Ad slots are interleaved at deterministic rail indices (after rail 1 and 3)
  rather than a single hard-coded AdSlot, so IOS-ADS-012 can swap them for
  native in-feed ad cards without moving call sites. AdSlot still renders
  nothing for subscribers.
- Each rail owns its own loading (HomeRailSkeleton, mirrors card shape so no
  layout jump) and empty (HomeRailEmpty, stays visible) state — no
  all-or-nothing spinner across the feed.
- XcodeGen picks up the new file automatically (project.yml sources glob is
  the whole DesMoinesInsider dir), so no project.yml change needed.

Verification status:
- No Swift/Xcode toolchain in the Linux dev container, so the acceptance
  "xcodebuild build" gate runs in the ios-native-build CI workflow on macOS,
  NOT here. Verified statically instead: balanced delimiters, no duplicate
  type definitions (each moved struct defined exactly once), all referenced
  APIs confirmed to exist with matching signatures (glassCard, CategoryBadge,
  ToastMessage, Skeleton.block/bar, EventInlineFilters/@Bindable,
  RightNowRibbon closure, FavoritesService not needed here). Leaving
  passes:false until CI confirms the compile; will flip to true on green.

**Learnings for future iterations:**
- When extracting `private struct`s out of a 800+ line SwiftUI View into a
  shared file, drop the `private` (they become internal) and grep the whole
  target for same-named types first — SwiftUI codebases often have parallel
  card types per feature that would collide.
- The Home tab already had strong native-only discovery surfaces (Ask Pulse,
  Swipe, Surprise Me); the problem was never missing features, it was four
  full-width CTAs stacked above the content. Consolidating them into one
  horizontal "Ways to explore" row is the single highest-leverage layout fix.
- EventsViewModel is cheap to instantiate twice (main feed + weekend rail);
  setting .selectedDatePreset triggers its own debounced fetch via didSet, so
  the weekend rail needs no bespoke fetch path.
---

## 2026-05-31 — IOS-IA-002 (Discover hub + IA for full web parity) [awaiting CI build]

Information architecture for reaching every web surface without overflowing the
6-item iPhone tab bar.

IA decision (source of truth lives in DiscoverHubView.swift header):
- Primary tabs stay lean and UNCHANGED (Home, Dining, Search, Map, Saved,
  Profile). Did NOT drop Search or Map for a Discover tab — both are high-use
  and Apple HIG caps comfortable iPhone tabs at ~5.
- Discover is a hub screen reached from Home (new "Discover" tile in
  WaysToExploreRow) on iPhone, and a first-class sidebar item on iPad.
- Every parity feature is a tile in the hub: Trip Planner, Articles & Guides,
  Where to Stay, This Weekend, Best Of, Music, Sports, Outdoors, Neighborhoods,
  Deals. Each routes to ComingSoonView (with a web fallback) until its
  IOS-PARITY-* story swaps in the native screen — nothing is a dead end.

Files added:
- ios/DesMoinesInsider/Views/Discover/DiscoverHubView.swift (224 lines) —
  DiscoverHubView (ownsNavigationStack flag so it can borrow Home's stack when
  pushed, or own one as an iPad sheet) + DiscoverDestination enum (title,
  subtitle, icon, gradient, slug, init?(slug:), webURL, destinationView) +
  DiscoverTile grid cell.
- ios/DesMoinesInsider/Views/Components/ComingSoonView.swift (48 lines) —
  reusable placeholder with an "Open on the web" sheet (WebViewPage) so parity
  content is reachable today before native screens land.

Files modified:
- Views/Home/HomeView.swift — added HomeDestination.discoverHub case +
  navigationDestination that pushes DiscoverHubView(ownsNavigationStack: false).
- Views/Home/HomeSections.swift — WaysToExploreRow now has a "Discover" tile
  (-> hub) and an "Attractions" tile (-> attractions); previously the single
  "Explore" tile only reached attractions.
- Views/MainTabView.swift — iPad sidebar gets a dedicated Discover section that
  presents DiscoverHubView as a sheet.
- Services/DeepLinkHandler.swift — added Destination.discover(DiscoverDestination)
  and parsing for both universal links (desmoinesinsider.com/<slug>, e.g.
  /trip-planner, /deals — handled in both the single-segment root branch and the
  default branch) and custom scheme (com.desmoines.aipulse://discover/<slug>).
  Stable routes per parity surface via DiscoverDestination.slug.

Verification status:
- Same as IOS-IA-001: no Swift toolchain locally; xcodebuild gate runs in macOS
  CI. Static checks pass — balanced delimiters across all 7 touched files,
  exhaustive HomeDestination switch, no duplicate symbols, WebViewPage label
  order matches existing SettingsView usage. passes:false until CI is green.

**Learnings / flags for future iterations:**
- PRE-EXISTING DRIFT (not introduced here, do not blame this PR): 
  DesMoinesInsiderTests/DeepLinkHandlerTests.swift calls `handler.handle(url:)`
  (labeled) with scheme `dsminsider://`, but DeepLinkHandler implements
  `handle(_ url:)` (unlabeled) and matches scheme `com.desmoines.aipulse`
  (Config.appBundleId). Those tests cannot compile/pass against the current
  implementation — the test file predates a refactor of the handler. I did NOT
  add new tests in that broken pattern and did NOT change the handler signature
  (additive changes only). Worth a dedicated chore: reconcile the test's
  expected scheme/label with the implementation (or vice-versa). Also note:
  DeepLinkHandler is defined + (intended to be) tested but is NOT yet consumed
  in DesMoinesInsiderApp.onOpenURL (only Supabase's client.handle(url) is wired
  there), so the .discover route is parseable but app-level navigation to it is
  separate pre-existing debt.
- ComingSoonView + DiscoverDestination.destinationView is the seam every
  IOS-PARITY-* story plugs into: replace the one `destinationView` case with the
  real screen, delete nothing else. Keeps those stories small and isolated.
---

## 2026-06-02 — IOS-IA-003 (unified content-card + skeleton system) [awaiting CI build]

Collapsed the app's parallel, visually-divergent card implementations into ONE
card layer so events, restaurants, attractions (and, soon, articles/hotels/
deals) look and behave consistently everywhere they appear.

File added:
- ios/DesMoinesInsider/Views/Components/ContentCard.swift (~620 lines) — the
  whole unified system:
  • ContentCardData — a normalized value type (title, image, placeholder
    icon/tint, optional event category overlay, meta primary/secondary lines,
    optional date badge, [CardPill], urgency, isFeatured/isPremium/isSponsored,
    favorite). Models map to it via `Event.cardData` / `Restaurant.cardData` /
    `Attraction.cardData` extensions (the seam every future content type plugs
    into — add a `cardData` and it renders in all four variants for free).
  • ContentCard view with four variants: .featured (260-wide hero, scrim +
    category overlay), .compact (180-wide rail card), .standard (full-width
    Events list card w/ date badge), .listRow (horizontal Dining/Attractions/
    Search row).
  • CardFavorite enum — `.managed(kind,id,title)` self-toggles via
    FavoritesService (event/restaurant/attraction) + drives a ToastMessage;
    `.external(isFavorited,name,onToggle)` defers to a parent that owns the
    state (used by the Attractions tab, which keeps its own toast).
  • CardFavoriteButton — one favorite affordance (HeartBurst micro-interaction,
    overlay vs inline style) shared by every variant.
  • SponsoredBadge (FTC-labeled "Sponsored" pill, the seam for IOS-ADS-011) +
    VipGoldBadge premium badge, both overlaid top-leading on the image.
  • CardDateBadge (ScaledMetric day/month badge).
  • ContentCardSkeleton(variant:) — a matching shimmer placeholder for EACH of
    the four variants, so every rail/list shows a skeleton of the right shape
    while loading (no layout shift, no all-or-nothing spinner).

Files modified (all now thin wrappers that build a `cardData` and delegate —
public init signatures unchanged so NO call sites changed except where noted):
- Views/Home/EventCardView.swift (207 -> 24 lines) — ContentCard(.standard).
- Views/Restaurants/RestaurantCardView.swift (138 -> 27) — ContentCard(.listRow).
- Views/Home/HomeSections.swift — FeaturedEventCard -> ContentCard(.featured,
  decorative), CompactRestaurantCard / CompactAttractionCard ->
  ContentCard(.compact, decorative), EventCardSkeleton ->
  ContentCardSkeleton(.standard). ~250 lines of duplicated card/skeleton bodies
  removed.
- Views/Search/SearchView.swift — AttractionCardView -> ContentCard(.listRow)
  (search attraction results now also expose the favorite affordance + open
  status, matching event/restaurant search rows).
- Views/Attractions/AttractionsView.swift — BrowseAttractionCardView ->
  ContentCard(.listRow) with `.external` favorite (keeps the screen's existing
  favoritesService + favoriteToast plumbing intact, signature unchanged);
  AttractionCardSkeleton -> ContentCardSkeleton(.listRow).
- Views/Restaurants/RestaurantsView.swift — RestaurantCardSkeleton ->
  ContentCardSkeleton(.listRow).

Design decisions:
- Decorative vs standalone: Home rail cards are wrapped in a NavigationLink that
  owns the tap + VoiceOver label, so they pass `decorative: true` — the card is
  hidden from VoiceOver AND omits the inline favorite button (avoids a button
  nested in a link label on a non-interactive rail). List/Search cards are
  standalone: `.accessibilityElement(children: .contain)` + combined label so
  the favorite button stays a separately-focusable element (preserves the prior
  EventCardView/RestaurantCardView a11y behavior exactly).
- Pills always carry an SF Symbol (FREE/$$, ★ rating, Open/Closed) so colour is
  never the only differentiator (Differentiate Without Color Alone). Variants
  cap how many pills show (.featured/.compact show the first; .standard/.listRow
  show up to 3) so small cards never overflow.
- Dynamic Type: titles use lineLimit(2) and reflow rather than truncate-to-one;
  the date badge uses @ScaledMetric. CachedAsyncImage keeps fixed aspect frames
  so there's no layout shift when images resolve.
- Rating consolidated to a single "★ 4.8" pill across all surfaces (the old
  per-screen 5-star strip in RestaurantCardView/BrowseAttractionCardView is gone
  in favor of the consistent compact pill) — intentional consistency tradeoff.

Verification status:
- No Swift toolchain locally; xcodebuild gate runs in macOS CI. Static checks
  pass — balanced delimiters across all 7 touched files; all referenced APIs
  verified against source (CachedAsyncImage(url:placeholder:), glassCard,
  HeartBurstView, CategoryBadge(category:size:), VipGoldBadge, PremiumTokens
  .imageScrim/.elevation4, Skeleton.bar/.block, ToastMessage.success/info/error,
  FavoritesService.toggleFavorite/toggleRestaurantFavorite/toggleFavoriteAttraction
  + the is*Favorited readers, Restaurant.displayLocation/ratingText,
  Attraction.compactCardAccessibilityLabel). No test files reference the card
  types. project.yml uses directory-based sources so ContentCard.swift is picked
  up automatically. passes:false until CI is green.

**Learnings / flags for future iterations:**
- ContentCardData + the `cardData` model extensions are the seam for the
  remaining parity content types: when IOS-PARITY-002 (Articles), -003 (Hotels)
  and -010 (Deals) land, give each model a `cardData` and it renders in all four
  variants with skeletons + badges for free — no new card code.
- The sponsored badge (SponsoredBadge) + `ContentCardData.isSponsored` are wired
  but not yet set by any data path; IOS-ADS-011 (sponsored/featured listings)
  flips `isSponsored = true` on the relevant cards' cardData and the badge +
  FTC labeling appear automatically.
- `isPremium`/VipGoldBadge is likewise ready; whichever story introduces
  VIP-exclusive content cards just sets `isPremium = true`.
- Rating display changed from a 5-star strip to a single ★-number pill. If
  product wants the strip back, add a dedicated variant/flag rather than
  re-forking per screen.

## 2026-06-02 — IOS-IA-004 (sticky search + persistent inline filter chips) [awaiting CI build]

Gave the three list screens a sticky search bar pinned under the nav title and
a persistent (no-longer-scrolls-away) filter bar, so users can reach search and
review/clear active filters without scrolling back to the top.

Key finding before coding: all three ViewModels (Events, Restaurants,
Attractions) ALREADY had `searchText` wired into their fetch pipelines +
`activeFilterCount` + `clearFilters` — but the search field was missing entirely
on Events/Restaurants and present-but-scrolls-away on Attractions. And the inline
filter pills + active chips already existed (one source of truth per screen) but
lived inside the scrolling VStack, so they scrolled out of view. This story was
therefore mostly a UI/placement change, not new filter plumbing.

File added:
- ios/DesMoinesInsider/Views/Components/StickyFilterBar.swift (~25 lines) — a
  reusable container (system `.bar` material + hairline `Divider`) that the
  three screens drop into `.safeAreaInset(edge: .top)` so their filter pills /
  chips pin under the nav title consistently.

Files modified:
- Views/Restaurants/RestaurantsView.swift — added
  `.searchable(..., placement: .navigationBarDrawer(displayMode: .always))`
  (native sticky search under the nav title) and a `.safeAreaInset(edge: .top)`
  StickyFilterBar holding RestaurantInlineFilters (pills) + the existing
  `activeChips` (count + removable chips + Clear all). Removed the in-flow copies
  of the pills + chips from the scrolling VStack; presets + AdSlot stay in flow.
- Views/Attractions/AttractionsView.swift — same treatment: `.searchable`
  replaces the old in-content custom `searchField` (deleted), and typeChips +
  featuredAndRatingRow + activeChips moved into the sticky `.safeAreaInset` bar.
- Views/Home/HomeView.swift — added `.searchable` (Home/Events had NO search
  before). Home is a content-first feed (IOS-IA-001), so its discovery controls
  (RightNowRibbon, WaysToExplore, smart presets, EventInlineFilters pills) stay
  in-flow; what pins is the native search PLUS — only while filtering
  (activeFilterCount > 0) — the removable active-filter chips bar via
  `.safeAreaInset`. Removed the in-flow `activeFiltersBar` call (now pinned).

Design decisions:
- Differentiated stickiness by screen type, documented inline: the pure list
  tabs (Dining/Explore) pin the FULL filter bar (pills + chips) because they
  have no hero content to preserve; Home pins only search + active chips so the
  IOS-IA-001 content-first feed (ribbon → ways-to-explore → presets → rails)
  isn't pushed below a filter bar. All three are consistent on the two things
  the AC cares most about: sticky search reachable without scrolling, and active
  filters that persist with a result count + Clear all.
- Used native `.searchable` rather than a bespoke pinned TextField: it gives the
  correct under-the-large-title placement, the system "always" sticky behavior,
  Cancel button, and dictation for free, and binds straight to the VM's existing
  `searchText` (same `$viewModel.searchText` binding the old Attractions
  TextField used, so no VM changes were needed).
- Used `.safeAreaInset(edge: .top)` (truly always-visible, content insets below
  it) instead of a pinned LazyVStack section header (which only pins AFTER you
  scroll to it) — lower risk, less structural churn to the existing ScrollViews,
  and a better "persistent" result.
- AC items already satisfied and left intact: smart presets + inline filters
  share one filter-state source of truth (the screen's @Observable VM); chips
  edit each facet (remove) while the inline pills/popovers edit facets directly
  without reopening a sheet. State survives tab switches because the iPhone
  TabView keeps each tab's view (and its @State VM) alive.

Verification status:
- No Swift toolchain locally; xcodebuild gate runs in macOS CI. Static checks
  pass — balanced delimiters across all 4 touched Swift files + the new
  component; every referenced symbol resolves (RestaurantInlineFilters /
  EventInlineFilters / typeChips / featuredAndRatingRow / activeChips /
  activeFiltersBar all still defined and now consumed from the sticky bar);
  iOS 17.0 deployment target supports `.searchable(placement:)` + `.safeAreaInset`
  + `Material.bar`. project.yml directory-based sources auto-include
  StickyFilterBar.swift. passes:false until CI is green.

**Learnings / flags for future iterations:**
- iPad caveat (pre-existing, out of scope): MainTabView.iPadLayout's
  `tabContent(for:)` is a `switch` in the split-view detail, so selecting a
  sidebar item RE-CREATES the screen and its @State VM — filter/search state
  would NOT persist across iPad sidebar switches the way it does in the iPhone
  TabView. IOS-IA-005 (iPad adaptive layouts) should make those VMs owned higher
  up (or use scene storage) if cross-navigation persistence is required there.
- EventInlineFilters self-pads `.horizontal, 16`; RestaurantInlineFilters only
  pads `.horizontal, 2` and historically relied on the parent VStack's
  `.padding(.horizontal)`. Inside the sticky bar I re-added `.padding(.horizontal,
  14)` around the Restaurant/Attraction rows; if these inline-filter components
  are ever reused elsewhere, normalize their internal horizontal padding.
- A "search" chip is intentionally NOT added to the active-chips rails — the
  native search field already shows the query and Clear all clears it. If a
  visible search chip is wanted, each screen's activeChips/activeFiltersBar is
  the single place to add it.

## 2026-06-02 — IOS-SUB-010 (contextual native paywall + per-feature upsell) [awaiting CI build]

Replaced the generic "push the full pricing screen" upsell with a reusable
contextual paywall that appears at the moment a user hits a locked feature,
with copy tailored to that feature, a preselected tier, a monthly/annual toggle,
the required auto-renew disclosure, restore + Terms/Privacy, and per-surface
conversion analytics.

File added:
- ios/DesMoinesInsider/Views/Subscription/PaywallView.swift (~443 lines):
  • PaywallContext — the reusable context value (id used as the analytics key,
    icon, headline, subheadline, benefit bullets, recommendedTier).
  • Eight tailored presets covering every surface the AC names: .unlimitedFavorites,
    .tripPlanner, .insiderTips, .diningTips, .savedSearches, .customAlerts,
    .advancedFilters, .adFree — plus .generic(tier:feature:) fallback that builds
    bullets from SubscriptionTier.features.
  • PaywallView — StoreKit-2-backed custom paywall (NOT Apple's
    SubscriptionStoreView, because we need to preselect the context's tier and
    drive the period toggle). Header + benefit checklist + Insider/VIP tier
    selector (recommended tier preselected, "Current" shown for a tier the user
    already holds, can't "buy down"), a Monthly/Annual segmented toggle that only
    appears when >1 period is available, a pinned purchase footer with the live
    StoreKit displayPrice, the legally-required auto-renew disclosure, and a
    Restore / Terms / Privacy row. Purchases go through the existing
    StoreKitService.purchase(); Terms/Privacy are Links to Config.siteURL.
  • Analytics: present / dismiss / purchase_start / purchase_complete / restore
    are all logged via AnalyticsService with the context id (a `didConvert` flag
    stops onDisappear from logging a phantom "dismiss" after a conversion).

Files modified:
- Services/StoreKitService.swift — additive billing-period API:
  SubscriptionPeriod enum (.monthly/.annual), product(for:period:),
  availablePeriods(for:), and a public static rank(_:). availablePeriods is
  derived from the loaded products, so it reports [.monthly] today and the
  Annual toggle simply appears once IOS-SUB-012 adds the annual SKUs — no
  paywall change needed.
- Services/AnalyticsService.swift — additive trackPaywallPresented/Dismissed/
  PurchaseStart/PurchaseComplete/Restore(context:…) methods.
- Views/Components/PremiumGate.swift — added optional `context: PaywallContext?`
  and now presents PaywallView (context ?? .generic(tier:feature:)) instead of
  SubscriptionView. Every existing PremiumGate becomes a contextual paywall.
- Routed the gated feature-hit entry points to the tailored paywall:
  FilterSheet (.advancedFilters via PremiumGate), AdBannerView (.adFree),
  EventDetailView insider-tips (.insiderTips), RestaurantDetailView dining-tips
  (.diningTips), SubscriptionBanner's FavoritesLimitBanner (.unlimitedFavorites).

Design decisions:
- Gated upsell vs management: feature-hit upsells now use PaywallView; the
  MANAGEMENT/overview entry points deliberately keep the full SubscriptionView
  (Settings "Manage subscription", and the Profile SubscriptionBanner CTA) —
  those want the current-plan badge + cross-platform "you also subscribe on the
  web" banner, which a focused paywall shouldn't duplicate.
- Custom StoreKit UI over SubscriptionStoreView: SubscriptionStoreView can't
  preselect a specific product/tier or inject per-feature headlines, which are
  the whole point of a contextual paywall. The custom UI carries the required
  3.1.2 disclosures itself (auto-renew terms, live price, Terms/Privacy, Restore).
- PremiumGate signature stayed source-compatible: `context` is an optional with
  a default, added before the trailing `content` closure, so all existing
  `PremiumGate(requiredTier:feature:){…}` call sites compile unchanged.

Verification status:
- No Swift toolchain locally; xcodebuild gate runs in macOS CI. Static checks
  pass — balanced delimiters across all 9 touched files; every referenced symbol
  verified (StoreKitService.purchase/restorePurchases/currentTier, the new
  product(for:period:)/availablePeriods/rank, Product.displayPrice/.subscription
  .subscriptionPeriod.unit, SubscriptionTier.features/displayName, Config.siteURL,
  AnalyticsService.trackEvent). async purchase()/restore() marked @MainActor since
  they touch the @MainActor StoreKitService + @State. project.yml directory-based
  sources auto-include PaywallView.swift. passes:false until CI is green.

**Learnings / flags for future iterations:**
- IOS-SUB-012 seam: add the annual products to App Store Connect + the product
  IDs to StoreKitService.{insider,vip}ProductIDs, and the Monthly/Annual toggle
  + annual pricing light up automatically (availablePeriods is product-derived).
- IOS-SUB-011 seam: PaywallContext is the "context" the story's planned single
  PremiumFeature enum should map onto (feature -> required tier -> PaywallContext).
  The favorites cap, saved-searches/alerts, advanced-filter, and trip-planner
  quota gates SUB-011 enforces should each present the matching preset here.
- AnalyticsService is still a local-logging facade (Firebase not yet wired) — the
  paywall events are logged via AppLogger today; when Firebase lands they flow to
  real analytics with zero call-site changes.
- The auto-renew disclosure string lives in PaywallView.autoRenewDisclosure; if
  legal wants different wording, that's the single source.
