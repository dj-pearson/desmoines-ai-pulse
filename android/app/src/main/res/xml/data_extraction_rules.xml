<?xml version="1.0" encoding="utf-8"?>
<!--
    Data extraction rules for Android 12+ (API 31+).
    Excludes sensitive data from cloud backup and device-to-device transfer.
    See: https://developer.android.com/about/versions/12/backup-restore
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude encrypted auth tokens and biometric preferences -->
        <exclude domain="sharedpref" />
        <!-- Exclude Room database (cached events/restaurants contain user-specific data) -->
        <exclude domain="database" />
        <!-- Exclude no_backup directory -->
        <exclude domain="no_backup" />
        <!-- Exclude internal cache -->
        <exclude domain="cache" />
    </cloud-backup>
    <device-transfer>
        <!-- Exclude encrypted auth tokens and biometric preferences -->
        <exclude domain="sharedpref" />
        <!-- Exclude Room database -->
        <exclude domain="database" />
        <exclude domain="no_backup" />
        <exclude domain="cache" />
    </device-transfer>
</data-extraction-rules>
