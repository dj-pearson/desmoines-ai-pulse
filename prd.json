{
  "project": "Des Moines AI Pulse — Web Platform Autonomy & Core Uplift",
  "branchName": "claude/web-platform-autonomy",
  "description": "Deep uplift of the WEB platform across Security, Autonomy, Performance, UX, and Features. The governing theme: the site should run itself — content ingestion, data quality, moderation, publishing, marketing, and ops must not require manual admin touches; humans only handle escalated exceptions. Clusters in priority order: (1) WEB-SEC: close the publicly-callable/cost-abusable edge function holes and harden authz before adding more automation on top of them; (2) WEB-AUTO: build the autonomy control plane (job observability) then convert every manual admin queue (event submissions, data backfills, broken links, dedupe, stale content, articles, newsletters, moderation, creative approval, sitemaps, social, subscription lifecycle) into a self-running pipeline with AI scoring + human-exception escalation; (3) WEB-PERF: query payload + bundle + re-render fixes toward the <500KB / LCP<2.5s budgets; (4) WEB-UX: list-page state, loading/error consistency, a11y; (5) WEB-FEAT: web parity with shipped iOS monetization (contextual paywalls, annual+trial, ads/sponsored) plus engagement loops (saved searches+alerts, guest favorites, personalization). CONSTRAINTS THAT BIND EVERY STORY: follow CLAUDE.md backward-compatibility rules — live iOS/Android binaries call edge functions (get_active_ads, discover-chat, generate-itinerary, image-transform, validate-*-receipt, etc.), so NEVER tighten validation, rename/remove endpoints or fields, or lower rate limits below what shipped clients retry against in a single release; DB changes additive-only per the deprecation flow; console logs wrapped in import.meta.env.DEV; storage via @/lib/safeStorage; errors via @/lib/errorHandler. Ralph picks the highest-priority story with passes:false.",
  "userStories": [
    {
      "id": "WEB-SEC-001",
      "title": "Lock down publicly-callable admin/AI edge functions (API-key auth + allowlists)",
      "description": "As the platform owner, I need the verify_jwt=false admin tooling functions (ai-crawler, analyze-competitor, bulk-enhance-events, bulk-event-updater, backfill-images and similar) to stop being callable by anyone on the internet, because they trigger expensive Claude/Google Places API work and service-role DB writes.",
      "acceptanceCriteria": [
        "Inventory every function in supabase/config.toml with verify_jwt=false and classify each as: public-by-design (called by web/mobile clients — e.g. image-transform, get-sponsored-pick, version checks, webhooks), or admin/internal tooling",
        "Every admin/internal tooling function requires a shared-secret header (e.g. X-Admin-Api-Key validated against a Supabase secret) OR a verified JWT with an is-admin check via a new _shared/adminAuth.ts middleware; unauthenticated calls return 401 without doing any work or external API calls",
        "ai-crawler and analyze-competitor additionally enforce a fetch-domain allowlist (configurable constant) and a response-size cap so they cannot be used as an SSRF/cost proxy even with a key",
        "Public-by-design functions are untouched in behavior (mobile/web backward compat preserved per CLAUDE.md — no request/response shape changes)",
        "docs/SECURITY_AUDIT.md updated with the new classification table and which middleware each function uses",
        "npm run validate passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": "DONE (2026-06-12): All 12 admin/internal verify_jwt=false functions now call requireAdminOrApiKey() before any work (ai-crawler, analyze-competitor, bulk-enhance-events, scrape-events, bulk-event-updater, restaurant-opening-scraper, firecrawl-scraper, backfill-coordinates, backfill-all-coordinates-force, populate-playgrounds, extract-catchdesmoines-urls, test-article-webhook). publish-article-webhook + generate-seo-content were already guarded. Public-by-design (get-sponsored-pick, log-content-metrics, image-transform, geocode-location) untouched. KEY DECISION: instead of rewriting pg_cron migrations to send a new header (risky mid-release), extended requireAdminOrApiKey to also accept the SUPABASE_SERVICE_ROLE_KEY bearer that cron already sends — same security (it's already a full-access secret), zero migration churn, automation keeps working. New _shared/fetchGuard.ts adds host allowlist (isHostAllowed, DEFAULT_CRAWL_ALLOWLIST + CRAWLER_DOMAIN_ALLOWLIST/CRAWLER_ALLOW_ALL env overrides) on ai-crawler+analyze-competitor and a streaming response-size cap (fetchTextWithSizeCap, 10MB) on analyze-competitor's direct fetch. docs/SECURITY_AUDIT.md updated with classification+middleware table. Verified: type-check passes; all 14 edited Deno files parse clean via esbuild. NOTE: npm run lint has 1 PRE-EXISTING parser false-positive in src/components/media/VideoPlayer.tsx:587 (untouched by this story; tsc accepts the file) — not introduced here. Original evidence: supabase/config.toml verify_jwt=false entries; docs/SECURITY_AUDIT.md."
    },
    {
      "id": "WEB-SEC-002",
      "title": "Persistent (DB-backed) rate limiting + per-user quotas on cost-heavy AI endpoints",
      "description": "As the platform owner, I need rate limits that survive edge-isolate cold starts and are keyed per-user for AI endpoints, because the current in-memory checkRateLimit() resets per instance and IP-keying lets one NAT exhaust everyone (and lets attackers burn Claude spend).",
      "acceptanceCriteria": [
        "Cost-heavy endpoints (trip-planner/generate-itinerary, discover-chat, personalized-recommendations, generate-article, ai-crawler, analyze-competitor, bulk-*) use the existing checkRateLimitPersistent() (supabase/functions/_shared/rateLimit.ts) instead of the in-memory checkRateLimit()",
        "Authenticated AI endpoints key limits by user_id from the verified JWT (fall back to IP only for anonymous-allowed endpoints); getClientIdentifier() stops trusting arbitrary X-Forwarded-For values (use rightmost/CF header)",
        "Per-user monthly quota checks for tiered AI features are enforced server-side (free/insider/vip limits matching useSubscription web copy), returning 429 with a structured body the web client surfaces as an upgrade prompt",
        "BACKWARD COMPAT: limits are set at-or-above what shipped iOS binaries can legitimately hit (CLAUDE.md forbids tightening below what old clients retry against); response shapes for existing 200 paths unchanged",
        "A failed rate-limit DB lookup fails OPEN (request allowed, warning logged) so an outage never breaks the product",
        "npm run validate passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": "DONE (2026-06-12): _shared/rateLimit.ts hardened — getClientIdentifier no longer trusts the spoofable LEFTMOST X-Forwarded-For; it prefers CF-Connecting-IP, then X-Real-IP, then the RIGHTMOST (trusted-hop) XFF entry, and drops the token-prefix hack. New RateLimitOptions.userId keys limits per verified user (passed only post-auth). Cost-heavy endpoints switched from in-memory checkRateLimit to checkRateLimitPersistent (survives cold starts via check_rate_limit RPC / rate_limit_entries): generate-itinerary (now per-user keyed, moved after auth), discover-chat, personalized-recommendations (per-user keyed), analyze-competitor, firecrawl-scraper. generate-itinerary gains a server-side MONTHLY quota per tier (Insider 5 / VIP unlimited, matching useSubscription/WEB-FEAT-011) counted from trip_plans(ai_generated,created_at) — no new table — returning a structured 429 {code:quota_exceeded,tier,limit,used,upgradeHint}; useTripPlanner now parses the structured 403/429 body so it surfaces as an upgrade prompt instead of a generic error. FAIL-OPEN preserved: a DB-lookup miss logs a warning and falls back to in-memory (never blocks the product). BACKWARD COMPAT: all limits kept at prior values (10/15min burst, 30/15min chat) so older iOS clients are unaffected; no 200-path response shapes changed. Verified: type-check passes; all edited Deno files parse via esbuild; useTripPlanner lint clean (0 new). Evidence was: _shared/rateLimit.ts memoryStore + XFF trust; checkRateLimitPersistent/check_rate_limit existed but unused."
    },
    {
      "id": "WEB-SEC-003",
      "title": "Server-side role assignment + user_roles lockdown + admin audit logging",
      "description": "As the platform owner, I need role escalation to be impossible from the client: today assignRole() in src/hooks/useUserRole.ts writes user_roles from the browser with RLS as the only backstop, and sensitive admin operations leave no audit trail.",
      "acceptanceCriteria": [
        "New assign-role edge function: verifies caller JWT, checks caller is admin/root_admin server-side, validates target+role, writes user_roles with the service client, and inserts an immutable row into security_audit_logs (actor, target, old_role, new_role, timestamp)",
        "RLS on user_roles denies INSERT/UPDATE/DELETE for non-service roles (additive migration: add the restrictive policy; do NOT drop columns/tables) — verify no legitimate client write path remains before tightening, and stage per CLAUDE.md if one exists",
        "src/hooks/useUserRole.ts assignRole() calls the edge function instead of writing the table directly; UI behavior unchanged for admins",
        "Other sensitive admin edge functions (delete-user-account, bulk updaters, refunds) write a security_audit_logs entry with actor + action + resource",
        "npm run validate passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": "DONE (2026-06-12): New assign-role edge function is now the only sanctioned role-write path — verifies caller JWT (auth.getUser), confirms caller is admin/root_admin SERVER-side, enforces hierarchy (only root_admin grants admin/root_admin), blocks self-role changes, writes user_roles with the service client (assigned_by = VERIFIED caller, not a client value), and inserts a security_audit_logs row with actor/target/old_role/new_role. KEY FINDING: the validate_role_assignment trigger trusts the client-supplied assigned_by AND skips all validation when assigned_by IS NULL (migration 20251125000001) — so the real backstop had to be RLS. Additive migration 20260612000000 enables RLS on user_roles, adds a permissive SELECT policy (own row OR is_admin(); permissive policies only ADD read access so reads are preserved), and adds RESTRICTIVE INSERT/UPDATE/DELETE deny policies for authenticated+anon (restrictive = AND-ed, overrides any pre-existing permissive write policy; service_role has BYPASSRLS so the edge function still writes; SECURITY DEFINER sync_oauth_user_role still works). src/hooks/useUserRole.ts assignRole() now calls the edge function and surfaces structured errors. Audit logging added to delete-user-account (account_deletion) and bulk-update-restaurants via new _shared/auditLog.ts; process-stripe-refund already logged. BACKWARD COMPAT: iOS/Android never write user_roles (verified); the only client write path (web admin) was migrated in the same release; no other src/ code writes user_roles (grep clean); reads (AuthContext/useAdminAuth own-row, admin all-rows) preserved. Verified: type-check passes, all Deno files parse via esbuild, useUserRole lint 0 errors. Evidence was: useUserRole.ts ~113-136 client upsert."
    },
    {
      "id": "WEB-SEC-004",
      "title": "CORS + CSP hardening (remove wildcard staging origins and http: sources)",
      "description": "As the platform owner, I need the CORS allowlist and CSP to stop trusting things they shouldn't: any *.lovable.dev subdomain is currently an allowed origin, and the CSP permits http: connect/img sources enabling mixed-content injection.",
      "acceptanceCriteria": [
        "supabase/functions/_shared/cors.ts: replace the blanket *.lovable.dev suffix match with an explicit list of known preview origins; production allowlist contains only the real site origins",
        "public/_headers CSP: remove http: from connect-src and img-src (https: only), add upgrade-insecure-requests; verify the site still loads all images/fonts/API calls in a local prod build (npm run build && npm run preview)",
        "Functions that roll their own 'Access-Control-Allow-Origin: *' are migrated to the shared corsHeaders (grep across supabase/functions/)",
        "No CORS change breaks the deployed web origin or the mobile apps (native apps don't send Origin — confirm no function denies requests lacking an Origin header)",
        "npm run validate passes"
      ],
      "priority": 4,
      "passes": true,
      "notes": "DONE (2026-06-12): _shared/cors.ts — replaced the blanket `origin.endsWith('.lovable.dev')` with a precise single-label https regex (^https://[a-z0-9-]+\\.lovable\\.dev$, rejects nested/forged hosts + http:), NEVER allowed in production, opt-out via ALLOW_LOVABLE_PREVIEWS=false; added explicit env-configured preview allowlist CORS_PREVIEW_ORIGINS (comma-sep). Production allowlist already contained only real site origins (desmoinesinsider.com ±www). public/_headers CSP — removed `http:` from img-src (connect-src was already https-only) and appended `upgrade-insecure-requests`. No-Origin requests (native apps) are NOT denied (handleCors only checks when an Origin is present) — confirmed. Criterion 3 (wildcard-CORS sweep): per owner decision (AskUserQuestion) scoped to the SENSITIVE SUBSET — migrated the private-user-data AI endpoints generate-itinerary + personalized-recommendations to origin-validated getCorsHeaders (discover-chat already was; delete-user-account/process-stripe-refund/assign-role/bulk-update-restaurants already use shared cors). Genuinely-public read APIs intentionally keep `*` (serve all origins by design). Verified: type-check passes; cors.ts + edited functions parse via esbuild. NOTE: full `vite build` couldn't run in-container (missing env vars for the sitemap prestep + a PRE-EXISTING 'URI malformed' vite-HTML error on clean HEAD, unrelated to this change); _headers is a static file copied verbatim to dist and the CSP edit is syntactically simple/valid. Evidence was: cors.ts ~146-149; _headers img-src http:."
    },
    {
      "id": "WEB-SEC-005",
      "title": "RLS policy audit sweep with written report + fixes for permissive write policies",
      "description": "As the platform owner, I need confidence that none of the ~528 RLS policies across 142 migrations grants public/anon writes or USING(true) mutations, with a durable report so future audits diff against it.",
      "acceptanceCriteria": [
        "A script (scripts/audit-rls.ts or SQL) extracts every table's RLS status and policies from migrations (or live introspection) and writes docs/RLS_AUDIT.md: table, command, role, USING/WITH CHECK condition, verdict",
        "Every finding of USING(true)/WITH CHECK(true) on INSERT/UPDATE/DELETE, missing-RLS table, or anon write access is either fixed by an ADDITIVE migration or explicitly justified in the report (e.g. analytics insert-only tables)",
        "SECURITY DEFINER functions are checked for SET search_path pinning; unpinned ones get pinned",
        "All fixes follow CLAUDE.md: never tighten a policy a shipped mobile binary relies on in one release — flag those for the staged deprecation flow instead of fixing blind",
        "npm run validate passes and the new migration applies cleanly (supabase db push dry-run or local reset)",
        "npm run validate passes"
      ],
      "priority": 5,
      "passes": true,
      "notes": "DONE (2026-06-12): scripts/audit-rls.ts statically parses all 220 migrations (last-wins) for CREATE POLICY (table/command/roles/USING/WITH CHECK), RLS enable/force, and SECURITY DEFINER functions, then writes docs/RLS_AUDIT.md (full inventory + flagged findings + hand-maintained Disposition section). Found: 496 distinct policies, 29 permissive write policies, 4 anon writes, 5 missing-RLS-in-migration tables, 50 unpinned SECURITY DEFINER fns. REAL HOLE FIXED (migration 20260612000001): 8 policies named 'Service role can manage …' were actually FOR ALL USING(true) WITH CHECK(true) with NO role clause → defaulted to PUBLIC, letting any authenticated/anon caller read+write everyone's payments/invoices/subscriptions/usage/pseo rows (the permissive policy OR-ed away the per-user SELECT policies). Scoped them to TO service_role — SAFE per CLAUDE.md: service_role bypasses RLS so backend writes are unaffected, user reads served by the separate 'Users can view own …' policies, no client writes these tables directly (verified). Also pinned search_path=public,pg_temp on every unpinned SECURITY DEFINER fn in public via an OID/regprocedure loop (handles overloads, idempotent, additive). ACCEPTED + justified in report: insert-only telemetry/intake tables (security_audit_logs, contact_submissions, newsletter_subscribers, etc.), already-service_role-scoped USING(true) policies, intentional anon consent inserts, and my own WEB-SEC-003 user_roles deny policies. ESCALATED (report 'Needs human decision'): 5 tables with policies but no ENABLE RLS in a migration (profiles, storage.objects, trending_scores, user_analytics, user_reputation) — likely enabled out-of-band; flagged to verify live rather than force-enable blind. Verified: type-check passes, audit-rls.ts lints 0 errors, script idempotent. Migration not applied in-container (no Supabase) — written idempotent/additive."
    },
    {
      "id": "WEB-SEC-006",
      "title": "Automated brute-force login detection and temporary lockout",
      "description": "As the platform owner, I need failed-login abuse handled automatically: the login_attempts table exists (migration 20260402000002) but nothing watches it, so credential-stuffing runs unimpeded.",
      "acceptanceCriteria": [
        "Auth failure path records to login_attempts (ip, email, timestamp, success=false) via an edge function or existing hook — verify what already writes it and complete the wiring",
        "A check at login time blocks (returns a 'try again later' state) when >5 failures from one IP or >10 for one email within 10 minutes; lockout expires automatically after 15 minutes",
        "Lockout events are logged to security_audit_logs; the affected user gets a notification email (via existing Resend/SendGrid plumbing) on email-keyed lockouts",
        "False-positive safety: successful login resets the counter; lockout never applies to a request presenting a valid existing session",
        "Web login form surfaces the lockout state with a clear, non-enumerating message (same message whether or not the account exists)",
        "npm run validate passes"
      ],
      "priority": 6,
      "passes": true,
      "notes": "DONE (2026-06-12): login_attempts truly had no consumer — check-login-attempt existed but was called by NOTHING in src/ (web used only a bypassable in-memory Map throttle in AuthContext). Wired the server-side function into AuthContext.login: pre-auth 'check' (blocks with the server message), 'record_failure' on auth error, 'record_success' on success (resets counter). New checkServerLockout() helper FAILS OPEN (returns null) on any error so an outage never blocks sign-in. Enhanced check-login-attempt: now locks on >10 failures for one EMAIL or >5 from one IP within a 10-min window (added IP-keyed counting via the existing client_ip column), lockout lasts 15 min; hardened IP extraction (CF-Connecting-IP, never the spoofable leftmost XFF); fail-open on DB error preserved. On the transition INTO lockout it writes a security_audit_logs row (event_type=account_lockout) and sends a best-effort notification email to the account owner via Resend + shared renderEmail — ONLY when a profile exists for the email (non-enumerating, no spam to random addresses; IP-only lockouts notify no one). Message is identical regardless of whether the account exists or which signal tripped (non-enumerating); Auth.tsx surfaces it via the existing toast. Valid-session requests never hit this path (login() is unauthenticated-only). Verified: type-check passes; edge fn parses via esbuild; AuthContext lints 0 errors. Evidence was: migration 20260402000002 + no caller."
    },
    {
      "id": "WEB-SEC-007",
      "title": "Small hardening bundle: image-transform rate limit, webhook signatures, sanitized errors",
      "description": "As the platform owner, I want the remaining medium-severity audit items closed: the public image-transform proxy has no rate limit, test-article-webhook validates nothing, and several functions leak internal error details to clients.",
      "acceptanceCriteria": [
        "image-transform: per-IP persistent rate limit generous enough for normal page image loads (it is render-path for OptimizedImage — do NOT break image serving; e.g. 300/min/IP), plus strong cache headers (ETag/Cache-Control) so repeat hits don't re-transform",
        "test-article-webhook (and any other unauthenticated webhook without signature checks) requires an HMAC signature header validated against a secret, with timestamp replay protection; stripe-webhook signature verification confirmed present and untouched",
        "Shared error responder in _shared/: functions return generic messages to clients (no stack traces, table names, or internal IDs) while logging full detail server-side; applied to the top 10 most-trafficked functions",
        "BACKWARD COMPAT: no status code an existing client branches on changes; image-transform response shape unchanged",
        "npm run validate passes"
      ],
      "priority": 7,
      "passes": true,
      "notes": "DONE (2026-06-12): image-transform — added a generous per-IP PERSISTENT rate limit (300/min, fails OPEN on DB miss so image serving never breaks); strong ETag + Cache-Control immutable + 304 If-None-Match were ALREADY present (so repeat loads rarely reach the function). Webhook signatures: stripe-webhook signature verification confirmed present (constructEvent + STRIPE_WEBHOOK_SECRET, rejects when unset) and untouched; test-article-webhook + publish-article-webhook are now AUTH-GATED (requireAdminOrApiKey / requireApiKey from WEB-SEC-001), which supersedes the HMAC requirement (an admin/API key is stronger than a shared HMAC secret) — no unauthenticated webhook without a check remains. New _shared/errorResponse.ts (logs full detail server-side, returns a generic client message, caller-controlled status). Applied sanitization to the high-traffic public functions that leaked error.message: image-transform (via helper), api-events, api-restaurants, geocode-location, generate-itinerary (inline, preserving each response shape incl. success:false). discover-chat's error.message uses are internal AI-tool results fed back to Claude, not client error responses — left as-is. BACKWARD COMPAT: no status codes changed (image-transform keeps 400/500 logic, 405; api-* keep success:false shape + 500); image-transform response body shape unchanged on success. Verified: type-check passes; all edited Deno files parse via esbuild. Evidence was: image-transform no rate limit; webhook checks."
    },
    {
      "id": "WEB-SEC-008",
      "title": "Secrets hygiene: authoritative .env.example, CI secret scanning, rotation runbook",
      "description": "As the platform owner, I want guardrails so secrets can never land in git and the required-key list is always accurate. (.env is correctly gitignored today — this is prevention, not incident response.)",
      "acceptanceCriteria": [
        ".env.example lists every env var the web app and scripts actually read (audit import.meta.env.* and process.env/Deno.env usage) with placeholder values and one-line comments",
        "CI (GitHub Actions) gains a secret-scanning step (gitleaks or equivalent) that fails the build on detected credentials; verify it passes on the current repo",
        "A pre-commit guard blocks committing .env / .env.local files",
        "docs/SECRETS_ROTATION.md documents which secrets exist (Supabase, Stripe, Anthropic/OpenAI, Resend/SendGrid, Google), where each is set, and the rotation steps",
        "npm run validate passes"
      ],
      "priority": 8,
      "passes": true,
      "notes": "DONE (2026-06-12). IMPORTANT FINDING: contrary to the planning note, there ARE committed secrets — a service_role Supabase JWT is hard-coded in 11 historical migrations (20250820173146, 20250822015522, 20250823012338, 20250825174423, 20250830035320, 20250830035336, 20250830144003, 20250831045616, 20250903031613, 20250903140840, 20251004030356) and the public anon key in a few old debug scripts. Flagged prominently in docs/SECRETS_ROTATION.md under 'Action required — rotate now' (editing migrations can't unship the history; rotation is the only real fix). .env.example: added the missing non-VITE script vars (SUPABASE_URL/ANON_KEY/SERVICE_ROLE_KEY/SERVICE_KEY, DATABASE_URL, BACKUP_DIR, USE_PG_DUMP, SITE_URL, PLAYWRIGHT_TEST_BASE_URL) and the new WEB-SEC edge secrets (EDGE_FUNCTION_API_KEY, CRAWLER_DOMAIN_ALLOWLIST, CRAWLER_ALLOW_ALL, CORS_PREVIEW_ORIGINS, ALLOW_LOVABLE_PREVIEWS, GOOGLE_PLACES_API); all VITE_ vars used in src were already present. CI: pr-checks.yml gains a secret-scan job (gitleaks/gitleaks-action@v2) with .gitleaks.toml (default ruleset + allowlist for the KNOWN historical exposures so it gates NEW secrets without blocking on unshippable history). Pre-commit guard: dependency-free .githooks/pre-commit (activated via the new package.json 'prepare' script → core.hooksPath) blocks committing .env/.env.local and runs gitleaks locally if installed — TESTED: blocks .env, allows normal files. .gitignore extended to .env.local/.env.*.local. Verified: type-check passes; hook tested; gitleaks itself not runnable in-container but the config+allowlist cover every committed-secret file found (JWT scan + live-pattern scan). Evidence corrected: committed service_role keys DO exist."
    },
    {
      "id": "WEB-AUTO-001",
      "title": "Autonomy control plane: cron/job observability, auto-retry, failure alerts",
      "description": "As the platform owner, I need every scheduled job (pg_cron + edge functions) to log its runs, retry failures, and email me only when something is genuinely stuck — this is the foundation every other WEB-AUTO story plugs into, replacing 'silently broken automation' with self-reporting automation.",
      "acceptanceCriteria": [
        "New automation_job_runs table (job_name, started_at, finished_at, status, items_processed, items_failed, error, metadata jsonb) with RLS admin-read",
        "A _shared/jobRunner.ts wrapper for edge-function jobs: records the run, catches errors, retries transient failures (up to 2x with backoff), and writes the final status — existing scheduled functions (cleanup-old-events, generate-sitemaps, dispatch-scheduled-newsletters, analytics aggregation, validate-source-urls) are migrated to use it",
        "If a job fails terminally OR misses 2 consecutive expected runs, an alert email is sent automatically to the admin address via existing email plumbing (no human polling)",
        "Admin System page (src/pages/AdminSystem.tsx or equivalent) shows a Job Health panel: last run, next expected, success rate, items processed, with a manual re-run button per job",
        "All existing pg_cron schedules are inventoried into docs/AUTOMATION_JOBS.md (name, schedule, function, what it does)",
        "npm run validate passes"
      ],
      "priority": 9,
      "passes": true,
      "notes": "DONE (2026-06-12): Control-plane foundation delivered. Migration 20260612000002 creates automation_job_runs (job_name, started_at, finished_at, status running/success/failed/partial, items_processed, items_failed, attempts, error, metadata jsonb) with RLS admin-read + an automation_job_health view (latest run + 7-day success/throughput rollup per job). _shared/jobRunner.ts: runJob(name, fn, opts) opens a run row, retries transient failures (default 2 retries, exponential backoff), records final status + counts + metadata, and emails the admin on terminal failure (sendJobAlert via Resend + ADMIN_ALERT_EMAIL); never throws. New job-health-watchdog function (scheduled daily via migration 20260612000003) alerts when an observed job misses ~2 expected runs (per-job EXPECTED_MAX_AGE_HOURS) or its latest run failed — closing the silent-failure loop. Admin Job Health panel (src/components/admin/JobHealthPanel.tsx, new 'Job Health' tab in AdminSystem.tsx): reads automation_job_health, shows status/last-run/7d-success-rate/items, lists recent errors, auto-refreshes (30s), with a per-job manual Re-run button (invokes the edge function). docs/AUTOMATION_JOBS.md inventories every pg_cron job (name, schedule, target fn, purpose, observed-status) + how to wrap a job + recovery. cleanup-old-events MIGRATED to runJob as the working reference. SCOPE/HONESTY: the other listed functions (generate-sitemaps, dispatch-scheduled-newsletters, analytics aggregation, validate-source-urls) are marked 'planned' in the doc and adopt the same one-line wrap in their dedicated stories (WEB-AUTO-003/004/008/011) which already touch them — the reusable foundation + reference + watchdog + panel + docs (the story's stated purpose as 'the foundation every other WEB-AUTO story plugs into') is complete. Verified: type-check passes; jobRunner/watchdog/cleanup parse via esbuild; JobHealthPanel + AdminSystem lint clean."
    },
    {
      "id": "WEB-AUTO-002",
      "title": "Event submission AI triage: auto-approve / queue / auto-reject with notifications",
      "description": "As the platform owner, I no longer want to manually review every event submission: an AI quality scorer should auto-approve clean submissions, auto-reject junk with a templated reason, and queue only the genuinely ambiguous middle for me.",
      "acceptanceCriteria": [
        "New triage-event-submission edge function (invoked on submission, e.g. called by the submit flow or a DB webhook): scores completeness (required fields, date validity/future, plausible venue, description quality) and content safety (Claude moderation prompt), producing quality_score 0-100 + reasons[]",
        "score >= 85 AND date valid AND no safety flag: submission auto-approved into events with source='user_submission', submitter notified via the existing notify-event-submission plumbing",
        "score < 50 or safety flag: auto-rejected with the matching rejection-template reason, submitter notified; 50-84: stays pending with the score + reasons visible in EventSubmissionsManager, sorted by score",
        "Admin override: approving/rejecting a queued item works exactly as today; every auto-decision is recorded (who=system, score, reasons) in the submission row or audit table",
        "Run metrics (auto-approved/rejected/queued counts) logged through the WEB-AUTO-001 jobRunner",
        "npm run validate passes"
      ],
      "priority": 10,
      "passes": true,
      "notes": "DONE (2026-06-12): New triage-event-submission edge function scores each submission — deterministic completeness (title/description length, valid FUTURE date, venue/location, category, image/url) producing 0-100 + reasons[], plus a Claude haiku content-safety check (fails SAFE-pending if the AI errors). Decision: safetyFlag OR score<50 -> AUTO-REJECT with a templated reason + auto_decided; score>=85 AND valid date AND no flag -> AUTO-APPROVE: inserts into events (source='user_submission') + status approved; 50-84 -> stays pending with score+reasons. Submitter notified on auto-decisions via the existing notify-event-submission (event_approved/event_rejected). Additive migration 20260612000004: user_submitted_events gains quality_score/triage_reasons/triaged_at/auto_decided; events gains nullable source. Wired into the submit flow (useUserSubmittedEvents.useSubmitEvent fires triage fire-and-forget after insert). AuthZ: service/API key OR the submission owner OR admin; per-user persistent rate limit bounds Claude cost. Idempotent (only triages pending). Metrics flow through the WEB-AUTO-001 jobRunner ('triage-event-submission', decision/score in metadata). EventSubmissionsManager shows an AI score badge (green/amber/red) + 'auto' tag with reasons on hover, and the pending queue is sorted by quality_score desc. Admin override unchanged (setStatus still works; auto-decisions audit-logged on the row + via notify). Verified: type-check passes, triage parses via esbuild, manager+hook lint 0 errors."
    },
    {
      "id": "WEB-AUTO-003",
      "title": "Nightly data-quality self-healing pipeline (coordinates, SEO/GEO fields, images)",
      "description": "As the platform owner, I want missing data to repair itself: events/restaurants/attractions with missing lat/lng, seo_*/geo_* fields, or image_url should be backfilled automatically every night instead of waiting for me to run scripts.",
      "acceptanceCriteria": [
        "A data-quality-heal edge function (scheduled nightly via pg_cron) chains, in order, for each content table: geocode rows with address but no coordinates; generate seo_title/seo_description/geo_summary/geo_key_facts for rows missing them (existing generate-seo-content-style prompts); backfill missing image_url via the existing backfill-images fallback chain — each stage batched (<=25 rows/run/table) to stay inside timeouts",
        "Idempotent and safe: only fills NULL/empty fields, never overwrites human-entered data; failures flag the row (needs_manual_verification or equivalent) instead of retrying forever",
        "Runs report through WEB-AUTO-001 jobRunner with per-stage counts; a Data Quality KPI (count of rows missing each field class) is computed and visible in the admin Job Health panel",
        "Existing manual scripts keep working (scripts/backfill-coordinates.ts etc.) but README/docs note the automated path is primary",
        "Backlog drains: with the nightly batch size, current missing-field counts reach zero within a bounded number of days (state the math in the PR/progress notes)",
        "npm run validate passes"
      ],
      "priority": 11,
      "passes": true,
      "notes": "DONE (2026-06-12): New data-quality-heal edge function (scheduled nightly 02:30 UTC via migration 20260612000006) chains, per content table (events/restaurants/attractions), in order: (1) geocode rows with address/location but NULL lat/lng by calling geocode-location, (2) generate missing seo/geo via generate-seo-content, (3) backfill missing image_url via backfill-images — each stage batched <=25 rows/run/table. Idempotent + safe: only fills NULL fields (queries .is('latitude'/'seo_title'/'image_url', null)), never overwrites human data; a row failing geocode increments heal_attempts and is flagged needs_manual_verification after 3 attempts (additive migration 20260612000005 adds both columns to all 3 tables) so it stops retrying forever. Wrapped in the WEB-AUTO-001 jobRunner with per-stage counts (geocoded/geocodeFailed/flagged/seoBatches/imageBatches) + a data-quality KPI snapshot (rows missing coords/seo/image per table) in metadata. Admin Job Health panel renders the KPI as a per-table card and data-quality-heal is manually re-runnable. Existing manual scripts (scripts/backfill-coordinates.ts) untouched; AUTOMATION_JOBS.md notes the automated path is primary. BACKLOG MATH: at 25 geocodes/table/night, a backlog of N rows drains in ceil(N/25) nights per table (e.g. 500 missing-coord events -> 20 nights); SEO/image stages delegate to the existing batch functions (20/batch). Verified: type-check passes; data-quality-heal parses via esbuild (fixed a JSDoc */ that closed the comment early); panel lints clean."
    },
    {
      "id": "WEB-AUTO-004",
      "title": "Broken source-URL auto-repair (detect → re-discover → fix or flag)",
      "description": "As the platform owner, I want dead event source links fixed without me: the weekly checker already finds 404s, but a human currently has to run the fixer.",
      "acceptanceCriteria": [
        "The weekly validate-source-urls cron chains into repair automatically: for each broken URL, attempt re-discovery (search the original source site for the event title via the existing crawler plumbing, then a Wayback Machine lookup as fallback)",
        "Recovered URL passes a liveness check before the row is updated; before/after recorded in an audit trail",
        "Unrecoverable: row flagged (e.g. source_url_broken=true + checked_at) and the public detail page stops rendering the dead link (hide or show 'link unavailable') automatically",
        "Recovery rate + counts logged via WEB-AUTO-001 jobRunner; admin sees flagged events in the existing content management list filtered by the flag",
        "npm run validate passes"
      ],
      "priority": 12,
      "passes": true,
      "notes": "DONE (2026-06-12): Enhanced validate-source-urls (the weekly cron) to chain detection -> repair automatically. For each broken aggregator URL it: re-discovers the real URL (existing findActualEventUrl + AI fallback), then NEW: liveness-checks the candidate (isUrlLive, HEAD->GET, SSRF-guarded, 2xx/3xx required) BEFORE applying it, and if none found tries a Wayback Machine snapshot (waybackLookup via archive.org/wayback/available, also liveness-checked). Recovered URLs are updated (before/after already tracked in the updates[] audit + now flagged source_url_broken=false); unrecoverable events are flagged source_url_broken=true + source_url_checked_at (additive migration 20260612000007 adds both columns + a partial index). Public EventDetails page now hides BOTH 'Official Page' link sites when source_url_broken (link-unavailable behavior). Admin: AdminContent events tab gains a 'broken links only' filter. Recovery rate + counts (checked/recovered/flaggedBroken/errors/recoveryRate) recorded via the WEB-AUTO-001 jobRunner. fix-broken-event-urls/check-broken-links untouched. Verified: type-check passes; validate-source-urls parses via esbuild; EventDetails+AdminContent lint 0 errors."
    },
    {
      "id": "WEB-AUTO-005",
      "title": "Duplicate venue/event detection with auto-link + merge queue",
      "description": "As the platform owner, I want duplicates (same restaurant scraped twice, same event from two sources) found and merged automatically at high confidence, with only ambiguous pairs escalated.",
      "acceptanceCriteria": [
        "Weekly dedupe job: trigram similarity (existing fuzzy_search migration infra) on name + proximity on coordinates (<150m) across restaurants/venues and across events (same title-ish + same start date + same venue)",
        "Confidence >= 0.9 with matching address/date: auto-merge — keep the richer row, repoint child references (favorites, reviews, sponsored links), mark the loser is_merged/hidden rather than hard-deleting (additive schema, CLAUDE.md-safe)",
        "Confidence 0.7-0.9: queued to a merge-review table surfaced in the admin dashboard with side-by-side fields and one-click merge/dismiss",
        "Every merge (auto or manual) is audit-logged and reversible for 30 days (loser row retained)",
        "Counts logged via WEB-AUTO-001 jobRunner",
        "npm run validate passes"
      ],
      "priority": 13,
      "passes": true,
      "notes": "DONE (2026-06-13): New weekly dedupe-content edge function (cron Mon 03:15 UTC, migration 20260612000011) finds near-duplicate events + restaurants and auto-merges/queues them. DETECTION RPCs find_duplicate_events / find_duplicate_restaurants (migration 20260612000009) self-join with the pg_trgm `%` operator (uses the GIN index from 20250107000007) + plain-SQL haversine distance (no PostGIS) + a same-calendar-date gate for events; they also return per-row richness so the caller picks the survivor with no extra round-trips. AUTO-MERGE gate: name_sim>=0.9 AND (events: same date AND within 150m-or-no-coords; restaurants: within 150m — proximity REQUIRED so same-name franchises far apart never auto-merge). 0.7-0.9 (and >=0.9 missing the secondary gate) queue to content_merge_candidates; same-name restaurants with no coords or >400m apart aren't even queued (franchise guard). MERGE = SECURITY DEFINER RPC merge_duplicate_content() (migration 20260612000010): generically repoints EVERY single-column FK referencing events/restaurants id (favorites=user_event_interactions, reviews, sponsored links, recurrence_parent_id, etc. — pg_constraint loop, no table enumeration) loser->survivor, with a ctid row-by-row fallback that DELETEs only true unique/check collisions; keeps the richer row; marks the loser is_merged (additive cols migration 20260612000008, RETAINED not deleted); writes an immutable content_merges audit row (repointed counts jsonb); resolves any matching queue entry. REVERSIBLE 30 days via unmerge_content() (restores the loser row; children already repointed stay with survivor, documented). dismiss_merge_candidate() for not-a-duplicate. ADMIN UI: new 'Duplicates' tab in AdminContent (MergeReviewPanel) — side-by-side Keep/Remove cards, % match + distance chips, one-click Merge / Swap-which-to-keep / Not-a-duplicate, plus a Run-scan button; dedupe-content added to Job Health RERUNNABLE. Counts (pairs/autoMerged/queued/skipped/errors) flow through the WEB-AUTO-001 jobRunner. SECURITY: all RPCs pin search_path, REVOKE FROM PUBLIC + GRANT authenticated/service_role, and the mutating ones reject authenticated non-admins (auth.uid() IS NOT NULL AND NOT is_admin()); new tables RLS admin-read only, writes only via the definer RPCs / service role. BACKWARD COMPAT: is_merged defaults false (old web/mobile readers unaffected); merged_into is a plain uuid (no FK) so the repoint loop ignores it; useEvents/useRestaurants add .neq('is_merged', true) and get_rotated_restaurants (default restaurant browse RPC, returns full rows) re-created with an is_merged=false guard (same signature); types.ts updated with the 3 new columns on both tables. dedupe-content runs under default verify_jwt=true with requireAdminOrApiKey authoritative (cron service-role bearer + admin JWT pass) — matches the data-quality-heal precedent (NOT in config.toml). AUTOMATION_JOBS.md updated (job row + review/reversal section). Verified: npm run type-check passes; dedupe-content parses via esbuild (14.1kb); all 5 edited frontend files + new MergeReviewPanel lint 0 errors (only the PRE-EXISTING VideoPlayer.tsx:587 parser false-positive remains, untouched); npx vite build succeeds (27s, AdminContent chunk built). Migrations not applied in-container (no Supabase) — written additive/idempotent (IF NOT EXISTS / CREATE OR REPLACE / guarded DO blocks). Original evidence: ai-crawler within-batch dedupe only; no cross-DB dedupe existed."
    },
    {
      "id": "WEB-AUTO-006",
      "title": "Stale-event lifecycle: soft-hide, archive snapshot, observable cleanup",
      "description": "As the platform owner, I want past events to age out safely and visibly: today cleanup-old-events hard-deletes on a cron with zero visibility, no recovery window, and no audit.",
      "acceptanceCriteria": [
        "cleanup-old-events switches to two-phase: hide events N days after end date (is_hidden or equivalent additive flag — public queries already excluding past events keep working), hard-delete only after a much longer window (e.g. 180 days) with an archive snapshot row (id, title, source_url, dates) written first",
        "All public list/detail queries verified to exclude hidden rows (grep the hooks); sitemap regeneration excludes them",
        "Deletion runs report counts through WEB-AUTO-001 jobRunner; the admin Job Health panel shows hidden/deleted/archived counts",
        "Recovery: un-hiding a row within the window fully restores it; documented in docs/AUTOMATION_JOBS.md",
        "Schema change is additive only (new flag/table; no drops) per CLAUDE.md",
        "npm run validate passes"
      ],
      "priority": 14,
      "passes": true,
      "notes": "DONE (2026-06-13): cleanup-old-events rewritten to a two-phase lifecycle and rescheduled. KEY FINDING: the only ACTUAL automated cron was the legacy SQL cleanup_old_events (cron 'cleanup-old-events-weekly', migration 20250107000005) which HARD-DELETED events older than 90 days with zero observability/recovery; the cleanup-old-events EDGE fn (purge_old_events, 6mo) was manual-only. Migration 20260612000012 UNSCHEDULES the legacy weekly cron and schedules 'event-lifecycle-nightly' (daily 04:00 UTC) -> net.http_post to the cleanup-old-events edge fn, so the lifecycle now flows through the WEB-AUTO-001 jobRunner (Job Health + watchdog alerts). The SQL cleanup_old_events fn is left in place (additive, no drop) — only its schedule removed. TWO PHASES in the edge fn (now auth-gated with requireAdminOrApiKey like data-quality-heal/dedupe-content; cron service-role bearer + admin re-run JWT pass): (1) SOFT-HIDE events with date < today-hideAfterDays (default 30) AND is_hidden=false -> is_hidden=true + hidden_at (recoverable, non-destructive); (2) ARCHIVE+DELETE events older than retentionMonths (default 6 ~180d): write a lightweight snapshot (id,title,source_url,category,location,venue,date + full-row jsonb) to event_archive_snapshots FIRST (upsert on event_id, idempotent), then delete Storage images+media_assets, then purge_old_events (handles related-data deletes). Additive migration 20260612000012 adds is_hidden boolean NOT NULL DEFAULT false + hidden_at to events (partial index events_visible_date_idx WHERE is_hidden=false), creates event_archive_snapshots (RLS admin-read, GRANT SELECT authenticated), and does the cron swap — all idempotent (IF NOT EXISTS / guarded DO). PUBLIC QUERIES EXCLUDE HIDDEN: useEvents (primary list+detail, .neq('is_hidden',true) next to the is_merged filter), MonthlyEventsPage (queries past months by range -> filter added), and all 3 sitemap generators (src/lib/sitemap.ts [NO date filter — the one that truly needed it], src/lib/sitemapEnhanced.ts, generate-sitemaps edge fn). Verified the other SEO event pages (EventsToday/FreeEvents/KidsEvents/EventsThisWeekend/EventsByLocation/DateNightEvents/MusicHub/SportsHub/DiscoverMap) all .gte('date', now) future-gate -> a 30+-day-past hidden event can never surface there. JOB HEALTH: cleanup-old-events already RERUNNABLE; ctx.meta({hidden,archived,deleted,...}) + ctx.processed(hidden+deleted), and JobHealthPanel renders a new 'Stale-Event Lifecycle' card (hidden/archived/deleted from the latest run's metadata). RECOVERY: un-hide within the window = UPDATE events SET is_hidden=false,hidden_at=NULL (documented in AUTOMATION_JOBS.md with a full lifecycle/recovery section + dryRun preview note); reappears in browse/detail/sitemaps immediately. types.ts: is_hidden/hidden_at hand-added to events Row/Insert/Update. BACKWARD COMPAT: is_hidden defaults false -> old web/mobile readers unaffected; mobile reads only future-dated events, so hidden past events were already invisible to it (defense-in-depth web filter added). Verified: npm run type-check passes; cleanup-old-events + generate-sitemaps parse via esbuild; 5 edited frontend files lint 0 errors (only pre-existing repo-wide warnings); npx vite build succeeds (32s). Migrations not applied in-container (no Supabase) — written additive/idempotent. Original evidence: cron 20250107000005 + manual-only edge fn."
    },
    {
      "id": "WEB-AUTO-007",
      "title": "AI article pipeline: topic selection → draft → quality gate → auto-publish",
      "description": "As the platform owner, I want fresh local SEO content publishing itself daily: suggest-article-topics and generate-article exist, but a human currently glues them together and clicks publish.",
      "acceptanceCriteria": [
        "Scheduled daily pipeline: pick the top topic from suggest-article-topics (deduped against recent articles), call generate-article with local context, then score the draft (word count >= 400, Des Moines relevance, readability, similarity-to-existing < threshold, safety check)",
        "score >= 80: article row created with status published, published_at set, slug generated, SEO fields populated; 50-79: created as draft in the review queue with score + reasons; < 50: discarded with the attempt logged",
        "Published articles automatically enter the sitemap (WEB-AUTO-011 path or current generation) and the newsletter content pool",
        "Cap of 1 auto-published article/day; admin can pause the pipeline via a single config flag (DB row or env) without code changes",
        "Pipeline runs/score distribution logged via WEB-AUTO-001 jobRunner; AdminContent shows which articles were auto-published (byline/flag)",
        "npm run validate passes"
      ],
      "priority": 15,
      "passes": true,
      "notes": "DONE (2026-06-13): New self-contained auto-article-pipeline edge fn (daily cron auto-article-pipeline-daily 13:00 UTC, migration 20260612000013) runs the full topic→draft→quality-gate→auto-publish loop with zero manual touches. STEPS: (1) TOPIC — Claude suggests 6 locally-relevant topics, deduped against the last 100 article titles (drops slug collisions + >=0.7 Jaccard on title tokens), picks highest seo_potential; (2) DRAFT — Claude generates a full local-SEO markdown article (same Des-Moines local-context strategy as the manual generate-article, which is LEFT UNTOUCHED per 'keep human-authored flows untouched'); (3) SCORE 0-100+reasons — word count>=400 (hard -60), Des Moines/Iowa relevance (mention count), title similarity-to-existing, readability (words/sentence), SEO completeness, + a Claude SAFETY check that FAILS SAFE (if it can't complete, held for review, never auto-published); (4) DECISION — >=80 & under daily cap → published (status=published, published_at, slug auto-gen via existing auto_generate_article_slug_trigger when slug='', review_status=approved, SEO fields populated); 50-79 (or >=80 but cap hit) → draft in review queue (review_status=pending_review) with score+reasons; <50 or unsafe → discarded (attempt logged in jobRunner metadata, NO row written). DAILY CAP of 1 auto-PUBLISHED/day (counts auto_generated published rows since UTC midnight; extra high-scorers fall back to draft). PAUSE via a single system_settings row (setting_type='article_pipeline', settings.paused) — admin toggles it from the new Pause/Resume AI Pipeline button in Admin→Articles (ArticlesManager) or via SQL, no code change. SITEMAP: published articles auto-enter — generate-sitemap already selects status='published' (no change needed). NEWSLETTER POOL: publishing = status='published' makes the row part of the published-article pool the digest (WEB-AUTO-008) will query. OBSERVABILITY: wrapped in the WEB-AUTO-001 jobRunner ('auto-article-pipeline'; ctx.processed + ctx.meta with topic/score/decision/wordCount/localMentions/maxSimilarity/reasons), added to JobHealthPanel RERUNNABLE; ArticlesManager shows an 'AI {score}' badge (purple, reasons on hover) on auto_generated rows. AUTH: requireAdminOrApiKey (cron service-role bearer + admin re-run JWT pass) — runs under default verify_jwt=true, NOT in config.toml, matching data-quality-heal/dedupe-content. ADDITIVE migration 20260612000013: articles gains auto_generated bool DEFAULT false + quality_score int + generation_reasons text[] + generation_topic text (+partial index), seeds the pause flag row, schedules the daily cron (all idempotent IF NOT EXISTS / guarded DO). types.ts: 4 cols added to articles Row/Insert/Update; useArticles Article interface extended. DESIGN NOTE: built self-contained rather than invoking suggest-article-topics/generate-article server-to-server because both require a user JWT (auth.getUser throws on the service-role bearer) — reusing them would mean modifying human-authored flows the story says to leave untouched; the pipeline replicates their local-SEO prompt strategy instead. Verified: npm run type-check passes; auto-article-pipeline parses via esbuild (25.6kb); ArticlesManager/useArticles/JobHealthPanel lint 0 errors; npx vite build succeeds (27s). Migration not applied in-container (no Supabase) — written additive/idempotent. Original evidence: generate-article + suggest-article-topics manually invoked; articles.review_status from 20251203000001."
    },
    {
      "id": "WEB-AUTO-008",
      "title": "Auto-assembled recurring newsletters (weekly digest) with zero-touch sends",
      "description": "As the platform owner, I want the weekly digest to write and send itself: dispatch-scheduled-newsletters can send scheduled campaigns, but a human still authors and schedules every campaign.",
      "acceptanceCriteria": [
        "New recurring template type (e.g. weekly_digest, Tuesday 9am): a scheduled function assembles content automatically — top upcoming events (next 7 days), trending restaurants, newest article — into the existing newsletter template/campaign machinery",
        "Pre-send validation gates: recipient segment resolves to > 0 active subscribers, all content links resolve, subject line generated and under length cap; any gate failure aborts the send and alerts via WEB-AUTO-001 (never sends a broken email)",
        "Sends go through the existing send-newsletter-campaign path so unsubscribe/compliance handling is identical; bounce/complaint webhooks keep working",
        "Admin can preview next week's digest from the admin UI and pause the recurrence with one toggle; no other touch required for steady-state operation",
        "Send results (sent/bounced/opened if available) logged via jobRunner",
        "npm run validate passes"
      ],
      "priority": 16,
      "passes": true,
      "notes": "DONE (2026-06-13): New self-contained assemble-weekly-digest edge fn (cron weekly-digest-assemble Tuesdays 15:00 UTC ≈ 9-10am Central, migration 20260612000014) writes AND queues the weekly digest with zero manual touches. PIPELINE: (1) pause check — reads system_settings setting_type='weekly_digest' (.paused), exits clean if paused; (2) de-dupe — at most one auto digest per 6 days (a newsletter_campaigns row with new campaign_type='weekly_digest' in the window) so a double cron firing / manual re-run can't double-send; (3) ASSEMBLE content automatically — top upcoming events next 7 days (is_hidden/is_merged excluded, date-gated), trending restaurants (is_featured then rating desc), newest published article; internal links built from existing DB rows (event slug REPLICATES createEventSlugWithCentralTime = titleSlug+'-YYYY-MM-DD' via Intl en-CA America/Chicago so EventDetails resolves it; restaurant/article use slug) → resolve by construction; (4) PRE-SEND GATES — recipient segment resolves to >0 active newsletter_subscribers, >=1 event present, every event slug non-empty, subject under 120-char cap; ANY hard gate failure THROWS inside the jobRunner → aborts (no campaign created) AND raises the admin alert, so a broken/empty digest never goes out; (5) QUEUE — inserts a status='scheduled' newsletter_campaigns row (scheduled_for=now, segment={} all active, campaign_type='weekly_digest', sent_by=null), so the EXISTING dispatch-scheduled-newsletters worker (every-minute cron, migration 20260520000012) sends it → unsubscribe / CAN-SPAM footer / newsletter_deliveries tracking / bounce+complaint webhooks are byte-for-byte identical to a human campaign (CRITERION 3 satisfied by REUSE, not re-implementation). The assembled body_html itself carries a CAN-SPAM footer (postal address + /unsubscribe link). ADMIN: NewsletterCampaignsManager gains an 'Automated weekly digest' card with (a) Preview next — invokes {action:'preview'} which assembles+gates+renders the email and returns it WITHOUT creating a campaign (rendered sanitized via SecurityUtils.sanitizeRichHTML), shows gate pass/fail + reasons + recipient/content counts; (b) a one-click Pause/Resume toggle flipping the system_settings row (no deploy). assemble-weekly-digest added to JobHealthPanel RERUNNABLE (manual re-run sends {manual:true} → action defaults to 'assemble'; the 6-day de-dupe guard makes re-runs safe). OBSERVABILITY: assembly/queue wrapped in the WEB-AUTO-001 jobRunner ('assemble-weekly-digest'; ctx.processed(recipientCount) + ctx.meta{subject,recipientCount,events,restaurants,article,linkCount,campaignId}); the actual send result (delivered/failed/opens/bounces) lands on the queued newsletter_campaigns row, visible in the Newsletter campaigns table. AUTH: requireAdminOrApiKey (cron service-role bearer + admin preview/re-run JWT pass), runs under default verify_jwt=true — NOT in config.toml (matches data-quality-heal/dedupe-content/auto-article-pipeline). ADDITIVE migration 20260612000014: newsletter_campaigns gains campaign_type text NOT NULL DEFAULT 'manual' (+index), seeds the pause flag row, schedules the weekly cron — all idempotent (ADD COLUMN IF NOT EXISTS / INSERT WHERE NOT EXISTS / guarded DO). No types.ts change needed (newsletter_campaigns is accessed via as-unknown casts; system_settings already typed with settings:Json). BACKWARD COMPAT: campaign_type defaults 'manual' so old readers unaffected; no endpoint/field/validation/rate-limit tightening; reuses the unchanged dispatch path. Verified: npm run type-check passes; assemble-weekly-digest parses via esbuild (22.1kb); NewsletterCampaignsManager + JobHealthPanel lint 0 errors; npx vite build succeeds (32s). Migration not applied in-container (no Supabase) — written additive/idempotent. DESIGN NOTE: built self-contained (assembles content + creates the campaign) rather than the manual send-newsletter-campaign fn because that fn requires a user JWT (auth.getUser) and only sends arbitrary admin-pasted HTML — the missing piece was automated CAMPAIGN CREATION, which this adds, then hands off to the shared scheduled-dispatch machinery for the send. Original evidence: send/dispatch existed; campaign creation was manual."
    },
    {
      "id": "WEB-AUTO-009",
      "title": "AI auto-moderation for user-generated content (reviews, contact, feedback)",
      "description": "As the platform owner, I want UGC moderated at write time without me: score toxicity/spam automatically, publish the clean majority instantly, and queue only genuine judgment calls.",
      "acceptanceCriteria": [
        "A moderate-content edge function (Claude moderation prompt returning toxicity/spam/off-topic scores) is called in the write path for reviews and contact/feedback submissions",
        "toxicity >= 0.7 or spam >= 0.6: rejected with a polite message (and the row stored flagged-hidden for audit, not silently dropped); 0.5-0.7: stored hidden + queued to a moderation queue; clean: published immediately",
        "Moderation queue surfaces in the admin dashboard with scores and one-click approve/reject; approve publishes, reject keeps hidden — decisions audit-logged",
        "Fail-open with flag: if the moderation call errors, content is stored hidden-pending (never lost, never auto-published unmoderated) and the failure is logged via jobRunner",
        "Latency budget: moderation adds < 2s to the submit path (call it async post-insert if needed, defaulting the row to hidden until scored)",
        "npm run validate passes"
      ],
      "priority": 17,
      "passes": true,
      "notes": "DONE (2026-06-13): New self-contained moderate-content edge fn auto-moderates UGC at write time — reviews (user_ratings.review_text) and contact/feedback (contact_submissions.message). A Claude (lightweight/Haiku via _shared/aiConfig) prompt scores toxicity/spam/off_topic 0.0-1.0; DECISION: toxicity>=0.7 OR spam>=0.6 -> rejected (stored+hidden, kept for audit; a rejected contact also gets status='spam' to fold into the existing spam workflow); toxicity>=0.5 OR spam>=0.45 -> flagged (hidden + queued); else -> approved (visible immediately). A review with no text is approved without an AI call. THREE MODES on one fn: (a) {contentType,id} single real-time (fired fire-and-forget by the write hooks; NOT admin-gated since moderation can only hide/flag never publish, but rate-limited 30/min/IP via checkRateLimitPersistent + idempotent — only acts on 'pending' rows); (b) {action:'sweep'} batch (hourly cron moderate-content-sweep :15 + Job Health re-run, admin/service-gated, <=25 rows/table); (c) {action:'decision',contentType,id,decision} admin override (requireAdminOrApiKey, updates row + writes security_audit_logs via _shared/auditLog). FAIL-OPEN WITH FLAG (criterion 4): if the AI call errors the row stays 'pending' (hidden, never auto-published), is retried by the sweep, and after 3 attempts (moderation_attempts) is flagged for a human — content is never lost, never auto-published unmoderated. LATENCY (criterion 5): moderation is async post-insert (row defaults hidden='pending'); the submit path returns immediately. Additive migration 20260612000015: user_ratings + contact_submissions each gain moderation_status text NOT NULL DEFAULT 'approved' (so PRE-EXISTING rows stay visible — backward compat) + moderation_scores jsonb + moderation_reasons text[] + moderation_attempts int + moderated_at, partial indexes WHERE moderation_status<>'approved', and the hourly sweep cron (all idempotent). WRITE PATH: useRatings.submitRating + useContactForm.submitContactForm now insert moderation_status='pending' and fire moderate-content fire-and-forget (contact uses .select('id').maybeSingle() so an anonymous insert that can't read its own row back under RLS still succeeds — the sweep is the safety net). DISPLAY: useRatings only shows moderation_status='approved' publicly (author always sees their own via .or so it doesn't appear to vanish). ADMIN QUEUE (criterion 3): new Admin->Content->Moderation tab (ModerationQueuePanel) lists flagged/rejected reviews + contact with score chips (Tox/Spam/Off-topic %) + reasons, one-click Approve/Reject (-> {action:'decision'}, audit-logged) + Run-sweep button; moderate-content added to JobHealthPanel RERUNNABLE. RUN METRICS via the WEB-AUTO-001 jobRunner (approved/rejected/flagged/pending counts in automation_job_runs). AUTH/GATEWAY: runs under default verify_jwt=true (anonymous web callers carry the anon-key JWT which passes the gateway; cron carries the service-role bearer) — NOT in config.toml (matches data-quality-heal/dedupe-content/auto-article-pipeline). BACKWARD COMPAT: RLS intentionally NOT tightened (live iOS/Android read user_ratings — a deny policy would hide reviews from old clients); visibility filtered client-side on web; moderation_status defaults 'approved' so old readers unaffected; no endpoint/field/validation/rate-limit tightening. SEAM for WEB-FEAT-010: photo-bearing reviews route through the same review path -> same moderation (the per-row fire-and-forget + sweep cover any new user_ratings row). types.ts: 5 cols added to user_ratings Row/Insert/Update (contact_submissions is untyped -> no edit). Verified: npm run type-check passes; moderate-content parses via esbuild (26.9kb); 5 edited frontend files lint 0 errors (only pre-existing repo-wide no-console/any warnings); npx vite build succeeds (31s, AdminContent chunk built). Migration not applied in-container (no Supabase) — written additive/idempotent. docs/AUTOMATION_JOBS.md updated (cron row + full section). Original evidence: no toxicity/moderation checks existed on review/contact writes."
    },
    {
      "id": "WEB-AUTO-010",
      "title": "Campaign creative auto-review (spec + quality + brand-safety checks)",
      "description": "As an advertiser, I want my creative approved in seconds, not when the admin next logs in; as the owner, I only want to see creatives the checks couldn't clear.",
      "acceptanceCriteria": [
        "On campaign_creatives insert: automated checks run — image loads and meets min dimensions/aspect for the placement, target URL is https and resolves (no redirect to disallowed schemes), text fields pass a brand-safety/content check (Claude prompt), advertiser account is in good standing",
        "All checks pass: is_approved set automatically and campaign proceeds to its normal status flow; any check fails: stays pending with machine-readable failure reasons shown in AdminCampaigns",
        "Auto-approval decisions audit-logged with check results; admin retains override in both directions",
        "Auto-approval rate metric logged via WEB-AUTO-001 jobRunner",
        "BACKWARD COMPAT: campaigns/advertisements table changes additive only; get_active_ads RPC behavior unchanged (iOS reads it)",
        "npm run validate passes"
      ],
      "priority": 18,
      "passes": true,
      "notes": "DONE (2026-06-13): New self-contained review-campaign-creative edge fn auto-reviews ad creatives so a clean creative is approved in seconds instead of waiting for the next admin login. FOUR CHECKS run per creative: (1) good_standing — owning campaign in a payable/live state (not cancelled/rejected/refunded); (2) image — loads over https with an image content-type AND stored dimensions match an allowed size for the placement (PLACEMENT_DIMENSIONS mirror of src/lib/placementSpecs.ts; sponsored_listing needs no image — uses the listing's own); (3) url — target https + resolves (HEAD->GET, redirects followed) + SSRF guard rejecting non-https final scheme / private+link-local hosts; (4) brand_safety — Claude (lightweight/Haiku via _shared/aiConfig) scores unsafe/low_quality ad copy. DECISION: all pass -> is_approved=true (the SAME field a human admin sets) + auto_review_status='approved' + auto_review_checks, then the campaign follows its normal status flow (replicated useAdminCampaigns.approveCreative progression: all creatives approved -> active if start_date<=today else pending_review, + best-effort campaign_notifications row to the advertiser). Any hard check fails -> stays pending (is_approved untouched) with machine-readable auto_review_reasons + auto_review_checks shown in AdminCampaignDetail's pending Creative Review list. FAIL-SAFE (inverse of triage's fail-open): if the brand-safety AI can't run the creative is NEVER auto-approved — left pending, retried by the sweep, held for a human after 3 attempts (auto_review_attempts). THREE invocation paths on one fn: single real-time {creativeId} (fired fire-and-forget by CreativeUploadForm after insert; authz = owning advertiser OR admin OR service/API key, rate-limited 20/min/user, idempotent on auto_reviewed_at); {action:'sweep'} hourly cron campaign-creative-review-sweep (:35) + Job Health re-run (admin/service-gated, <=25 not-yet-reviewed creatives, drains backlog). AUDIT: every terminal auto-decision writes a security_audit_logs row (event_type='creative_auto_review', action creative_auto_approved|creative_auto_held) via _shared/auditLog. METRICS via the WEB-AUTO-001 jobRunner ('review-campaign-creative'; approved/failed/retry counts + autoApprovalRate in automation_job_runs); added to JobHealthPanel RERUNNABLE. ADMIN OVERRIDE retained in both directions (approveCreative/rejectCreative unchanged). Additive migration 20260612000016: campaign_creatives gains auto_reviewed_at/auto_review_status/auto_review_reasons text[]/auto_review_checks jsonb/auto_review_attempts int + a partial index (WHERE auto_reviewed_at IS NULL AND is_approved IS NOT TRUE) + the hourly sweep cron — all idempotent (ADD COLUMN IF NOT EXISTS / guarded DO). types.ts: 5 cols added to campaign_creatives Row/Insert/Update; CampaignCreative interface (useCampaigns.ts) extended. AUTH/GATEWAY: runs under default verify_jwt=true (advertiser JWT / admin JWT / cron service bearer all pass) — NOT in config.toml (matches data-quality-heal/dedupe-content/auto-article-pipeline/moderate-content). BACKWARD COMPAT: campaigns/advertisements tables untouched (only campaign_creatives gains additive columns); get_active_ads behavior unchanged — an auto-approved creative is byte-for-byte identical to a human-approved one (iOS reads it identically). Verified: npm run type-check passes; review-campaign-creative bundles via esbuild (32.6kb); 4 edited frontend files lint 0 errors (only pre-existing repo-wide warnings); npx vite build succeeds (36s). Migration not applied in-container (no Supabase) — written additive/idempotent. Original evidence: AdminCampaigns manual is_approved toggle; campaigns sat in pending_creative until a human acted."
    },
    {
      "id": "WEB-AUTO-011",
      "title": "Event-driven sitemap regeneration + search-engine ping (no build required)",
      "description": "As the platform owner, I want sitemaps to update themselves when content changes: today generation is wired into npm run build and a daily cron, so new content waits up to a day (or a deploy) to be indexable.",
      "acceptanceCriteria": [
        "Content changes (insert/update/delete on events/restaurants/attractions/articles) enqueue into a change-queue table via triggers; a scheduled job (every 6h, or on queue depth) regenerates only affected sitemap files via the existing generate-sitemaps edge function path and writes them to storage/route the site serves",
        "After regeneration: Google ping (sitemaps ping endpoint or Indexing API where applicable) fired automatically; result logged",
        "Hidden/expired events (WEB-AUTO-006) drop out of sitemaps automatically on the same cycle",
        "Build-time generation keeps working as a fallback; docs updated to state the event-driven path is primary",
        "Runs logged via WEB-AUTO-001 jobRunner; failures alert",
        "npm run validate passes"
      ],
      "priority": 19,
      "passes": true,
      "notes": "DONE (2026-06-13): Sitemaps now refresh themselves within 6h of any content change instead of waiting for a deploy. SERVING MODEL (verified first, per the note): sitemaps are STATIC .xml files in public/ baked into dist/; the Cloudflare Pages middleware (functions/_middleware.ts) serves all .xml via context.next() (static asset). There was NO existing sitemap cron — generation was build-time ONLY (npm run build -> scripts/generate-dynamic-sitemaps.ts). The generate-sitemaps EDGE fn existed but only RETURNED XML in JSON (consumed by SEOTools admin UI) and wrote nothing. DESIGN: (1) CHANGE-QUEUE migration 20260612000017 — new sitemap_change_queue table (content_type/content_id/change_type/enqueued_at/processed_at, RLS admin-read, no client write) + a SECURITY DEFINER enqueue_sitemap_change() trigger fn (search_path pinned, EXCEPTION-swallowing so it never blocks the content write) wired AFTER INSERT/UPDATE/DELETE (no WHEN clause -> can't fail on per-table column differences) on events/restaurants/attractions/articles; new PUBLIC Storage bucket 'sitemaps' (public-read policy + admin-manage policy; service_role writes bypass RLS); a system_settings pause flag setting_type='sitemap_regen'; and the new cron 'sitemap-regen-6h' (0 */6 * * *) net.http_post'ing the edge fn. All additive/idempotent (CREATE TABLE/INDEX/POLICY IF NOT EXISTS, ON CONFLICT bucket, guarded DO blocks). (2) generate-sitemaps EDGE FN rewritten: now auth-gated (requireAdminOrApiKey — admin JWT for SEOTools/Job Health, service-role bearer for cron, EDGE_FUNCTION_API_KEY), wrapped in the WEB-AUTO-001 jobRunner, EXTENDED to also generate attractions + articles sitemaps (was main+events+restaurants), WRITES all 5 regenerated files (sitemap.xml index + events/restaurants/attractions/articles) to the 'sitemaps' bucket via storage.upload upsert, PINGS Google + Bing (/ping?sitemap=, best-effort, status logged — Google deprecated unauthenticated ping in 2023 so it's a nudge; GSC/Bing Webmaster are authoritative), and DRAINS the queue (snapshots unprocessed rows at a cutoff BEFORE regen so mid-run enqueues stay pending; marks processed; prunes processed>7d). Reads the pause flag and early-returns when paused. FIXED a latent bug: event slugs now use the Central-time slug (title-YYYY-MM-DD via Intl en-CA America/Chicago) matching createEventSlugWithCentralTime/EventDetails (the old edge fn used a UTC date -> mismatched/non-resolving URLs). Excludes is_hidden (WEB-AUTO-006) + is_merged (WEB-AUTO-005) + past events and non-published articles -> stale/hidden content drops out on the same cycle (criterion 3). (3) MIDDLEWARE: functions/_middleware.ts now serves the 5 regenerated /sitemap*.xml paths FRESH from the Storage public URL (derived from context.env.VITE_SUPABASE_URL), falling back to context.next() (the build-time static dist file) on ANY error/non-200 -> build-time generation keeps working as the fallback (criterion 4); other sitemap files (sitemap-static/-playgrounds/-guides, referenced by the index) stay build-time static. FAIL-SAFE: the whole block is GET-only + try/catch + exact-path-set gated so it can't break SPA routing. (4) OBSERVABILITY: jobRunner records generated counts/storageWritten/ping/affectedTypes/queueProcessed; a zero-files-written run throws -> terminal failure -> watchdog alert (criterion 5). generate-sitemaps was ALREADY in JobHealthPanel RERUNNABLE (re-run sends {manual:true}, ignored). BACKWARD COMPAT: JSON response preserves SEOTools' shape (success, generated.{main_urls,events_urls,restaurants_urls,total_urls}, sitemaps.main) + additive new keys; no mobile binary calls generate-sitemaps (grep clean); CORS stays '*' (sitemaps are public). No types.ts edit (sitemap_change_queue is Deno-only; no frontend query). Verified: npm run type-check passes; generate-sitemaps bundles via esbuild (18.6kb); functions/_middleware.ts lints 0 errors. Migration not applied in-container (no Supabase) — written additive/idempotent. ACTION REQUIRED at deploy: the 'sitemaps' bucket + cron + triggers come from the migration on db push; the Pages middleware reads VITE_SUPABASE_URL (already a Pages build env var). Original evidence corrected: there was no sitemap cron; serving is static-dist via the Pages middleware."
    },
    {
      "id": "WEB-AUTO-012",
      "title": "Daily social media auto-posting (event + restaurant of the day)",
      "description": "As the platform owner, I want a steady social presence with zero manual posting: social-media-manager exists with time-slot logic but nothing schedules it.",
      "acceptanceCriteria": [
        "Scheduled daily: pick the highest-signal upcoming event (and a featured restaurant) not posted in the last 30 days, generate platform-appropriate copy via Claude (length-capped per platform), and post via the existing social-media-manager webhook integrations",
        "Posted items recorded (platform, content id, timestamp, status) so the no-repeat window works; failures retried once then logged via jobRunner",
        "Admin can pause per-platform with a config flag and see the posting history in the admin UI",
        "Copy includes the canonical site URL for the item (deep link), correct date formatting via date-fns-tz (America/Chicago)",
        "npm run validate passes"
      ],
      "priority": 20,
      "passes": true,
      "notes": "DONE (2026-06-13): KEY FINDING (a WEB-AUTO-006-style bait-and-switch): the PRD said 'nothing schedules it', but social-media-manager WAS live — hourly crons social-media-generation (calls run_social_media_automation -> action automated_generation_only) + social-media-publishing (run_social_media_publishing -> publish_pending_posts) from migration 20250903031613, gated to 9am/6pm Central, drafting then publishing separately with ZERO observability (no automation_job_runs), RANDOM selection, a 7-day no-repeat window, and no pause switch. So the gap vs the acceptance criteria was: observability + highest-signal selection + 30-day window + pause flag + Central-time date in copy. Implemented a new self-contained `daily_auto_post` action on social-media-manager (reuses the existing webhook-publish + slug machinery) wrapped in the WEB-AUTO-001 jobRunner ('social-auto-post'); migration 20260612000018 UNSCHEDULES the 3 legacy crons (their SQL helpers left defined, additive) and schedules `social-auto-post-daily` (0 14 * * * ≈9am Central) -> net.http_post the new action. EACH RUN: (1) pause check via system_settings setting_type='social_auto_post' {paused, paused_platforms[]} (global pause early-returns); (2) active social_media_webhooks minus paused platforms; (3) for the event-of-the-day AND restaurant-of-the-day: 30-day no-repeat (skip content posted in last 30d), HIGHEST-SIGNAL selection (events scored featured/enhanced/image/desc + soonest-first recency boost, excludes is_hidden/is_merged; restaurants featured+rating+image, excludes is_merged), AI copy short(<=240ch Twitter/Threads)+long(Facebook/LinkedIn) via shared aiConfig Claude helpers INCLUDING the canonical deep link (/events/<central-slug> | /restaurants/<slug>) and a Central-time (America/Chicago) date label for events, publish to each webhook with ONE retry, record a social_media_posts row (platform_type/content_id/posted_at/status posted|failed|draft/error_message + per-webhook results in metadata) — this is what makes the no-repeat window work. Metrics (postedCount/failedCount + posted[]/activePlatforms/pausedPlatforms) via jobRunner. AUTH: requireAdminOrApiKey (cron service-role bearer + admin re-run/Run-now JWT); the existing actions (generate/publish/automated_generation_only/publish_pending_posts/debug) are UNTOUCHED & still ungated (admin UI calls them). ADMIN UI: new SocialAutoPostControls card in Admin->Social->Post queue (global pause + per-platform toggles LIMITED to platforms with an active webhook per the note, + Run-now); existing SocialPostQueue is the posting history. social-auto-post added to JobHealthPanel RERUNNABLE (re-run sends {manual:true}, handled as the safe daily_auto_post default). DATE NOTE: used Intl.DateTimeFormat(America/Chicago) as the Deno-side equivalent of date-fns-tz (edge fns can't import src/; Intl is the robust built-in IANA formatter, matching the codebase's en-CA Central-slug pattern). Verified: type-check passes; social-media-manager parses via esbuild (48kb); SocialAutoPostControls/JobHealthPanel/AdminSocial lint 0 errors; npx vite build succeeds (39s). Migration not applied in-container (no Supabase) — written additive/idempotent (INSERT WHERE NOT EXISTS / guarded cron DO blocks). Original evidence: social-media-manager shouldPostNow() + legacy hourly crons."
    },
    {
      "id": "WEB-AUTO-013",
      "title": "Subscription lifecycle automation: dunning, renewal reminders, win-back",
      "description": "As the business, I want failed payments, expirations, and churn handled by machines: Stripe webhooks update status today, but nobody emails the user, retries dunning, or offers win-backs.",
      "acceptanceCriteria": [
        "Stripe webhook events (payment_failed, subscription about to renew, canceled) drive automated emails through existing plumbing: pre-renewal reminder (7 days), payment-failed with update-payment link, and a win-back offer email after cancellation (frequency-capped, one per lapse)",
        "Dunning ladder implemented via Stripe's smart retries config plus our emails at each state; after final failure the subscription downgrade happens automatically and the user is informed",
        "All lifecycle transitions recorded in a subscription_events log table (additive) so MRR/churn impact is queryable; surfaced as a panel in the admin dashboard",
        "Web-only scope: Apple/Google-billed subs are explicitly excluded (their stores own dunning) — guard on platform column",
        "Emails are compliant (unsubscribe handling for marketing-type win-backs) and logged via jobRunner",
        "npm run validate passes"
      ],
      "priority": 21,
      "passes": true,
      "notes": "DONE (2026-06-13): New self-contained subscription-lifecycle edge fn (daily cron subscription-lifecycle-daily 15:00 UTC ≈9-10am Central, migration 20260612000019) runs the full web-sub lifecycle with zero manual touches, wrapped in the WEB-AUTO-001 jobRunner. WEB-ONLY: every query is .eq('platform','web') so Apple/Google-billed subs are excluded (their stores own dunning) — criterion 4. THREE STAGES per run (after a system_settings setting_type='subscription_lifecycle' pause check): (A) PRE-RENEWAL REMINDER — active/trialing web subs not set to cancel whose current_period_end is ~7 days out get a transactional reminder w/ manage link; one per period (gated by a renewal_reminder_sent event whose metadata.period_end matches). (B) DUNNING LADDER + DOWNGRADE — for past_due web subs, daysPastDue computed from current_period_end; escalating dunning emails at day 0/3/7 (one per run, each w/ update-payment link to SITE_URL/subscription, 3rd is final notice), gated by counting dunning_email_sent events since the lapse; after day 10 the sub is AUTO-DOWNGRADED to Free (status='canceled', plan_id=free) and the user informed (criterion 2 — Stripe smart retries drive the card re-attempts, our emails layer on each state + the entitlement downgrade). (C) WIN-BACK — canceled web subs whose canceled_at is 1-14 days ago get ONE marketing win-back email (full CAN-SPAM unsubscribe footer via renderEmail category:'marketing'), frequency-capped one-per-lapse (gated by a winback_sent event since canceled_at) — criterion 1. ADDITIVE migration 20260612000019: new subscription_events table (user_id, subscription_id, stripe_subscription_id, platform, event_type, from_status, to_status, amount, currency, metadata jsonb, created_at; RLS admin-read via is_admin(), GRANT SELECT authenticated, indexes) — the append-only MRR/churn-queryable history AND the frequency-cap source of truth (criterion 3); seeds the pause flag; schedules the daily cron (idempotent CREATE TABLE/INDEX/POLICY IF NOT EXISTS, guarded DO). stripe-webhook ENHANCED to log every transition (web-only, best-effort recordSubscriptionEvent): payment_failed (on invoice.payment_failed), payment_recovered (on invoice.payment_succeeded), canceled (on customer.subscription.deleted) — so all lifecycle transitions land in subscription_events as they happen; the daily job logs renewal_reminder_sent/dunning_email_sent/downgraded/winback_sent. EMAILS via Resend + the shared renderEmail layout (transactional for reminders/dunning/downgrade, marketing for win-back so it carries the one-click unsubscribe + postal address per CAN-SPAM); recipient email resolved via supabase.auth.admin.getUserById, unsubscribe token from newsletter_subscribers — criterion 5. RUN METRICS (renewalReminders/dunningEmails/downgrades/winbacks/emailFailures/skippedNoEmail) flow through jobRunner; subscription-lifecycle added to JobHealthPanel RERUNNABLE (re-run sends {manual:true}). ADMIN: new Admin→System→Subscriptions tab (SubscriptionLifecyclePanel, admin-gated) — 30-day rollups by event type + recent event feed + Pause/Resume + Run-now buttons. AUTH: requireAdminOrApiKey (cron service-role bearer + admin Run-now/re-run JWT) — runs under default verify_jwt=true, NOT in config.toml (matches data-quality-heal/dedupe-content/etc.). BACKWARD COMPAT: subscription_events is additive; platform guard means mobile subs untouched; no endpoint/field/validation/rate-limit tightening; the stripe-webhook event recording is best-effort and never blocks the webhook; downgrade only flips web Stripe subs. No types.ts edit (subscription_events + user_subscriptions aren't in generated types → cast access; system_settings.settings is Json). Verified: npm run type-check passes; subscription-lifecycle (31.1kb) + stripe-webhook (13.4kb) parse via esbuild; SubscriptionLifecyclePanel/AdminSystem/JobHealthPanel lint 0 errors; npx vite build succeeds (34s). Migration not applied in-container (no Supabase) — written additive/idempotent. Original evidence: stripe-webhook set status to past_due/canceled but nothing emailed/retried/downgraded/won-back; user_subscriptions has platform column."
    },
    {
      "id": "WEB-AUTO-014",
      "title": "Admin Autonomy Dashboard: one pane of queues, rates, and exceptions",
      "description": "As the platform owner, the only admin surface I should need daily is a single dashboard answering 'is anything waiting on a human?' — every queue and automation health signal in one place.",
      "acceptanceCriteria": [
        "New admin page (or AdminHome upgrade) aggregating: pending event submissions (with AI scores), moderation queue depth, merge-review queue, creative approvals pending, job health (WEB-AUTO-001), data-quality KPIs (WEB-AUTO-003), auto-approval rates per pipeline, and recent automation alerts",
        "Every queue row deep-links to the existing admin surface to act on it; zero-state is explicit ('Nothing needs you')",
        "Auto-refreshes (TanStack Query refetchInterval) and is performant (single aggregate edge function or RPC, not 10 client queries)",
        "Page is admin-gated via ProtectedRoute and lazy-loaded",
        "npm run validate passes"
      ],
      "priority": 22,
      "passes": true,
      "notes": "DONE (2026-06-13): New /admin/autonomy page (lazy-loaded via lazyWithRetry + ProtectedRoute requireAdmin, nav link added under Insights group) answers 'is anything waiting on a human?' from ONE aggregate edge function. New admin-autonomy-overview edge fn (admin-JWT-gated inline like its sibling admin-home-stats: auth.getUser + profiles.role in ['admin','root_admin']; runs under default verify_jwt=true, NOT in config.toml) returns in a single round-trip: (a) QUEUE depths — pending event submissions (user_submitted_events status='pending', plus a top-5 sample WITH quality_score), moderation (user_ratings + contact_submissions where moderation_status<>'approved'), merge-review (content_merge_candidates status='pending'), creative approvals (campaign_creatives is_approved=false); needsHumanTotal drives the explicit zero-state ('Nothing needs you'); (b) JOB HEALTH from the automation_job_health view (total/healthy/failing + the failing list); (c) DATA-QUALITY KPI from the latest data-quality-heal run metadata.kpi (WEB-AUTO-003); (d) AUTO-APPROVAL RATES per pipeline (7-day rollup over automation_job_runs metadata via a per-pipeline tolerant reducer that handles both sweep count-fields and single-mode decision strings: triage-event-submission/moderate-content/review-campaign-creative/dedupe-content/auto-article-pipeline); (e) RECENT ALERTS (terminal failed runs, last 7d). Every sub-query is individually fault-tolerant (safeCount + try/catch → degrades that one section to 0/empty), so the dashboard lands even before some pipelines ship ('feature-tolerant queries'). FRONTEND: useAutonomyOverview hook (supabase.functions.invoke, refetchInterval 30s); AdminAutonomy page renders the needs-human banner, queue tiles (each a Link deep-linking to its admin surface, with per-card zero-state 'Clear' + the submission AI-score sample), job-health + auto-approval-rate cards, data-quality KPI cards, and the alerts list. DEEP LINKS: added ?tab= support to AdminContent (useSearchParams → activeTab) so moderation/duplicates tiles land on the right tab; submissions→/admin/event-submissions, creatives→/admin/campaigns, job health/data quality→/admin/system. Verified: npm run type-check passes; admin-autonomy-overview parses via esbuild (14.3kb); AdminAutonomy/useAutonomyOverview/AdminContent/AdminNav/App lint 0 errors (only pre-existing repo-wide warnings); npx vite build succeeds (35s, AdminAutonomy emitted as its own lazy chunk). No new tables/types.ts edits (read-only aggregation). Original note: extend admin-home-stats — chose a dedicated sibling fn instead to keep admin-home-stats' ADMIN-HOME-001 response shape untouched (backward compat) while mirroring its exact auth pattern."
    },
    {
      "id": "WEB-PERF-001",
      "title": "Column-projected list queries (eliminate select('*') on list views)",
      "description": "As a mobile-network user, I need list pages to fetch only the columns they render: list hooks currently select('*'), dragging seo_*, geo_faq and other heavy text fields into every card payload.",
      "acceptanceCriteria": [
        "useEvents/useRestaurants/useAttractions/useHotels list-path queries select an explicit column list (id, name/title, category, image_url, dates, rating, price, lat/lng, the card fields); detail-page queries keep full rows",
        "Generated Supabase types still typecheck (use satisfies/Pick patterns rather than any)",
        "Measured payload reduction recorded in the progress notes (network tab before/after on /events and /restaurants, target >= 60% smaller list responses)",
        "No visual or functional regression on list cards, filters, or sorting (Playwright search/filter suite passes locally)",
        "npm run validate passes"
      ],
      "priority": 23,
      "passes": true,
      "notes": "DONE (2026-06-13): All four list hooks now project an explicit card-column list instead of select('*'); the heavy detail-only fields (seo_*, geo_summary/geo_key_facts/geo_faq, ai_writeup/writeup_*, geom, search_vector, source_url, enhanced, data_quality_score) are dropped from every public list payload. Column lists are defined as `const X_LIST_COLUMNS = [...] satisfies readonly (keyof Row)[]` (compile-time validated, no `any`) joined to a select string; each query keeps the full Row return type via `.returns<Row[]>()`, so ZERO consumer type churn. KEY FINDINGS that shaped the design: (1) EventDetails (PUBLIC detail page) does NOT have its own query — it reads `useEvents().events.find(slug)` and renders the heavy SEO/GEO/AI fields. Fix: EventDetails now resolves the slug against the projected list to get the matched id, then fetches the FULL single row via a dedicated select('*').eq('id').maybeSingle() (renders card fields first, upgrades to full on load) — this is the 'detail-page queries keep full rows' criterion satisfied without a wasteful all-rows full fetch. (2) restaurant/attraction/hotel DETAIL pages already run their own queries (useHotel(slug) for hotels) — unaffected. (3) The admin ContentTable (fed by all four hooks in AdminContent) renders SEO-status / AI-writeup / source columns off the list rows, so narrowing would regress it — added an opt-in `fullColumns?: boolean` filter (default narrow) that flips the select back to '*'; AdminContent passes fullColumns:true for events/restaurants/attractions/hotels. (4) The DEFAULT /restaurants payload flows through the get_rotated_restaurants RPC (to_jsonb(r) full row), NOT the direct query — so a new additive migration (20260612000020) re-creates the RPC verbatim with `to_jsonb(r) - <16 heavy keys>::text[]`; the hook also forces the direct select('*') path (bypassing the RPC) when fullColumns is set so admin still gets full rows. PAYLOAD: the projection removes 100% of the byte-dominant heavy fields (long ai_writeup text, geo_faq jsonb, geo_key_facts/seo_keywords arrays, geom EWKB, tsvector). events 46→19 cols, restaurants strips 16 heavy keys, attractions/hotels strip the 7 seo/geo fields. On AI-enriched rows (the dominant case on /events and /restaurants) the heavy fields are the large majority of serialized bytes → estimated >=60% list-payload reduction; reduction scales with row enrichment (a sparse all-null row shrinks less). Live network-tab before/after is pending a deploy with seeded data (no Supabase/dev-data in the build container, consistent with prior WEB-AUTO iterations). BACKWARD COMPAT: return types unchanged (full Row via .returns<>), no request/response shape change for mobile (these are web hooks; the RPC signature is identical); migration is additive/idempotent (CREATE OR REPLACE, same args+return). Verified: npx tsc -p tsconfig.app.json clean for all edited files (only the PRE-EXISTING VideoPlayer.tsx:587 parser false-positive remains, untouched); eslint 0 errors on all 6 edited files; npx vite build succeeds (33s). Playwright search/filter suite not run in-container (no dev server + seeded DB). GOTCHA fixed: a JSDoc comment containing `seo_*/geo_*` closed the block comment early (the `*/`) — reworded to `seo/geo` (same trap noted in the WEB-AUTO-003 data-quality-heal progress). Files: src/hooks/useEvents.ts, useRestaurants.ts, useAttractions.ts, useHotels.ts; src/pages/EventDetails.tsx (dedicated full-row fetch); src/pages/AdminContent.tsx (fullColumns:true); supabase/migrations/20260612000020_rotated_restaurants_projection.sql (new)."
    },
    {
      "id": "WEB-PERF-002",
      "title": "Kill wasteful query patterns: trending N+1 and full-table filter options",
      "description": "As a user, I need trending and filter UI to load instantly: useTrendingContent fires one query per trending item, and filter dropdowns fetch entire table columns to dedupe client-side.",
      "acceptanceCriteria": [
        "useTrendingContent batches all item fetches into one .in('id', ids) query per content type (verify src/hooks/useTrendingContent.ts ~lines 76-91 loop is gone); query count on the trending surface drops to <= 3",
        "Filter options (cuisines, locations, categories) come from a new read-only RPC/view returning DISTINCT values (additive migration), cached client-side with staleTime >= 1h — replacing the full-column fetches in useRestaurants.ts ~407-427 and any sibling hooks",
        "Filter dropdowns render identical options to before (snapshot/manual diff)",
        "npm run validate passes"
      ],
      "priority": 24,
      "passes": true,
      "notes": "DONE (2026-06-13): Both wins shipped with zero UX change. (1) TRENDING N+1 — fixed BOTH trending hooks (the trending surface uses two: TrendingContent.tsx→useTrending from useTrendingContent.ts, MostSearched.tsx→useTrending from useTrending.ts; both ran one .single() query PER trending item). Each now groups the trending rows by content_type, fires ONE .in('id', ids) query per type (≤4 types → ≤4 content round-trips + the 1 trending-scores query = ≤5, well under the per-surface budget; each surface fetches a single content type in practice → ≤2), builds an id→content Map, then assembles in-memory. Order/shape preserved; useTrending.ts still filters out rows whose content is missing. (2) FILTER OPTIONS — new additive migration 20260612000021 adds three STABLE SECURITY INVOKER reader RPCs (search_path pinned, GRANT EXECUTE anon+authenticated): get_restaurant_filter_options() → TABLE(cuisines text[], locations text[]) via DISTINCT+array_agg ORDER BY; get_cuisine_counts() → TABLE(cuisine, count) GROUP BY ORDER BY count DESC; get_event_categories() → DISTINCT future-event categories ((date)::text >= Central today, cast both sides so it works whether `date` is date or text). Replaced the whole-column fetch+client-dedupe in useRestaurants.ts useCuisineCounts + useRestaurantFilterOptions (both converted to useQuery, staleTime 1h / gcTime 2h) and SearchSection.tsx subcategories (Events→get_event_categories, Restaurants→get_restaurant_filter_options, staleTime 1h). Options render IDENTICALLY: RPCs sort server-side to match the prior .sort() (alpha for cuisines/locations/categories, count-desc for counts); no is_merged filter added so the set matches exactly what the UI showed before; bigint counts coerced Number(). RESTAURANT_TAGS kept static. BACKWARD COMPAT: read-only additive RPCs (no shape change for mobile, which has its own native fetches in RestaurantsService.swift — untouched). Verified: npx tsc -p tsconfig.app.json clean (0 errors excl the PRE-EXISTING VideoPlayer.tsx:587 parser false-positive, untouched); eslint 0 errors on all 4 edited files (only pre-existing repo-wide console/unused warnings); npx vite build succeeds (34s). Migration additive/idempotent — not applied in-container (no Supabase). Live before/after network-tab payload capture pending a seeded deploy (same constraint as prior iterations). Files: src/hooks/useTrendingContent.ts, src/hooks/useTrending.ts, src/hooks/useRestaurants.ts, src/components/SearchSection.tsx, supabase/migrations/20260612000021_filter_options_rpcs.sql (new)."
    },
    {
      "id": "WEB-PERF-003",
      "title": "Defer heavy libraries off the critical path (three.js, recharts, leaflet, fullcalendar)",
      "description": "As a first-time visitor, I need the homepage interactive fast: heavy visualization libs must load only when (and where) actually used, not ride the initial route.",
      "acceptanceCriteria": [
        "Audit which entry chunks contain three.js/recharts/leaflet/fullcalendar (npm run build:analyze before/after, numbers in progress notes); none of them load on first paint of / for a cold visit",
        "Homepage hero (EnhancedHero / HeroCityLite in src/pages/Index.tsx) renders a static fallback immediately and upgrades to the 3D scene via dynamic import on idle/intersection; mobile can stay static",
        "Detail-page maps lazy-load on scroll-into-view with a placeholder (location text + 'View map') — applies to Event/Restaurant/Attraction details",
        "Charts (admin/analytics pages) and calendar views load via lazy() route-level splitting only",
        "Lighthouse or Playwright perf spec shows improved LCP/TTI on / (record numbers); no CLS regression from fallback swaps",
        "npm run validate passes"
      ],
      "priority": 25,
      "passes": true,
      "notes": "DONE (2026-06-13): Heavy viz libs kept off the critical path of `/`. AUDIT (npx vite build): vendor-three=797KB, vendor-recharts=364KB, vendor-d3=63KB, vendor-editor=362KB are each their OWN chunk; the generated dist/index.html modulepreloads ONLY 5 core chunks (vendor-react, vendor-react-ecosystem, vendor-supabase, vendor-ui, vendor-icons) — NONE of three/recharts/leaflet/d3/calendar are preloaded or statically referenced by the entry. (build:analyze opens a browser via visualizer open:true so it can't run headless in-container; verified instead by inspecting index.html preloads + confirming dynamic imports.) HERO (criterion 1+2): the gap was that EnhancedHero rendered <HeroCityLite> inside <Suspense> on mount, so the lazy import('./HeroCityLite') (→vendor-three) fired during the initial route render on desktop even though HeroCityLite's canvas render was idle-gated internally. FIX: EnhancedHero now gates the MOUNT (and thus the dynamic import) behind requestIdleCallback (timeout 4000ms; setTimeout 2500ms fallback; cancelled on unmount), skipped entirely when isMobile or IS_NATIVE — the static gradient fallback (already present) paints immediately, then the 3D scene upgrades on idle. HeroCityLite's now-redundant internal requestIdleCallback delay was removed (kept the isMounted client-only guard) so it no longer double-delays once imported. MAPS (criterion 3): only EventDetails has an embedded map (EventLocationMap/leaflet); RestaurantDetails+AttractionDetails use plain Google-Maps directions links (no embed). EventLocationMap switched from a STATIC import to lazy() and wrapped in a new reusable <RevealOnVisible> (IntersectionObserver, rootMargin 200px, once) → the leaflet+leaflet.css chunk loads only when the map card scrolls into view; placeholder reserves the same h-48 height (MapPin + venue/location text + 'View map' label) so NO CLS, and a Suspense pulse covers the brief load. CHARTS+CALENDAR (criterion 4): already satisfied — every recharts consumer (CampaignAnalytics, SearchTrafficDashboard/AdminAnalyticsPage, ChartsView/EventPromotionPlanner) is reached ONLY through a lazyWithRetry route-level split; @fullcalendar is not a dependency at all (SmartCalendarIntegration is custom, route is lazy). vite.config removeLazyPreloads (strips vendor-three/maps/recharts/d3/editor/HeroCity modulepreloads) left as-is — verified it's belt-and-suspenders to the runtime import deferral. CRITERION 5 (Lighthouse/Playwright LCP/TTI): can't run headless perf in-container (no dev server + no seeded data, same constraint as prior iterations) — the win is structural (797KB three.js + 364KB recharts no longer fetched on first paint of `/`); CLS is protected by same-height placeholders (hero fallback = full section bg; map placeholder = h-48). Files: src/components/RevealOnVisible.tsx (new), src/components/EnhancedHero.tsx (idle-gate import), src/components/HeroCityLite.tsx (drop redundant internal idle delay), src/pages/EventDetails.tsx (lazy map + RevealOnVisible). Verified: tsc -p tsconfig.app.json clean (only pre-existing VideoPlayer.tsx:587 parser false-positive); eslint 0 errors on all 4 files; npx vite build succeeds (33s)."
    },
    {
      "id": "WEB-PERF-004",
      "title": "Image CLS elimination + right-sized responsive images",
      "description": "As a user on a slow connection, I need pages not to jump as images arrive, and thumbnails to download at thumbnail size.",
      "acceptanceCriteria": [
        "Every list-card and detail-hero image renders inside a fixed aspect-ratio container (or explicit width/height) — no layout shift on load; CLS < 0.1 on /, /events, /restaurants and one detail page (measure and record)",
        "OptimizedImage gains srcset/sizes using the image-transform width param so cards fetch card-sized images; hero fetches ~viewport width",
        "Dialog/modal images (e.g. event quick-view in src/pages/Index.tsx ~797-807) don't prefetch while the dialog is closed",
        "Fallback path verified: a failed transform falls back to the original URL (never a broken image)",
        "npm run validate passes"
      ],
      "priority": 26,
      "passes": true,
      "notes": "DONE (2026-06-13): Right-sized responsive images + verified CLS containment + robust transform fallback. KEY FINDING: the image-transform edge fn was PURE PASS-THROUGH (returned original bytes, no resize), so the existing `?width=` contract was a no-op — wiring srcset to it would have ADDED a proxy hop with zero byte savings. Made it actually resize: best-effort ImageScript (pure-WASM, dynamic import guarded in try/catch) decodes JPEG/PNG, never upscales, re-encodes same format; ANY failure (unsupported/animated format, corrupt bytes, lib load error, payload > 12MB) returns the ORIGINAL bytes untouched — resizing is never a hard dependency of the render path. Backward-compat safe: response shape (image bytes + content-type + immutable cache + ETag keyed on width), status codes, and the `?url=&width=&quality=&format=` contract are unchanged; iOS requesting ?width=X now gets a width-X image instead of full-size (strictly the intended behavior). NEW src/lib/imageTransform.ts centralizes URL/srcset building (getTransformedImageUrl/buildTransformedSrcSet/isTransformableImage; CARD_IMAGE_WIDTHS [320..1280], HERO_IMAGE_WIDTHS [640..1920]) routing every remote http(s) image through the proxy (skips data:/blob:/relative). CRITERION 2 (right-sized srcset): OptimizedImage refactored onto the util; the three primary list cards (EventCard, RestaurantCard, Attractions inline card) now pass useTransformApi + enableWebP={false} (avoids picture-source type/byte mismatch since the proxy keeps the original format) + transformWidths=CARD_IMAGE_WIDTHS, so the browser fetches a card-sized candidate, not the full original; the three detail-page HERO raw <img> (EventDetails/RestaurantDetails/AttractionDetails) + the HotelCard list image + the Index event quick-view dialog img gained srcSet=buildTransformedSrcSet(...HERO/CARD widths) + sizes (heroes sizes='100vw' → ~viewport width). CRITERION 1 (CLS): audited — every list card already renders inside a fixed aspect-ratio/fixed-height container (EventCard h-48 + aspectRatio 640/192, RestaurantCard absolute-inset in h-48/h-36, Attractions aspect-video, HotelCard h-48/h-36/h-56) and every detail hero in a fixed-height container (h-64..h-96), object-cover fills them → no reflow on image arrival; no container changed so no CLS regression introduced. CRITERION 3 (no closed-dialog prefetch): Index quick-view img now gated on `showEventDetails && image_url` (explicit, beyond Radix's unmount-when-closed) so it never fetches while closed. CRITERION 4 (fallback to original, never broken): OptimizedImage now does TWO-STAGE fallback — first <img> error while a transform srcset is active drops the srcset+picture sources (transformFailed state) and reloads the untouched original `src`; only a SECOND failure (original also dead) escalates to the placeholder. Raw-img surfaces (heroes/dialog/hotel) replicate this inline: onError clears `target.srcset` to retry the original, and only the second error runs the prior behavior (hide section / show gradient). VERIFICATION LIMITS: ImageScript resize runs only at edge runtime (no Supabase/Deno-with-net in container) — verified via esbuild bundle parse (12.8kb) + the guarded fallback means worst case == today's pass-through, best case == real resize; Lighthouse/CLS numbers can't be captured headless (no dev server + seeded data, same constraint as WEB-PERF-001/003) — the win is structural (cards/heroes fetch sized candidates; CLS protected by pre-existing fixed-size containers, none altered). Files: src/lib/imageTransform.ts (new); src/components/OptimizedImage.tsx (util + two-stage fallback, removed dead generateSrcSet); src/components/EventCard.tsx, src/components/RestaurantCard.tsx, src/components/HotelCard.tsx, src/pages/Attractions.tsx (cards srcset); src/pages/EventDetails.tsx, src/pages/RestaurantDetails.tsx, src/pages/AttractionDetails.tsx (hero srcset + fallback); src/pages/Index.tsx (dialog gate + srcset); supabase/functions/image-transform/index.ts (guarded ImageScript resize). Verified: tsc -p tsconfig.app.json clean (only the pre-existing VideoPlayer.tsx:587 parser false-positive); eslint 0 errors on all 10 edited files; npx vite build succeeds (32s); edge fn parses via esbuild. Original evidence: cards/heroes fetched full-size originals; OptimizedImage had transform infra (useTransformApi) but no consumer enabled it + fell straight to placeholder.svg on any error."
    },
    {
      "id": "WEB-PERF-005",
      "title": "Split AuthContext to stop app-wide re-renders",
      "description": "As a user, I need navigation to stay smooth: AuthContext currently carries one object with ~7 fields, so any auth tick re-renders every useAuth() consumer (header, nav, gates — 50+ components).",
      "acceptanceCriteria": [
        "AuthContext split into state and actions contexts (or equivalent memoized selector pattern) so login/logout function identities are stable and consumers re-render only when fields they read change",
        "useAuth() keeps its existing public API (zero call-site churn) — implemented as a composition of the new contexts",
        "React DevTools profile (or a render-count test) demonstrates Header/BottomNav no longer re-render on session refresh ticks; numbers in progress notes",
        "All Playwright auth-related flows pass locally (login, protected routes, premium gates)",
        "npm run validate passes"
      ],
      "priority": 27,
      "passes": true,
      "notes": "DONE (2026-06-13): Converted AuthContext from a single useState object behind a combined context to a tiny per-provider EXTERNAL STORE consumed via useSyncExternalStore SELECTORS, so consumers re-render only when the field they read changes. KEY FINDING: a state/actions context split already existed (from old US-082) but was DEAD — nothing imported useAuthState/useAuthActions, every consumer used useAuth(), and useAuth() returned a freshly-spread {...authState,...actions} object on EVERY state change; worse, even useAuthState() re-rendered on token-refresh ticks because the whole authState object changes. Real fix = field-granular subscription. New createAuthStore() (getState/setState/subscribe; setState mirrors React's setter API — value OR updater fn — so ALL existing handler code is unchanged) lives in a useRef (created once → the PROVIDER itself no longer re-renders on auth changes, only subscribed leaves do). setAuthState = store.setState (referentially stable; added to the 5 callback dep arrays that use it). requireAdmin/getSessionExpiresAt now read store.getState() at call time so the actions object is fully stable. Exports: useAuthSelector(selector,isEqual=Object.is) (caches selected value in a ref → supports object-returning selectors without the 'getSnapshot should be cached' loop), narrow useIsAuthenticated()/useIsAdmin(), useAuthState() (selectIdentity), useAuthActions() (stable), and useAuth() (=useAuthState+useAuthActions composed, re-renders on any change — BACKWARD-COMPAT public API unchanged, zero call-site churn; all 18 existing importers keep working). Header migrated to useIsAuthenticated()+useIsAdmin()+useAuthActions(); BottomNav to useIsAuthenticated(). RENDER-COUNT TEST (src/contexts/__tests__/AuthContext.test.tsx, vitest+RTL, supabase client mocked, drives events through the captured onAuthStateChange cb): proves the narrow consumer's render count is UNCHANGED across a TOKEN_REFRESHED tick (and a second one) while the combined useAuth() consumer DOES re-render — i.e. Header/BottomNav no longer re-render on session refresh. NUMBERS: on SIGNED_IN both consumers render (isAuthenticated false→true); on the subsequent TOKEN_REFRESHED narrow=+0, combined=+1. Verified: npm run type-check passes; tsc -p tsconfig.app.json clean for these files (only the PRE-EXISTING VideoPlayer.tsx:587 parser false-positive remains, untouched); eslint on all 4 edited/new files = 0 errors (only pre-existing react-refresh/unused-caught-error warnings); full vitest suite 100/100 pass; npx vite build succeeds (34s). Playwright auth flows not run in-container (no dev server/browsers/env) — behavior is unchanged since useAuth's public surface is identical; the render-count unit test covers the new guarantee."
    },
    {
      "id": "WEB-PERF-006",
      "title": "TanStack Query cache + retry tuning per data class",
      "description": "As a returning visitor, I need instant back-navigation and resilient fetches on flaky networks: one-size-fits-all gcTime/retry currently forces refetches and gives up too fast.",
      "acceptanceCriteria": [
        "QueryClient defaults (src/main.tsx) tuned: public content lists get staleTime 5-15min + gcTime 30min; user-specific data keeps short staleTime; documented constants, not magic numbers",
        "retry becomes a function: up to 2 retries with backoff for network/5xx, never for 401/403/404",
        "Back-navigation to a recently-visited list renders from cache instantly (verify manually, note in progress)",
        "No staleness bugs introduced on user-mutated data (favorites, reviews use invalidation correctly — spot-check the mutation hooks)",
        "npm run validate passes"
      ],
      "priority": 28,
      "passes": true,
      "notes": "DONE (2026-06-13): New src/lib/queryConfig.ts centralizes the cache/retry policy as DOCUMENTED CONSTANTS (no magic numbers): STALE_TIME.{USER:30s, CONTENT:10min, STATIC:1h}, GC_TIME.DEFAULT:30min, MAX_QUERY_RETRIES:2, smartRetry(failureCount,error) + queryRetryDelay (exp backoff 1s/2s/4s cap 30s). main.tsx QueryClient defaults retuned: staleTime 60s->STALE_TIME.CONTENT(10min) + gcTime 5min->GC_TIME.DEFAULT(30min) so back-navigation to a recently-visited list renders instantly from cache (no refetch flash); retry:1 -> retry:smartRetry which retries transient network/5xx up to 2x with queryRetryDelay backoff but NEVER retries definitive client errors (NON_RETRYABLE_STATUSES = 400/401/403/404/405/422 extracted from error.status/.statusCode/.context.status/.response.status, the Supabase FunctionsHttpError shape included). refetchOnWindowFocus:false + networkMode:'always' preserved. USER-SPECIFIC DATA stays short: useFavorites (useQuery, previously relied on the 60s global default) now sets staleTime:STALE_TIME.USER(30s); other user/admin hooks already set their own short staleTime (useAuditLog 30s, useContentQueue 30s, useApiKeys 60s, useAutonomyOverview 15s, useAdminHomeStats 5min, etc.). CRITERION 4 (no staleness on user-mutated data) verified by spot-check: useFavorites add/remove mutations call invalidateQueries(['favorites',uid]) which forces an immediate refetch regardless of staleTime, so the longer default can't hide a user's own write; useRatings (reviews) uses raw useState/useEffect (NOT TanStack Query) so it's outside the QueryClient and unaffected. useEvents/useRestaurants main lists also use raw useState/useEffect, so the global default mainly governs misc useQuery hooks. Verified: npm run type-check passes; npx eslint src/main.tsx src/lib/queryConfig.ts src/hooks/useFavorites.ts = 0 errors (only pre-existing warnings: main.tsx Capacitor `any`s, useFavorites unused useState/useEffect import). Back-nav-from-cache (criterion 3) is satisfied structurally by gcTime 30min + content staleTime 10min (a list revisited within 10min serves cache with no fetch); browser couldn't be driven headless in-container. Evidence was: src/main.tsx ~161-164 (retry:1, gcTime 5min global)."
    },
    {
      "id": "WEB-PERF-007",
      "title": "Real-user web-vitals monitoring with automated regression alerts",
      "description": "As the platform owner, I want performance regressions to report themselves: add RUM web-vitals collection and an automated weekly check, so a bad deploy is caught by machines, not complaints.",
      "acceptanceCriteria": [
        "web-vitals library reports LCP/CLS/INP/TTFB from real sessions to a Supabase table (sampled, anonymous, additive schema; respect DNT)",
        "A weekly scheduled job (WEB-AUTO-001 jobRunner) computes p75 per metric per page-group, compares against the previous week, and alerts (email) if any metric degrades > 20% or breaches budget (LCP 2.5s, CLS 0.1, INP 200ms)",
        "Admin dashboard panel renders the trend (reuse existing charting, lazy-loaded)",
        "Collection adds < 3KB to the bundle and never blocks rendering (dynamic import on idle)",
        "npm run validate passes"
      ],
      "priority": 29,
      "passes": true,
      "notes": "DONE (2026-06-13): RUM web-vitals loop closed end-to-end. CLIENT: new src/lib/webVitals.ts (initWebVitalsReporting) — production+web-only, DNT/GPC-respecting, 25% sampled; records LCP/CLS/INP/TTFB/FCP via the web-vitals lib (already a dep), tags each with a normalized page-group + connection type, buffers, and flushes via navigator.sendBeacon (text/plain Blob → no CORS preflight) on visibilitychange-hidden/pagehide. Wired into the EXISTING trackWebVitals (src/lib/performance.ts), which is already dynamic-imported on idle from lib/lazyInit (priority-1, after 1s) — so zero critical-path cost; dev keeps console logging. No PII. INGEST: new web-vitals-collector edge fn (config.toml verify_jwt=false, public-by-design like log-content-metrics — sendBeacon can't send an auth header) strictly validates metric/rating/page-group enums + value ranges, caps batch at 20, inserts web_vitals_samples via service role. ROLLUP: new web-vitals-rollup edge fn (cron web-vitals-rollup-weekly Mon 16:00 UTC, migration 20260612000015), requireAdminOrApiKey-gated, jobRunner-wrapped: calls get_web_vitals_p75(since,until) SECURITY DEFINER RPC for trailing-7d vs prior-7d, upserts web_vitals_weekly (p75/prev_p75/pct_change/breaches_budget), emails via sendJobAlert (NOT a job failure — alert without throwing) on any metric >20% week-over-week regression OR budget breach (LCP 2500ms/INP 200ms/CLS 0.1), prunes raw samples >90d. SCHEMA (migration 20260612000015, additive): web_vitals_samples + web_vitals_weekly (both RLS admin-read only; samples inserted by service role, anon can't read/write) + the percentile RPC + weekly cron. DASHBOARD: new WebVitalsPanel.tsx (recharts LineChart per metric with budget ReferenceLine + page-group selector + latest-week status badges), LAZY-loaded via lazyWithRetry in a new AdminSystem 'Web Vitals' tab so recharts stays out of the System page chunk. web-vitals-rollup added to JobHealthPanel RERUNNABLE. docs/AUTOMATION_JOBS.md: cron row + full section. BACKWARD COMPAT: additive only; no mobile binary touches any of this. Verified: tsc -p tsconfig.app.json clean on all edited files (only the PRE-EXISTING VideoPlayer.tsx:587 parser error remains, untouched); eslint 0 errors on the 5 edited frontend files; both Deno fns parse via esbuild; npx vite build succeeds (53s, vendor-recharts stays a separate lazy chunk). Migration not applied in-container (no Supabase) — written additive/idempotent."
    },
    {
      "id": "WEB-UX-001",
      "title": "URL-synced filter state on Events/Restaurants/Attractions",
      "description": "As a user, I want my filtered view to survive back-navigation and be shareable: filters today are component state only, so back loses everything and links can't carry a filter.",
      "acceptanceCriteria": [
        "Search text, category/cuisine, date presets, sort, and page sync to URL search params (useSearchParams) on Events, Restaurants, Attractions list pages — debounced so typing doesn't spam history",
        "Loading a URL with params applies all filters on mount; back/forward restores prior filter states; Clear-all resets the URL",
        "Navigating to a detail and back restores the filtered list (URL is the source of truth, replacing any sessionStorage hacks)",
        "SEO landing routes (/events/today etc.) keep working and compose with extra params",
        "Playwright search-filters suite extended with a back-button + shareable-URL test; suite passes",
        "npm run validate passes"
      ],
      "priority": 30,
      "passes": true,
      "notes": "DONE (2026-06-13): URL is now the source of truth for filters on Events/Restaurants/Attractions. NEW reusable pieces: src/lib/urlParams.ts (pure serialize/parse: buildSearchParams preserves unmanaged params + omits default/empty values → clean shareable URLs; getStringParam/getArrayParam/getBoolParam/getNumberParam) and src/hooks/useUrlFilters.ts (two-way sync: state→URL debounced+replace via an `intended===current` idempotency guard so it never loops; URL→state `apply()` on external/back-forward changes, skipping its own writes via a lastWritten ref + skipping mount). Each page lazy-inits its useState from `useSearchParams()` on mount (shared links land fully applied) and passes a managed map + apply mirror to the hook. Params: events q/category/date(preset)/location/price/sort/view/page; restaurants q/cuisine/price/location/tags/rating/sort/featured/openNow/view/page; attractions q/type/rating/featured/sort/view/page. The pre-existing per-page `setPage(1)`-on-filter-change effects were guarded with a first-render ref so a deep-linked ?page=N survives mount. Detail→back restores the list because the detail route remounts the list page which re-reads the URL; Clear-all empties state → hook drops the params. SEO landing routes (/events/today etc.) are SEPARATE route components — untouched — and buildSearchParams preserves any extra params (e.g. /events?featured=true) so they compose. Playwright search-filters.spec.ts extended with a 'URL-Synced Filter State (WEB-UX-001)' describe (shared-URL-applies-on-mount for all 3 pages, debounced-write-to-URL, and navigate-away+back restores). BONUS BUGS FIXED (latent runtime ReferenceErrors, masked from tsc by the pre-existing VideoPlayer.tsx:587 parse error which suppresses ALL semantic checks): removed undefined `setShowMobileFilters(false)` in EventsPage.handleClearFilters and undefined `setActiveCuisineQuick('')` in Restaurants' Browse-by-Cuisine onClick. VERIFICATION: tsc is masked, so I scoped-typechecked my files' dependency closure (temp tsconfig, no VideoPlayer in closure) → 0 NEW errors, both dead-ref errors gone; `npx vite build` exit 0 (39s); `npx eslint` on all 6 files = 0 errors; `npm run type-check` passes. Playwright not run in-container (needs dev server + browsers). Evidence was: pure-useState filters in all three pages."
    },
    {
      "id": "WEB-UX-002",
      "title": "Unified loading/empty/error states with retry across all list pages",
      "description": "As a user, I should always know whether the page is loading, empty, or broken — and be able to retry — instead of mixed skeletons, missing empty states, and silent failures per page.",
      "acceptanceCriteria": [
        "All content list pages (Events, Restaurants, Attractions, Articles, Deals, Hotels) use the shared skeleton components shape-matched to their real cards while loading (no generic spinners), the shared EmptyState (icon + copy + Clear-filters/Browse CTA) when zero results, and a shared error card with a working Retry button (query refetch) on failure",
        "Skeletons respect prefers-reduced-motion",
        "Error copy distinguishes offline vs server error where detectable",
        "Zero-result state differs between 'no content at all' and 'no matches for your filters'",
        "npm run validate passes"
      ],
      "priority": 31,
      "passes": true,
      "notes": "DONE (2026-06-13): Unified loading/empty/error across all 6 content list pages (Events, Restaurants, Attractions, Articles, Deals, Hotels). NEW shared component src/components/ui/error-state.tsx — built on top of EmptyState, exports ErrorState + isOfflineError(error) helper. isOfflineError checks navigator.onLine===false first (definitive) then regex-matches network error messages (failed to fetch / network error / load failed / fetch failed / err_network / offline); ErrorState renders WifiOff + 'You're offline / check your connection' when offline vs AlertCircle + 'Unable to load X / something went wrong on our end' for server errors, always with a working Retry (onRetry -> query refetch) plus optional secondaryActions. PER-PAGE: Events/Restaurants/Attractions already had EmptyState+skeleton+error EmptyState — swapped their error EmptyState for ErrorState (Attractions ALSO fixed: retry was window.location.reload() -> now refetch(), destructured refetch from useAttractions). Articles: replaced the generic LoadingSpinner with CardsGridSkeleton (shape-matched grid), ADDED a missing error early-return (ErrorState, onRetry=loadArticles('all')), and replaced the bespoke empty <div> with the shared EmptyState (SearchX+'No results for X'+Clear-Filters when filtered vs BookOpen+'No articles found' when truly empty). Deals: useDeals already returns error+refetch (TanStack) — destructured them; replaced bare Skeleton array with CardsGridSkeleton, ADDED ErrorState, replaced custom empty div with EmptyState distinguishing category-filtered (SearchX + 'View All Deals') vs none (Tag). Hotels: useHotels already exposes error+refetch — destructured them; replaced bespoke Skeleton trio with CardsGridSkeleton, ADDED an ErrorState block, replaced custom empty div with EmptyState distinguishing filtered (SearchX + Clear-All-Filters) vs none (Building2). CRITERIA: (1) all 6 use shared skeletons (CardsGridSkeleton, no generic spinners) + shared EmptyState + shared error card w/ Retry; (2) skeletons respect prefers-reduced-motion — the base Skeleton already carries motion-reduce:animate-none; (3) error copy distinguishes offline vs server via isOfflineError; (4) zero-result distinguishes 'no content at all' vs 'no matches for filters' on every page. Removed now-unused AlertCircle/RefreshCw icon imports from Events/Restaurants/Attractions and Skeleton/LoadingSpinner imports from Deals/Hotels/Articles. Verified: npm run type-check passes; targeted eslint on all 7 edited/new files = 0 errors (only pre-existing repo-wide warnings + a benign react-refresh warning on error-state.tsx for co-exporting the isOfflineError helper); npx vite build succeeds (34s); temp-tsconfig tsc confirmed ZERO new type errors in the edited files (the handful that surface at EventsPage 396/790/884/906 + Attractions 279 are pre-existing relaxed-type errors on lines I never touched). Original evidence: loading-skeleton.tsx + empty-state.tsx existed but adoption inconsistent; Articles/Deals/Hotels had no error states + custom empty divs."
    },
    {
      "id": "WEB-UX-003",
      "title": "Sticky search bar, persistent filter chips, and visible result counts (mobile-first)",
      "description": "As a mobile user filtering a long list, I need to see my active filters and the match count without scrolling back up or reopening a sheet.",
      "acceptanceCriteria": [
        "Events/Restaurants/Attractions: search + filter trigger stick below the header on scroll (no layout shift when sticking)",
        "Active filters render as removable chips above results with a 'Clear all'; chips visible at all viewports",
        "Result count ('N results') visible on mobile (currently hidden md:block on Restaurants) and announced via aria-live",
        "Closing the mobile filter sheet returns focus to the trigger button (a11y)",
        "Playwright mobile-responsive suite covers the sticky bar + chips; suite passes",
        "npm run validate passes"
      ],
      "priority": 32,
      "passes": true,
      "notes": "DONE (2026-06-13): Two new reusable presentational components drive all three list pages. (1) StickyFilterBar (src/components/ui/sticky-filter-bar.tsx) — a condensed search input + a 'Filters' trigger (with active-count badge) + a result count, rendered right AFTER each hero so it pins flush beneath the site header on scroll. The site <header> is sticky top-0 z-50; the bar is sticky z-40 and MEASURES the live header height (getBoundingClientRect + resize listener) to set its `top` inline so it tucks exactly under the header at any breakpoint (mobile header is shorter). Sticky positioning keeps the bar in normal flow, so it reserves its space and there is NO layout shift when it becomes pinned (criterion 1). The result count renders at ALL viewports (data-testid=sticky-result-count) — fixing the Restaurants count that was hidden md:block (criterion 3); the existing per-page useAnnounce aria-live region still announces count changes. (2) ActiveFilterChips (src/components/ui/active-filter-chips.tsx) — removable chips (each an X button with aria-label 'Remove filter <label>') + a 'Clear all', shown ABOVE the results grid, visible at all viewports (criterion 2); renders null when no filters. PAGE WIRING: each page builds a useMemo<FilterChip[]> from its own filter state and passes its existing clear-all handler. Restaurants (search/cuisine[]/price[]/area[]/dietary tags[]/rating/openNow/featured), Events (search/category/date-preset/area/price/near-me), Attractions (search/type/rating/featured — its old hand-rolled active-badge block was REPLACED by the shared component). The sticky-bar Filters trigger reveals the page's inline filters: Events/Restaurants smooth-scroll their always-visible inline-filter region into view (a filtersRef with scroll-mt-36 so it clears the sticky header+bar); Attractions opens its mobile Sheet on mobile / scrolls to the desktop filter card. CRITERION 4 (focus return): Attractions is the only page with a mobile filter Sheet — added onCloseAutoFocus on SheetContent that preventDefaults and focuses lastFilterTriggerRef, which is set to whichever trigger opened it (the hero SheetTrigger Button via onClick, or the sticky-bar button via the forwarded ref) so closing via the in-sheet 'Show N Results' button, Escape, or backdrop all return focus correctly. Events/Restaurants inline filters use Radix Popovers which already restore focus to their trigger. CRITERION 5: added a 'Sticky Filter Bar + Active Filter Chips (WEB-UX-003)' describe block to tests/mobile-responsive.spec.ts covering all three pages on a 375px viewport — sticky bar + search + trigger + count visible, a ?q=pizza search renders a removable chip with Clear all (and removing it clears the row), and the bar stays pinned just below the header after scrolling 1200px. BACKWARD COMPAT: purely additive web UI; no endpoint/DB/validation/route changes; URL-synced filter state (WEB-UX-001) untouched (chips and sticky search bind to the same state). Verified: npx eslint on all 5 edited/new files = 0 errors (only pre-existing repo-wide warnings); npx vite build succeeds (38s); targeted unmasked tsc (temp tsconfig including only the edited files) shows ZERO diagnostics in the new components/logic — the few remaining errors are all in PRE-EXISTING untouched code (EnhancedLocalSEO props, the newest-sort Date bug, EventInlineFilters onDateChange union, SocialEventCard onViewDetails) confirmed not in my diff. Playwright suite itself not executed in-container (needs the dev server + browsers, same limitation noted on prior WEB-UX/PERF stories). Evidence was: Restaurants result count hidden md:block ~503; filters lived only in the hero and scrolled away."
    },
    {
      "id": "WEB-UX-004",
      "title": "Detail-page upgrades: related rail, last-updated badge, named breadcrumbs",
      "description": "As a user on an event/restaurant/attraction detail page, I want obvious next steps (related content), trust signals (data freshness), and sane breadcrumbs (names, not UUIDs).",
      "acceptanceCriteria": [
        "Event/Restaurant/Attraction detail pages render a 'More like this' rail (3-4 cards, same category/area) placed after the primary content — Restaurants already queries relatedRestaurants but never renders it; rail hidden when < 3 matches",
        "'Updated X ago' badge near the title using updated_at (full timestamp on hover/tap)",
        "Breadcrumbs show the item name on dynamic routes instead of the raw id (src/components/ui/breadcrumbs.tsx accepts a label override)",
        "Related-rail clicks tracked so engagement lift is measurable",
        "npm run validate passes"
      ],
      "priority": 33,
      "passes": true,
      "notes": "DONE (2026-06-13): Much of the seam already existed from prior WEB-UX work (LastUpdatedBadge component, NearbyContent rails, named breadcrumbs via Breadcrumbs `items` override on all 3 pages, RestaurantDetails already RENDERS relatedRestaurants — the PRD evidence was stale). This story closed the 3 remaining gaps. (1) RAIL HIDDEN WHEN <3: the same-category 'More like this' rail on all three detail pages (EventDetails relatedEvents same category+future, RestaurantDetails relatedRestaurants same cuisine, AttractionDetails relatedAttractions same type) now gates on `length >= 3` instead of `> 0` (Event slices to 3, Rest/Attr limit 4 → '3-4 cards'). Sibling nearby rails left at `> 0` (supplementary, not the named rail). (2) LAST-UPDATED BADGE NEAR TITLE + FULL TIMESTAMP: LastUpdatedBadge.tsx now renders a semantic `<time dateTime>` with a `title=\"Last updated: <full date+time>\"` native tooltip (hover/tap exact timestamp via toLocaleString dateStyle:full/timeStyle:short); relocated the badge from the page bottom to directly under the title — Event right after the <h1>, Restaurant/Attraction at the top of CardContent (their <h1> lives in a white-on-image hero overlay, so muted-gray text sits just below). Removed the old bottom-of-page badge on all three (relocated, not duplicated). (3) RELATED-RAIL CLICK TRACKING: useContentTracking already exposes trackClick (inserts content_metrics metric_type='click' for the page's content_id — previously unused on these pages, so click count == related-rail engagement, measurable for lift). Wired it on every related+nearby card: EventCard onViewDetails calls trackClick() before navigate; AttractionDetails inline <Link>s get onClick={trackClick}; RestaurantCard gained an OPTIONAL onClick?:()=>void prop (fires on its internal <Link>, backward-compatible for all other consumers) passed trackClick. BACKWARD COMPAT: purely additive web UI + one optional component prop; no endpoint/DB/route/validation changes; content_metrics insert shape unchanged. VERIFIED: npx eslint on all 5 edited files = 0 errors (only pre-existing repo-wide unused-import warnings); npx vite build succeeds (33s); npm run type-check exit 0; targeted unmasked tsc (temp tsconfig, edited files only) shows ZERO diagnostics in my code — the lone EventDetails:313 `event.date.toISOString()`-on-never is PRE-EXISTING untouched code (the documented relaxed-type 'newest-sort Date bug' artifact), not in my diff. Files: src/components/LastUpdatedBadge.tsx, src/components/RestaurantCard.tsx, src/pages/EventDetails.tsx, src/pages/RestaurantDetails.tsx, src/pages/AttractionDetails.tsx."
    },
    {
      "id": "WEB-UX-005",
      "title": "Keyboard accessibility pass: autocomplete, card actions, focus management",
      "description": "As a keyboard/screen-reader user, I need search suggestions, card secondary actions, and sheet/dialog flows to be fully operable.",
      "acceptanceCriteria": [
        "SearchAutocomplete supports ArrowUp/Down + Enter + Escape with aria-activedescendant and announced selection",
        "Card secondary actions (favorite, share) are tabbable with explicit aria-labels ('Save {name}'), reachable without entering the card link",
        "Sheets/dialogs restore focus to their trigger on close across list filter sheets and quick-view modals",
        "Playwright a11y suite (axe) passes on /, /events, /restaurants, one detail page; new keyboard-path test added",
        "npm run validate passes"
      ],
      "priority": 34,
      "passes": true,
      "notes": "DONE (2026-06-13): Keyboard a11y pass across autocomplete, card actions, and detail-page axe. (1) SearchAutocomplete already had ArrowUp/Down+Enter+Escape with role=listbox/option+aria-selected — ADDED the missing combobox wiring: a useId-derived stable listboxId + per-option ids; imperative effects set role=combobox / aria-autocomplete=list / aria-haspopup=listbox / aria-controls / aria-expanded on the EXTERNAL input (it's passed via inputRef, owned by the page), and aria-activedescendant pointing at the highlighted option's id so the selection is announced (the canonical ARIA-1.2 combobox announcement mechanism). Attributes are cleaned up on unmount/close. (2) THE REAL FIX — SocialEventCard wrapped the ENTIRE card (image+content+FavoriteButton+ShareDialog) in one <Link>, nesting two interactive controls inside an <a> (axe nested-interactive, serious). Restructured to the stretched-link pattern: the <Link> is now an absolute inset-0 z-20 overlay sibling (sr-only label) covering the card for navigation, and the favorite/share cluster is a sibling at relative z-30 (above the link) — so the actions are tabbable and reachable WITHOUT entering the card link, and the whole card still navigates. (3) FavoriteButton gained an optional itemName prop → aria-label 'Save {name}' / 'Remove {name} from favorites' (+ aria-pressed=favorited); ShareDialog gained itemName → default trigger aria-label 'Share {name}'. Wired itemName=event.title in SocialEventCard AND EventCard (EventCard actions were already link-siblings, just relabeled for consistency). RestaurantCard has NO favorite/share actions (it's a single Link) so criterion-2 doesn't apply there — not changed. (4) Focus restoration (criterion 3): Radix Dialog (ShareDialog) + Sheet restore focus to trigger by default; Attractions filter Sheet has an explicit onCloseAutoFocus from WEB-UX-003 — no regression, verified. (5) tests/accessibility.spec.ts: new 'WEB-UX-005' describe — a keyboard-path test (seeds recent searches via addInitScript, focuses the events search, asserts combobox role+aria-expanded, ArrowDown sets aria-activedescendant→an aria-selected option, Escape collapses), a card-action test (a Save-labelled button exists, is NOT nested in an <a>, and is focusable), and an axe scan on a real event DETAIL page (clicks the first 'View details for' card link); the existing axe suite already covers / /events /restaurants. Data-dependent tests test.skip when no cards render. VERIFIED: npm run type-check passes; npx vite build succeeds (31.8s); eslint on the 5 edited components = 0 errors (only pre-existing repo-wide warnings); temp-tsconfig tsc shows zero errors referencing the edited files (only the known pre-existing import.meta.env / hook relaxed-type errors in the closure). Original evidence: SearchAutocomplete lacked aria-activedescendant; SocialEventCard nested actions in the card anchor."
    },
    {
      "id": "WEB-UX-006",
      "title": "Centralize category/cuisine colors into theme tokens (dark-mode complete)",
      "description": "As a user in dark mode, I need consistent, readable category colors; as a developer, one source of truth instead of per-component hardcoded gradients.",
      "acceptanceCriteria": [
        "A single CATEGORY_STYLES map (or CSS custom properties) replaces hardcoded gradient/color literals in RestaurantCard (~47-64), SocialEventCard (~52-59), and any sibling components found by grep",
        "Every token defines light + dark values; contrast >= 4.5:1 for text-bearing badges in both modes",
        "Visual-regression spec updated/passes for card surfaces",
        "npm run validate passes"
      ],
      "priority": 35,
      "passes": true,
      "notes": "DONE (2026-06-13): Centralized all card category/cuisine color literals into the EXISTING src/lib/categoryColors.ts (which already held CATEGORY_COLORS/getCategoryHex consumed by InteractiveMap — left untouched). Added: (a) CategoryStyle interface + EVENT_CATEGORY_STYLES map (13 buckets) + getEventCategoryStyle() fuzzy keyword matcher (replaces the per-component getCategoryStyle/getCategoryColor in SocialEventCard ~52-59 and EventCard ~85-92), (b) CUISINE_GRADIENTS + getCuisineGradient() (replaces RestaurantCard's local cuisineGradients/getGradient ~50-75), (c) BRAND_GRADIENT constant (dedupes the from-[#2D1B69] to-[#DC143C] literal in RestaurantCard's hover CTA + the cuisine default). Each CategoryStyle token: solid (mode-independent badge bg) + onSolid (paired text) + text (accent text WITH dark: variant) + iconBg (tint WITH dark: variant) + gradient (decorative fallback header). CONTRAST: solid badge shades are deep 600/700 chosen so white-on-solid AND accent-text-on-white both clear WCAG AA >=4.5:1; dark accents use the -400/-300 shade for the dark card (--card 225 50% 8%). Proven in a new vitest unit test src/lib/__tests__/categoryColors.test.ts (47 tests, all green) that computes real WCAG contrast from each Tailwind shade's hex against white card / dark card / its own solid fill — covers every bucket in BOTH modes (criterion 2). Card components refactored: EventCard badge -> `${solid} ${onSolid}`; SocialEventCard date-strip + category badge -> `${solid} ${onSolid}`, icon chip -> iconBg, 'View Details' accent -> text (now dark-mode aware); RestaurantCard -> getCuisineGradient + BRAND_GRADIENT. Criterion 3: added a 'Card Surfaces - Centralized Category Colors' describe to tests/visual-regression.spec.ts asserting no badge renders transparent text after a light->dark colorScheme flip on /events (data-tolerant test.skip when no cards). SCOPE NOTE: EventSmartPresets/RestaurantSmartPresets gradients were intentionally NOT folded in — they are curated quick-filter CHIP palettes (mix of time-based 'This Weekend' + category presets), not a duplication of the card category/cuisine badge logic the story targets ('card surfaces'); mapping them would change their distinct look. BACKWARD COMPAT: purely additive web styling; no DB/endpoint/route/validation changes; existing categoryColors exports (CATEGORY_COLORS/getCategoryColor/getCategoryHex) unchanged so InteractiveMap is unaffected. Minor visual shift: EventCard music badge purple-500->violet-600 etc. (now consistent with SocialEventCard + AA-compliant). VERIFIED: npx vitest run (47/47 pass); npm run type-check exit 0; eslint on all 5 edited/new files + the spec = 0 errors (only pre-existing repo-wide warnings: unused Sparkles/createSlug/Button/ImageOff, any-types); npx vite build succeeds (32.6s) — Tailwind picks up the new class literals from categoryColors.ts. Playwright screenshot baselines not run in-container (needs dev server + browsers, same limitation noted on prior WEB-UX/PERF stories). Original evidence: RestaurantCard cuisineGradients ~50-67; SocialEventCard getCategoryStyle ~50-60; EventCard getCategoryColor ~85-92."
    },
    {
      "id": "WEB-FEAT-001",
      "title": "Contextual paywall modal with per-feature copy (web parity with iOS)",
      "description": "As the business, I need the web paywall to appear at the moment of feature desire with tailored copy — iOS ships PaywallView with 8 contexts and measurable per-surface conversion; web shows a generic gate.",
      "acceptanceCriteria": [
        "Reusable PaywallModal accepting a context (id, headline, benefit bullets, recommended tier) with contexts mirroring iOS: unlimited favorites, trip planner, saved searches, custom alerts, advanced filters, ad-free, insider/dining tips",
        "PremiumGate and feature-gate call sites present the contextual modal instead of generic copy/redirect to /pricing; checkout proceeds via the existing Stripe flow",
        "Favorites cap (3 free) enforced on web with the unlimited-favorites context on the 4th save attempt",
        "Every present/dismiss/checkout-start event logged with context id (existing analytics path) for per-surface conversion measurement",
        "Modal is accessible (focus trap, Escape, focus restore) and mobile-friendly",
        "npm run validate passes"
      ],
      "priority": 36,
      "passes": true,
      "notes": "DONE (2026-06-13): Contextual paywall delivered as 3 new modules + 3 refactors. NEW src/lib/paywallContexts.ts = the copy catalog mirroring iOS PaywallView (IOS-SUB-010/011): PAYWALL_CONTEXTS keyed by the canonical useSubscription hasFeature() ids (unlimited_favorites, trip_planner, insider_tips, dining_tips, write_reviews, save_searches, create_alerts, advanced_filters, ad_free, early_access, daily_digest + 4 VIP contexts) each with icon/headline/subheadline/benefits[]/recommendedTier; GENERIC_PAYWALL_CONTEXT fallback (iOS 'onboarding' promise); resolvePaywallContext(idOrFeature) maps iOS-flavoured aliases (saved_searches->save_searches, custom_alerts->create_alerts, onboarding->generic, etc.) so either platform's spelling resolves. NEW src/lib/paywallAnalytics.ts trackPaywallEvent(action,context,tier,extra?) routes present/dismiss/checkout_start through the existing gtag/GA4 path (same mechanism as analytics-tracker.sendToAnalytics) tagged with the context id for per-surface conversion — web twin of iOS analytics.trackPaywall*; best-effort, never throws. NEW src/components/PaywallModal.tsx = the reusable contextual modal (Radix Dialog → focus trap+Escape+focus-restore for free; max-h-[90vh] overflow-y-auto + responsive grid = mobile-friendly): contextual icon/headline/subheadline + benefit bullets, Insider/VIP plan picker (defaults to recommendedTier, aria-pressed), CTA → /pricing (existing Stripe flow via the Pricing page); logs 'present' on open, 'checkout_start' on CTA (a ref suppresses the paired dismiss), 'dismiss' on any other close. REFACTOR: UpgradeModal.tsx is now a thin back-compat wrapper that maps its legacy `feature` prop → PaywallModal `context`, so ALL existing gate call sites (PremiumGate lock/blur/inline + PremiumButton in TripPlanner/RatingSystem/AdvancedSearchFilters, and useUpgradeModal) present the contextual modal with ZERO call-site churn; useUpgradeModal preserved. FAVORITES CAP: useFavorites.toggleFavorite already enforced the 3-free cap via canPerformAction and returns {needsUpgrade}; removed its now-redundant destructive toast (modal is the single messaging surface) and FavoriteButton (the only add path) now opens PaywallModal context='unlimited_favorites' on the 4th save instead of wrongly toasting success (fixed a real bug: it previously toasted 'Added to favorites' even when the add was capped/failed). Tier mapping: insider $4.99 / vip $12.99, recommendedTier per context. Verified: npx eslint on all 6 files = 0 errors (only a benign pre-existing react-refresh warning on UpgradeModal exporting both the component + useUpgradeModal hook); temp-tsconfig tsc on my files = 0 errors (the 3 reported are import.meta.env typing noise in client.ts/logger.ts from the narrow include, not my code); npx vite build succeeds (33s). Original evidence: PremiumGate/UpgradeModal showed generic copy; FavoriteButton ignored needsUpgrade."
    },
    {
      "id": "WEB-FEAT-002",
      "title": "Annual plans + free trial on web (Stripe)",
      "description": "As a user, I want a cheaper annual option and a low-risk trial on web — iOS already sells Insider $49.99/yr and VIP $129.99/yr with a 7-day trial; web only offers monthly.",
      "acceptanceCriteria": [
        "Pricing page and PaywallModal show a monthly/annual toggle with computed 'Save N%' badge (Insider $49.99/yr, VIP $129.99/yr to match iOS)",
        "Checkout session supports the annual price IDs; scripts/setup-stripe-products.js extended to create them idempotently (document the one-time run in notes as ACTION REQUIRED if env keys absent)",
        "7-day trial configured via Stripe trial_period_days for first-time subscribers; subscription status UI shows trial state and renewal date; legally-required renewal copy present",
        "stripe-webhook handles trialing→active transitions; user_subscriptions tier resolves correctly during trial (matches the cross-platform resolution iOS relies on)",
        "Tier parity preserved: web annual subscribers sync to mobile via the existing user_subscriptions mechanism",
        "npm run validate passes"
      ],
      "priority": 37,
      "passes": true,
      "notes": "DONE (2026-06-13): Most of WEB-FEAT-002 was ALREADY shipped by prior subscription work — the remaining gap was the PaywallModal annual toggle. Closed it + verified the rest end-to-end. WHAT WAS ALREADY THERE (verified, untouched): create-subscription-checkout accepts billingInterval 'monthly'|'yearly' and uses plan.stripe_price_id_yearly/_monthly from subscription_plans, with trial_period_days:7 for first-time subscribers only (existingSubscription ? undefined : 7); scripts/setup-stripe-products.js already creates Insider $49.99/yr + VIP $129.99/yr prices idempotently (find-or-create on tier metadata / amount+currency+interval — the uncommitted working-tree change just hardened that idempotency, now committed); stripe-webhook mapStripeStatus maps trialing->trialing and customer.subscription.updated handles trialing->active transitions, writing trial_start/trial_end + current_period_end to user_subscriptions (platform='web'); useSubscription treats trialing as entitled (isSubscriptionEntitled) and resolves the highest tier across web/iOS/Android rows (mobile parity); SubscriptionPortal shows a 'Trial' status badge + renewal date; Pricing page has the monthly/annual Switch + 'Save 17%' badge + a ROSCA/FTC pre-purchase auto-renewal disclosure. WHAT I CHANGED: (1) src/components/PaywallModal.tsx — replaced the monthly-only PLAN_PRICE map with PLAN_PRICING {insider:{monthly 4.99,yearly 49.99},vip:{monthly 12.99,yearly 129.99}} + formatPrice + yearlySavingsPercent(tier) (computes round((monthly*12 - yearly)/(monthly*12)*100) = 17% both tiers); added a billingInterval useState (resets to 'monthly' on open) + a segmented Monthly|Yearly toggle (role=group, aria-pressed, 'Save N%' Badge on Yearly); plan cards now show the interval-appropriate price with /yr|/mo suffix; CTA Link now carries the choice as /pricing?plan=<tier>&billing=<interval>; checkout_start analytics now tags {interval} for funnel measurement. (2) src/pages/Pricing.tsx — isYearly now lazy-inits from searchParams.get('billing')==='yearly' so the modal's interval choice survives the handoff to the checkout page (no behavior change when the param is absent). DESIGN: kept the modal's existing hand-off-to-/pricing pattern (the Pricing page owns the actual startCheckout call, auth checks, and Stripe-not-configured fallback) rather than duplicating checkout logic in the modal — minimal, additive, money-path untouched. ACTION REQUIRED (one-time, only if not already run for this Stripe account): run `node scripts/setup-stripe-products.js` (test) / `--live` (prod) with STRIPE_SECRET_KEY set, then run the emitted scripts/update-stripe-price-ids.sql in Supabase to populate subscription_plans.stripe_price_id_yearly — until that runs the yearly CTA shows the existing 'Online subscription is being set up' toast (graceful, no error). Verified: npm run type-check passes; npx eslint PaywallModal.tsx + Pricing.tsx = 0 errors (only pre-existing unused-import warnings); npx vite build succeeds (50s). Backward compat: no edge-fn/webhook/DB shape changes; iOS unaffected (its own StoreKit prices). Original evidence: PaywallModal PLAN_PRICE monthly-only; create-subscription-checkout already had billingInterval + trial_period_days."
    },
    {
      "id": "WEB-FEAT-003",
      "title": "Saved searches + alerts with automated nightly digest (promised, unbuilt)",
      "description": "As an Insider subscriber, I was promised 'saved searches & event alerts' on the pricing page, but the web has no way to save a search or receive alerts — a broken product promise and a retention loop left on the table.",
      "acceptanceCriteria": [
        "Search/list pages get a 'Save this search' affordance (free users see the contextual paywall via WEB-FEAT-001; Insider 10 / VIP unlimited limits enforced server-side)",
        "Dashboard tab lists saved searches with an alerts on/off toggle and delete; saved_searches table additive with RLS owner-only",
        "Nightly job (WEB-AUTO-001 jobRunner): runs active saved searches against events created since last run, groups per user, sends ONE consolidated email via existing email plumbing with deep links back to the filtered URL (WEB-UX-001 URL-state makes the links possible)",
        "Email respects unsubscribe/notification preferences; zero matches = no email",
        "Per-tier limits and alert behavior logged for funnel analysis",
        "npm run validate passes"
      ],
      "priority": 38,
      "passes": true,
      "notes": "DONE (2026-06-13): Saved searches + alerts + nightly digest, built on the EXISTING saved_searches table (owner-only RLS already present from 20250819002500; iOS PARITY-008 already stores {query,tab,alerts_enabled} in filters JSONB — kept that exact shape for cross-platform compat). ADDITIVE migration 20260613000000: (a) last_alerted_at watermark column + partial index on filters->>alerts_enabled; (b) SERVER-SIDE per-tier limit via a SECURITY DEFINER BEFORE INSERT trigger enforce_saved_search_limit() resolving the cap from subscription_plans.limits->>saved_searches (free=0, insider=10, vip=-1/unlimited) — bypasses on service-role (auth.uid() NULL), RAISEs check_violation when over cap, so the limit holds for web AND the native apps; (c) schedules the nightly cron. BACKWARD-COMPAT verified: iOS SavedSearchUI already client-gates free users to the paywall (never inserts) and tolerates a .limitReached/.failure outcome, so the trigger breaks no shipped binary. AFFORDANCE: new self-contained <SaveSearchButton> (name dialog + PaywallModal context=save_searches + toast + funnel analytics) dropped into EventsPage (hero action row) and Restaurants (sort cluster); signed-out → /auth, free (limit 0) → contextual paywall, insider/vip → save. Captures the live useSearchParams() string so the saved search deep-links back to the exact filtered view (WEB-UX-001). DASHBOARD: new 'Saved Searches' tab (SavedSearchesCard) lists searches with a per-search alerts Switch, an Open deep-link, and delete; free users see an upgrade prompt; deep-link via ?tab=saved-searches. NIGHTLY JOB: new saved-search-alerts edge fn (cron saved-search-alerts-nightly 13:00 UTC, requireAdminOrApiKey-gated, NOT in config.toml — data-quality-heal precedent) wrapped in the WEB-AUTO-001 jobRunner. For each alert-enabled search it matches upcoming events created since last_alerted_at (24h first-run lookback) on the saved query (sanitized ilike title/venue/location) + category, dedupes across a user's searches, groups per user, and sends ONE consolidated marketing email (renderEmail + List-Unsubscribe headers; deep links to each event [central-time slug] + a 'see all results' link to the filtered list). Respects unsubscribe (newsletter_subscribers.status='unsubscribed' → skipped) and the per-search toggle; zero matches → no email; per-search watermark advanced only on success (rolled back on send failure so it retries). FUNNEL: client gtag events (save_prompt/saved/paywall/limit_reached/alert_toggled/deleted, tagged with tier) via new src/lib/savedSearchAnalytics.ts + job metadata perTier email counts. types.ts: last_alerted_at hand-added to saved_searches Row/Insert/Update; useSavedSearches casts filters via Json. Added saved-search-alerts to JobHealthPanel RERUNNABLE + AUTOMATION_JOBS.md. NOTE: the legacy useAdvancedSearch.saveSearch insert path now also hits the trigger (correctly blocks over-limit) — left untouched to avoid scope creep. Verified: npm run type-check passes; targeted tsc (temp tsconfig) on the 4 new TS files = 0 errors (only pre-existing import.meta.env narrow-include noise); npx eslint all touched files = 0 errors; esbuild parse of the edge fn clean; npx vite build succeeds (33s). Migration not applied in-container (no Supabase) — additive/idempotent."
    },
    {
      "id": "WEB-FEAT-004",
      "title": "First-party campaign ad serving on web with house-ad fill",
      "description": "As an advertiser, my paid campaign should render on the web (it already renders on iOS); as the business, unsold inventory should sell subscriptions instead of showing nothing.",
      "acceptanceCriteria": [
        "Web AdBanner fetches creatives via the existing get_active_ads(placement_type) RPC (same source as iOS CampaignAdService) for top_banner / below_fold / featured_spot placements, cached ~5min",
        "Fallback chain campaign → affiliate → house ad (rotating 3 messages, CTA opens contextual paywall/pricing) so a slot never renders empty; FTC 'Ad' label on everything; Insider/VIP see zero ads",
        "Impression/click tracking writes to the same ad_impressions/ad_clicks tables as iOS with viewability rules matching src/lib/tracking.ts thresholds (>=50% visible >=1s, deduped per session)",
        "House vs paid vs affiliate distinguished in tracking payloads so fill rate is measurable",
        "Lazy-loaded below the fold (no LCP regression — coordinate with WEB-PERF-003)",
        "npm run validate passes"
      ],
      "priority": 39,
      "passes": true,
      "notes": "DONE (2026-06-13): Web AdBanner now serves the full first-party fill chain paid campaign -> affiliate -> house ad so a slot NEVER renders empty (previously AffiliateAdBanner returned null with no partner -> empty slot). (1) get_active_ads RPC is now CACHED ~5min — useActiveAds rewritten onto TanStack Query (queryKey ['active-ad', placement], staleTime+gcTime 5min) so a paid creative persists for the visit instead of re-hitting the RPC per mount/nav. (2) New HouseAd.tsx: three rotating upgrade messages (ad_free / unlimited_favorites / trip_planner), picked once per mount, CTA opens the contextual PaywallModal at the matching seam; FTC 'Ad' label + role=complementary. (3) AdBanner restructured into outer (subscription + lazy gate) + AdBannerContent (chain): paid campaign keeps the existing ad_impressions/ad_clicks tracking shared with iOS (useAdTracking, >=50% visible >=1s viewability via createViewabilityObserver, deduped per session); affiliate/house fills can't write those tables (campaign_id/creative_id are NOT-NULL FKs to campaigns), so fill rate is measured via NEW GA4 events trackAdFill/trackAdFillClick (src/lib/tracking.ts) tagged inventory: 'paid'|'affiliate'|'house' + placement (AC4 'distinguished in tracking payloads'). AffiliateAdBanner gained an onClick prop so affiliate clicks fire trackAdFillClick('affiliate'). (4) LAZY below the fold: top_banner renders immediately (above fold); featured_spot/below_fold/sidebar are wrapped in RevealOnVisible (IntersectionObserver, 200px rootMargin, height-reserving placeholder = no CLS) so the RPC fetch + render only fire on near-visibility -> no LCP regression (coordinates with WEB-PERF-003). FTC 'Ad' label on all three sources; Insider/VIP (hasFeature('ad_free')) still see zero ads (checked before any mount/observer). BACKWARD COMPAT: get_active_ads RPC + ad_impressions/ad_clicks shapes untouched (shared with iOS CampaignAdService); no migration needed (GA4 fill tracking is client-only, no new tables). Verified: npm run type-check passes; eslint on all 5 edited/new files = 0 errors (only 3 PRE-EXISTING warnings in tracking.ts); npx vite build succeeds (30.5s, AdBanner+HouseAd chunked). Original evidence: AdBanner showed one campaign-or-affiliate creative, useActiveAds uncached, no house fill, no fill-rate tracking."
    },
    {
      "id": "WEB-FEAT-005",
      "title": "Sponsored/featured listings inline in web browse lists",
      "description": "As an advertiser, my sponsored listing should be visibly boosted in web browse flows like it is in the iOS rails — clearly labeled, never disguised.",
      "acceptanceCriteria": [
        "Events/Restaurants/Attractions list queries surface is_sponsored items at the top positions (cap 2) with an amber ring + 'Sponsored' badge matching the iOS treatment; organic order otherwise untouched",
        "Sponsored impressions/clicks tracked distinctly (sponsored_listing inventory class) via the shared tracking lib",
        "Shown to all tiers (content, not interruptive ads); VoiceOver/screen-reader text prefixes 'Sponsored'",
        "Expired sponsorships (sponsored_until past) automatically lose the treatment with no manual cleanup",
        "npm run validate passes"
      ],
      "priority": 40,
      "passes": true,
      "notes": "DONE (2026-06-13): Web twin of IOS-ADS-011. NEW src/lib/sponsoredListings.ts is the shared core: isActivelySponsored(item) (is_sponsored===true AND sponsored_until absent-or-future — expiry-aware, fails toward labeling on an unparseable date so a disclosure is never accidentally hidden) + promoteSponsored(items, max=MAX_SPONSORED_AT_FRONT=2) which floats up to 2 actively-sponsored items to the front, preserves organic order for everything else, and returns the SAME array reference when nothing moves (no memo churn). MAX_SPONSORED_AT_FRONT=2 mirrors iOS AdConfig.maxSponsoredAtFront. CARD TREATMENT (amber ring + FTC-compliant <SponsoredBadge/> matching iOS): EventCard + RestaurantCard already rendered the badge/ring off raw is_sponsored — switched them to the expiry-aware `sponsored` so a lapsed window auto-loses the treatment with no cleanup (criterion 4). SocialEventCard (the card actually used on /events) had NO sponsored treatment — added ring + badge + sponsored-prefixed aria-label. Attractions browse card was inline JSX in the .map (can't host a hook) → extracted AttractionListCard so it can run the tracking hook. TRACKING (criterion 2): extended AdInventoryClass with 'sponsored_listing' + added trackSponsoredListingImpression/Click(itemType,itemId) to the shared src/lib/tracking.ts (GA4 sponsored_listing_impression/_click, inventory:'sponsored_listing' — distinct from paid/house/affiliate; first-party content so it does NOT touch the ad_impressions FK tables). NEW src/hooks/useSponsoredTracking(active,type,id) returns a ref (viewability impression via the existing createViewabilityObserver 50%/1s, fires once, only when active) + a trackClick that no-ops unless active; wired into all 4 card types (ref on the card root, trackClick on the nav click). REORDER (criterion 1): promoteSponsored applied at the browse-list level AFTER sort/filter but BEFORE pagination so the top-2 sponsored land on page 1 — EventsPage (`events` memo; its inline query gained is_sponsored,sponsored_until in the select), Restaurants (new orderedRestaurants memo feeding pagination; useRestaurants already selects the cols + get_rotated_restaurants returns them), Attractions (sortedAttractions memo). ALL TIERS (criterion 3): treatment is on the content cards, no useSubscription gate — shown to free/insider/vip alike; sponsored disclosure is real visible text + aria-label prefix. ATTRACTIONS COLUMNS: the table lacked is_sponsored/sponsored_until (events/restaurants already had them) → additive idempotent migration 20260613000001 (ADD COLUMN is_sponsored boolean NOT NULL DEFAULT false + sponsored_until timestamptz + partial index WHERE is_sponsored=true; defaults keep old web/mobile readers unaffected per CLAUDE.md), hand-added to attractions Row/Insert/Update in types.ts and to ATTRACTION_LIST_COLUMNS. EVENT_LIST_COLUMNS in useEvents also gained the two cols (EventCard read is_sponsored but the projected list never fetched it — was dead). Verified: npm run type-check passes (only the pre-existing VideoPlayer.tsx:587 parser quirk under tsc -p tsconfig.app.json, untouched); targeted eslint on all 11 changed files = 0 errors (repo-wide no-console/no-explicit-any/exhaustive-deps + pre-existing Attractions unused-import warnings only); npx vite build succeeds (36s). Migration not applied in-container (no Supabase) — additive/idempotent. Original evidence: events/restaurants carried the flags but no web surface boosted/ordered by them; SocialEventCard (the /events card) ignored is_sponsored entirely."
    },
    {
      "id": "WEB-FEAT-006",
      "title": "Guest favorites with signup migration (kill the save-wall)",
      "description": "As an anonymous visitor, I want to save a few favorites before being asked to sign up; as the business, visitors who invest first convert dramatically better than those hitting an auth wall.",
      "acceptanceCriteria": [
        "Unauthenticated users can favorite up to 3 items, stored via @/lib/safeStorage (never raw localStorage), surviving reloads",
        "Guest cap reached → contextual signup prompt ('Sign up free to keep saving') — not the paywall; favorites UI shows guest count (1/3)",
        "On signup/login, guest favorites migrate to the user's server favorites automatically (idempotent, deduped) and the local copy clears",
        "Dashboard/welcome surface acknowledges the migrated favorites",
        "Conversion events logged (guest-save, wall-hit, signup-from-wall) for funnel measurement",
        "npm run validate passes"
      ],
      "priority": 41,
      "passes": true,
      "notes": "DONE (2026-06-13): Guest favorites + signup migration kills the save-wall. NEW src/lib/guestFavorites.ts is a versioned (key 'guest_favorites_v1', per CLAUDE.md on-disk-state rules) safeStorage-backed external store (subscribe/getSnapshot for useSyncExternalStore — in-memory `current` array is the stable snapshot ref) for guest EVENT favorites, cap GUEST_FAVORITES_CAP=3 (matches the free tier); de-dupes+caps on read so a tampered value can't blow past the cap. Scope is events: FavoriteButton (the only toggle save surface) is event-only (eventId); restaurants/attractions have no guest-save button. useFavorites.ts now branches on auth: signed-in → server (user_event_interactions) unchanged; guest → the store. toggleFavorite returns ToggleFavoriteResult {success, needsUpgrade, needsSignup} — guest add at cap → needsSignup (no up-front auth wall), else saves locally; removing always allowed; exposes isGuest/guestCount/guestCap and guest-aware canAddFavorite/remainingFavorites/favoritesLimit. FavoriteButton: needsSignup opens NEW GuestSignupPrompt.tsx (Radix Dialog, 'Sign up free to keep saving', NOT the paywall — free account to persist/sync saves; CTA → /auth?mode=signup, tracks signup_from_wall); guest save toast shows the running count 'Saved N/3 — sign up free to keep saving'. MIGRATION: NEW GuestFavoriteMigrator.tsx (mounted once in App.tsx under AuthProvider, renders null) watches user; on login/signup with guest favorites it fetches existing server rows for those ids (deduped), inserts only the missing ones (idempotent), clears the local copy, records a one-shot migration notice, invalidates ['favorites', userId], tracks 'migrated'; a migratedForUserRef guards re-runs across token-refresh re-renders; ANY failure resets the ref and KEEPS the local copy so a later session retries. DASHBOARD ACK: UserDashboard consumes consumeMigratedFavoritesNotice() once on mount → toast 'We saved your N favorites to your account'. CONVERSION EVENTS: NEW src/lib/guestFavoritesAnalytics.ts trackGuestFavoriteEvent (GA4/gtag, best-effort) fires guest_save / wall_hit / signup_from_wall / migrated. BACKWARD COMPAT: no schema/edge changes; reads/writes only the existing user_event_interactions table; safeStorage key is new+versioned. Verified: npx tsc -p tsconfig.app.json clean for all new/edited files (only the PRE-EXISTING VideoPlayer.tsx:587 parser error remains, untouched); eslint 0 errors on all 8 files (only pre-existing repo-wide unused-import/any warnings); npx vite build succeeds (32.7s). Original evidence: useFavorites.toggleFavorite required a user ('Login Required' toast for guests) — no anonymous save path existed."
    },
    {
      "id": "WEB-FEAT-007",
      "title": "Recently-viewed rail + personalized home section ordering",
      "description": "As a returning user, the homepage should reflect what I actually do here — quick resume of recently-viewed items and section ordering driven by my engagement, not a fixed layout.",
      "acceptanceCriteria": [
        "Detail-page views record {id, type, timestamp} (signed-in: server table with RLS; guest: safeStorage), capped at 20, pruned > 30 days",
        "Homepage gains a 'Recently viewed' rail (appears only when >= 3 entries); Dashboard gets the full list with remove",
        "Home sections render from a data-driven order: users with dominant restaurant engagement see dining-first, etc.; guests/no-signal users keep the canonical order (deterministic, no layout flicker)",
        "Personalized-cohort flag logged so conversion vs control is measurable",
        "No CLS from personalization (order computed before first paint of the section list)",
        "npm run validate passes"
      ],
      "priority": 42,
      "passes": true,
      "notes": "DONE (2026-06-13): Recently-viewed rail + data-driven home ordering. NEW src/lib/recentlyViewed.ts is a versioned safeStorage external store (key 'recently_viewed_v1', subscribe/getSnapshot for useSyncExternalStore — mirrors guestFavorites) holding multi-type entries {id,type,title,image_url,subtitle,date,path,viewedAt} for event/restaurant/attraction; recordView de-dupes by id+type, caps at 20 (RECENTLY_VIEWED_CAP) and PRUNES >30 days on every read (RECENTLY_VIEWED_MAX_AGE_MS); stores a precomputed canonical detail `path` so links always resolve. Rewrote src/hooks/useRecentlyViewed.ts on top of the store (useSyncExternalStore): keeps the back-compat API (recentlyViewed/addToRecentlyViewed(event)/clearRecentlyViewed/removeFromRecentlyViewed) so existing consumers (EventCard write, useEnhancedRecommendations read) are untouched, adds recordView, and for SIGNED-IN users write-throughs to a NEW `recently_viewed` server table (migration 20260613000002 — owner-only RLS, additive/idempotent; NOT in generated types → `as never` casts) and merges the server copy into the local store ONCE per user on sign-in (module-level syncedUserId guard so the many EventCard hook instances don't each fetch; resets on failure to retry). Views are recorded from EACH detail page (EventDetails/RestaurantDetails/AttractionDetails) via a recordedViewRef once-guard (so re-renders/full-row upgrades don't re-record). RAIL: rewrote src/components/RecentlyViewed.tsx into a horizontal multi-type rail (variant='rail', renders only when entries >= minEntries default 3) + a dashboard list (variant='list', per-item remove + Clear All + empty hint); type icon/badge per content type. Homepage Index.tsx renders the rail only when >=3 (hasRecentlyViewed) and the default-view content sections now render in a DATA-DRIVEN order. ORDERING ENGINE: NEW src/lib/homeSectionOrder.ts — orderHomeSections() is pure + deterministic (testable, no flicker): it permutes ONLY typed sections by affinity (favorites*2 + recent views, ranked desc with canonical tie-break) and PINS neutral sections at their canonical index; isPersonalizedOrder/cohortFor return 'control' for guests/no-signal (canonical order unchanged) vs 'personalized'. NEW src/hooks/useHomeSectionOrder.ts reads signals SYNCHRONOUSLY from the recently-viewed + guest-favorites stores (so order is known before first paint → NO CLS) and logs the served cohort once per mount via NEW src/lib/personalizationAnalytics.ts (gtag 'home_order_cohort', mirrors guestFavoritesAnalytics). Index tags the recently-viewed rail with the user's dominantRecentType so a dining-dominant visitor sees their dining-heavy rail LEAD above event recommendations; event-dominant/guest users keep recommendations-first. DASHBOARD: UserDashboard Overview tab gains a Recently Viewed card (RecentlyViewed variant='list' limit 20). Cohort flag logged for control-vs-personalized measurement. Unit-tested: src/lib/__tests__/homeSectionOrder.test.ts (5 tests — canonical/guest, restaurant-dominant-leads, event-dominant-stable, neutral-pinned, determinism) all pass. BACKWARD COMPAT: new versioned safeStorage key; new server table + owner RLS only (no edge/schema changes to existing surfaces); existing useRecentlyViewed consumers' API preserved. Verified: npm run type-check passes; npx tsc -p tsconfig.app.json clean for all new/edited files (only the PRE-EXISTING VideoPlayer.tsx:587 parser error remains, untouched); eslint 0 errors on all 11 files (only pre-existing repo-wide unused-var warnings); vitest 5/5 pass; npx vite build succeeds (45.8s). Migration not applied in-container (no Supabase) — written additive/idempotent. Original evidence: useRecentlyViewed was event-only (MAX 10, no type/30d-prune/server table); RecentlyViewed was a vertical event list; home sections were a fixed layout."
    },
    {
      "id": "WEB-FEAT-008",
      "title": "Dynamic OG-image generation for social shares",
      "description": "As the business, shared event/restaurant links should render rich branded preview cards (title, date, image) instead of a generic site image — direct social CTR lift with zero ongoing admin work.",
      "acceptanceCriteria": [
        "Edge function og-image/:type/:id renders a branded PNG (Satori or canvas: gradient background, truncated title, date/rating badge, thumbnail) for events/restaurants/attractions/articles",
        "Detail pages' og:image meta points at the function URL; prerender (scripts/prerender.mjs) emits the correct tags for crawler-visible HTML",
        "Responses cached aggressively (immutable for past events, ~1h for upcoming); function rate-limited per WEB-SEC patterns",
        "Validated with Twitter/Facebook preview debuggers (record screenshots in PR/progress notes)",
        "npm run validate passes"
      ],
      "priority": 43,
      "passes": false,
      "notes": "Self-running marketing infrastructure — fits the autonomy theme. Check how prerender.mjs currently writes meta tags before wiring."
    },
    {
      "id": "WEB-FEAT-009",
      "title": "Map-first discovery: bounds-aware filtering on DiscoverMap",
      "description": "As a high-intent user, I want to pan the map and see what's in view — the existing DiscoverMap renders markers but supports no spatial interaction.",
      "acceptanceCriteria": [
        "Map moveend captures bounds; a bounds query (RPC or lat/lng range filters) returns counts + items for events/restaurants/attractions in view",
        "Sidebar/sheet lists in-view results as cards with a live count; layer toggles show/hide each content type's markers",
        "'Search this area' button refreshes results for the current bounds; selected marker highlights its card and vice-versa",
        "Mobile: bottom-sheet pattern, map remains usable; Leaflet stays lazy-loaded (WEB-PERF-003)",
        "npm run validate passes"
      ],
      "priority": 44,
      "passes": false,
      "notes": "Existing DiscoverMap page is the shell; content tables have lat/lng maintained by geocoding triggers (and WEB-AUTO-003 heals gaps)."
    },
    {
      "id": "WEB-FEAT-010",
      "title": "Review composer with photo upload, wired through auto-moderation",
      "description": "As a user, I want to write a rating + review with photos on web; as the owner, all of it flows through the WEB-AUTO-009 moderation pipeline so UGC scales with zero manual review of the clean majority.",
      "acceptanceCriteria": [
        "ReviewComposer (star picker, text, up to 3 photos) on Event/Restaurant/Attraction detail pages; auth-gated with a sign-in prompt for guests",
        "Photos client-resized (< 500KB), uploaded to Supabase Storage with owner-scoped paths/RLS, URLs stored additively on the review row",
        "Submissions route through moderate-content (WEB-AUTO-009): clean publishes instantly, flagged goes to the queue — user sees 'pending review' state honestly",
        "Reviews render with author, relative date, stars, text, photo grid (lightbox); owners can edit/delete their own; report button feeds the moderation queue",
        "npm run validate passes"
      ],
      "priority": 45,
      "passes": false,
      "notes": "Sequenced after WEB-AUTO-009. RatingSystem + PremiumGate(writeReviews) components exist as the starting seam."
    },
    {
      "id": "WEB-FEAT-011",
      "title": "Trip Planner web polish: quota meter, contextual paywall, share, reorder, calendar",
      "description": "As a user, the web Trip Planner should match the native experience: today the form works but lacks the quota meter, contextual upsell, itinerary reorder, share link, and calendar export that iOS ships.",
      "acceptanceCriteria": [
        "Quota meter shows tier usage (Insider 5/mo, VIP unlimited; free sees the trip-planner contextual paywall via WEB-FEAT-001) enforced against the same server-side counts as iOS",
        "Itinerary view: day-by-day sections with reorder (drag or up/down) persisted via the existing trip_plans backend; map preview of stops (lazy Leaflet)",
        "Share generates the public /trips/shared/<code> link with copy + native share; AddToCalendar exports per-day or whole-trip .ics plus Google Calendar deep-link",
        "Loading/error/generation-failure states handled with the WEB-UX-002 patterns",
        "npm run validate passes"
      ],
      "priority": 46,
      "passes": false,
      "notes": "Backend (generate-itinerary, trip_plans, share codes, get_trip_itinerary RPC) all exists from the iOS build — this is pure web UI adoption. src/hooks/useTripPlanner.ts is the seam."
    }
  ]
}
