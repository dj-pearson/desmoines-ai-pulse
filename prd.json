{
  "project": "Des Moines AI Pulse — iOS Layout & Monetization",
  "branchName": "claude/ios-layout-monetization-plan-5IQFu",
  "description": "Bring the native iOS app (DesMoinesInsider, Swift/SwiftUI, currently v1.1.30) to full feature parity with the web/desktop product and maximize revenue across BOTH engines in parallel: (1) consumer subscriptions via StoreKit IAP and (2) local advertising/sponsored inventory (mostly outside Apple's commission). The app already ships a strong native base (6-tab shell, StoreKit 2 + cross-platform entitlement sync, account deletion, privacy manifest, affiliate ads, PremiumGate, Ask Pulse AI / Swipe / Surprise Me, App Clip, security & platform services) — those are recorded here as passes:true baselines. The new work: a content-first Home + Discover-hub information architecture so every web surface is reachable; a unified card/skeleton system; sticky search + persistent filter chips; a contextual paywall with per-feature placement, annual SKUs, trials, and win-back offers; first-party campaign ad serving, sponsored/featured listings, native in-feed + capped interstitial ads, house-ad fill, AI-flow sponsored placements, advertiser self-serve linkout, and ad analytics parity; native builds of Trip Planner, Articles/Guides, Stay, Weekend, Best-Of voting, Music/Sports/Outdoors/Neighborhood hubs, Dashboard, saved searches/alerts, reviews, and Deals; plus iPad adaptivity, personalization, and an accessibility/offline/store-readiness pass. Ralph picks the first story with passes:false.",
  "userStories": [
    {
      "id": "IOS-BASE-NAV-001",
      "title": "Native 6-tab navigation shell (baseline)",
      "description": "As a user, I need a native tab-based shell so the app feels like an iOS app, not a website.",
      "acceptanceCriteria": [
        "MainTabView renders 6 tabs (Home, Dining, Search, Map, Saved, Profile) on iPhone",
        "iPad uses NavigationSplitView sidebar",
        "Glass-morphism translucent bar appearance",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 1,
      "passes": true,
      "notes": "ALREADY SHIPPED: ios/DesMoinesInsider/Views/MainTabView.swift (167 lines). Recorded as the baseline the IOS-IA-* redesign stories build on. NOTE: the redesign in IOS-IA-002 revisits whether 6 flat tabs is the right IA once parity features (Trip Planner, Articles, Stay, hubs) land."
    },
    {
      "id": "IOS-BASE-SUB-001",
      "title": "StoreKit 2 subscription engine + native store UI (baseline)",
      "description": "As a user, I need to buy Insider/VIP inside the iOS app via Apple IAP so purchasing is compliant and frictionless.",
      "acceptanceCriteria": [
        "StoreKitService loads products, runs purchase flow, listens for transactions, verifies entitlements",
        "SubscriptionView uses native SubscriptionStoreView with Restore Purchases + policy links",
        "Insider ($4.99/mo, prod_U4oa7Cpn0bRnuo) and VIP ($12.99/mo, prod_U4oaGFEy12auTx) in group 21957951",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 2,
      "passes": true,
      "notes": "ALREADY SHIPPED: Services/StoreKitService.swift (518 lines) + Views/Subscription/SubscriptionView.swift (198) + Models/AppEnums.swift SubscriptionTier. StoreKit config Resources/Products.storekit wired in project.yml scheme. Deepened by IOS-SUB-010..014."
    },
    {
      "id": "IOS-BASE-SUB-002",
      "title": "Cross-platform subscription entitlement sync (baseline)",
      "description": "As a user who paid on web or Android, I need iOS to honor that tier so I never pay twice.",
      "acceptanceCriteria": [
        "StoreKitService resolves currentTier as max(localTier from StoreKit, backendTier from user_subscriptions)",
        "Cross-platform banner surfaces active web/Android subs with manage links",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 3,
      "passes": true,
      "notes": "ALREADY SHIPPED: StoreKitService backendTier + resolveHighestSubscription; user_subscriptions has platform/apple_* columns (migration 20260327000001). Web=Stripe, Android=Play, iOS=StoreKit all converge."
    },
    {
      "id": "IOS-BASE-SUB-003",
      "title": "Receipt validation + App Store Server Notifications v2 backend (baseline)",
      "description": "As the platform, I need server-side validation and lifecycle webhooks so entitlements stay correct without app launches.",
      "acceptanceCriteria": [
        "validate-ios-receipt edge function verifies JWS transactions with Apple App Store Server API",
        "appstore-server-notifications-v2 verifies cert chain, is idempotent via notificationUUID, handles SUBSCRIBED/DID_RENEW/EXPIRED/REFUND/etc.",
        "Writes user_subscriptions with platform=ios + apple_transaction_id/original/product_id"
      ],
      "priority": 4,
      "passes": true,
      "notes": "ALREADY SHIPPED: supabase/functions/validate-ios-receipt + appstore-server-notifications-v2. Mirrors Android validate-android-receipt + play-rtdn-webhook."
    },
    {
      "id": "IOS-BASE-ADS-001",
      "title": "Affiliate hotel ad banner — free-tier, FTC-labeled (baseline)",
      "description": "As the business, I need ad revenue from free users so the free tier is monetized.",
      "acceptanceCriteria": [
        "AffiliateAdService rotates Hyatt/IHG/Marriott creatives (30-min session stickiness)",
        "AffiliateAdBanner shows FTC 'Ad' label; hidden for Insider/VIP; upgrade upsell beneath",
        "AdSlot placed in Home feed + restaurant detail",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 5,
      "passes": true,
      "notes": "ALREADY SHIPPED: Services/AffiliateAdService.swift (129) + Views/Components/{AffiliateAdBanner,AdBannerView,AdSlot}.swift. This is affiliate-only; first-party campaign serving + sponsored listings + house ads are added by IOS-ADS-010..016."
    },
    {
      "id": "IOS-BASE-GATE-001",
      "title": "PremiumGate + insider/dining tips gating (baseline)",
      "description": "As the business, I need a reusable gate so premium content drives conversions.",
      "acceptanceCriteria": [
        "PremiumGate component locks content behind .insider/.vip",
        "EventDetailInsiderTips + RestaurantDetailDiningTips gated; Ask Pulse has a quota gate",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 6,
      "passes": true,
      "notes": "ALREADY SHIPPED: Views/Components/PremiumGate.swift (112) + SubscriptionBanner/SubscriptionTierCard/VipGoldBadge. Gating today is narrow (tips + Ask Pulse quota); IOS-SUB-011 widens it to favorites cap, saved searches, alerts, advanced filters, Trip Planner."
    },
    {
      "id": "IOS-BASE-DISCOVERY-001",
      "title": "Ask Pulse AI chat + Swipe Discover + Surprise Me (baseline)",
      "description": "As a user, I need fun, low-effort discovery surfaces so I keep coming back.",
      "acceptanceCriteria": [
        "AskPulseView conversational discovery (edge fn discover-chat) with picks + follow-ups",
        "DiscoverView swipe deck + SurpriseMeView single random reveal",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 7,
      "passes": true,
      "notes": "ALREADY SHIPPED: Views/AskPulse, Views/Discover (DiscoverView/SwipeCard/SwipeCardStack), Views/SurpriseMe, plus ForYouRail/ForYouService/SwipeSessionService/GroupSessionView. These are strong native-only differentiators to lean on in the layout redesign and to monetize (IOS-ADS-015)."
    },
    {
      "id": "IOS-BASE-COMPLY-001",
      "title": "Account deletion flow (baseline)",
      "description": "As a user, I need to delete my account from the app so Apple guideline 5.1.1(v) is satisfied.",
      "acceptanceCriteria": [
        "Delete Account button in Profile + Settings with consequence-stating confirmation",
        "Calls delete-user-account edge function then signs out",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 8,
      "passes": true,
      "notes": "ALREADY SHIPPED: Views/Profile/{ProfileView,SettingsView}.swift + ProfileViewModel.deleteAccount; two-step token flow on backend (SEC-025)."
    },
    {
      "id": "IOS-BASE-COMPLY-002",
      "title": "Privacy manifest + permission strings + ATT-correct (baseline)",
      "description": "As the platform, I need a complete privacy posture so the app passes review.",
      "acceptanceCriteria": [
        "PrivacyInfo.xcprivacy declares collected types (location/email/userID/name) all non-tracking",
        "Info.plist has location/calendar/FaceID usage strings + ITSAppUsesNonExemptEncryption=false",
        "No ATT prompt (no cross-app tracking) — correct as-is"
      ],
      "priority": 9,
      "passes": true,
      "notes": "ALREADY SHIPPED. IMPORTANT CONSTRAINT for IOS-ADS-*: keep first-party ad serving non-tracking (no IDFA / cross-app SDK) so we do NOT trip ATT. If a future 3rd-party ad SDK is added, ATT + manifest updates become a hard prerequisite."
    },
    {
      "id": "IOS-BASE-PLATFORM-001",
      "title": "Platform & security services (baseline)",
      "description": "As a user, I need a secure, resilient native app so it handles auth, offline, and OS integrations well.",
      "acceptanceCriteria": [
        "Biometric lock, session timeout, certificate pinning, jailbreak detection, keychain",
        "NetworkMonitor + OfflineBanner, QueryCache, CrashReporting/MetricKit",
        "Spotlight indexing, App Intents/Siri, DeepLinkHandler, universal links, App Clip target",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 10,
      "passes": true,
      "notes": "ALREADY SHIPPED: Services/{Biometric,SessionTimeout,CertificatePinning,Jailbreak,Keychain,NetworkMonitor,QueryCache,CrashReporting,MetricKit,Spotlight}; Intents/PulseAppIntents; DesMoinesInsiderClip target. New screens must reuse OfflineBanner/NetworkMonitor (see IOS-COMPLY-004)."
    },
    {
      "id": "IOS-IA-001",
      "title": "Redesign Home into a content-first, personalized 'Today' feed",
      "description": "As a user, I open Home and want to see what's happening today right away, not scroll past a stack of six promotional CTA cards before reaching real content.",
      "acceptanceCriteria": [
        "HomeView.swift (currently 820 lines, ~6 stacked CTA/hero cards before content) is decomposed so the first screenful is content: RightNowRibbon + a single primary 'Discover' entry + the first real event cards above the fold on an iPhone 13/15",
        "Surprise Me, Swipe-to-Discover, Explore-Attractions, Ask-Pulse entry points are consolidated into ONE compact, swipeable 'Ways to explore' row (not four full-width stacked cards)",
        "Home is reorganized into clearly-titled, reorderable sections rendered from a single [HomeSection] model: Right Now, Featured, For You, Popular Restaurants, Trending Attractions, This Weekend, From the Blog — each a horizontal rail with a 'See all' that deep-links into the relevant tab/hub",
        "Each section lazy-loads independently with skeletons (no all-or-nothing spinner) and an empty/error state that does not collapse layout",
        "Ad + house-ad slots are positioned between sections at deterministic indices (handed to IOS-ADS-012) instead of a single hard-coded AdSlot",
        "Section order is data-driven so IOS-IA-007 can later personalize it without another refactor",
        "Scroll performance: LazyVStack/ScrollView stays at 60fps on an iPhone 12-class device with 100+ feed items (verify in Instruments or via no dropped-frame UI test)",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 11,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-IA-002",
      "title": "Information architecture for full web parity (Discover hub + tab restructure)",
      "description": "As a user, I need to reach every web surface (Trip Planner, Articles, Stay, Weekend, Best-Of, Music/Sports/Outdoors/Neighborhoods, Deals, Dashboard) from the app without the 6-tab bar overflowing or features hiding where nobody finds them.",
      "acceptanceCriteria": [
        "Define the target IA in a short doc comment at the top of MainTabView.swift and implement it: keep primary tabs lean and introduce a dedicated 'Discover'/'Explore' hub screen that is the home for parity features that don't deserve a permanent tab",
        "Decision recorded for each parity feature: permanent tab vs Discover-hub tile vs Home rail vs deep section — no feature is unreachable",
        "Discover hub is a native grid/list of entry tiles (Trip Planner, Articles & Guides, Where to Stay, This Weekend, Best Of / Voting, Music, Sports, Outdoors, Neighborhoods, Deals) with icons + one-line descriptions, each pushing its screen",
        "Tab bar remains <=5 primary tabs on iPhone if any tab is added/removed (Apple HIG); the 5th tab may become 'Discover' replacing one of Search/Map if usage data warrants — document the tradeoff, do not silently drop Search or Map",
        "Deep links + Spotlight + App Intents updated so each parity destination has a stable route (DeepLinkHandler cases added)",
        "iPad sidebar lists the same destinations as top-level items",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 12,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-IA-003",
      "title": "Unified content-card + skeleton system across all surfaces",
      "description": "As a user, I want events, restaurants, attractions, articles, hotels and deals to look consistent and load gracefully everywhere they appear.",
      "acceptanceCriteria": [
        "A single set of card components (Featured / Standard / Compact / List-row variants) is used by Home rails, list tabs, Search results, Favorites, hub screens and detail 'related' rows — eliminating the current parallel EventCardView/RestaurantCardView/AttractionCardView/Compact/Featured duplication where it diverges visually",
        "Matching skeleton placeholders (Shimmer) for every card variant; every list/rail shows skeletons while loading",
        "Cards expose a consistent affordance for favorite, sponsored badge (IOS-ADS-011) and premium/VIP badge",
        "Image loading uses CachedAsyncImage with correct aspect ratios and no layout shift",
        "Dynamic Type: cards reflow (not truncate-to-unreadable) up to XXL; verified at AX sizes",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 13,
      "passes": false,
      "notes": "Implemented 2026-06-02 (awaiting macOS CI xcodebuild gate). Added Views/Components/ContentCard.swift: a single ContentCardData value type + ContentCard view with four variants (.featured/.compact/.standard/.listRow), matching ContentCardSkeleton per variant, a shared CardFavoriteButton (managed + external modes), SponsoredBadge (FTC, seam for IOS-ADS-011) and VipGoldBadge premium affordances, and Event/Restaurant/Attraction.cardData adapters. EventCardView, RestaurantCardView, AttractionCardView (Search), BrowseAttractionCardView, FeaturedEventCard, CompactRestaurantCard, CompactAttractionCard and all four list skeletons are now thin wrappers delegating to ContentCard, eliminating the divergent duplication. Call-site init signatures unchanged. Decorative (rail) vs standalone (list/search) a11y modes preserve prior VoiceOver behavior. Dynamic Type reflow + CachedAsyncImage fixed aspect frames (no layout shift). passes=false until CI green."
    },
    {
      "id": "IOS-IA-004",
      "title": "Sticky search + persistent inline filter chips on list screens",
      "description": "As a user filtering Events/Dining/Attractions, I want to see and change my active filters without reopening a sheet, and reach search without scrolling back to the top.",
      "acceptanceCriteria": [
        "Events, Restaurants, Attractions list screens keep a sticky search/filter bar pinned under the nav title on scroll",
        "Active filters render as removable inline chips that persist while the list is shown (not hidden behind the filter button) with a result count and 'Clear all'",
        "Tapping a chip edits that facet directly; the full filter sheet remains for advanced/combined edits",
        "Smart presets (EventSmartPresets/RestaurantSmartPresets) and inline filters share one filter-state source of truth per screen",
        "State survives tab switches and is restored on return",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 14,
      "passes": false,
      "notes": "Implemented 2026-06-02 (awaiting macOS CI xcodebuild gate). Found all three VMs already wire searchText into fetch + activeFilterCount + clearFilters; the gap was UI placement. Added Views/Components/StickyFilterBar.swift (a .bar-material + divider container) and pinned filters via .safeAreaInset(edge:.top). Restaurants & Attractions: .searchable(navigationBarDrawer, displayMode:.always) for sticky search + a sticky bar holding the inline filter pills (RestaurantInlineFilters / typeChips+featuredAndRatingRow) and the existing removable active-filter chips (result count + Clear all); removed the in-flow copies. Attractions' old in-content searchField deleted in favor of .searchable. Home/Events (no search before): added .searchable; kept the IOS-IA-001 content-first controls in-flow but pinned the removable active chips while filtering. Presets+inline filters already share one VM source of truth per screen; chips remove facets, pills/popovers edit them without reopening a sheet; iPhone TabView keeps each tab VM alive so state survives tab switches. iPad sidebar re-creates VMs (pre-existing, flagged for IOS-IA-005). passes=false until CI green."
    },
    {
      "id": "IOS-SUB-010",
      "title": "Contextual native paywall component + per-feature upsell sheets",
      "description": "As the business, I need a beautiful, reusable paywall that appears at the moment a user hits a locked feature, with copy tailored to that feature, so conversion is higher than a generic pricing screen.",
      "acceptanceCriteria": [
        "New Paywall view (reusable) that accepts a context (feature id, headline, benefit bullets, recommended tier) and presents the StoreKit purchase affordance inline",
        "PremiumGate and all gated entry points present this contextual paywall instead of pushing the generic SubscriptionView",
        "At least these contexts have tailored copy: unlimited favorites, AI Trip Planner, insider/dining tips, saved searches, custom alerts, advanced filters, ad-free",
        "Paywall shows the right tier for the context (Insider vs VIP) preselected, with a clear monthly/annual toggle and the legally-required auto-renew disclosure",
        "Restore Purchases + Terms/Privacy always reachable from the paywall",
        "Every paywall present/dismiss/purchase/restore event is logged via AnalyticsService with the context id so we can measure per-surface conversion",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 15,
      "passes": false,
      "notes": "Implemented 2026-06-02 (awaiting macOS CI xcodebuild gate). Added Views/Subscription/PaywallView.swift: a reusable StoreKit-2-backed contextual paywall driven by a PaywallContext (id/icon/headline/benefits/recommendedTier) with 8 tailored presets — unlimitedFavorites, tripPlanner, insiderTips, diningTips, savedSearches, customAlerts, advancedFilters, adFree — plus a generic(tier:feature:) fallback. Paywall preselects the context's tier (Insider/VIP, shows 'Current' for held tiers, no buy-down), has a Monthly/Annual segmented toggle that appears only when >1 period exists, a pinned footer with live StoreKit displayPrice, the required auto-renew disclosure, and Restore + Terms + Privacy. StoreKitService got additive SubscriptionPeriod + product(for:period:)/availablePeriods(for:)/rank() (period list is product-derived so the Annual toggle lights up automatically when SUB-012 adds annual SKUs). AnalyticsService got trackPaywallPresented/Dismissed/PurchaseStart/PurchaseComplete/Restore(context:) so every lifecycle event is logged with the context id for per-surface conversion. PremiumGate now presents PaywallView (optional context, generic fallback) instead of SubscriptionView; routed gated entry points to tailored contexts: FilterSheet(.advancedFilters), AdBannerView(.adFree), EventDetailView(.insiderTips), RestaurantDetailView(.diningTips), FavoritesLimitBanner(.unlimitedFavorites). Management entry points (Settings manage, Profile upgrade banner) intentionally keep the full SubscriptionView. Monthly/annual toggle infra is in place; annual SKUs themselves land in SUB-012. passes=false until CI green."
    },
    {
      "id": "IOS-SUB-011",
      "title": "Paywall placement strategy across all gated surfaces",
      "description": "As the business, I need premium limits actually enforced and surfaced in the app, because today only insider tips and the Ask Pulse quota gate — most premium features are advertised but not gated natively.",
      "acceptanceCriteria": [
        "Free-tier favorites cap (3, per SubscriptionTier.maxFavorites) is enforced in FavoritesService with a FavoritesLimitBanner + paywall on the 4th save",
        "Saved searches and custom alerts (IOS-PARITY-008) are gated to Insider+ with a count limit per tier",
        "Advanced filters in FilterSheet gate premium-only facets behind the paywall",
        "AI Trip Planner (IOS-PARITY-001) enforces the tier quota (Insider 5/mo, VIP unlimited) with a quota meter + paywall when exhausted",
        "Each gate is a soft gate (shows the value, then the paywall) — never a dead end",
        "A single PremiumFeature enum centralizes feature->required-tier mapping, mirroring the web useSubscription hasFeature() list so web/iOS stay consistent",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 16,
      "passes": false,
      "notes": "Implemented 2026-06-02 (awaiting macOS CI xcodebuild gate). Centralized feature->tier mapping + enforced the free favorites cap. Fixed a real bug: FavoritesService capped EVERY user at 3 (hardcoded SubscriptionTier.free.maxFavorites, so paying Insider/VIP were capped) and skipped restaurants entirely. Now enforceFavoritesCap() reads the user's actual tier, is a TOTAL across events+restaurants+attractions (matches web 'Save up to 3'), and applies to all three add paths; premium = unlimited. Added Models/PremiumFeature.swift mirroring web useSubscription().hasFeature() (same raw keys) with requiredTier + paywallContext per feature, plus QuotaAction (favorites/savedSearches/alerts/tripPlans) limit/isWithinLimit/remaining helpers. Added SubscriptionTier.maxSavedSearches/maxAlerts/maxTripPlansPerMonth (free 0/0/0, insider 10/10/5, vip -1) and StoreKitService.hasFeature(_:). On cap hit FavoritesService throws limitReached AND posts .favoritesLimitReached; MainTabView has ONE listener that presents PaywallView(.unlimitedFavorites), so the soft gate fires from every save surface (cards, detail hearts, attractions) without per-site sheets; redundant toasts suppressed. FilterSheet's advanced-filters gate routes via hasFeature(.advancedFilters) + presents PaywallView(.advancedFilters). Saved searches/alerts (PARITY-008) and Trip Planner quota (PARITY-001) are not yet enforceable (their screens don't exist) but their PremiumFeature entries, per-tier limits, and quota helpers are ready as the seam. passes=false until CI green."
    },
    {
      "id": "IOS-SUB-012",
      "title": "Annual SKUs + StoreKit introductory offer / free trial",
      "description": "As a user, I want an annual plan and a free trial so committing is cheaper and lower-risk; as the business, annual + trial materially lifts LTV.",
      "acceptanceCriteria": [
        "Annual products created in App Store Connect for Insider and VIP and added to Products.storekit; SubscriptionTier/product mapping updated",
        "Paywall shows monthly vs annual with the % savings badge (mirror web: ~17% / Insider $49.99yr, VIP $129.99yr)",
        "Introductory offer (e.g., 7-day free trial) configured as a StoreKit intro offer and shown with correct, localized, compliant trial+renewal copy",
        "Eligibility handled correctly (intro offers only for users who never had one) via StoreKit",
        "validate-ios-receipt + appstore-server-notifications-v2 handle the new product ids and trial/intro states; user_subscriptions tier resolves correctly during trial",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 17,
      "passes": false,
      "notes": "Implemented 2026-06-02 (awaiting macOS CI xcodebuild gate). Added annual Insider/VIP SKUs (prod_insider_annual $49.99/yr, prod_vip_annual $129.99/yr ≈ 17% off monthly×12) each with a 7-day free-trial introductoryOffer. StoreKitService gained insiderAnnualID/vipAnnualID in all product-ID sets, so SUB-010's data-driven period helpers now expose [.monthly,.annual] and the paywall toggle appears. Products.storekit mirrors both annual products + free trial for simulator testing. PaywallView: custom Monthly/Annual buttons with a live-computed 'Save N%' badge on annual; refreshTrialCopy() shows '7-day free trial, then $X/yr' ONLY when StoreKit.isEligibleForIntroOffer is true (eligibility fully delegated to StoreKit; purchase() auto-applies the offer for eligible users only), CTA flips to 'Start Free Trial', and the auto-renew disclosure switches to trial->paid language. validate-ios-receipt + appstore-server-notifications-v2 got the annual IDs added to INSIDER/VIP sets; tier already resolves correctly during trial (future expiresDate -> active, tier from product ID). ACTION REQUIRED: create the two annual subs + 7-day trial in App Store Connect with these exact Product IDs (or update the constants). passes=false until CI green."
    },
    {
      "id": "IOS-SUB-013",
      "title": "Onboarding free-trial moment + post-onboarding soft paywall",
      "description": "As the business, I need a well-timed first-session upsell because trial starts during onboarding convert far better than ones discovered later.",
      "acceptanceCriteria": [
        "OnboardingView gains a final, skippable value+trial screen after interest selection that presents the annual-trial paywall",
        "Skipping is one tap and never blocks app use (no hard wall at first launch — App Store-safe)",
        "A post-onboarding soft paywall re-appears at a sensible engagement threshold (e.g., after N favorites or first Trip Planner attempt), frequency-capped so it is not nagging",
        "All onboarding paywall events logged with cohort/source for funnel analysis",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 18,
      "passes": false,
      "notes": "Implemented 2026-06-02 (awaiting macOS CI xcodebuild gate). OnboardingView gains a final, skippable trialStep after the value pages (last button 'Continue' -> trial screen) that presents PaywallView(.onboarding, preferredPeriod: .annual) so the 7-day annual free trial is the headline. 'Maybe later' + paywall dismissal both complete onboarding in one tap; info-page Skip bypasses everything — no hard wall (App Store-safe). Added PaywallView preferredPeriod init param + .onboarding context. New SoftPaywallService re-surfaces a soft upsell only for free, onboarded users at an engagement threshold (2nd favorite save; considerAfterTripPlannerAttempt() seam for PARITY-001), frequency-capped (>=7-day cooldown, max 3); fires .softPaywallTriggered -> single MainTabView sheet(item:) presenter. FavoritesService calls considerAfterFavorite on each successful add. Showing the onboarding trial seeds the soft-paywall cooldown (noteOnboardingUpsellShown) so no same-session double upsell. Analytics: trackOnboardingTrial(shown/start_tapped/skipped, cohort=onboarding) + trackSoftPaywall(source); paywall present/purchase events carry context 'onboarding' for the funnel. No interest-selection step exists in onboarding, so 'after interest selection' maps to 'after the value pages'. passes=false until CI green."
    },
    {
      "id": "IOS-SUB-014",
      "title": "Win-back & promotional offers for lapsed/expiring subscribers",
      "description": "As the business, I want to recover churned and lapsing subscribers using Apple's offer mechanisms instead of losing them silently.",
      "acceptanceCriteria": [
        "StoreKit promotional/win-back offers configured and surfaced to eligible lapsed users on next launch",
        "Offer-code redemption entry point in Settings (presentCodeRedemptionSheet)",
        "Expiring-soon and billing-retry/grace states (from appstore-server-notifications-v2) drive a non-intrusive in-app banner prompting renewal/update-payment with deep link to Apple subscription management",
        "Eligibility + display rules logged via AnalyticsService",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 19,
      "passes": false,
      "notes": "Implemented 2026-06-02 (awaiting macOS CI xcodebuild gate). In-app win-back machinery done; discounted-offer config + backend signing documented in ios/RELEASE_ACTIONS.md §3. StoreKitService: SubscriptionRenewalState (none/active/expiringSoon/billingRetry/grace/expired) + refreshRenewalState() reading Product.SubscriptionInfo.status(for:group) (state, renewalInfo.willAutoRenew, transaction.expirationDate), refreshed via updatePurchasedProducts. New SubscriptionStatusBanner (self-contained, dismissible, stacked under OfflineBanner in MainTabView): shows only for states needing attention — expiringSoon/billingRetry/grace deep-link to apps.apple.com/account/subscriptions; expired presents PaywallView(.winBack). Lapsed users see it on next launch (state refreshed at launch). Settings gains a 'Redeem Offer Code' row via .offerCodeRedemption(isPresented:) (iOS 16+). Added .winBack PaywallContext + analytics (trackRenewalBanner action/state, trackOfferCodeRedeem). Did NOT add promotional offers to Products.storekit (they need server-signed signatures to redeem; documented instead). Remaining: ASC offer/code/promo/win-back config + a signing edge function for discounted promotional offers (iOS 18 win-back offers are system-surfaced, target is 17). passes=false until CI green."
    },
    {
      "id": "IOS-ADS-010",
      "title": "First-party campaign ad serving in-app (real campaigns, not just affiliate)",
      "description": "As an advertiser, I paid for a campaign on the web and need it to actually render in the iOS app; today the app only shows hard-coded hotel affiliate creatives.",
      "acceptanceCriteria": [
        "New AdService (or extend AffiliateAdService) fetches active, approved creatives from the existing campaigns/advertisements tables via the get_active_ads(placement_type) RPC for the placements the app supports (top_banner, featured_spot, below_fold, sidebar->feed, sponsored_listing)",
        "AdSlot renders a real campaign creative when one is available for the placement and falls back to affiliate, then to a house ad (IOS-ADS-013), so a slot is never empty",
        "Creatives load via CachedAsyncImage; tap opens the campaign target (in-app SFSafariViewController / WebView)",
        "Free-tier only (hidden for Insider/VIP), FTC 'Ad' label preserved",
        "No IDFA / no third-party ad SDK — stays first-party so ATT is not triggered (per IOS-BASE-COMPLY-002)",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 20,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-ADS-011",
      "title": "Sponsored / featured listings inline in feeds",
      "description": "As an advertiser, I want my event/restaurant boosted in the normal browse flow, because in-feed native placements outperform banners.",
      "acceptanceCriteria": [
        "Cards for items linked via sponsored_listing_links (is_sponsored) render a ring/elevated treatment + a 'Sponsored' badge using the unified card system (IOS-ADS-011 depends on IOS-IA-003)",
        "Sponsored items are injected at defined positions in Home rails and the Events/Dining/Attractions lists (e.g., position 1 and every Nth) — clearly labeled, never disguised as organic",
        "Sponsored placements are shown to all tiers (they are content, not interruptive ads) but counted/【tracked】 distinctly",
        "Tapping opens the normal detail screen; impression+click logged against the campaign",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 21,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-ADS-012",
      "title": "Native in-feed ad cards + frequency-capped interstitial",
      "description": "As the business, I want higher-value native ad inventory beyond a single banner, without wrecking the experience.",
      "acceptanceCriteria": [
        "A native in-feed ad card variant (image + headline + CTA + 'Ad' label) is inserted between Home sections and into long lists at deterministic indices coordinated with IOS-IA-001",
        "An optional interstitial can show on a low-frequency, capped basis (e.g., at most once per session, not on first launch, never mid-task) on a major navigation boundary — free-tier only",
        "All ad density is centrally configurable so we can tune without code changes (remote-config-style constant or Supabase-driven config)",
        "Insider/VIP see zero ads (banner, in-feed, interstitial); verify via UI test for a mocked premium user",
        "Respects Reduce Motion and never blocks content the user was reading",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 22,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-ADS-013",
      "title": "House-ad fill: promote subscription when no paid campaign",
      "description": "As the business, I want unsold ad inventory to sell the subscription instead of showing blank space, turning ad slots into a second conversion surface.",
      "acceptanceCriteria": [
        "When no paid campaign/affiliate creative is available for a slot, the slot renders a house ad promoting Insider/VIP (ad-free + premium benefits) that opens the contextual paywall (IOS-SUB-010)",
        "House ads rotate among a few messages and are also free-tier only",
        "House-ad impressions/taps logged distinctly from paid inventory so fill rate and house-ad conversion are measurable",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 23,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-ADS-014",
      "title": "Ad analytics parity (viewability, dedupe) wired to backend",
      "description": "As an advertiser, I need trustworthy impression/click numbers from the app so reporting matches the web.",
      "acceptanceCriteria": [
        "Impressions count only on >=50% viewable for >=1s (match the web useAdTracking thresholds), deduped per slot per session",
        "Clicks tracked with campaign_id/creative_id/placement_type and posted to the same tracking endpoint/tables the web uses (campaign analytics)",
        "Distinguishes paid campaign vs affiliate vs sponsored-listing vs house ad in the event payload",
        "Offline-safe: events queue and flush when NetworkMonitor reports connectivity",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 24,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-ADS-015",
      "title": "Sponsored results inside Ask Pulse / Surprise Me / Trip Planner",
      "description": "As an advertiser, I want a labeled placement inside the AI discovery flows, which are high-intent and uniquely native.",
      "acceptanceCriteria": [
        "Ask Pulse can surface at most one clearly-labeled 'Sponsored pick' among its picks when an eligible campaign matches the query/context",
        "Surprise Me can occasionally return a labeled sponsored result (capped, never every roll)",
        "Trip Planner (IOS-PARITY-001) can include a labeled sponsored stop/recommendation",
        "All AI-surfaced ads carry a visible 'Sponsored' label, are free-tier only, and respect the same tracking + frequency caps",
        "Sponsored injection is server-driven (edge function) so relevance/labeling rules live with the existing campaign system, not hard-coded in the app",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 25,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-ADS-016",
      "title": "In-app 'Promote this listing' entry for business owners",
      "description": "As a local business owner using the app, I want to start promoting my event/restaurant from the listing, creating a new advertiser-acquisition funnel — handled so it does not run afoul of IAP rules.",
      "acceptanceCriteria": [
        "A 'Promote' / 'Advertise this' affordance appears on event/restaurant detail (and in a Profile 'For Businesses' area) for owners/admins",
        "Because this is advertising for a real-world business (not an in-app digital good for the purchasing user), checkout is handled via the existing web campaign flow (create-campaign-checkout / Stripe) opened in SFSafariViewController, NOT StoreKit — document the App Review rationale in code comments",
        "Entry point pre-fills the listing via the existing ListingPicker/campaign create path on web",
        "No purchase UI for advertising is rendered natively in StoreKit; the app only links out to the advertiser portal",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 26,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-PARITY-001",
      "title": "Native AI Trip Planner (itinerary builder) — premium hook",
      "description": "As a user, I want to build an AI itinerary in the app; today the app advertises 'AI Trip Planner' in subscription copy but has zero implementation, which is both a parity gap and a broken promise.",
      "acceptanceCriteria": [
        "New native Trip Planner flow (input: dates, interests, party, budget, neighborhood) calling the same backend the web useTripPlanner/trip-planner uses",
        "Generated itinerary renders as native day-by-day cards (events/restaurants/attractions) with map preview, reorder, save, share, and add-to-calendar",
        "Saved itineraries persist to the backend and are accessible from Dashboard (IOS-PARITY-007) and a Saved filter",
        "Tier quota enforced (Insider 5/mo, VIP unlimited) via IOS-SUB-011 with a quota meter and contextual paywall when exhausted",
        "Reachable from the Discover hub (IOS-IA-002) and offered as a Home entry point",
        "Loading, offline, and generation-error states handled (IOS-COMPLY-004)",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 27,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-PARITY-002",
      "title": "Articles / Guides hub + native reader",
      "description": "As a user, I want to read the site's guides and blog in the app for SEO content, dwell time and ad/affiliate revenue.",
      "acceptanceCriteria": [
        "Articles list/hub screen backed by the same content the web /articles uses (TanStack/web equivalent query against the articles content source)",
        "Native reader renders rich content (markdown/HTML) with images, headings, links (in-app browser), share, and related-content rail",
        "Free-tier ad slots within the reader (IOS-ADS-010/012); affiliate/sponsored inline allowed",
        "Saveable to Favorites/Dashboard; deep-linkable; Spotlight-indexed",
        "Dynamic Type + VoiceOver friendly long-form reading",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 28,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-PARITY-003",
      "title": "Where to Stay (hotels) browse + affiliate booking",
      "description": "As a user planning a trip, I want to browse hotels in the app; as the business, this is direct affiliate revenue beyond the banner.",
      "acceptanceCriteria": [
        "Stay screen lists hotels/accommodations (same data source as web /stay) with filters (area, price, rating)",
        "Hotel detail with photos, amenities, map, and an affiliate 'Book' CTA opening the partner in SFSafariViewController with the affiliate link (revenue, outside IAP)",
        "Affiliate clickouts tracked via AnalyticsService/ad tracking",
        "Reachable from Discover hub + Trip Planner cross-links",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 29,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-PARITY-004",
      "title": "This Weekend guide screen",
      "description": "As a user, I want a curated weekend view in the app, not just a 'Free Weekend' filter preset.",
      "acceptanceCriteria": [
        "Dedicated Weekend screen (same curation as web /weekend) grouping Fri/Sat/Sun with featured picks across events/dining/attractions",
        "Surfaced as a Home rail ('This Weekend') and a Discover-hub tile",
        "Shareable; deep-linkable; refreshes by current week",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 30,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-PARITY-005",
      "title": "Best Of / community voting",
      "description": "As a user, I want to vote in Best-Of and see winners in the app, driving engagement and repeat visits.",
      "acceptanceCriteria": [
        "Best-Of screen lists categories + nominees (same backend as web /best-of) with native voting that requires auth and respects per-user vote rules",
        "Results/leaderboard view with vote counts; optimistic vote UI with revert on failure",
        "Winners/badges surface on the relevant listing cards (unified card system)",
        "Voting can be positioned as an engagement driver and a soft funnel to account creation",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 31,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-PARITY-006",
      "title": "Content hubs: Music, Sports, Outdoors, Neighborhoods (curated screens)",
      "description": "As a user, I want real curated hub screens like the web, not just category filters buried in the events list.",
      "acceptanceCriteria": [
        "Each hub (Music, Sports, Outdoors, Neighborhoods) is a curated native screen mixing featured content, relevant events/restaurants/attractions, and editorial blurbs — parity with web /music, /sports, /outdoors, neighborhood guides",
        "Hubs reachable from the Discover hub (IOS-IA-002) and via deep links/Spotlight",
        "Reuse unified cards + rails; each hub supports its own ad/sponsored slots",
        "Neighborhood hub integrates location (nearby in this neighborhood)",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 32,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-PARITY-007",
      "title": "User Dashboard (activity, saved, recommendations)",
      "description": "As a signed-in user, I want a home base showing my saved itineraries, favorites summary, alerts, and tailored recommendations — parity with web /dashboard.",
      "acceptanceCriteria": [
        "Dashboard screen aggregates: saved Trip Planner itineraries, favorites overview, saved searches/alerts (IOS-PARITY-008), recently viewed, and For-You recommendations",
        "Subscription status card with manage/upgrade entry (paywall for free users)",
        "Reachable from Profile and/or as the authenticated Discover landing",
        "Empty/guest state prompts sign-in; never a dead end",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 33,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-PARITY-008",
      "title": "Saved searches + custom alerts (premium + push)",
      "description": "As a power user, I want to save searches and get alerts for new matching events; as the business, this is a high-value premium feature and a push re-engagement driver.",
      "acceptanceCriteria": [
        "Users can save a search (query + filters) and toggle alerts on it; gated to Insider+ with per-tier limits (IOS-SUB-011)",
        "Backend stores saved searches/alerts (reuse web schema); a scheduled job + PushNotificationService deliver new-match alerts",
        "Manage saved searches/alerts from Dashboard and Search",
        "Alert pushes deep-link to the filtered results",
        "Free users see the feature and a contextual paywall",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 34,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-PARITY-009",
      "title": "Write reviews & ratings (premium)",
      "description": "As a user, I want to rate and review places in the app; as the business, this deepens content and is a gated premium benefit.",
      "acceptanceCriteria": [
        "Native review/rating composer on event/restaurant/attraction detail, writing to the existing reviews/ratings tables with RLS",
        "Writing reviews is gated to Insider+ (web parity) via the contextual paywall; reading reviews is free",
        "Reviews display with author, rating, date; user can edit/delete own; basic moderation/report affordance",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 35,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-PARITY-010",
      "title": "Deals page (incl. recurring deals) native",
      "description": "As a user, I want to see local deals — including recurring happy-hour-style rules — in the app.",
      "acceptanceCriteria": [
        "Deals screen lists active deals (same backend as web useDeals) including the recurring fields days_of_week/start_time/end_time (DEAL-RECURRING-001) rendered as a readable schedule",
        "Filter by category/neighborhood/active-now; deep link to the related listing",
        "Deals are a natural sponsored/featured surface (coordinate with IOS-ADS-011) and a Discover-hub tile",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 36,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-IA-005",
      "title": "iPad / large-screen adaptive layouts",
      "description": "As an iPad user, I want the extra space used well, not a stretched phone layout, across the new screens.",
      "acceptanceCriteria": [
        "NavigationSplitView sidebar lists all primary + Discover destinations (IOS-IA-002)",
        "List/hub/detail screens use multi-column grids and a list+detail two-pane where it makes sense (e.g., Events list + detail)",
        "Trip Planner, Articles reader, Dashboard, and hubs all have iPad-optimized layouts",
        "Verified in portrait + landscape on iPad via UI snapshot tests (Fastlane snapshot targets exist)",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 37,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-IA-006",
      "title": "Personalized Home section ordering via For-You signals",
      "description": "As a returning user, I want Home to lead with what I care about, increasing engagement and ad/premium exposure.",
      "acceptanceCriteria": [
        "The data-driven [HomeSection] order (IOS-IA-001) is personalized using ForYouService signals (interests, recent interactions, location)",
        "Ordering is computed deterministically with a sensible default for new/guest users",
        "Personalization respects consent (ConsentService) and is first-party only",
        "A/B-friendly: ordering strategy is centralized so it can be experimented on",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 38,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-COMPLY-003",
      "title": "Full accessibility audit across new + existing screens",
      "description": "As a user relying on assistive tech, I need the whole app — including all new parity/monetization screens — to be fully usable, which is also an App Review risk area.",
      "acceptanceCriteria": [
        "VoiceOver: every screen navigable; cards aggregated into single elements with meaningful labels; ads/paywalls/sponsored badges announced correctly",
        "Dynamic Type: all text scales to AX5 without clipping or broken layout (cards reflow per IOS-IA-003)",
        "Reduce Motion: parallax/scale/burst animations (ParallaxHeroHeader, HeartBurst, SwipeCard, theme crossfade) respect the setting",
        "Color contrast meets 4.5:1; verified in light/dark/OLED themes",
        "Hit targets >=44x44 (minHitTarget) on all new controls",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 39,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-COMPLY-004",
      "title": "Offline + error states across all new screens",
      "description": "As a user on a flaky connection, I need every new screen to fail gracefully, not show blank/broken UI (also an App Review checkpoint).",
      "acceptanceCriteria": [
        "Every new screen (Trip Planner, Articles, Stay, Weekend, Best-Of, hubs, Dashboard, Deals) reuses OfflineBanner + NetworkMonitor and shows skeletons, empty states, and retryable error states",
        "Network requests use NetworkRetry with backoff; cached content (QueryCache) shown when available offline",
        "No screen presents a dead end on error — always a retry or a navigate-away path",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 40,
      "passes": false,
      "notes": ""
    },
    {
      "id": "IOS-COMPLY-005",
      "title": "App Store metadata, screenshots & review notes for the new feature set + paywalls",
      "description": "As the release owner, I need store assets and reviewer notes updated so the expanded app (with paywalls, ads, sponsored content, Trip Planner, etc.) passes review and markets well.",
      "acceptanceCriteria": [
        "Fastlane snapshot screens added/updated for the redesigned Home, Discover hub, Trip Planner, a paywall, and a content hub (ios/fastlane + DesMoinesInsiderUITests)",
        "App Store description/keywords updated to cover Trip Planner, guides, deals, premium tiers",
        "App Review notes document: IAP for consumer subscriptions, the advertiser/'Promote' flow links out (not a digital good for the purchasing user), sponsored-content labeling, and the demo account for premium",
        "MARKETING_VERSION bumped in project.yml; release cut via ios-native-release workflow",
        "iOS build passes (cd ios && xcodegen generate && xcodebuild -scheme DesMoinesInsider build) and existing unit/UI tests stay green"
      ],
      "priority": 41,
      "passes": false,
      "notes": ""
    }
  ]
}
